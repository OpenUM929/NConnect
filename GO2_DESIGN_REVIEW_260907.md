# Go2 예선 캠페인 설계 검수 요청서 (260907)

> **이 문서의 목적** — 외부 검토자(다른 AI 또는 사람)에게 **우리 캠페인 기획 자체가 틀렸는지**를
> 판정받기 위한 자료다. 결론을 설득하려는 문서가 아니라 **반증당하려는 문서**다.
> 모든 수치는 로컬 산출물에서 재계산했고 §11에 파일 경로를 붙였다. 검토자는 §10의 질문에
> 답해 주면 된다. 내 판단이 틀렸다고 보이면 그 근거를 지목해 주기 바란다.
>
> 작성: Claude (Opus 5) · 검수 대상: 260901~260907 Go2 튜닝 캠페인 (서버 산출물 17건)
> 정본 원장: `GO2_PROJECT_STATE.md` · 근거 원장: `GO2_REWARD_EVIDENCE_MASTER.md`

---

## 0. 요약 (검토자가 이것만 읽어도 되는 부분)

우리는 17건의 서버 실행으로 **단 한 건도 채택하지 못했다.** 나는 이것이 실행 실패가 아니라
**기획 실패**라고 판단한다. 핵심 주장 4가지:

| # | 주장 | 증거 강도 |
|---|---|---|
| **E1** | **승급 게이트가 대회 채점식과 불일치한다.** 채점은 `생존 x 추종`(곱)인데 우리 게이트는 생존을 경성 제약으로 걸었다. 그 결과 **점수가 오른 실행을 기각**했다. | **강함 — 반박 불가에 가까움.** A017은 7개 시나리오 중 6개가 개선, 총점 +3.708인데 기각 사유가 `G4_survival_regressed_over_0.1` 단 하나다. G4조차 proxy는 +0.0159로 개선이었다. |
| **E2** | **절대 점수가 아니라 delta만 봤다.** 대회는 절대 점수로 채점하는데 우리는 전부 기준선 대비 delta로 판정했고, 그래서 계측 비대칭에 통째로 노출됐다. | **강함.** 절대 점수는 이미 유효하고 서열이 확정된다(§4). 이 표를 처음부터 볼 수 있었다. |
| **E3** | **1000 iter 서열로 최종 후보를 골랐다.** 운영진 파일이 명시한 성격 판별 하한(3000 iter)보다 낮다. **게다가 우리는 이것을 260905에 이미 알고 적어놓고도 우선순위 4번으로 밀었다**(§12-b R5). | **중간~강함.** 파일 인용은 확실하나, 1000 iter 서열이 실제로 뒤집히는지는 미측정. |
| **E4** | ~~9개 다이얼 중 6개만 탐색했다~~ → **정정(§12-g 정정 6): 9개 중 8개를 탐색했다.** `undesired_contacts`(값 `null`)·`termination_penalty`(항 부재)는 **260903에 실측으로 닫혔다.** 진짜 미탐색은 `track_ang_vel_z_exp` **1개**뿐이다. | **약함.** 감사에서 대폭 축소됐다. 남은 1개도 "0.75 = Go2 기본값이라 그대로 풀면 변화 없음"이라 주석이 붙어 있다. |

**그리고 §4에서 나온 결정적 사실:** 후보 정책의 **절대 점수는 계측 결함과 무관하게 유효**하며,
그 서열은 학습 로그(evaluator와 완전히 독립된 계측)로 **교차 검증된다**(§5 정정 2).
즉 GPU를 1시간도 더 쓰지 않고 제출 후보를 확정할 수 있다.

> **§12 추가 (독립 메타 감사).** 위 4개 주장을 독립 감사관이 원장 전체로 대조한 결과
> **내 주장 5건이 반박됐다.** E1의 실사례는 2건 → **1건**으로, E4는 "9개 중 3개 미탐색" →
> **"9개 중 1개"**로 축소됐고, E3는 "새 발견"이 아니라 "이미 적어놓고 실행하지 않은 것"으로
> 성격이 바뀌었다. 반대로 감사는 더 무거운 사실 하나를 확정했다 — **E1은 모르고 저지른 오류가
> 아니라 계약 테스트에 명시적으로 고정된 정책 선택**이었고, 그 핵심 숫자
> `max_survival_regression = 0.10` 의 도출 근거는 **저장소 어디에도 없다.** 위 표는 이 정정을
> 반영한 것이며, 반박 원문은 §12-b, 본문 조치는 §12-g에 있다.

**§7(제안 계획)도 감사 대상이다.** 이 문서를 쓰는 동안 §5에서 **내 이전 주장 3건이 실측으로
반증**됐고(§12에서 5건 추가로 반박돼 합계 8건), 그중 정정 3은 **내가 제안했던 계획 자체를 무효화**했다(`--resume`이 지형
커리큘럼을 리셋한다). §7은 그 결과로 전면 교체된 두 번째 판이며, §7-f에 **기각한 대안 4개와
그 기각 근거**를 함께 실었다. §10 Q5-a~f는 계획의 **선택 자체**를 공격해 달라는 요청이다.
§9 W1·W4·W5·W9·W10·W11이 이 계획의 알려진 구멍이다.

---

## 1. 배경 — 규칙과 제약 (검토자용 사전 지식)

### 1-a. 대회 채점식
- 시나리오 점수 = **생존율 x 추종 점수** (곱셈)
- Go2 시뮬 70점 + 설계 의도 20점 + 리포트 10점 = 100점
- 시나리오 7종 가중치: G1 0.15 · G2 0.15 · G3 0.20 · G4 0.15 · G5 0.15 · G6 0.10 · G7 0.10
  - G1 `forward_fast` · G2 `diagonal_left` · G3 `rough_forward` · G4 `slope_plus_20`
  - G5 `stairs_15_up` · G6 `push_pos_x` · G7 `dr_seed_{seed}`

### 1-b. 우리 내부 프록시 (공식 채점기가 비공개라 자체 구현)

```
tracking_proxy       = exp(-(rmse/0.5)^2)
scenario_proxy       = survival_proxy x tracking_proxy
simulation_points_70 = 70 x SUM(weight x scenario_proxy)
```

채점식의 **곱셈 구조는 정확히 재현**했다. 문제는 채점기가 아니라 그 위에 올린 **게이트**다(§3-a).

### 1-c. 규정상 제약 (변경 불가)
- 참가자는 `quadruped_rewards.py` **의 가중치 값만** 수정한다 (예선 규정 제2조 5항).
  파일 주석: *"줄 추가/삭제/주석(#) 자유"* → **주석 처리된 다이얼을 푸는 것은 허용된다.**
- 학습은 운영진 제공 서버에서만. 외부 장비 학습 정책 제출 금지 (제14조).
  제출물은 서버 학습 이력과 대조될 수 있다.
- 제출 3종: `policy.pt` · `env.yaml` · 리포트. `policy.pt`는 학습만으론 안 생기고 `play.py` 필요.
- 로봇 유형별 최고점만 합산. **미제출 = 0점** (제10조).

### 1-d. 자원 제약
- RTX 5080 단일 GPU. 1000 iter x 4096 envs = **56분 41초** (A017 실측 `Training time: 3407.67 seconds`).
- 남은 GPU 예산: **약 10시간** (사용자 고지, 260907 기준).
- 이미 소진: Go2 산출물 17건 = **14~15 GPU-시간** (실행별 1~2 arm 학습, 근사치).

### 1-e. 참고 — H1(이족)은 이미 자체예상 92.73/100
동일 파이프라인·동일 인력으로 이족은 잘 됐고 사족이 안 됐다. 이 비대칭 자체가 §6의 주제다.
(92.73은 자체예상이며 공식 점수가 아니다.)

---

## 2. 우리가 한 일 — 전체 실행 대장

| 실행 | 변경 | 기준선 | 절대 점수/70 | 판정 | 기각 사유 |
|---|---|---|---:|---|---|
| track_lin_vel_120 | track 1.0→1.2 | Default-01 | **20.627** | FAIL | `target_G1_improvement_below_0.05` |
| G-A010 | lin_vel_z −3→−2 | Default-01 | 19.794 (v1) | FAIL | — |
| G-A013 | flat_orientation 0→−1 | Default-01 | 15.704 | FAIL | 총점 delta |
| G-A015 | pilot feet_air 0.2→0.35 | Pilot-01 | 16.369 | FAIL | 총점 delta |
| G-A016 | pilot ang_vel_xy −0.05→−0.15 | Pilot-01 | 1.376 | FAIL | 총점 delta |
| **G-A017** | **pilot track 1.2→1.4** | **Pilot-01** | **50.199** | **FAIL** | **`G4_survival_regressed_over_0.1`** |
| G-A018 | pilot action_rate −0.01→−0.008 | Pilot-01 | 2.093 | FAIL | 총점 delta |
| G-A020 | chain lin_vel_z −3→−2 | Chain-01 | 0.428 | FAIL | 전멸 |
| G-A021 | chain ang_vel_xy −0.08→−0.05 | Chain-01 | 12.003 | FAIL | 총점 delta |
| G-A022 | chain feet_air 0.01→0.20 | Chain-01 | 4.883 | FAIL | 총점 delta |
| G-A024 | ang_vel_xy −0.08→−0.15 | Default-01 | 0.000 | FAIL | 전멸 |
| G-A025 | flat_orientation 0→−1 (재측정) | Default-01 | 15.704 | FAIL | **A013의 비트 동일 재현 — 순수 낭비** |

기준선 자체의 절대 점수: **Pilot-01 46.491** · Chain-01 2.308(69-case) · Default-01 17.132.

**결과: 12회 실험, 채택 0회.** 그리고 최고 점수(50.199)를 낸 실행이 기각됐다.

---

## 3. 설계 오류

### 3-a. E1 — 게이트가 목적함수와 불일치한다 (치명적)

**게이트 사양** (실험 명세 JSON, 전 실행 공통):

```json
"gates": {
  "min_total_points_delta": 1.0,
  "max_survival_regression": 0.10,
  "minimum_points_70": 60.0,
  "required_survival_proxy": 0.95,
  "required_tracking_proxy": 0.70
}
```

`max_survival_regression`은 **어떤 시나리오에서도 생존율이 0.1 넘게 떨어지면 기각**한다.
그런데 채점식은 `생존 x 추종`이다. 생존이 이미 1에 가깝고 추종이 바닥인 지점에서,
이 조항은 **점수를 올리는 유일한 거래(생존 조금 ↔ 추종 크게)를 구조적으로 금지**한다.

**증명 — G-A017 판정 원문** (`reports/TIER1_DECISION.json`, 값 그대로):

```
candidate_points_70      : 50.19915737233102
baseline_points_70       : 46.4912409491015
candidate_minus_baseline : +3.70791642322952
failure_reasons          : ["G4_survival_regressed_over_0.1"]     <- 단 하나
status                   : INTERNAL_EARLY_KILL_FAIL
```

시나리오별 delta:

| 시나리오 | 생존 delta | 추종 delta | **proxy delta (= 채점식)** |
|---|---:|---:|---:|
| G1 forward_fast | 0.000 | +0.0099 | **+0.0099** |
| G2 diagonal_left | 0.000 | +0.0412 | **+0.0412** |
| G3 rough_forward | **+0.094** | +0.0315 | **+0.0845** |
| **G4 slope_plus_20** | **−0.219** | **+0.175** | **+0.0159**  ← *개선인데 기각 사유* |
| G5 stairs_15_up | 0.000 | +0.1521 | **+0.1093** |
| G6 push_pos_x | 0.000 | −0.0070 | −0.0068 |
| G7 dr_seed_101 | +0.063 | +0.0658 | **+0.1031** |

**7개 중 6개 개선, 나머지 하나는 −0.0068. 기각 사유가 된 G4조차 채점식으로는 +0.0159 개선.**
게이트는 채점식이 개선이라 말하는 결과를 죽였다.

> **정정 4·5 (§12-b R1·R2, §12-g).** 이 절의 초판에는 "**두 번째 사례**"로 `track_lin_vel_120`
> (+3.090)을 들고, "그 조항을 강등할 때 이미 기각한 결과를 재검토하지 않았다"고 썼다.
> **둘 다 틀렸다.**
> - 그 정책(Chain-01)은 260905 전용 재측정에서 **`2.308/70`, 6/7 시나리오 생존 전멸**로 확정됐다
>   (`GO2_PROJECT_STATE.md:553` G-F139, `:555` G-F141). 개선이 아니었다. **이 문서 §4-b 서열표가
>   그것을 7위에 놓고 있으므로 초판은 자기모순이었다.**
> - 재검토를 안 한 것도 아니다 — 그 결과는 **강등의 근거 표에 1행으로 올라 있고**
>   (`GO2_CAMPAIGN_SCHEDULE.md:359`), 전용 재측정 실행(G-A023)까지 발주됐다(`:545` G-D90).
>
> **따라서 E1의 실사례는 A017 1건이다.** 사례 수는 줄었지만 그 1건의 성질은 그대로다 —
> 채점식이 개선이라 말하는 결과를 게이트가 죽였다.

**그리고 감사가 이 절에 내린 더 무거운 지적:** 이건 **모르고 저지른 사양 오류가 아니다.**
계약 테스트에 두 테스트가 **나란히** 고정돼 있었다 —
`test_pinned_scenario_gate_no_longer_kills_a_net_positive`("총점 양수를 죽이는 건 오살이므로 막는다")와
`test_survival_regression_still_kills_a_total_positive_candidate`("총점 양수라도 생존이 무너지면 죽인다").
즉 260903 시점에 우리는 "총점 양수를 죽이는 조항"의 개념을 이미 갖고 있었고,
생존 조항에 대해서만 **의식적으로 반대 선택**을 했다. 초판의 "판단 실수가 아니라 사양 오류다"는
**"사양 오류가 아니라 정책 선택 오류다"**로 뒤집힌다. 심각도는 낮아지지 않는다 —
**위험을 알면서 12회 내내 그 조항만 재검토하지 않았다**는 뜻이고, 실제로
`max_survival_regression = 0.10` 이라는 숫자의 도출 근거는 **저장소 어디에도 없다**(§12-b).

### 3-b. E2 — 절대 점수가 아니라 delta에 매달렸다

대회는 절대 점수로 채점하는데 모든 판정을 "기준선 대비 delta"로 했다. 두 가지 결과:

1. **계측 비대칭에 전면 노출됐다.** delta는 두 arm이 같은 자로 측정돼야만 의미가 있다(§4-a).
2. **§4-b의 서열표를 보지 못했다.** 절대 점수는 delta와 달리 비대칭의 영향을 받지 않는다.

### 3-c. E3 — 성격 판별 하한 미만에서 서열을 매겼다

`quadruped_rewards.py` 파일 하단 (운영진 작성, 원문):

```
#   0) 그대로 학습(3000 iter) -> play.py 로 "기본 성격" 관찰 (기준점).
#   ※ 걸음 '성격' 은 3000~5000 iter 면 드러납니다 -> 실험은 짧게, 제출본만 길게.
```

`PRELIM_RL_GUIDE.md:180`:

```
-> (1)~(3) 을 반복하며 성격을 잡고, **최종 제출본만 길게**(10000~15000 iter) 학습합니다.
```

**우리는 전부 1000 iter로 스크리닝했고, 최종 장기 학습은 한 번도 하지 않았다.**
운영진이 명시한 성격 판별 하한의 1/3 지점에서 12개 설정의 우열을 가린 셈이다.

다만 이 오류의 **실질적 영향은 §5 정정 2에서 부분적으로 반박된다** — 1000 iter에서도
A017은 `terrain_levels` 4.25로 2위(1.81)의 2.4배였다. 서열이 뒤집힐 만큼 미성숙했다는
증거는 없다.

### 3-d. E4 — 탐색 공간의 1/3을 열지 않았다 → **감사에서 1/9로 축소됨 (정정 6)**

> **정정 6 (§12-b R3·R4, §12-g).** 이 절의 초판 주장 "3개를 12회 실행 동안 한 번도 확인조차
> 하지 않았다"는 **3개 중 2개가 틀렸다.**
> `GO2_CAMPAIGN_SCHEDULE.md:314-316`(260903): "**smoke test는 취소한다.** … 실측 결과
> `undesired_contacts: null`이고 `termination_penalty`는 항 자체가 없다. **두 레버 모두 사용 불가**."
> 근거 G-F77(`GO2_PROJECT_STATE.md:357`), 결정 G-D61(`:368`). 감사관이 로컬 산출물
> `workspace/_keep/go2_g_a025_flat_orientation_m1/training/env.yaml:883` 로 독립 재확인했다.
> **즉 이 두 레버는 놓친 것이 아니라 측정으로 닫은 것이다.** §15-d가 1순위로 지목한 바로 그
> 문서의 두 절 뒤에서, 같은 날 닫혔다.
>
> 또한 초판이 근거로 든 **H1의 `termination_penalty` 효과 인용은 `AGENTS.md:250` 위반**이다 —
> "Go2 작업에서는 H1 시나리오·임계값·학습 단가·reward 결론을 복사해 사실처럼 쓰지 않는다".
> Go2 env에 그 항이 아예 없다는 실측까지 있으므로 논거로 성립하지 않는다. **삭제한다.**
>
> **남는 E4는 `track_ang_vel_z_exp` 1개뿐이다.** 이것도 파일 주석이 "0.75 = 현재 유효값(Go2 기본)
> 이라 그대로 풀면 변화 없음"이라 명시하므로, 값을 바꿔야만 의미가 있다. reward 대장에
> "불변 / 부분 만족" 행으로 등재돼 **의식은 하고 있었으나 보류를 명시한 결정문이 없다**는 것이
> 정확한 형태의 비판이다.

아래는 초판이 인용했던 원문이다(기록 보존용):

```python
# "track_ang_vel_z_exp": 0.75,  # 회전 민첩. 0.75 = 현재 유효값(Go2 기본)이라 그대로 풀면 변화 없음
# "undesired_contacts":  -1.0,  # 배/무릎 닿음 패널티 (Go2 기본값엔 미정의일 수 있음 -> skip 경고 가능)
# "termination_penalty": -200.0,# 넘어짐 벌점 (있으면 적용, 없으면 경고 후 skip)
```

파일 주석이 "줄 추가/삭제/주석(#) 자유"라고 명시하므로 `track_ang_vel_z_exp` 를 여는 것은
규정상 문제가 없다. 나머지 두 항은 **환경에 존재하지 않아** 열 수 없다(위 정정 6).

---

## 4. 계측 결함 — 그리고 그 결함이 **닿지 않는** 곳

### 4-a. 결함: 두 arm이 다른 자로 채점됐다

평가기는 두 세대가 있다.

- `termination_only_v1` (`schema_version: 1`) — 넘어져 종료된 env만 사망 처리
- `posture_gate_v2` (`schema_version: 2`) — `{grace_s 0.5, height_rel_min_m 0.18, hold_s 0.5,
  tilt_cos_max 0.5}`로 **웅크린 채 종료되지 않은** 상태도 사망 처리

기준선 arm은 캐시(`STATUS.txt: SOURCE=VERIFIED_G_A006`)를 재사용하는데, 그 캐시는 260901
시점 v1 산출물이다. 후보 arm은 매번 새로 v2로 채점된다.

**실측 지문** (각 case `summary.json`의 `schema_version`, 7개 case 사전순):

| 실행 | baseline arm | candidate arm | 대칭? |
|---|---|---|---|
| G-A010 | `1111111` | `1111111` | OK (구세대끼리 대칭) |
| G-A013 | `1211111` | `2222222` | **비대칭** |
| G-A015 | `2222222` | `2222222` | OK |
| G-A016 | `2222222` | `2222222` | OK |
| **G-A017** | `2222222` | `2222222` | **OK** |
| G-A018 | `2222222` | `2222222` | OK |
| G-A020 | `1211111` | `2222222` | **비대칭** |
| G-A021 | `1211111` | `2222222` | **비대칭** |
| G-A022 | `1211111` | `2222222` | **비대칭** |
| G-A024 | `1211111` | `2222222` | **비대칭** |
| G-A025 | `1211111` | `2222222` | **비대칭** |

(`1211111`의 두 번째 자리가 2인 이유: `dr_seed_101`은 seed 의존이라 캐시가 없어 매번 새로
계산되고, 따라서 v2로 채점된다.)

**여기서 내 이전 주장 하나가 틀렸다.** 나는 "Pilot-01 branch도 비대칭"이라고 말했었다.
실측 결과 **A015~A018(Pilot-01 branch 4건)은 양쪽 모두 v2로 대칭**이다.
비대칭은 **Default-01 / Chain-01 계열 6건**에 한정된다.
**따라서 A017의 판정 +3.708은 계측 결함과 무관하게 유효하다** — E1의 증거는 오히려 강해졌다.

### 4-b. 결함이 닿지 않는 곳: 절대 점수

비대칭은 **두 arm을 뺄 때만** 오염된다. 각 후보의 `candidate_points_70`은
**그 후보 자신의 rollout을 posture_gate_v2로 채점한 값**이며 기준선과 무관하다.
후보 arm은 A010 하나를 빼고 **전부 v2**임을 위 표에서 확인했다.

**따라서 아래 서열은 재측정 없이 지금 유효하다:**

| 순위 | 정책 | 절대 점수/70 | 채점기 |
|---:|---|---:|---|
| **1** | **G-A017 (Pilot-01 + track 1.4)** | **50.199** | v2 |
| 2 | Pilot-01 기준선 | 46.491 | v2 |
| 3 | G-A015 | 16.369 | v2 |
| 4 | G-A013 = G-A025 | 15.704 | v2 |
| 5 | G-A021 | 12.003 | v2 |
| 6 | G-A022 | 4.883 | v2 |
| 7 | Chain-01 | 2.308 | v2 |
| 8 | G-A018 | 2.093 | v2 |
| 9 | G-A016 | 1.376 | v2 |
| 10 | G-A020 | 0.428 | v2 |
| 11 | G-A024 | 0.000 | v2 |
| — | G-A010 / track_120 | 19.794 / 20.627 | **v1 — 비교 불가** |

**1·2위와 3위 사이에 3배의 단절이 있다.** 이 표는 260904(A017 완료) 시점부터 계산 가능했다.
delta에만 매달리느라 3일을 더 썼다.

---

## 5. 내 이전 주장 중 **틀린 것** (검토자가 알아야 할 정정)

정직성을 위해 먼저 밝힌다. 이 문서를 쓰며 실측으로 확인한 결과 **내가 이전에 사용자에게
말한 두 가지가 사실과 다르다.**

### 정정 1 — "policy.pt가 없어서 제출이 불가능하다" → **틀렸다. 이미 있다.**

```
workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/evaluation/candidate/policy.pt
  크기   1,159,510 B
  sha256 8f0c7d13ed4021515cdf1218180b19a20b2e988224087a9c7fa111647cd45dac
  형식   TorchScript (policy/code/__torch__/rsl_rl/models/mlp_model.py 포함 — 정상 export)

workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/training/env.yaml            (29,578 B)
workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/training/model_best.pt
  sha256 0563deffae52552cdf0343d16b95baaa2bdcc924eca4c53e24f58a0c53d295a4
  == training/logs/rsl_rl/quadruped/2026-09-04_22-24-51/model_900.pt  (비트 동일 -> resume 가능)
```

`env.yaml`의 실제 가중치를 열어 확인: `track_lin_vel_xy_exp 1.4` · `feet_air_time 0.2` ·
`lin_vel_z_l2 −2.0` · `ang_vel_xy_l2 −0.05` · `action_rate_l2 −0.01` · `flat_orientation_l2 0.0`
→ Pilot-01 + track 1.4가 맞다.

**함의: 제출은 지금 당장 가능하며 GPU 0시간이다.** 내가 "제출 불가"라고 말한 것은
확인하지 않고 단정한 결과다. 계획 1단계 비용이 0.3h → **0h**로 바뀐다.

**남은 미확인 1건:** 학습 로그는 `model_best`를 `iter=856, reward=19.7808780670166`으로
기록했는데 파일은 `model_900.pt`와 비트 동일하다. best 선별기가 로그의 최고보상 행을
출력하고 실제로는 저장된 체크포인트(900) 중에서 골랐을 가능성이 높다. `--resume` 시
iter 900부터 재개된다는 뜻이며 실질 영향은 없으나, 검토자가 이의 있으면 지적 바란다.

### 정정 2 — "학습 로그상 로봇이 못 걷는다(terrain 0.79, error_vel 1.51)" → **A017에는 해당 없다.**

그 수치는 Default-01 계열 것이었다. **각 후보의 1000 iter 종료 시점 실측:**

| 실행 | `Curriculum/terrain_levels` | `error_vel_xy` | `time_out` | `base_contact` | 절대 점수 |
|---|---:|---:|---:|---:|---:|
| **G-A017** | **4.2503** | **0.7324** | 0.8939 | 0.1061 | **50.199** |
| G-A015 | 1.8052 | 1.0452 | 0.8951 | 0.1049 | 16.369 |
| G-A018 | 1.1671 | 1.1612 | 0.7928 | 0.2072 | 2.093 |
| G-A021 | 1.0124 | 1.3879 | — | — | 12.003 |
| G-A020 | 0.9690 | 1.2271 | — | — | 0.428 |
| G-A022 | 0.9151 | 1.4092 | — | — | 4.883 |
| G-A025 | 0.8163 | 1.3931 | 0.9600 | 0.0400 | 15.704 |
| G-A016 | 0.0000 | 1.7948 | 0.9775 | 0.0225 | 1.376 |
| G-A024 | 0.0000 | 1.7887 | — | — | 0.000 |

이건 정정을 넘어 **이 문서에서 가장 강한 증거**다:

> **`terrain_levels`와 `error_vel_xy`는 rsl_rl이 학습 중에 기록하는 값으로, posture_gate_v2
> 평가기와 코드·데이터 경로가 완전히 분리돼 있다. 그 독립 계측이 A017을 2위의 2.4배로
> 1등에 놓는다.**

즉 A017의 우위는 결함 있는 계측 하나에 의존하지 않는다. 두 개의 독립된 자가 같은 답을 낸다.
그리고 `time_out 0.894 / base_contact 0.106`은 "안 움직여서 살아남는" 패턴이 아니다 —
A025(0.960 / 0.040)와 A016(0.978 / 0.023)이 그 패턴이고, 그쪽이 점수가 낮다.

### 정정 3 — 내가 제안했던 계획(`--resume`으로 이어 학습)은 **구조적으로 틀렸다**

이 문서 §7을 감사 가능한 형태로 쓰려고 `--resume`의 실제 동작을 확인하다 발견했다.
`workspace/training/quadruped/README.md` 원문:

```
> resume 을 하면 이어받을 때 **지형 난이도(curriculum)가 쉬운 단계로 리셋**됩니다.
> 따라서 이어받은 직후 보상이 잠깐 부풀 수 있어요.
> 그래서 제출본은 가능하면 `from scratch` 로 한 번에 길게 돌리는 게 깔끔해요.
```

**A017의 유일한 강점이 `terrain_levels 4.2503`인데, resume은 바로 그 커리큘럼을 리셋한다.**
내가 "버려지는 학습이 없다"고 말한 근거가 통째로 무너진다. 게다가 운영진이 명시적으로
"제출본은 from scratch로 한 번에 길게"라고 써 둔 것을 나는 읽지 않고 계획을 세웠다.

**연쇄 함의:** 스크리닝(1000 iter) → 승자를 resume으로 장기화, 라는 2단 구조가 성립하지
않는다. 그리고 남은 예산으로는 스크리닝 결과를 from-scratch 장기 학습으로 전환할 여유가
없다(§7-f 대안 B). **즉 스크리닝 자체가 무의미해진다.** §7을 전면 교체했다.

이것은 §3-c(E3)에서 지적한 것과 **같은 종류의 오류를 내가 계획 단계에서 다시 범한** 사례다
— 파일에 답이 적혀 있는데 열지 않고 결론을 세웠다(규칙 G-D110 위반).

---

## 6. 4족이 2족보다 어려운가? (사용자 질문 — E4와 연결)

일반론으로는 이족이 어렵다(정적 안정 지지면이 없다). **우리 경우에 사족이 어려웠던 건
로봇 때문이 아니라 우리가 처한 조건 때문이다.**

### 6-a. 명령 속도 범위 — 걸음 영역(regime)이 다르다

```python
# quadruped/go2_task/env_cfg.py:72        humanoid/h1_task/env_cfg.py:118
lin_vel_x = (-1.0, 2.0)                   lin_vel_x = (-1.0, 1.5)
lin_vel_y = (-0.6, 0.6)                   lin_vel_y = (-0.5, 0.5)
ang_vel_z = (-1.0, 1.0)                   ang_vel_z = (-1.0, 1.0)
```

Froude 수 `Fr = v^2 / (g x L)` (L = 고관절 높이). Alexander의 보행→주행 전이 임계 ≈ 0.5.

| 로봇 | 최대 명령 v | L | **Fr** | 영역 |
|---|---:|---:|---:|---|
| Go2 | 2.0 m/s | ≈0.386 m | **1.06** | **주행 (비행 구간 필요)** |
| H1 | 1.5 m/s | ≈0.90 m | **0.26** | 보행 |

**Go2에게는 "달려라", H1에게는 "걸어라"라고 명령한 것이다.** 추종 보상이 `exp(-err^2/0.5^2)`라
2 m/s 명령에서 초기 보상 ≈ 0 → gradient 없음.

운영진도 이 실패 모드를 알고 있었다 — `env_cfg.py:68-71` 주석 원문:

```
# ⚠️ 이전 (-1.5, 2.5) 는 과확장: 추종 보상이 exp(-err²/0.5²) 라 2 m/s+ 명령은
#   보상 ≈ 0 → 학습 초기 gradient 없음 → "전진 포기 + 제자리 회전" 국소최적
#   (실측: track_ang 만 0.55 학습, track_lin 0.13 · error_vel_xy 1.99 · terrain 0).
#   검증된 IsaacLab Go2 기본은 (-1.0, 1.0). 게임 cap(1.6) 여유분만 남기고 축소.
```

**축소한 뒤에도 2.0은 검증된 IsaacLab 기본값 1.0의 2배다.** 그리고 이 파일은 규정상 우리가
수정할 수 없다.

### 6-b. 그러나 이 논거는 A017이 부분적으로 반박한다 (자기반박)

**동일한 `env_cfg.py`, 동일한 2.0 m/s 명령에서 A017은 `terrain_levels` 4.25를 달성했다.**
환경이 유일한 병목이었다면 어떤 보상 조합도 그 벽을 못 넘었어야 한다. 넘었다.
→ **환경은 난이도를 올렸지만, 우리를 막은 것은 보상 가중치 선택이었다.**

이 자기반박을 남기는 이유: "환경이 어려워서"는 우리 책임을 덜어주는 설명이고, 그런
설명일수록 검증을 세게 해야 한다는 것이 이번 실패의 교훈이기 때문이다(§8).

### 6-c. 나머지 비대칭 (사실만)

| | Go2 | H1 |
|---|---|---|
| 활성 보상 다이얼 | 6 (3개 주석) | 9 |
| `termination_penalty` | **주석 처리됨** | 활성 (−200) |
| `track_ang_vel_z_exp` | **주석 처리됨** | 활성 (1.0) |
| 최장 학습 | 1,000 iter | 10,000 iter |
| 실행 횟수 | 17 (전부 단기) | 1 (장기) |
| 계측 결함 노출 | 있음 | 없음 |

**H1에서 통한 방식(다이얼 다 열고 한 번 길게)을 Go2에는 적용하지 않았다.** Go2에서만
다이얼을 좁히고 짧게 여러 번 돌렸다. 이 방법론 차이가 결과 차이와 정확히 겹친다.

---

## 7. 제안 계획 (제2판) — **§13 감사에서 폐기됨. §14를 보라.**

> **이 절은 기록 보존용이다.** §13 적대적 감사가 이 계획에 `치명` 3건을 냈고, 그중 하나는
> "현재 저장소의 어떤 스크립트로도 S1→S2 흐름을 실행할 수 없다"는 코드 수준 증명이다
> (13-c(d): 러너가 `TRAIN_RC == 0`을 요구하는데 SIGINT 경로는 130을 반환 → `exit 3` → PARTIAL).
> 채택 조건 ②는 **이미 삭제된 체크포인트의 값**을 재고 있었다(13-b F1, W12).
> **대체 계획은 §14다.** 아래는 무엇이 어떻게 틀렸는지 대조하기 위해 남긴다.

> 이 절은 §5 정정 3(resume이 커리큘럼을 리셋한다) 때문에 **전면 교체된 두 번째 판**이다.
> 첫 판은 "1000 iter 스크리닝 2회 → 승자를 `--resume`으로 6000 iter"였고, 그 구조는
> 성립하지 않는다. 검토자는 §7-f(기각한 대안)까지 함께 보고 **선택 자체**를 판정해 달라.

### 7-a. 판정 기준의 교체 (E1 시정)

승급 판정을 **시나리오별 생존 회귀 → 절대 `simulation_points_70`**으로 바꾼다.
`max_survival_regression` 조항은 **삭제**한다. 규정 채점식이 곱셈이므로 생존 하락은
추종 상승으로 상쇄될 수 있고, 그 상쇄를 금지할 근거가 없다.

**대신 두 개의 안전장치를 넣는다** (§5 정정 2의 교훈을 규칙으로 고정):

| 장치 | 내용 | 근거 |
|---|---|---|
| **계측 대칭성** | 비교 대상 두 정책의 `schema_version` · `survival_proxy_source`가 모두 `2` / `posture_gate_v2` 여야 한다. 하나라도 다르면 점수를 계산하지 않고 `EVALUATOR_MISMATCH`로 중단. | 규칙 G-D109 (§4-a 결함의 재발 방지) |
| **독립 계측 교차확인** | 절대 점수가 이겨도 **학습 로그의 `Curriculum/terrain_levels`가 A017의 4.2503 이상**이어야 채택한다. 두 계측이 어긋나면 **채택하지 않고 불일치를 기록**한다. | §5 정정 2 — 우리 프록시(W2)가 틀렸을 경우의 방어선 |

### 7-b. 선택한 계획 — **단일 from-scratch 장기 학습, 시간 박스**

설정: **A017 조합 그대로.** 실측 최고(절대 50.199 / terrain 4.25)이자 유일하게 두 계측이
일치하는 조합이다. 주석 3개는 **풀지 않는다**(이유 7-e).

```python
REWARD_WEIGHTS = {
    "track_lin_vel_xy_exp": 1.4,     # Pilot-01(1.2) 대비 A017의 단일 변경점
    "feet_air_time":        0.2,
    "lin_vel_z_l2":        -2.0,
    "ang_vel_xy_l2":       -0.05,
    "action_rate_l2":      -0.01,
    "flat_orientation_l2":  0.0,
}
```

| # | 작업 | GPU | 누적 | 산출물 |
|---:|---|---:|---:|---|
| **S0** | **A017 제출 세트 확정** — 기존 `policy.pt` TorchScript 로드 검증 + `env.yaml` + 리포트 초안. **이 시점부터 최소 50.199/70이 확보된다.** | **0h** | 0 | 제출 가능 상태 |
| **S1** | 위 조합으로 **from-scratch 학습**, `--max_iterations 10000`으로 띄우고 **벽시계 6.8h에서 하드 스톱** | 6.8h | 6.8 | `model_<iter>.pt` (100 iter마다) |
| **S2** | 최신 체크포인트 → tier-1 평가(seed 101, 7 case, v2) + `play.py` → `policy.pt` + 영상 | 0.5h | 7.3 | 제출 3종 |
| — | 리포트·설계의도 20점 문서 (GPU 0, S1과 **병렬**) | 0h | 7.3 | 리포트 |

**남는 예산 2.7h** — 실패 흡수용이며 선점하지 않는다.

**왜 `--max_iterations`를 고정값이 아니라 10000으로 띄우고 시간으로 자르는가:**
반복 시간 추정(3.41 s/iter, A017 실측)이 지형 난이도 상승에 따라 흔들릴 수 있다.
`save_interval = 100`(`go2_task/agent_cfg.py:21`)이므로 **어느 시점에 잘라도 손실은 최대
100 iter**다. 고정값을 찍고 추정이 빗나가 시간을 넘기는 것보다, 예산을 끝까지 쓰고
가진 것을 export하는 쪽이 항상 낫다.

**예상 도달 iteration:**

| 반복 시간 가정 | 6.8h = 24,480s 로 도달 |
|---|---:|
| 3.41 s/iter (A017 실측, terrain 낮을 때) | ≈ 7,180 iter |
| 3.60 s/iter (지형 상승 여유) | ≈ 6,800 iter |
| 3.80 s/iter (보수적) | ≈ 6,440 iter |

즉 **6,400~7,200 iter**. 1000 iter 대비 **6~7배**이며, A017의 `terrain_levels`가 1000 iter
시점에도 여전히 상승 중이었으므로 이 구간에서 가장 큰 개선이 기대되는 축이다.

### 7-c. 채택 판정 (사전등록 — 실행 전 고정)

최종 제출 = 아래 두 후보 중 절대 점수 최대:

```
후보 1: A017          absolute = 50.199 / 70   (posture_gate_v2, seed 101, 7 case)  [이미 측정됨]
후보 2: S1 장기 학습본  absolute = ?             (동일 조건 · 동일 채점기로 측정)
```

**후보 2를 채택하는 조건 (AND):**
1. `candidate_points_70` > 50.199
2. 최종 `Curriculum/terrain_levels` ≥ 4.2503
3. 두 측정의 `schema_version`이 모두 2

**하나라도 불만족 → A017 제출.** 이 경우 점수 손실은 0이고 GPU 시간만 잃는다.

### 7-d. 실패 분기 (사전등록)

| 분기 | 사건 | 조치 |
|---|---|---|
| B1 | 학습 프로세스가 죽는다 | `logs/rsl_rl/quadruped/<run>/model_<최대iter>.pt`를 `exported/model_best.pt`로 복사 → `play.py`로 export. 손실 ≤ 100 iter |
| B2 | 하드 스톱 시각 도달 | B1과 동일 절차. 이것이 **정상 종료 경로**다 |
| B3 | 장기 학습본이 7-c를 통과 못 함 | A017 제출. 이미 `policy.pt` 보유 → **점수 손실 0** |
| B4 | 서버 세션이 끊긴다 | `tmux` 안에서 실행하므로 학습은 계속. 재접속 후 체크포인트 확인 |
| B5 | "시간이 남으니 resume으로 더" 유혹 | **금지.** 정정 3 — 커리큘럼 리셋. 남는 2.7h는 예비이지 추가 학습분이 아니다 |
| B6 | W7(서버 학습 이력)이 A017 제출을 막는 것으로 판명 | S1이 **이번 세션 서버 학습**이므로 이력이 남는다. 즉 S1은 W7의 해소책이기도 하다 |

### 7-e. 이 계획이 **포기하는** 것 (명시)

| 포기 항목 | 이유 |
|---|---|
| `track_lin_vel_xy_exp` 1.6 탐색 — **정점 미확인** | 스크리닝 결과를 장기 학습으로 전환할 예산이 없다(7-f 대안 B). 전환 못 할 스크리닝은 정보가 아니라 비용이다 |
| `termination_penalty` 등 주석 3개 — **E4 미시정** | 단 한 번뿐인 장기 학습을 **미검증 다이얼**에 거는 것은, 우리가 방금 세운 규칙("측정된 것에 건다")의 정면 위반이다. E4는 옳은 지적이지만 이번 예산에서는 실행 불가 |
| E3 완전 시정 — 6,400~7,200 iter는 가이드의 **10000~15000 미만** | 예산 상한. 10000 iter는 9.7h로 여유 0.3h — 실행 17건 중 3건이 무효였던 이력에 비추어 무모하다(7-f 대안 C) |

### 7-f. 기각한 대안 (검토자가 이 표를 공격해 주기 바란다)

| 대안 | 내용 | 시간 | 기각 이유 |
|---|---|---:|---|
| **A** | 1000 iter 스크리닝 2회 → 승자를 `--resume` 6000 iter (**내 첫 계획**) | 7.4h | **성립하지 않음.** resume이 지형 커리큘럼을 리셋한다(정정 3). A017의 강점이 재개 시점에 소멸 |
| **B** | track 1.4 / 1.6 각 4000 iter from-scratch 2회 + 평가 2회 | 8.6h | 최종 정책이 4000 iter — 가이드 하한(3000~5000) 언저리. **E3를 반복**한다. 여유 1.4h로는 실패 1건도 흡수 못 함 |
| **C** | 10000 iter 단일 from-scratch | 9.7h | 여유 0.3h. 17건 중 3건 무효 이력 감안 시 무모 |
| **D** | GPU 미사용, A017 그대로 제출 | 0h | 50.199/70 = 71.7%에 고정. 남은 예산 전액 미사용은 그 자체로 손실 |
| **채택** | **단일 from-scratch, 시간 박스 6.8h (≈6,400~7,200 iter)** | 7.3h | 여유 2.7h. 실패 시 점수 손실 0(B3). 유일하게 두 계측이 일치한 조합을 씀 |

---

## 8. 캠페인이 **잘한** 것 (균형을 위해)

검토자가 "전부 틀렸다"로 읽지 않도록 명시한다.

1. **산출물 무결성 규율.** 모든 실행에 SHA256·manifest·계보를 강제했다.
   **이 규율이 없었으면 §4-a의 계측 결함을 지금도 못 찾았을 것이고**, "Default-01은 얇은
   균형점"이라는 **틀린 물리 결론 위에서 계속 실험**하고 있었을 것이다.
2. **사전등록(pre-registration).** 게이트를 실행 전에 JSON으로 고정해 사후 재협상을 막았다.
   E1은 사전등록이 **잘못된 게이트**를 고정한 것이지, 사전등록 자체는 옳았다.
3. **posture_gate_v2 통찰 자체는 맞았다.** "웅크린 채 종료되지 않은 상태"를 잡아야 한다는
   판단은 옳았다. 틀린 것은 그 새 자를 한쪽 arm에만 적용한 것이다.
4. **단일변수 통제.** 매 실행 1개 변수만 바꿔 실패가 해석 가능했다.

> **가장 정직한 요약: 엄격한 기계를 만들어 놓고, 그 기계로 잘못된 양을 최적화했다.**

---

## 9. 우리 주장의 약점 (검토자가 공격할 지점)

내가 스스로 찾은 구멍이다. 더 있으면 지적 바란다.

| # | 약점 |
|---|---|
| **W1** | **A017의 50.199는 1000 iter 시점 값이다.** 7000 iter에서 서열이 유지된다는 보장이 없다. E3(1000 iter 서열은 못 믿는다)를 지적하면서 정작 제출 조합을 1000 iter 서열로 고르고 있다 — **자기모순이다.** 정정 3 이후 `--resume`이라는 완화책도 사라졌다(from-scratch라 학습 궤적이 처음부터 다시 간다). **현재 이 구멍에 대한 방어는 §7-c 조건 2(terrain 교차확인)와 §7-d B3(실패해도 A017 제출) 두 개뿐이다.** 즉 "A017 조합이 최선"이라는 전제가 틀렸을 경우 6.8h를 잃는다 — 다만 점수는 잃지 않는다. |
| **W2** | **절대 점수는 우리 자체 프록시다.** 공식 채점기는 비공개이며 우리 `tracking_proxy`의 σ=0.5, 생존 판정 임계값이 공식과 같다는 증거가 없다. 서열이 공식 채점에서 뒤집힐 수 있다. |
| **W3** | **G4 생존 −0.219를 "허용 가능"이라 판단하는 근거는 우리 프록시뿐이다.** 공식 채점의 생존 정의가 우리보다 엄격하면 A017은 실제로 위험할 수 있다. |
| **W4** | **최종 학습이 6,400~7,200 iter로 가이드의 10000~15000에 미달한다.** E3를 완전히 고치지 못했다. 예산 상한에 따른 의도적 타협이며(7-f 대안 C), 이 미달이 점수에 얼마나 영향을 주는지는 **미측정**이다. |
| **W5** | **`termination_penalty`가 Go2 환경에 정의돼 있는지 끝내 확인하지 못했다.** 파일 주석은 "있으면 적용, 없으면 경고 후 skip"이라고 한다. 이번 계획에서 탐색을 포기했으므로(7-e) 이 불확실성은 **해소되지 않은 채 남는다.** 캠페인 전체에서 가장 큰 미탐색 영역이다. |
| **W9** | **단일 실행에 예산의 68%를 건다.** 분산이 0이다. cudnn 비결정성으로 같은 seed·같은 설정이라도 결과가 달라지므로(파일 주석 명시), 이번 from-scratch가 A017만큼 잘 나온다는 보장이 없다. 완화책은 §7-d B3(실패 시 A017 제출)뿐이며, 이는 "손실 0"이 아니라 "**개선 0**"이다. |
| **W10** | **§7-c 조건 2(terrain ≥ 4.2503)의 임계값에 이론적 근거가 없다.** A017의 실측값을 그대로 가져온 자의적 기준이다. 7000 iter 학습이 1000 iter보다 terrain이 낮을 이유는 없으므로 실질적으로는 거의 항상 통과할 것이고, 그렇다면 이 장치는 **형식적일 뿐 실효가 없을 수 있다.** |
| **W11** | **"스크리닝 결과를 장기 학습으로 전환할 예산이 없으므로 스크리닝은 무의미하다"는 논증이 순환일 수 있다.** 전환 못 해도 스크리닝은 다음 라운드(있다면)의 정보가 된다. 남은 라운드 수가 미측정(W8)이므로 이 논증의 전제가 확인되지 않았다. |
| **W6** | **A010과 track_120의 절대 점수(19.794 / 20.627)는 v1이라 서열표에 못 넣었다.** 재채점하려면 rollout 원본이 필요한데 캐시에 `steps.csv`가 없어 로컬 재계산이 불가능하다. 이 둘이 실은 3위권일 가능성을 배제 못 한다. |
| **W7** | **서버 학습 이력 대조(규정 제14조) 리스크 미확인.** 서버는 접속마다 초기화되는 휘발성 환경이다. A017(260904 학습, `run_id: train_260904-Go2_pilot_track_lin_vel_xy_140_1000`)의 이력이 대회 서버에 남아 있는지 확인한 적이 없다. 남아 있지 않다면 제출 자체가 문제될 수 있다. **GPU를 안 쓰고 확인해야 하는 최우선 항목이다.** |
| **W8** | **남은 라운드 수와 마감 시각이 여전히 미측정.** 10h가 마지막 예산인지 라운드당 예산인지 확인되지 않았다. |

---

## 10. 검토자에게 묻는 질문

**Q1.** §3-a의 E1(게이트–목적함수 불일치)이 "17회 0채택"의 주원인이라는 판단에 동의하는가?
A017 판정 원문(7개 중 6개 개선, 총점 +3.708, 기각 사유 1개)이 이 주장을 지지하는가,
아니면 내가 하나의 사례를 과대해석하고 있는가?

**Q2.** §4-b의 논리 — "계측 비대칭은 delta만 오염시키고 절대 점수는 유효하다" — 가 맞는가?
후보 arm이 전부 v2로 채점됐다는 §4-a 표가 이 결론을 뒷받침하기에 충분한가?
내가 놓친 오염 경로가 있는가?

**Q3.** §5 정정 2의 교차검증 논거 — "rsl_rl 학습 로그(`terrain_levels`, `error_vel_xy`)는
평가기와 독립이므로 A017의 우위를 독립 확인한다" — 가 성립하는가?
두 계측이 실제로는 공통 원인을 공유해서 독립이 아닐 가능성은?

**Q4.** §9 W1(자기모순: E3를 지적하면서 1000 iter 서열로 조합을 선정)이 계획을 무효화할
정도인가? 정정 3으로 `--resume` 완화책이 사라진 지금, 이 구멍을 메울 다른 방법이 있는가?

---

### Q5 계열 — **§7 계획 자체에 대한 감사** (이 문서의 핵심 요청)

**Q5-a. 대안 선택이 옳은가?** §7-f 표에서 나는 **채택안(단일 from-scratch 6.8h 시간 박스)**
을 골랐다. 대안 A는 정정 3으로 성립하지 않는다는 게 확실하지만, **B(4000 iter 두 개)와
C(10000 iter 하나)의 기각 근거는 논쟁적**이다.
  - B 기각 근거: "최종 정책이 4000 iter라 E3를 반복한다." → 그러나 **두 조합을 신뢰할 수
    있는 길이에서 실제로 비교**한다는 이점이 있다. 이게 7000 iter 단일보다 나쁜가?
  - C 기각 근거: "여유 0.3h는 무모하다." → 그러나 `save_interval=100`이라 언제 잘려도
    체크포인트가 남는다면, C는 사실상 채택안과 같고 도달 iteration만 높은 것 아닌가?
    (내가 C를 별도 대안으로 세운 것 자체가 틀렸을 수 있다.)

**Q5-b. 시간 박스 6.8h가 옳은가?** 예비 2.7h는 과한가, 모자란가? 17건 중 3건 무효라는
이력(무효율 18%)에서 6.8h 실행의 실패 확률을 어떻게 잡아야 하는가?

**Q5-c. 설정 선택이 옳은가?** A017 조합(track 1.4)을 **한 글자도 안 바꾸고** 장기화하는
것이 맞는가, 아니면 장기 학습에서는 짧은 학습과 최적 가중치가 다를 수 있으므로 조정해야
하는가? 특히 `feet_air_time 0.2`·`lin_vel_z_l2 −2.0`은 1000 iter 기준으로 고른 값이다.

**Q5-d. §7-c 채택 조건이 실효가 있는가?** W10 지적대로 조건 2(terrain ≥ 4.2503)가
형식적이라면, "우리 프록시가 틀렸을 경우"(W2)를 실제로 잡아내는 다른 교차확인이 있는가?

**Q5-e. E4를 포기한 판단이 옳은가?** §7-e는 "단 한 번의 장기 학습을 미검증 다이얼에 걸 수
없다"는 이유로 `termination_penalty`를 포기했다. 그런데 그 다이얼은 H1에서 가장 큰 효과가
측정된 것이다. 예산의 15%(1.2h)를 떼어 확인하고 나머지를 장기 학습에 쓰는 편이 나은가?

**Q5-f. W11이 맞는가?** "전환 못 할 스크리닝은 비용"이라는 §7-e의 논증은, 남은 라운드가
있다면 틀린다. 이 논증의 결함이 계획을 바꿀 만큼 큰가?

**Q6.** §6-a의 "Go2가 어려웠던 건 로봇이 아니라 Fr=1.06 명령 범위 때문"이라는 설명과,
§6-b의 자기반박("A017이 같은 조건에서 terrain 4.25를 냈으므로 환경이 유일 병목은 아니다")
중 어느 쪽이 더 옳은가? 이 진단이 남은 시간의 실행에 영향을 주는가, 아니면 사후 설명뿐인가?

**Q7.** W7(서버 학습 이력 대조 가능 여부)을 GPU 없이 확인할 방법이 있는가?
없다면 이것이 A017 제출을 막는 리스크로 취급돼야 하는가?

**Q8.** 우리가 **완전히 놓친** 것이 있는가? §3의 오류 4개 외에 이 대장(§2)과 데이터에서
보이는 다른 기획 결함이 있다면 지목해 달라.

---

## 11. 증거 색인 (전부 로컬에서 재현 가능)

기준 경로: `C:\dev\Nconnect\`

| 주장 | 파일 | 확인 방법 |
|---|---|---|
| A017 판정·게이트·시나리오 delta | `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/reports/TIER1_DECISION.json` | 직접 열람 |
| track_120 판정 (+3.090, target 조항 기각) | `workspace/_keep/go2_track_lin_vel_120_v1/reports/TIER1_DECISION.json` | 직접 열람 |
| 절대 점수 서열 (§4-b) | 각 `workspace/_keep/go2_g_a0*/reports/TIER1_DECISION.json`의 `candidate_points_70` | 전체 순회 |
| 계측 지문 표 (§4-a) | 각 `evaluation/{baseline_tier1,candidate}/cases/seed_101/*/summary.json`의 `schema_version`, `survival_proxy_source` | 전체 순회 |
| A017 제출 3종 존재 (§5 정정 1) | `.../go2_g_a017.../evaluation/candidate/policy.pt`, `.../training/env.yaml`, `.../training/model_best.pt` | `sha256sum`, `zipfile`로 TorchScript 확인 |
| A017 env.yaml 가중치 = Pilot-01 + track 1.4 | `.../training/env.yaml` 823-827(1.4) · 835-837(−2.0) · 839-841(−0.05) · 851-853(−0.01) · 876-882(0.2) · 885-887(0.0) | 직접 열람 |
| 학습 로그 지표 (§5 정정 2) | 각 `workspace/_keep/go2_g_a0*/logs/candidate_training.log`의 `Learning iteration 999/1000` 블록 | `awk` 범위 추출 |
| 1000 iter 소요 시간 | 위 로그의 `Training time: 3407.67 seconds` | 직접 열람 |
| 주석 처리된 3개 다이얼 (§3-d) | `workspace/training/quadruped/quadruped_rewards.py` 87-93행 | 직접 열람 |
| 3000 iter 하한 (§3-c) | `workspace/training/quadruped/quadruped_rewards.py` 하단 "추천 실험 순서" | 직접 열람 |
| 명령 속도 범위 (§6-a) | `workspace/training/quadruped/go2_task/env_cfg.py:67-74` / `workspace/training/humanoid/h1_task/env_cfg.py:117-120` | 직접 열람 |
| **resume이 커리큘럼을 리셋 (§5 정정 3)** | `workspace/training/quadruped/README.md` "이어서 학습 (resume)" 절 말미 인용문 | 직접 열람 |
| `save_interval = 100` (§7-b 시간 박스 근거) | `workspace/training/quadruped/go2_task/agent_cfg.py:21` | 직접 열람 |
| `play.py`는 `exported/model_best.pt`를 재생하고 `--checkpoint` 직접 지정도 존중 (§7-d B1) | `workspace/training/quadruped/play.py:89-95, 151-155` | 직접 열람 |
| resume 시 best 선별 warmup 제외 로직 | `workspace/training/quadruped/go2_task/_finalize.py:75-81` | 직접 열람 |
| A025 = A013 비트 동일 | 두 `TIER1_DECISION.json`의 `candidate_points_70` 16자리 일치 (15.704282168220187) | 비교 |
| 전체 실행 이력·결정 원장 | `GO2_PROJECT_STATE.md` (§33, §33-e) | 직접 열람 |
| 문헌 근거 원장 | `GO2_REWARD_EVIDENCE_MASTER.md` | 직접 열람 |
| 규정 인용 | `workspace/training/PRELIM_RL_GUIDE.md`, 예선 규정집 제2·4·8·10·14조 | 직접 열람 |

---

*이 문서는 검수를 받기 위해 작성됐다. 반박·수정·추가 지적을 그대로 받아 §12로 추가한 뒤
계획을 갱신한다. 지적을 반영해 결론이 바뀌면 §0 요약을 먼저 고친다.*

---

## 12. 메타 감사 — 과거 감사가 왜 E1~E4에 도달하지 못했는가

> 독립 감사관(읽기 전용)이 작성한 원문을 그대로 싣는다. 대상: `GO2_PROJECT_STATE.md`(원장),
> `GO2_REWARD_EVIDENCE_MASTER.md` §16~§18, `GO2_CAMPAIGN_SCHEDULE.md`, `AGENTS.md`,
> 게이트 엔진 코드·계약 테스트, 각 실행의 `TIER1_DECISION.json` 원본.
> **이 감사는 §0~§11의 주장 5건을 반박했다.** 반박은 §12-b에 있고, 그에 따른 본문 정정은
> §12-f에 정리했다.

### 12-a. 과거 감사가 놓친 것 — 발견 가능 시점 회계

| 오류 | 최초 발견 가능 시점 | 그때 있었던 증거 | 왜 못 봤나 | 근거(파일:줄) |
|---|---|---|---|---|
| **E1** (게이트 ↔ 곱셈 목적함수 불일치) | **260902** — 게이트 조항이 처음 문서화된 날 | 공식 곱셈식은 이미 260831자 규칙으로 명문화돼 있었다(`시나리오 점수 = survival_rate × tracking_score`). 같은 날 사전등록된 PRD가 `survival … 0.10 초과 하락`을 **파생 근거 없이** 조기종료 사유로 못박았다. | 조항을 **도입할 때** 목적함수와 대조하는 절차가 없었다. PRD §4는 조항 3개를 나열만 하고 각 조항이 목적함수의 어느 항을 대리하는지 쓰지 않는다. `minimum_points_70=60`·`required_survival_proxy=0.95`·`required_tracking_proxy=0.70`은 근거가 원장에 있으나(G-D32, "사용자 결정 260902"), **`max_survival_regression=0.10`만은 저장소 전체에서 도출 근거가 발견되지 않는다**(전 문서 grep 결과 적용 사례만 존재). | `workspace/PRELIM_RL_GUID.md:95`, `AGENTS.md:158`, `workspace/training/quadruped/upload/plan/GO2_TRACK_LIN_VEL_120_SCREENING_PRD.md:60-66`(특히 `:64`), `GO2_PROJECT_STATE.md:212`(G-D32), `workspace/training/quadruped/go2_tiered_eval_report.py:34-41`(0.10 하드코딩) |
| **E1** (두 번째 창) | **260903** — 게이트를 목적함수 기준으로 실제로 개정한 날 | 이날 게이트 3개 조항 중 **`target_scenario` 하나는 목적함수와 대조돼 강등**됐다("규정이 채점하는 값은 `Σ(weight × 생존율 × 추종 점수)`이므로 게이트도 같은 값을 본다"). 같은 문장 안에서 생존 가드는 **대조 없이 존치**됐다. | 대조를 **한 번은 했으나 조항 단위로 전수하지 않았다.** 존치 사유는 "총점만 올리고 생존을 무너뜨리는 후보를 계속 차단"이라는 **위험선호 진술**이지 목적함수 대조가 아니다. | `GO2_PROJECT_STATE.md:398`(G-D68), `GO2_CAMPAIGN_SCHEDULE.md:355-366`(§18-b), `workspace/training/quadruped/go2_tuning_engine_v1_3.VERIFICATION.md:20-34`, `workspace/training/quadruped/go2_tuning_eval_report.py:28-31` |
| **E1** (결정적 창) | **260904** — G-A017 회수·분석일 | **결정 JSON이 이미 곱셈 delta를 계산해 인쇄해 놓았다.** `scenario_deltas.G4.proxy = 0.015871072796955432`(직접 열람 확인). 원장은 **그 필드가 든 바로 그 파일을 근거로 인용**하면서 `survival −0.21875`와 `tracking +0.17525` 두 인수만 옮겨 적고 곱을 옮기지 않았다. | 게이트가 발화한 시나리오의 **곱셈 프록시 부호를 확인하는 단계가 절차에 없었다.** 두 인수를 각각 기록하는 관행(`AGENTS.md:188` "둘 다 측정한다")은 있었지만 **곱을 보라는 규칙은 없었다.** | `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/reports/TIER1_DECISION.json`(`scenario_deltas.G4.proxy`), `GO2_PROJECT_STATE.md:464-465`(G-F119·G-F120), `:471`(G-D79), `:428`(G-D74) |
| **E2** (절대 점수 서열 부재) | **260904** — A017의 절대 점수가 기준선을 넘은 순간 | `candidate_points_70 = 50.19916` > `Pilot-01 46.49124`가 **같은 판정문 한 줄에** 나란히 기록됐다. 회차별 절대값은 G-F102·G-F113·G-F119·G-F125·G-F148·G-F152·G-F156으로 전부 원장에 있다. | 값은 있었으나 **회차를 가로지르는 표로 모으는 산출물이 없었다.** 원장의 "순위" 표는 전부 *한 정책 내부의 시나리오별 가중 실점 순위*이지 정책 간 절대 서열이 아니다. | `GO2_PROJECT_STATE.md:464`(G-F119), `:413`, `:443`, `:485`, `:600`, `:616`, `:640`; 대조 — 실점 순위표는 `:337`, `GO2_CAMPAIGN_SCHEDULE.md:378` |
| **E3** (1000 iter 서열) | **260903** — 계획과 실행이 같은 날 서로 어긋난 지점 | 캠페인 일정표는 단계 C를 **`단일 변수 학습 3,000~5,000 iter`**로 예산까지 붙여 사전등록했는데, 같은 문서 14절 뒤 G-A013 실행표는 **`1,000 iter`**로 고정한다. 두 값 사이를 잇는 **결정(G-D###)은 저장소에 없다**(전 문서 grep). 또한 "screening 1k → 폐기 / 3k~5k 확장" 재평가 지점이 사전등록돼 있었으나 12회 실행 동안 **한 번도 발화하지 않았다.** | 게이트가 매번 `INTERNAL_EARLY_KILL_FAIL`을 내면서 "3k 확장" 분기 자체가 도달 불가능해졌다 — **E1이 E3를 은폐한 구조**다. | `GO2_CAMPAIGN_SCHEDULE.md:255`(계획 3,000~5,000) vs `:326`(실행 1,000), `:50`(재평가 지점 4), `:36`; 실행 고정값 `:434`, `:502`, `:568`, `:660`, `:728` |
| **E3** (두 번째 창) | **260905** — 문헌 감사일. **이미 원장에 적혔다.** | §17-b가 "**애초에 1,000 iter가 빠른 gait를 형성하기엔 짧을 수 있다 … 우리 1,000 iter 스크리닝은 방향 탐색용이지 최종 gait 형성용이 아니었을 가능성**"이라고 명시하고, §17-d 4행이 "iteration 수를 3,000~5,000으로 늘린 동일 reward 재실행"을 사전등록했다. | 가설을 **적어만 두고 우선순위 4번(1·2·3 전부 실패 시)으로 밀어 넣었다.** 1·2·3이 실제로 전부 실패했는데도(G-F153) 4번 대신 G-D103(무변경 대조군)으로 갈아탔다. | `GO2_REWARD_EVIDENCE_MASTER.md:257-259`(§17-b), `:277`(§17-d 4행), `GO2_PROJECT_STATE.md:617`(G-F153), `:623`(G-D103) |
| **E4** (3개 다이얼 미개봉) | **260903** — 2개는 이 날 **측정으로 닫혔다**(12-b R3 참조) | 서버 학습 산출 `env.yaml` 실측: `undesired_contacts: null`, `termination_penalty` 항 자체 부재. 감사관이 로컬 산출물에서 직접 재확인(`env.yaml`의 rewards 블록 존재 항목은 `track_lin_vel_xy_exp`·`track_ang_vel_z_exp`·`feet_air_time`·`undesired_contacts(null)`·`flat_orientation_l2`·`dof_pos_limits`·`time_out`·`base_contact`뿐). | 이 두 항은 **놓친 것이 아니라 닫은 것**이다. 남은 한 항 `track_ang_vel_z_exp`만이 진짜 미탐색이며, 이것도 reward 대장에 "불변 / 부분 만족" 행으로 등재돼 **의식은 하고 있었으나 보류를 명시한 결정문이 없다**. | `GO2_PROJECT_STATE.md:357`(G-F77), `:368`(G-D61), `GO2_CAMPAIGN_SCHEDULE.md:314-316`, `workspace/_keep/go2_g_a025_flat_orientation_m1/training/env.yaml:883`, `GO2_REWARD_EVIDENCE_MASTER.md:25`, `:26` |

**E1에 대한 종합 판정.** "게이트가 목적함수와 불일치한다"는 사실 주장은 **유지**된다(A017의 `G4.proxy=+0.0159`가 조항 발화와 정면 충돌). 그러나 **"우리가 몰랐다"는 서사는 원장이 반박한다** — 이 동작은 계약 테스트에 *의도적으로 고정*돼 있었다:

```python
tools/test_go2_tuning_engine_contract.py:199-200
def test_survival_regression_still_kills_a_total_positive_candidate(self) -> None:
    """G-A013 shape: a candidate must not buy points by collapsing survival."""
```
```python
tools/test_go2_tuning_engine_contract.py:225-226
def test_pinned_scenario_gate_no_longer_kills_a_net_positive(self) -> None:
    """Regression guard for the measured G-A010 and G-A011 mis-kills."""
```

같은 파일에 "**총점 양수를 죽이는 것은 오살(mis-kill)이므로 막는다**"는 테스트와 "**총점 양수라도 생존이 무너지면 죽인다**"는 테스트가 **나란히** 있다. 즉 260903 시점에 캠페인은 "총점 양수 후보를 죽이는 조항"의 개념을 이미 갖고 있었고, 생존 조항에 대해서만 **의식적으로 반대 선택**을 했다. E1은 사양 오류이되 **무지의 산물이 아니라 명시적으로 채택·테스트·문서화된 위험정책**이다. 이 구분이 §3-a의 "이건 판단 실수가 아니라 사양 오류다"라는 문장과 어긋난다 — 원장 기준으로는 **정책 선택 오류**다.

### 12-b. 내 주장 중 원장이 반박하는 것

**5건 반박, 2건 부분 반박.**

**R1 — "target_scenario 강등 때 그 조항이 죽인 +3.090짜리 결과는 재검토하지 않았다"(§3-a) → 전면 반박.**
되돌아본 정도가 아니라 **그 결과가 강등의 근거 그 자체**였고, 이후 GPU 예산까지 써서 재측정했다.

1. 강등 근거표에 그 실행이 1행으로 올라 있다 — `GO2_CAMPAIGN_SCHEDULE.md:359` `| G-A011 track_lin_vel_xy_exp 1.0→1.2 | +3.0903 | 없음 | FAIL (G1 고정 목표) |`, 같은 표가 엔진 검증문에도 있다(`workspace/training/quadruped/go2_tuning_engine_v1_3.VERIFICATION.md:26-30`).
2. 원장 확정 사실로도 기록됐다 — `GO2_PROJECT_STATE.md:387`(G-F92) "총점을 올린 두 실험은 지정 시나리오 절만으로 조기 종료됐다."
3. 엔진 코드 주석에 그 세 실행 이름이 박혀 있다 — `workspace/training/quadruped/go2_tuning_eval_report.py:28-31`.
4. 260905에 그 체크포인트를 **동결 기준선 Chain-01로 승격**했다 — `GO2_CAMPAIGN_SCHEDULE.md:701-712`, `GO2_PROJECT_STATE.md:507`(G-F132), `:544`(G-D84).
5. 260905에 "G-A011이 지정 시나리오 절만으로 조기 종료됐기 때문"이라며 **학습 없는 전용 재측정 실행(G-A023)을 발주**했다 — `GO2_PROJECT_STATE.md:545`(G-D90).
6. 그 재측정 결과 그 정책의 참값은 **`2.307745/70`, 6/7 시나리오 생존 전멸**이었다 — `GO2_PROJECT_STATE.md:553`(G-F139), `:555`(G-F141).

**R2 — "우리가 측정한 두 개의 가장 큰 개선(+3.708, +3.090)"(§3-a) → 반박. 그리고 검수 문서 내부 모순.**
`+3.090`짜리는 개선이 아니었음이 260905에 실측으로 확정됐다(R1-6). 그리고 **이 검수 문서 자신의 §4-b 서열표가 그 정책(Chain-01)을 7위 `2.308`에 놓고 있다.** 같은 문서 안에서 §3-a는 그것을 "죽은 최대 개선"으로, §4-b는 "7위 열등 정책"으로 다룬다. E1의 사례는 **A017 1건**으로 축소해야 하며, "두 건" 주장은 철회 대상이다.

**R3 — E4 "12회 실행 동안 확인조차 하지 않았다"(§3-d) → 3개 중 2개 반박.**
`GO2_CAMPAIGN_SCHEDULE.md:314-316`(260903): "**smoke test는 취소한다.** … 실측 결과 `undesired_contacts: null`이고 `termination_penalty`는 항 자체가 없다. **두 레버 모두 사용 불가**." 근거는 `GO2_PROJECT_STATE.md:357`(G-F77), 결정은 `:368`(G-D61). 감사관이 로컬 산출물로 독립 재확인했다(`workspace/_keep/go2_g_a025_flat_orientation_m1/training/env.yaml:883` `undesired_contacts: null`, rewards 블록에 `termination_penalty` 부재). 즉 §15-d가 1순위로 지목한 두 레버는 **지목한 그 문서의 두 절 뒤에서, 같은 날, 측정으로 닫혔다.** "확인조차 하지 않았다"는 사실과 반대다. **미탐색이 남는 것은 `track_ang_vel_z_exp` 1개뿐이며, E4는 "9개 중 3개"가 아니라 "9개 중 1개"로 정정돼야 한다.**

**R4 — E4의 H1 근거 인용(§3-d) → 규칙 위반 지적.**
인용한 수치 자체는 정확하다(`H1_REWARD_EVIDENCE_MASTER.md:47`, `PROJECT_STATE.md:62-63`). 그러나 `AGENTS.md:250`은 "**Go2 작업에서는 H1 시나리오·임계값·학습 단가·reward 결론을 복사해 사실처럼 쓰지 않는다**"고 명시한다. Go2 env에 그 항이 없다는 실측(R3)까지 감안하면 이 논거는 성립하지 않는다.

**R5 — E3 "1000 iter가 짧다"는 260907의 새 결론이 아니다 → 반박(신규성).**
`GO2_REWARD_EVIDENCE_MASTER.md:257-259`(260905)에 이미 문장으로 있고, `:277`이 3,000~5,000 재실행을 사전등록했다. **검수 문서는 이를 인용하지 않는다.** 다만 E3의 *다른* 절반(운영진 파일 주석 대조 부재)은 12-c에서 유지된다.

**R6(부분) — §2 실행 대장의 "판정" 열이 원장의 260907 결정보다 낡았다.**
§2는 G-A025를 `FAIL`로 적지만, **같은 날 원장은 이미 `UNDETERMINED`로 재분류**했다(`GO2_PROJECT_STATE.md:657`, G-D104). 같은 이유로 A010·A020·A021·A022·A024의 `FAIL`도 `:659`(G-D105)에서 **잠정으로 강등**됐다. §2 표는 원장의 자기 정정을 반영하지 않았다.

**R7(부분) — §2의 절대 점수 열이 채점기 세대를 섞는다.**
`track_lin_vel_120 20.627`에 세대 표기가 없다. 감사관이 후보 arm의 `schema_version`을 전수 확인한 결과, 이 실행과 `go2_g_a010_lin_vel_z_m2`의 후보 arm만 `schema_version:1`(v1)이며 나머지 11건은 전부 `2`다. §4-b는 이를 "**v1 — 비교 불가**"로 정확히 표시하므로 문서 전체로는 자기수정이 되어 있으나, §2만 읽는 독자에게는 v1/v2가 한 열에 섞여 보인다. 그리고 이 혼합은 **G-D109(계측 지문 일치 후에만 판정)가 금지하려던 바로 그 오류의 회차 간 판본**이다.

**찾았으나 원장이 반박하지 못한 것(= 주장 유지):**
- `max_survival_regression=0.10`의 **도출 근거는 저장소 어디에도 없다.** 전 문서 grep(`0.10 초과`, `후퇴 상한`, `regression.*0.10`) 결과 적용 사례 8건뿐이고, 유일한 근거 진술은 `GO2_CAMPAIGN_SCHEDULE.md:363-366`의 목적 서술이다. **[미확인] — 0.10이라는 숫자의 출처는 확인하지 못했다.**
- **회차를 가로지르는 절대 점수 서열표는 원장·일정표·근거 원장 어디에도 없다.** E2의 사실 주장은 그대로 유지된다.

### 12-c. 감사 절차의 구조적 결함

#### (a) "감사가 항상 직전 실패만 대상으로 삼고 캠페인 전체 설계는 대상으로 삼지 않았다" → **기각**

원장에 캠페인 스코프 감사가 최소 3건 있다.

- **260903 · 게이트 사양 자체를 감사** — 개별 실행이 아니라 **측정된 4건 전체**를 한 표에 놓고 조항의 부호 정합성을 판정했다: `GO2_CAMPAIGN_SCHEDULE.md:355-366`(§18-b "게이트 개정 — 실행 전 선언"), `GO2_PROJECT_STATE.md:398`(G-D68).
- **260905 · 캠페인 리셋** — 이틀 전 자기 결정(G-D69)을 감사해 뒤집었다: `GO2_PROJECT_STATE.md:504`(G-F130), `:543`(G-D83).
- **260905 · 상설 지시 이행 감사** — 특정 실행이 아니라 §2 대장 **전체**를 대상으로 "논문 인용 0건"을 판정했다: `GO2_REWARD_EVIDENCE_MASTER.md:198-205`.
- **260907 · 다중 실행 원인 분석** — G-F166은 G-A020·G-A023·A010v2/A024/A025 **세 시점을 가로질러** 하나의 공통 원인을 지목한다: `GO2_PROJECT_STATE.md:675`.

가설의 강한 형태는 성립하지 않는다. 남는 잔여 사실은 "**모든 감사의 착화점이 결과 회수였다**" — 결과와 무관하게 예정된 설계 감사는 260907 이전에 0건이다(그 자체는 (d)에서 다룬다).

#### (b) "감사가 새 규칙을 늘리기만 하고 기존 규칙/게이트 조항을 폐기한 적이 없다" → **기각**

| 폐기된 것 | 폐기 결정 | 근거(파일:줄) |
|---|---|---|
| G-D59(10-iter smoke test) | G-D61이 **대체** | `GO2_PROJECT_STATE.md:368` |
| tier-1 고정 시나리오 게이트 | G-D63 → G-D68이 **강등·교체** | `:370`, `:398` |
| 사전등록된 후속 G-A014 | G-D67이 **취소** | `:394` |
| 동결 기준선 Pilot-01(G-D69) | G-D83이 **완전 철거** | `:543` |
| Chain-01 기준선(G-D84) | G-D91이 **즉시 하차** | `:559` |
| G-D99·G-D101(다이얼 최종 기각) | G-D105가 **잠정으로 강등** | `:659` |
| G-D103(무변경 대조군 우선) | G-D105가 **보류** | `:659` |
| G-D91·G-D92 | G-D108이 "**근거 무효, 재검토 필요**"로 표시 | `:661` |
| G-A019(사전등록 실행) | 리셋으로 **실행하지 않음** | `:498` |

오히려 문제는 반대 방향이다 — **폐기가 잦고 근거 무효 표시가 누적되면서, 폐기되지 않고 12회 내내 살아남은 유일한 조항(`max_survival_regression`)이 "검토를 통과한 조항"처럼 보이게 됐다.** 실제로는 한 번도 검토 대상이 된 적이 없다.

#### (c) "감사가 절차 위반만 찾고 목적함수와의 정합성은 찾지 않았다" → **기각(단, 1회성)**

> `GO2_CAMPAIGN_SCHEDULE.md:363-364` — "4건 전부에서 고정 시나리오 절과 목적함수의 부호가 반대다. 규정이 채점하는 값은 `Σ(weight × 생존율 × 추종 점수)`이므로 **게이트도 같은 값을 본다.**"

이것은 정확히 "목적함수와의 정합성" 감사이며, 그 결과 조항 하나가 실제로 강등됐다(`GO2_PROJECT_STATE.md:398`). 코드에도 반영됐다(`workspace/training/quadruped/go2_tuning_eval_report.py:28-31`).

**따라서 기각.** 다만 잔여 결함을 정확히 특정한다: 이 대조는 **① 단 한 번, ② 게이트 조항 3개 중 1개에만, ③ 총점 수준에서만** 수행됐고 **시나리오 수준의 곱(`scenario_proxy`)에는 적용되지 않았다.** 바로 그 미적용 지점에서 A017이 죽었다. "정합성 감사가 없었다"가 아니라 "**정합성 감사가 부분 적용된 뒤 재개되지 않았다**"가 옳은 진술이다.

#### (d) "감사관과 피감사자가 동일해서 자기 계획의 전제는 감사 대상에서 빠졌다" → **채택**

- **동일성 사실.** 260901~260906의 모든 감사(§16, §22, §27, §28, §33)는 캠페인을 수행한 동일 주체가 작성했다. `AGENTS.md:260`이 정의하는 `go2-evaluation-auditor`도 같은 파이프라인 내부 역할이다. 저장소 최초의 외부 감사는 **260907**자 `GO2_INDEPENDENT_AUDIT_CODEX_260907.md:3-5`이며 독립성 통제를 명시한다 — 즉 E1~E4 미발견 구간 전체가 자기감사 구간이다.
- **결과가 실제로 나타난 증거.** 원장이 스스로 이 실패 양식을 확정했다:
  - `GO2_PROJECT_STATE.md:672`(G-F164) "**결론(재측정 필요)을 먼저 세우고 그에 맞는 증거 둘을 찾은 뒤 멈춘 확증편향 형태다.**"
  - `:673`(G-F165) "후보 arm만 검증하고 기준선 arm은 **검증 대상에서 제외**했다."
  - `:677`(G-F168) "논문을 **인용**은 했으나 논문처럼 **일하지는** 않았다."
- **자기 계획의 전제가 빠진 구체적 사례 2건.**
  1. **게이트 사양은 12회 중 단 1회만 피감사 대상이 됐다**(260903, 그것도 1개 조항). 나머지 11회의 감사는 전부 "이번 후보가 왜 실패했나"였고 "**이번 판정 기준이 옳은가**"는 묻지 않았다 — G-F119가 총점 개선과 기각을 한 줄에 적고도 G-D79가 그대로 기각을 집행한 것이 그 표본이다(`GO2_PROJECT_STATE.md:464`, `:471`).
  2. **스크리닝 길이는 단 한 번도 피감사 대상이 아니었다.** 계획(3,000~5,000, `GO2_CAMPAIGN_SCHEDULE.md:255`)과 실행(1,000, `:326`) 사이의 하향 변경을 기록한 결정문이 없고, 사전등록된 "3k~5k 확장" 재평가 지점(`:50`)이 12회 동안 한 번도 평가되지 않았다.
- **반증 시도(공정성).** 자기감사가 무력했다고는 말할 수 없다 — 검수 문서 §5는 자기 주장 3건을 실측으로 반증했고, G-F146·G-F157·G-F126은 자기 기록을 정정한 사례다. 그러나 **정정된 것은 언제나 "사실"이었고, "판정 기준"이 정정된 것은 260903 단 1회다.** 채택의 근거는 이 비대칭이다.

### 12-d. G-D109~112 의 E1~E4 예방력 판정

원문: `GO2_PROJECT_STATE.md:681`(G-D109) · `:682`(G-D110) · `:683`(G-D111) · `:684`(G-D112).

| 규칙 | E1 | E2 | E3 | E4 | 판정 근거 |
|---|---|---|---|---|---|
| **G-D109** 계측 대칭성 불변식 | **불가** | **불가** | 불가 | 불가 | A017은 양쪽 arm이 모두 v2로 **대칭**이었다. 지문 검사를 통과한 뒤 게이트가 정상 발화해 죽인 사례이므로 이 규칙은 발화조차 하지 않는다. E2에도 무력하다 — 규칙 대상이 *한 판정문 안의 두 arm*이지 *회차 간 절대값 비교*가 아니어서, §2 표 같은 v1/v2 혼합 서열은 막히지 않는다(R7). |
| **G-D110** 전제 검증 규칙 | **불가** | 불가 | 불가 | **해당 없음(이미 준수)** | 적용 조건이 "재측정 기획"으로 한정된다. A017 기각은 재측정이 아니라 신규 실험의 정상 판정이라 트리거되지 않는다. E4는 이 규칙이 요구하는 행동(반증 파일 열람)을 **260903에 이미 수행**했다(`GO2_PROJECT_STATE.md:357`, `:368`). |
| **G-D111** 이상 신호 규칙 | **불가** | 불가 | 불가 | 불가 | 트리거가 "극단적 결과 3회 연속"이다. A017은 총점이 오른 **비**극단 회차이므로 발화하지 않는다. 조치도 전부 계측 지문 검사이지 게이트 사양·학습 길이 검토가 아니다. |
| **G-D112** 문헌 사용 규칙 확장 | **불가** | 불가 | **부분 가능** | 불가 | E3에만 걸린다: R-Sci-1·R-Sci-2가 수만 iter 학습이라는 사실과 우리 1,000 iter가 매 사전등록마다 대면된다. 다만 규칙 대상이 "문헌"이라 **운영진 제공 원문**(`quadruped_rewards.py:119`의 "3000~5000 iter", `test/14강의…html:963`의 `max_iterations 1000`)은 대조 의무 밖이고, 공식 채점식은 문헌이 아니므로 E1도 잡히지 않는다. |

**요약: G-D109~112 네 규칙은 E1·E2·E4를 하나도 예방하지 못하고, E3만 부분적으로 예방한다.** 이유는 구조적이다 — 네 규칙 모두 §33-e가 발견한 **계측 비대칭 한 사건**에서 역산됐고, **판정 기준 자체의 타당성**과 **탐색 설계**를 대상으로 하는 규칙은 하나도 신설되지 않았다. 이것은 12-c-(d)에서 채택한 결함이 **재발방지 규칙에까지 그대로 재생산된** 형태다.

### 12-e. 신설 제안 규칙

세 규칙 모두 기존 G-D109~112와 대상이 겹치지 않으며, **사람의 판단 없이 파일에서 기계적으로 위반 여부를 판정**할 수 있다.

#### 제안 1 — G-D113 · 게이트–목적함수 부호 불변식 (E1 대응)

> **어떤 tier-1 실패 사유도, 그 사유가 지목한 시나리오의 곱셈 프록시 delta가 음수가 아닌 한 기각 판정을 발행할 수 없다.**
> `tier1_decision()`은 `failure_reasons`의 각 항목에서 시나리오 ID `S`를 파싱해 `scenario_deltas[S].proxy`를 검사한다. `proxy ≥ 0`인 시나리오 절이 발화하면 그 판정의 `status`는 `INTERNAL_EARLY_KILL_FAIL`이 아니라 **`OBJECTIVE_GATE_CONFLICT`**이며, 이 상태의 실행은 자동 기각 대상이 아니라 **사람의 명시적 재판정 대상**으로 원장에 올린다. 총점 절은 시나리오를 지목하지 않으므로 제외한다.

```
for r in decision["failure_reasons"]:
    S = 앞머리 시나리오 토큰(G1..G7)      # 없으면 skip
    assert decision["scenario_deltas"][S]["proxy"] < 0, r
```
**소급 검증:** A017의 `TIER1_DECISION.json`에 적용하면 `G4_survival_regressed_over_0.1` ↔ `scenario_deltas.G4.proxy = +0.015871`로 **즉시 발화**한다. 나머지 11건은 발화하지 않는다(전멸 회차는 proxy도 전부 음수). 즉 이 규칙 하나로 E1의 유일한 실사례가 260904에 잡혔고, 오탐은 0건이다.

#### 제안 2 — G-D114 · 절대 점수 단일 서열 원장 (E2 대응, R7 동시 차단)

> **모든 결과 회수 시, `GO2_PROJECT_STATE.md`의 단일 표 "절대 점수 서열"에 한 행을 append 한다.** 필수 열은 `실행ID · candidate_points_70 · 후보 arm survival_proxy_source · 후보 arm schema_version · 기준선명`. **서열(정렬·순위)은 `survival_proxy_source`가 동일한 행들 사이에서만 매기며, 서로 다른 source의 행을 같은 순위 열에 섞어 표기하는 것을 금지한다.**

1. `len(표의 행) == len(glob("workspace/_keep/*/reports/TIER1_DECISION.json"))`
2. 각 행의 `candidate_points_70`이 해당 JSON 값과 소수점 6자리까지 일치
3. 순위 열이 있는 표에서 `len(set(그 표의 survival_proxy_source)) == 1`

**소급 검증:** 260904에 존재했다면 A017 행 삽입 시점에 `50.199 > 46.491`이 표 최상단으로 올라온다. 동시에 §2의 `track_lin_vel_120 20.627`(v1)이 v2 행들과 같은 열에 놓인 것도 검사 3에 걸린다.

#### 제안 3 — G-D115 · 운영진 원문 충돌 등록 의무 (E3·E4 동시 대응)

> **실험 파라미터를 고정하거나 탐색 공간을 닫는 모든 결정문(G-D###)은, 그 파라미터·다이얼을 언급하는 운영진 제공 원문을 전부 `파일:줄`로 열거해야 한다.** 대상 원문 집합은 `workspace/training/quadruped/quadruped_rewards.py`, `workspace/PRELIM_RL_GUID.md`, `test/*.html`로 고정한다. 열거된 원문들이 **서로 다른 값을 지시하면 결정문에 `SOURCE_CONFLICT` 태그와 함께 채택한 쪽·기각한 쪽·이유를 남긴다.**

```
파라미터 P를 고정하는 결정문 D에 대해
  SRC = { (file,line) | grep -n "<P의 별칭>" in {quadruped_rewards.py, PRELIM_RL_GUID.md, test/*.html} }
  CITED = D 본문에서 파싱한 file:line 집합
  assert SRC ⊆ CITED
  값이 서로 다른 인용이 2개 이상이면 assert "SOURCE_CONFLICT" in D
```
**소급 검증 — E3:** 반복수 `iter`에 대해 `SRC`는 `quadruped_rewards.py:113,117,119`(3000 / 5000~15000 / 3000~5000)와 `test/14강의…html:963`(`max_iterations 1000`)를 포함한다. 값이 충돌하므로 G-D21·G-D35·G-D47·G-D62 전부가 `SOURCE_CONFLICT` 명시 없이는 통과 못 한다. `GO2_CAMPAIGN_SCHEDULE.md:255` → `:326`의 **무기록 하향**도 결정문 부재로 검출된다.
**소급 검증 — E4:** `track_ang_vel_z_exp`를 닫는 결정문 G-D80·G-D82·G-D89·G-F127은 `quadruped_rewards.py:89-90`을 인용하지 않으므로 전부 실패한다 — 그 시점에 "9개 중 1개는 아직 열려 있다"가 강제로 드러난다. 반대로 `undesired_contacts`·`termination_penalty`를 닫은 G-D61은 원문 인용(`quadruped_rewards.py:92-93`)과 반증 측정(`env.yaml`)을 모두 갖췄으므로 **통과한다** — 이 규칙은 12-b R3의 "제대로 닫은 사례"를 오탐하지 않는다.

### 12-f. 감사관의 [미확인] 목록

- `max_survival_regression = 0.10`의 수치 도출 근거 — 저장소 전 문서 검색으로 발견하지 못했다. 사용자 구두 결정일 가능성이 있으나 확인할 수 없다.
- E3의 실질 영향(1,000 iter 서열이 3,000~5,000에서 뒤집히는가)은 측정 자체가 존재하지 않는다.
- `G-F140`은 "G-A011~G-A022 전체가 fix2 엔진(`a0304277…`) 하나로 실행됐다"고 적지만(`GO2_PROJECT_STATE.md:554`), 그 엔진은 260905에 빌드됐고(`:507`, G-F132) G-A011은 260902 실행이다. 또 G-A013 후보 arm은 `schema_version:2`인데(`:642` G-F157) 같은 문장은 그 계보를 v1로 묶는다. **원장 내부에 세대 귀속의 불일치가 있으며, 이 감사에서는 해소하지 못했다.**

### 12-g. 이 감사를 받아들여 본문에 적용한 정정 (정정 4~8)

| # | 반박 | 본문 조치 |
|---|---|---|
| **정정 4** | R2 — "두 개의 가장 큰 개선이 게이트에 죽었다"는 틀렸다. `+3.090`짜리(Chain-01)는 260905 재측정에서 `2.308/70`, 6/7 시나리오 생존 전멸로 확정됐다. | §3-a의 "두 번째 사례" 단락을 철회하고 **E1의 실사례를 A017 1건으로 축소**했다. §4-b 서열표(Chain-01 7위)와의 자기모순도 이로써 해소된다. |
| **정정 5** | R1 — "`target_scenario` 강등 때 그 조항이 죽인 결과를 재검토하지 않았다"는 틀렸다. 그 결과가 **강등의 근거였고**, 이후 전용 재측정(G-A023)까지 발주됐다. | 해당 비판을 삭제했다. 남는 정확한 비판은 "**곱셈 대조가 총점 수준에서 1회, 조항 1개에만 적용되고 시나리오 수준으로 확장되지 않았다**"이다(12-c(c)). |
| **정정 6** | R3 — E4의 "9개 중 3개 미탐색"은 틀렸다. `undesired_contacts`(값 `null`)와 `termination_penalty`(항 부재)는 **260903에 실측으로 닫혔다.** | E4를 **"9개 중 1개(`track_ang_vel_z_exp`)"**로 정정하고 증거 강도를 낮췄다. H1 `termination_penalty` 근거는 `AGENTS.md:250` 위반이므로 삭제했다(R4). |
| **정정 7** | R5 — "1000 iter가 짧다"는 260907의 새 발견이 아니다. `GO2_REWARD_EVIDENCE_MASTER.md:257-259`(260905)에 이미 있다. | E3의 신규성 주장을 철회하고, **"이미 적어놓고 우선순위 4번으로 밀어 그대로 실행하지 않았다"**는 더 무거운 형태로 바꿨다. |
| **정정 8** | R6·R7 — §2 표가 원장의 260907 재분류(G-D104·G-D105)를 반영하지 않았고, v1/v2 절대 점수를 한 열에 섞었다. | §2 표에 재분류·세대 표기를 반영했다. |

**감사관이 §3-a에 대해 내린 가장 무거운 지적은 따로 있다** — 계약 테스트에 "총점 양수를 죽이는 것은 오살이므로 막는다"와 "총점 양수라도 생존이 무너지면 죽인다"가 **나란히** 고정돼 있었다는 사실이다. 즉 E1은 **모르고 저지른 사양 오류가 아니라 명시적으로 채택·테스트·문서화된 위험정책**이다. §3-a의 "판단 실수가 아니라 사양 오류다"라는 문장은 **"사양 오류가 아니라 정책 선택 오류다"**로 뒤집힌다. 이 정정은 E1의 심각도를 낮추지 않는다 — 오히려 **우리가 그 위험을 알면서 12회 내내 재검토하지 않았다**는 뜻이므로 12-c(d)의 자기감사 실패와 직결된다.

---

## 13. 적대적 감사 — §7 계획은 현재 형태로 실행할 수 없다

> 독립 감사관(읽기 전용)이 문서의 인용 증거를 직접 열어 검증한 원문. **§7 계획에 `치명` 3건,
> `중대` 8건이 나왔고, 그중 하나는 "현재 저장소의 어떤 스크립트로도 S1→S2 흐름을 실행할 수
> 없다"는 코드 수준 증명이다.** 이에 따라 §7은 §14로 전면 재작성한다.

### 13-a. 사실 주장 검증 결과

| 문서의 주장 | 검증 | 실제 | 심각도 | 근거 |
|---|---|---|---|---|
| A017 `candidate_points_70` 50.19915737233102, `baseline` 46.4912409491015, delta +3.70791642322952, `failure_reasons` 1개 | **검증됨** | 전부 16자리까지 일치. 단 문서가 인용한 키 이름 `candidate_minus_baseline`은 실제로 `candidate_minus_baseline_points_70` | 경미(표현) | `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/reports/TIER1_DECISION.json` |
| §3-a 시나리오별 delta 표(G1~G7), "7개 중 6개 개선, G4 proxy +0.0159" | **검증됨** | G1 +0.0099044 / G2 +0.0412009 / G3 +0.0844718(생존 +0.09375) / **G4 +0.0158711(생존 −0.21875)** / G5 +0.1093010 / G6 −0.0067562 / G7 +0.1030981(생존 +0.0625). 전부 일치 | — | 같은 JSON `scenario_deltas` |
| §4-a 계측 지문표 (11행, `1211111` / `2222222` 등) | **검증됨** | 11행 전부 일치. `survival_proxy_source`도 일치 | — | 각 `evaluation/*/cases/seed_101/*/summary.json` 전수 순회 |
| §5 정정 2 학습로그 표 | **검증됨** | 9행 전부 소수 4자리까지 일치. 문서가 `—`로 비운 A020·A021·A022·A024의 `time_out`/`base_contact`도 로그에 존재하며, A024는 0.9905/0.0095로 "안 움직여 생존" 논거를 오히려 강화 | 경미 | 각 `logs/candidate_training.log` |
| §5 정정 1 제출 후보물 | **검증됨** | `policy.pt` 1,159,510 B / `8f0c7d13…`, `env.yaml` 29,578 B, `model_best` `0563deff…` = `model_900.pt`. 전부 일치 | — | `sha256sum` 실행 |
| `save_interval = 100` | **검증됨** | 코드 및 실행된 run의 `params/agent.yaml:9` 양쪽 확인 | — | `go2_task/agent_cfg.py:21` |
| `play.py`가 `exported/model_best.pt`를 재생하고 `--checkpoint`를 존중 | **검증됨** | `_stage_best()`(`play.py:107`), `_inject_load_args()`(`play.py:148-155`) | — | `play.py:107,148-155` |
| README의 "resume은 지형 커리큘럼을 리셋한다" 인용 | **인용은 정확 / 함의는 과장** | 원문 존재(`README.md:103-104`). 그러나 **from-scratch도 동일한 초기 지형에서 시작한다** — A017의 `terrain_levels[0] = 3.5275`이고 iter 163에 0.4762로 붕괴, iter **914**가 되어서야 3.5275를 재돌파했다. 즉 resume이 잃는 것과 from-scratch가 잃는 것이 같은 크기이며, resume 쪽은 가중치를 보존한다 | **중대** | `README.md:103-104` · A017 `candidate_training.log` |
| §3-c E3 "운영진이 명시한 성격 판별 하한은 3000 iter" | **반증됨** | `quadruped_rewards.py:119`는 "3000~5000"이라 하지만, **`PRELIM_RL_GUIDE.md:175`는 "실험 단계는 짧게(`--max_iterations 1000~5000`)", `:182`는 "걸음 '성격'은 1000~5000 iter 면 드러납니다"**라고 쓴다. 문서는 같은 파일의 `:180`만 인용하고 `:175`·`:182`를 인용하지 않았다. **1000 iter 스크리닝은 가이드 하한 위다** | **중대** | `workspace/training/PRELIM_RL_GUIDE.md:175,180,182` |
| §7-e·W4 "6,400~7,200 iter는 가이드의 10000~15000에 미달" | **과장** | `quadruped_rewards.py:117`은 "최종만 길게(**5000~15000** iter)"다. 계획은 이 범위 **안**이다. 문서는 두 상충 지침 중 늘 자기에게 불리한 쪽만 인용한다 | 경미 | `quadruped_rewards.py:117` |
| §2 표 "G-A010 … 기각 사유 —" | **불일치** | A010의 실제 사유는 `['target_G1_improvement_below_0.05']`, 절대 점수 19.794 vs 기준선 17.537(**+2.257**). **같은 조항에 죽은 세 번째 실행이 표에서 빈칸 처리됐다** | **중대** | `go2_g_a010_lin_vel_z_m2/reports/TIER1_DECISION.json` |
| §2 표 "G-A013 기각 사유: 총점 delta" | **불일치** | 실제는 5건(`weighted_proxy_regressed`, `G2_…`, `G4_…`, `G5_…`, `G6_survival_regressed_over_0.1`) | 경미 | `go2_g_a013_flat_orientation_m1/reports/TIER1_DECISION.json` |
| §4-b 절대 점수 서열표 | **부분 반증 — 척도가 섞였다** | 1~6·8~11위는 tier-1 **7 case · seed 101 · env 32개** 값인데 7위 `Chain-01 2.308`만 **69-case×3seed worst-case**다. 같은 Pilot-01이 tier-1에서 **46.491**, 69-case×3seed 평균 **45.031**·worst **33.793** — 척도가 바뀌면 27% 움직인다. 한 표에 섞어 "서열이 확정된다"고 말할 수 없다 | **중대** | `meta/G_A017….json` `branch.on_representative_pass` · `GO2_PROJECT_STATE.md` G-F70/G-D69 |
| §4-a "G-A010 `1111111`/`1111111` → OK(구세대끼리 대칭)" | **형식은 맞고 실질은 틀림** | 대칭성은 뺄셈만 보호한다. **캠페인이 이미 반례를 보유**한다 — track_120은 v1-v1 대칭에서 +3.090이었으나 그 정책의 v2 실측은 `2.307745/70`, 6/7 시나리오 `survival_proxy=0.0`. v1은 웅크림을 못 보므로 **두 arm이 함께 붕괴한 상태의 차분**이 나온다 | **중대** | `GO2_PROJECT_STATE.md` G-F139 |
| W9 "cudnn 비결정성으로 같은 seed라도 결과가 달라진다" | **자체 실측으로 반증됨** | A013과 A025는 다른 날 from-scratch로 각각 학습했는데 `model_best.pt`가 **비트 동일**(`676cc1cb93c70cd6…`), best 선별 로그 동일(`step 884 → model_900.pt`), `candidate_points_70`도 16자리 동일. **문서는 자기 데이터 대신 파일 주석을 근거로 삼았다** | **중대** | 두 run `model_best.pt` sha256 · `TRAIN_STATUS.txt` |
| §1-d "1000 iter = 56분 41초(3407.67 s)" | **인용 정확 / 대표값 편향** | 3407.67 s는 5개 run 중 **최솟값**이다(A015 3427.89 · A025 3519.72 · A018 3525.16 · A016 3657.21). 평균 3507.5 s = 3.51 s/iter. 최솟값을 중앙 추정으로, +11%를 "보수적"으로 썼다 | 경미 | 각 run `candidate_training.log` |
| W6 "v1 실행은 rollout 원본이 없어 재채점 불가" | **검증됨** | v2 summary는 `survival_proxy_v1`·`_v2`를 둘 다 담지만 v1 summary에는 `survival_proxy` 하나뿐이고 `fallen_env_count`·`height_rel_*`도 없다 | — | 두 세대 `summary.json` 키 비교 |

### 13-b. §7 계획의 결함

**F1 — 채택 조건 ②의 임계값 `terrain_levels ≥ 4.2503`은, 제출될 정책의 값이 아니라 이미 삭제된 다른 체크포인트의 값이다.** `치명(계획 무효)`

A017의 4.2503은 **iter 999** 값이다. 그런데 실제 평가·제출 후보가 된 체크포인트는 `model_900.pt`(`Best by 'Train/mean_reward': step 856 → model_900.pt`)이고 **그 시점 `terrain_levels`는 3.4268**이다. 게다가 finalize가 `[CLEAN] logs/ 의 intermediate .pt 10개 삭제`를 실행해 **iter 999 체크포인트는 존재하지 않는다.** 즉 조건 ②는 (a) 제출물과 다른 객체를 재고, (b) 그 객체는 복구 불가능하다. §5 정정 2의 "독립 계측이 A017을 2위의 2.4배로 1등에 놓는다"도 같은 mispairing 위에 서 있다.
근거: `launcher.log:33272-33278`, run 폴더에 `model_900.pt` 단독 잔존.
**제안:** 조건 ②를 폐기하거나 "**export될 체크포인트 시점** terrain ≥ (A017 동일 정의 값 = **3.4268**)"로 재정의한다.

**F2 — 조건 ②는 어느 쪽으로 정의하든 정보량이 0이다.** `중대`

A017 terrain은 iter 0에 **3.5275**(초기화값)로 시작해 iter 163에 0.4762까지 붕괴한 뒤 iter **914**에 출발점을 회복하고 iter 999에 4.2503으로 끝난다. **1000개 표본 중 4.2503 이상은 iter 999 단 하나** — 상승 곡선의 종점(=최댓값)을 그대로 임계값으로 삼았다. 천장은 `num_rows: 10` → 레벨 0~9(`env.yaml:200`). 마지막 100 iter 기울기 **+0.0083/iter**. 유일한 장기 실측(H1 10,000 iter)에서 terrain은 4.2503을 **iter 740**에 통과하고 iter 7000에 **5.81**에 도달한다. **7,000 iter run이 걷기만 배우면 자동 통과, 못 배우면 조건 ①이 먼저 잡는다.**

**F3 — 예산의 14%가 증명 가능하게 중복이다.** `중대`

A013/A025 비트 동일 재현은, 같은 seed(42)·같은 envs·같은 가중치로 다시 돌리면 **처음 1000 iter가 A017의 `model_900.pt`를 비트 단위로 다시 만든다**는 뜻이다. 3407.67 s ≈ **0.95 GPU-h**를 이미 손에 든 파일 재생산에 쓴다.
동시에 이 사실은 **대안 A(resume) 기각 근거를 약화**시킨다. 커리큘럼 리셋 비용은 from-scratch와 resume이 **동일**하고(둘 다 3.5275에서 출발), from-scratch만 추가로 "무작위 가중치에서 3.5275를 회복하는 914 iter"를 지불한다. §5 정정 3의 "구조적으로 불가"·"스크리닝 자체가 무의미"라는 결론은 이 산수를 하지 않았다.
**제안:** ① 계획에 **seed를 명시**하라(문서 어디에도 없다). ② 대안 A를 "resume 후 terrain 회복까지의 iter 수"라는 측정 가능한 비용으로 재평가하라 — A017 기준 상한 914 iter(0.87h).

**F4 — 대안 B·C 기각 근거가 팀이 보유한 유일한 장기 학습 데이터와 어긋난다.** `중대`

H1 10,000 iter run의 terrain 이득 분해: iter 1000 **47%** · 2000 73% · 3000 **87%** · 4000 **92%** · 5000 95% · 7000 **97%** · 10000 100%(총 +2.448).
따라서 (a) 대안 C가 채택안(≈7000) 대비 사는 것은 총 이득의 **약 3%**다. (b) 대안 B(4000×2)의 각 정책은 총 이득의 **92%**에 도달한다 — **"최종 정책이 4000 iter라 E3를 반복한다"는 기각 근거는 정량적으로 성립하지 않는다.** 4000 iter는 포화 평탄부다. B가 잃는 건 5%의 지형 이득이고 얻는 건 두 번째 설정/시드다.

**F5 — 대안 D를 기각한 근거에 기대값 계산이 없다.** `중대`

§7-f D행 기각 사유 전문은 "50.199/70 = 71.7%에 고정. **남은 예산 전액 미사용은 그 자체로 손실**"이다. P(개선)×개선폭 대 P(실패)×비용 계산이 문서 어디에도 없다. **미사용 GPU 시간은 점수가 아니다** — 대회 점수 함수에 예산 소진 항이 없다.
더 나아가 계획은 살아 있는 원장 결정을 무시한다: `GO2_PROJECT_STATE.md`의 **LATEST NEXT는 "G-A026(Default-01 posture_gate_v2 실측, 학습 없음)"**이고, **G-D107은 엔진 수정 전 신규 tier-1 실행을 금지**하고 있다. §7은 이 둘을 언급조차 하지 않는다. G-F169는 G-A026 하나로 **5건을 재학습 없이 재판정**할 수 있다고 못 박는다.

**F6 — 규정 리스크(W7)는 "미확인"이 아니라 확인됐고 나쁜 쪽이며, 원인이 우리 스크립트에 있다.** `중대`

운영진 배포 파이프라인에는 학습 종료 시 `model_best.pt + env.yaml + report.html`을 운영 서버로 올리는 자동 백업이 있고, 목적이 코드 주석에 **"서버가 꺼지거나 컨테이너가 재생성돼도 학습 이력이 남도록"**이라 명시돼 있다(`_finalize.py:841-848`).
**우리 러너는 모든 Go2 실행에서 이를 끄고 실행했다** — `server_run_go2_tuning_engine_v1.sh:107`의 `NO_AUTO_SUBMIT=1`, A017 실행 로그 `launcher.log:33288`에 `[backup] NO_AUTO_SUBMIT 설정됨 — 자동 백업 건너뜀`.
**H1도 같다** — `server_run06_long.sh:59`가 동일하고 `train_260831-06_run05cfg_10000/train.log:370246`에 같은 줄이 있다. 92.73/100짜리 H1 후보도 이 경로로는 이력을 남기지 않았다.
**이것은 GPU 0시간에 확인 가능한 항목이었다.** 문서가 W7을 "최우선 항목"이라 적어 놓고 확인하지 않은 그 파일이 저장소 안에 있었다.

**F7 — 조건 ③("두 측정의 `schema_version`이 모두 2")은 층위가 모호하다.** `경미 → 중대`

`TIER1_DECISION.json`의 최상위 `schema_version`은 **3**이고 case `summary.json`의 것이 **2**다. 조건문이 어느 파일을 가리키는지 쓰여 있지 않다. 게다가 G-D109가 "문서 규칙이 아니라 **엔진 코드와 계약 테스트에 넣어 강제**"를 요구했으므로 문서 규칙으로만 남기는 것 자체가 G-D109 미이행이다.

**F8 — 제출 결정 전체가 7 케이스 · 1 시드 · 시나리오당 32 env 위에 서 있다.** `중대`

모든 case `summary.json`이 `rows 32000 / steps 1000` → **env 32개**. E1의 핵심인 G4 생존 −0.21875는 **32개 중 7개**의 차이다(이항 SE ≈0.073, 약 3 SE). G4의 proxy 이득 +0.0159는 그 잡음 안에 완전히 들어간다.
A017은 `INTERNAL_EARLY_KILL_FAIL`로 끝났으므로 사양서의 `on_tier1_pass`(seed 202·303)와 `on_representative_pass`(69-case)가 **한 번도 실행되지 않았다.** 채택 조건 ①의 기준값 50.199는 캠페인에서 **가장 검증이 얕은 숫자**인데 6.8h짜리 결정의 기준선으로 쓰인다.

### 13-c. 하드 스톱·체크포인트·export 경로 실증 확인

**(a) 중간 체크포인트는 정상 저장된다 — 확인.**
`save_interval = 100`이 `go2_task/agent_cfg.py:21`과 실제 실행의 `params/agent.yaml:9` 양쪽에 있다. rsl_rl은 **마지막 iteration에도 저장**한다(A016·A020·A022의 best 선별 로그가 `→ model_999.pt`). **"어디서 잘라도 손실 ≤ 100 iter"는 성립한다.**

**(b) `model_best.pt` 선별 로직은 중간에 죽은 run에서 "동작은 하지만, 계획이 기대하는 파일을 만들지 않는다" — 구멍.**

1. **신호 의존성.** `train.py:120-124`가 `run_training()`을 `except KeyboardInterrupt`로 감싸 `exit_code = 130`으로 만들고 `:131-132`에서 `finalize()`를 부른다. 즉 **SIGINT여야만** finalize가 돈다. `kill`(SIGTERM)이나 `kill -9`면 finalize는 **돌지 않는다.** 더구나 학습은 `subprocess.run`으로 띄운 **자식**이므로(`train.py:97`), 부모만 죽이면 Isaac Sim이 GPU를 물고 살아남는다. §7-d B1/B2는 어느 신호인지 명시하지 않는다.
2. **선택 기준이 계획과 다르다.** §7-d B1은 "`model_<최대iter>.pt`를 복사"라고 쓰지만 `find_best_model()`(`_finalize.py:40-105`)은 **`Train/mean_reward` 최대 지점**을 고른다(앞 10% 워밍업 제외). A017 실측은 `step 856 → model_900.pt` — **최대 iter가 아니었다.** A017의 late-phase reward는 비단조이고(iter 850~999 구간에서 reward와 terrain의 상관 **−0.010**), **7,000 iter run에서 best-by-reward가 어느 지형 수준의 정책을 집을지 예측 불가**다.
3. **그리고 나머지를 지운다 — 가장 큰 구멍.** `_finalize.py:178-180`이 기본값 `cleanup=True`로 `_cleanup_intermediate_checkpoints(run_dir, keep_iter=<best의 실제 iter>)`를 호출해 **다른 `model_*.pt`를 전부 삭제**한다(`:108-134`). A017 로그가 실제로 그랬다: `[CLEAN] … 10개 삭제 (66MB 회수)` / `[KEEP] … model_900.pt 보존`.
   → **§7-d B1의 복구 절차는 finalize가 한 번이라도 돌면 실행 불가능하다.** finalize는 SIGINT 직후 자동으로 돌고 사람이 개입할 틈이 없다. 회피책은 `_finalize.py --no-cleanup`(`:920`)뿐인데 계획에 그 플래그가 없다.

**(c) `play.py`는 export할 수 있다 — 확인.**
`_stage_best()`(`play.py:107`) → `_inject_load_args()`(`:148-155`)가 절대경로 `--checkpoint`로 넘기고 사용자 지정 `--checkpoint`를 존중한다. `_export_policy_to_exported()`는 **이번 실행에서 새로 쓰인** `policy.pt`만 복사한다(`:175-215`). **"임의의 `model_<iter>.pt`를 `exported/model_best.pt`에 올려놓고 play를 돌리면 export된다"는 참이다** — 단 (b)3 때문에 그 파일이 남아 있을 때만.

**(d) 그러나 하드 스톱은 현행 자동화 러너를 통과하지 못한다 — 치명.**

`server_run_go2_tuning_engine_v1.sh:107-121`:
```
NO_AUTO_SUBMIT=1 ... isaaclab.sh -p train.py ... | tee "$KEEP/logs/candidate_training.log"
TRAIN_RC=${PIPESTATUS[0]}
...
[[ "$TRAIN_RC" == 0 && -s exported/model_best.pt && -s exported/env.yaml ]] || {
  echo '[FAIL] candidate training or finalize failed'; exit 3; }
```
SIGINT 경로에서 `train.py`는 **130**을 반환한다. `exported/model_best.pt`가 정상 생성되더라도 `TRAIN_RC == 0`이 아니므로 **러너는 `exit 3`으로 죽고, `trap on_exit EXIT`(`:92`)가 `package_result PARTIAL`만 남긴다.** 평가(tier-1 7 case)도, `play.py` export도, 결과 ZIP 정식 생성도 실행되지 않는다.
→ **§7의 S1→S2 흐름은 현재 저장소에 있는 어떤 스크립트로도 실행할 수 없다.** 새 러너 또는 `TRAIN_RC` 게이트 완화 패치가 선행돼야 하는데 §7 어디에도 그 작업이 없다. 참고로 학습은 `tmux new-session -d`(`:66`)로 뜨므로 `tmux kill-session`은 SIGHUP이라 finalize도 트랩도 돌지 않는다.

### 13-d. 문서에 없는 대안

**E1 (권장) — A017을 69-case × 3seed · posture_gate_v2로 실측한다. GPU ≈ 0.5h.**
- **비용 근거:** G-F76 "실행 비용은 로그 타임스탬프 `06:01:11Z→06:30:04Z` 약 **29분**, eval 순수 wall 합계 1,391초"(G-A012, 동결 Pilot-01 69-case×3seed). G-F32도 "정책 하나 69-case 약 22분". **학습 없음.** 스크립트도 이미 있다 — `server_run_go2_chain01_baseline.sh`(G-A023)가 정확히 이 작업을 동결 checkpoint에 수행하며, 원장에 "이름만 교체해 복제"한 선례가 있다(G-D90).
- **기대값:** 계획 전체의 유일한 전제 — "A017이 우리가 가진 최고 정책" — 을 검증한다. 현재 근거는 7 case · 1 seed · 32 env뿐이다. **캠페인은 같은 전제가 무너진 전례를 이미 보유**한다: track_120/Chain-01은 tier-1 +3.090 → 69-case 실측 `2.307745/70`, 6/7 생존 0(G-F139). Pilot-01조차 tier-1 46.491 → 69-case worst 33.793.
  - A017이 69-case에서 Pilot-01의 33.793을 넘으면 → 조건 ①의 기준선이 **검증된 숫자**로 교체되고, 리포트(설계의도 20점)의 근거가 생긴다.
  - A017이 69-case에서 붕괴하면 → **6.8h를 A017 가중치에 거는 계획이 실행 전에 반증된다. 0.5h가 6.8h를 구한다.**

**E2 — 하드 스톱을 없앤다: 이미 만들어져 있는 `go2_terrain_5k_v1` 패키지를 A017 가중치로 재사용. GPU 5.7h(5000 iter) / 6.8h(6000 iter).**
- `server_run_go2_terrain_5k_v1.sh`는 **from-scratch 학습 → 1000/2000/3000/4000 iter 사다리 tier-1 평가 → 최종 정책 69-case×3seed → 동결 Pilot-01 69-case 대조 → 영상 7종 → 패키징**을 한 스크립트로 수행하고, 학습 길이는 `GO2_MAX_ITERATIONS`로 조절된다(`:30`). **로컬 검증까지 끝난 상태로 `WITHDRAWN_PRE_LAUNCH`로 남아 있다(GPU 소비 0).**
- **이점:** (a) 13-c(d)의 `TRAIN_RC` 문제를 만나지 않는다(학습이 정상 종료). (b) 사다리 평가가 **"몇 iter가 최적인가"에 Go2 실측 답**을 준다 — 저장소에 Go2의 iteration-대-점수 데이터는 **한 건도 없다.** (c) 69-case 대조가 내장돼 조건 ②보다 훨씬 강한 교차확인이 자동으로 붙는다.
- **필요 수정:** 철회 사유였던 판정 기준 `terrain_levels ≥ 7.0`을 절대 점수 기준으로 교체(파일 편집, GPU 0).

**E3 — `track_ang_vel_z_exp` 확인. GPU 0h.**
파일 주석이 "0.75 = 현재 유효값(Go2 기본)이라 그대로 풀면 변화 없음"이라 적으므로 **값을 바꿔야만 의미가 있고**, 정의 여부는 A017의 `training/env.yaml`을 열면 GPU 0으로 확인된다 — G-A013 때 `undesired_contacts`/`termination_penalty`를 정확히 그 방법으로 닫았다. **계획이 E4를 "예산 부족"으로 포기한 것은 비용을 잘못 계산한 것이다.**

**권장 재배치 (총 7.3h, 예비 2.7h 유지):**

| 단계 | 작업 | GPU | 이 단계가 사는 것 |
|---|---|---:|---|
| S0 | A017 제출 세트 확정 + **`NO_AUTO_SUBMIT` 해제 후 A017 이력 업로드**(F6) + `env.yaml`로 `track_ang_vel_z_exp` 정의 확인(E3) | 0h | 50.199 확보 + 제14조 리스크 해소 |
| S0.5 | **E1 — A017 69-case×3seed 실측** | 0.5h | 계획 전제의 검증 / 6.8h 조기 회수 가능성 |
| S1 | E1이 A017 우위를 지지하면 **E2 러너로 A017 가중치 from-scratch**(`GO2_MAX_ITERATIONS`를 **고정값**으로; 하드 스톱 없음) | 5.7~6.3h | 사다리 + 69-case 대조 자동 포함 |
| — | E1이 A017을 반증하면 S1 대신 Pilot-01 장기화 또는 G-A026 | — | — |

기대값 비교: 채택안은 P(개선)×ΔPoints를 계산할 재료가 하나도 없고, **E1은 0.5h로 그 P를 처음으로 추정 가능하게 만든다.** 문서가 D를 기각한 논리("예산 미사용은 손실")를 그대로 적용하더라도 E1+E2는 예산을 쓰면서 D의 안전성(점수 손실 0)을 유지한다 — **D 대 채택안의 이지선다 자체가 거짓 딜레마다.**

### 13-e. 저자가 아직 못 본 약점 (W12~)

| # | 약점 | 심각도 | 근거 |
|---|---|---|---|
| **W12** | **평가된 정책과 지표를 인용한 정책이 다르다.** §5 정정 2·조건 ②가 쓰는 `terrain_levels 4.2503`은 iter 999 값이지만, 채점된 것은 `model_900.pt`(terrain **3.4268**)이고 iter 999 체크포인트는 cleanup으로 **삭제됐다.** 문서 전체에서 이 구분이 한 번도 이루어지지 않는다 | 치명 | `launcher.log:33272-33278` |
| **W13** | **`terrain_levels`는 "정책 품질"이 아니라 경로 의존 상태변수다.** A017은 3.5275(초기화)에서 출발해 0.4762로 붕괴한 뒤 914 iter 만에 출발점을 회복했다. 값이 0에서 시작하지 않으므로 **"A017 4.25 = 2위 1.81의 2.4배"라는 비율 표현은 성립하지 않는다.** 옳은 서술은 "**A017만이 초기 지형 수준을 회복한 유일한 run이고, 나머지 8건은 전부 출발점 아래에서 끝났다**"다 | 중대 | A017 terrain 계열, A024 `tl[0]=3.5275 → tl[999]=0.0` |
| **W14** | **캠페인이 운영진의 학습 이력 백업을 전 실행에서 껐다 — Go2도 H1도.** W7이 "미확인"으로 남아 있던 답이 저장소 안에 있었고, 답은 "**이력이 남지 않았다**"다 | 중대 | `server_run_go2_tuning_engine_v1.sh:107`, `server_run06_long.sh:59`, `launcher.log:33288`, `_finalize.py:841-848` |
| **W15** | **v1-v1 대칭이 delta의 유효성을 보장한다는 §4-a의 판정은 자체 반례로 무너진다.** track_120은 v1-v1 대칭에서 +3.090, v2 실측 `2.307745/70`·6/7 생존 0. 같은 논리로 "OK" 처리된 A010(+2.257)과 69-case에서 +3.8656을 기록한 `feet_air_time 0.20`(G-A006)도 v1 채점이라 같은 위험에 있다 | 중대 | G-F139, G-F93 |
| **W16** | **파이프라인은 비결정적이지 않다.** A013/A025 비트 동일 재현. ⓐ W9의 시드 분산 우려가 과대평가, ⓑ 같은 seed로 S1을 돌리면 **처음 0.95h가 기존 파일의 재생산**, ⓒ **계획에 seed가 명시돼 있지 않다** | 중대 | 두 run `model_best.pt` sha256 |
| **W17** | **채택 조건 ①의 기준값 50.199가 3중으로 얕다** — 7 case · seed 101 단독 · 시나리오당 env 32개. G4 생존 −0.21875는 32개 중 7개이며 이항 SE ≈0.073. E1의 핵심 수치 G4 proxy +0.0159는 이 잡음 안에 있다 | 중대 | 각 `summary.json`, `meta/G_A017….json` |
| **W18** | **A017의 G4 우위는 자세 게이트 임계값에 아슬아슬하게 걸쳐 있다.** 후보 `height_rel_p10 = 0.2079`로 게이트 `height_rel_min_m 0.18` 바로 위, `recovery_rate` 0.711 → **0.320**, `recovery_rate_upright` 0.711 → **0.258**. 공식 채점의 자세/회복 정의가 조금만 엄해도 G4는 무너진다 | 중대 | `evaluation/candidate/cases/seed_101/slope_plus_20/summary.json` |
| **W19** | **§7이 살아 있는 원장의 `LATEST NEXT`와 G-D107 금지를 대체하면서 그 사실을 적지 않았다.** 원장은 다음 작업을 G-A026(학습 없음)으로 사전등록했고, G-F169는 그 한 건으로 5개 실행을 재학습 없이 재판정할 수 있다고 명시한다 | 중대 | `GO2_PROJECT_STATE.md` LATEST NEXT · G-D106 · G-D107 · G-F169 |
| **W20** | **G-D109를 §7-a가 문서 규칙으로만 재선언했다.** G-D109 원문은 "문서 규칙이 아니라 **엔진 코드와 계약 테스트에 넣어 강제**한다"이다 — 즉 G-D109가 막으려던 실패 양식(사람이 기억해서 지키는 규칙)을 그대로 재현한다 | 중대 | G-D109 |
| **W21** | **Go2에는 iteration-대-점수 데이터가 단 한 건도 없다.** 유일한 장기 실측은 H1이고 거기서 지형 이득의 87%가 3,000 iter에 도달한다. "6~7배이며 이 구간에서 가장 큰 개선이 기대되는 축"(§7-b)은 **어떤 Go2 측정에도 근거하지 않은 서술**이다. 이 공백을 메울 수 있었던 사다리 내장 실행은 실행 전 철회됐다 | 중대 | H1 `train.log` terrain 계열 · `GO2_CAMPAIGN_SCHEDULE.md:283-287` |
| **W22** | **`--headless` play의 export는 창을 닫는 동작에 의존한다고 코드가 경고한다.** `play.py:205-213`이 "IsaacLab은 시뮬 창을 정상적으로 닫을 때 export한다 … 지금 `exported/policy.pt`는 **이전 실행의 파일**입니다"라고 명시 경고한다. S2에 0.5h만 배정하고 이 실패 모드의 분기가 §7-d에 없다 | 경미 | `play.py:205-213` |

### 13-f. 검증 결과 문서가 맞았던 것

1. **A017 `TIER1_DECISION.json`의 모든 수치** — 16자리까지 인용 그대로. `failure_reasons`가 `G4_survival_regressed_over_0.1` **단 하나**인 것도 사실.
2. **§3-a 시나리오별 delta 표 7행 전부** — G4 proxy **+0.015871** 포함 일치. **G4가 채점식으로 개선인데 기각 사유가 됐다는 E1의 핵심 관찰은 원문 대조로 확인된다.**
3. **§4-a 계측 지문표 11행 전부** — 전수 순회로 문자열 단위 일치. Pilot-01 branch(A015~A018) 4건이 양쪽 v2 대칭이라는 정정도 사실이며, **따라서 A017 delta가 계측 비대칭의 산물이 아니라는 결론은 유효하다.**
4. **§5 정정 2의 학습 로그 표 9행 전부** — 일치. A017의 `time_out 0.894 / base_contact 0.106`이 "안 움직여 생존"이 아니라는 판정도 데이터가 지지한다(A024가 0.9905/0.0095로 그 패턴이고 점수 0.000).
5. **§5 정정 1의 제출 후보물** — 전부 일치. **"제출은 지금 당장 가능하며 GPU 0시간"은 참이다.**
6. **`save_interval = 100`과 "어디서 잘라도 손실 ≤ 100 iter"** — 코드·산출물 양쪽 확인.
7. **`play.py`의 재생/export 경로** — §11 색인의 줄 지시가 정확하다.
8. **README의 resume-커리큘럼 리셋 인용** — 원문 그대로. *(단 거기서 도출한 "대안 A는 구조적으로 불가"는 F3에서 반박된다.)*
9. **A025 = A013 비트 동일 재현이 순수 낭비였다는 §2의 판정** — 사실. *(다만 이 사실은 동시에 W9를 반증한다.)*
10. **W6("v1 실행은 로컬 재채점 불가")** — 사실.
11. **W10("조건 ②는 형식적일 뿐 실효가 없을 수 있다")** — **저자의 의심이 맞다.** 실제로는 저자가 우려한 것보다 더 나쁘다(무의미할 뿐 아니라 잘못된 객체를 잰다).

---

## 14. 제안 계획 (제3판) — §12·§13 감사를 반영해 전면 재작성

> 제1판(`--resume` 스크리닝)은 §5 정정 3에서 폐기됐다. 제2판(단일 from-scratch + 하드 스톱)은
> §13에서 **실행 불가능**으로 판명됐다. 이 제3판은 감사관이 제시한 대안 E1·E2 위에 세운다.
> **바뀐 원칙 하나: 남은 예산으로 무엇을 학습할지 정하기 전에, 우리가 가진 최고 정책이
> 정말 최고인지를 0.5h로 먼저 잰다.**

### 14-a. 제2판이 무엇 때문에 폐기됐는가 (요약)

| # | 감사 지적 | 제3판의 조치 |
|---|---|---|
| 13-c(d) `치명` | 러너가 `TRAIN_RC == 0`을 요구. SIGINT는 130 → `exit 3` → PARTIAL. **하드 스톱하면 평가도 export도 안 돈다.** | **하드 스톱을 폐기.** 학습 길이를 `GO2_MAX_ITERATIONS` **고정값**으로 주고 정상 종료시킨다. |
| 13-b F1 · W12 `치명` | 조건 ②의 `terrain 4.2503`은 **iter 999** 값인데 제출된 건 `model_900.pt`(terrain **3.4268**)이고 iter 999 체크포인트는 cleanup으로 **삭제됐다.** | **조건 ②를 폐기.** 69-case 실측으로 교체(14-d). |
| 13-c(b)3 `치명` | `_finalize.py:178-180`이 best 외 체크포인트를 **전부 삭제**한다. §7-d B1의 복구 절차는 실행 불가. | 실패 분기에서 `--no-cleanup`(`_finalize.py:920`)을 **사전 지정**. |
| 13-b F6 · W14 `중대` | **모든 Go2·H1 실행이 `NO_AUTO_SUBMIT=1`로 운영진 학습 이력 백업을 껐다.** 규정 제14조 대조 대상이 남지 않았다. | **S0의 최우선 항목으로 승격.** GPU 0h. |
| 13-b F5 · W19 `중대` | 원장 `LATEST NEXT`는 **G-A026(학습 없음)**이고 **G-D107이 신규 tier-1 실행을 금지** 중인데 §7이 언급조차 안 했다. | S0에서 **G-D107을 먼저 해소**(엔진 수정 + 계약 테스트)한 뒤에만 학습을 띄운다. |
| 13-b F8 · W17 `중대` | 기준값 50.199는 **7 case · seed 101 · env 32개**. G4 생존 −0.21875는 32개 중 7개(≈3 SE). | 기준값을 **69-case×3seed 실측치**로 교체. |
| W16 `중대` | 파이프라인은 **결정적**이다(A013/A025 비트 동일). 계획에 **seed가 없었다.** | seed를 **42로 명시**. |
| 13-a `중대` | E3의 근거가 편향 인용이었다 — `PRELIM_RL_GUIDE.md:175,182`는 **"1000~5000 iter면 성격이 드러난다"**고 쓴다. | **E3를 근거로 학습 길이를 정하지 않는다.** 길이는 H1 실측 포화 곡선으로 정한다(14-c). |

### 14-b. 계획 — S0 → S0.5 → S1

| 단계 | 작업 | GPU | 벽시계 | 이 단계가 사는 것 |
|---|---|---:|---:|---|
| **S0** | ① A017 제출 세트 확정(`policy.pt`·`env.yaml`·리포트 초안) ② **`NO_AUTO_SUBMIT` 해제 후 A017 학습 이력 업로드**(F6/W14) ③ `training/env.yaml`로 `track_ang_vel_z_exp` 정의 여부 확인(E4 잔여 1항) ④ **G-D107 해소** — `_default_baseline_payload()`/`_chain01_baseline_payload()` 수정 + `survival_proxy_source` 동등성 계약 테스트 + G-D113(게이트–목적함수 부호 불변식) 구현 | **0h** | 1.0h | **50.199 확보 · 제14조 리스크 해소 · 원장 금지 해제** |
| **S0.5** | **A017을 69-case × 3seed · posture_gate_v2로 실측** (`server_run_go2_chain01_baseline.sh` 복제, 학습 없음) | **0.5h** | 0.6h | **계획 전제의 검증 — 여기서 반증되면 5h를 아낀다** |
| **S1** | S0.5가 A017 우위를 지지하면 → **`server_run_go2_terrain_5k_v1.sh`를 A017 가중치·seed 42·`GO2_MAX_ITERATIONS=5000`으로 실행** | **6.4h** | 6.4h | 사다리(1k~5k) + 최종 69-case×3seed + Pilot-01 대조 + 영상, **전부 한 스크립트 안** |
| | **합계** | **6.9h** | **8.0h** | 예비 **3.1h** |

**S1이 `terrain_5k` 러너인 이유** — 이 스크립트는 이미 작성·로컬 검증까지 끝나고 `WITHDRAWN_PRE_LAUNCH`로 남아 있다(**GPU 소비 0**). 학습 → **1000/2000/3000/4000 iter 사다리 tier-1 평가** → 최종 정책 **69-case×3seed** → 동결 Pilot-01 69-case 대조 → 영상 7종 → 패키징을 한 번에 수행한다. 즉 (a) 하드 스톱이 필요 없고, (b) **저장소에 한 건도 없는 Go2의 iteration-대-점수 데이터**(W21)를 사다리가 그대로 만들어 주며, (c) 69-case 대조가 내장돼 폐기한 조건 ②보다 훨씬 강한 교차확인이 자동으로 붙는다.
**필요 수정(GPU 0):** 철회 사유였던 판정 기준 `terrain_levels ≥ 7.0`을 절대 점수 기준으로 교체.

### 14-c. 왜 5000 iter인가 (제2판의 "6,400~7,200"을 대체)

유일하게 존재하는 장기 학습 실측은 H1 10,000 iter run이다. terrain 이득의 누적 분해:

| iter | 1000 | 2000 | 3000 | 4000 | **5000** | 7000 | 10000 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 총 이득 대비 | 47% | 73% | 87% | 92% | **95%** | 97% | 100% |

- 5000 → 7000은 **+2%p**, 5000 → 10000은 **+5%p**. 제2판이 6,400~7,200을 목표한 것은 이 곡선을 보지 않은 선택이었다.
- 5000 iter는 `quadruped_rewards.py:117`의 "최종만 길게(**5000~15000** iter)" 범위의 하단이며 규정·가이드에 부합한다. 제2판의 "가이드 미달"이라는 자기비판(W4)은 §13에서 과장으로 판정됐다.
- 비용: 3.51 s/iter(5개 run **평균** — 제2판은 최솟값 3.41을 썼다) × 5000 = **4.88h**, 사다리·69-case·영상 포함 **약 6.4h**.
- 남는 2%p를 사는 대신 **예비 3.1h**를 확보한다. §13이 지적한 대로 미사용 GPU 시간은 점수가 아니지만, **초과는 제출 실패**다.

### 14-d. 채택 판정 (사전등록 — 실행 전 고정)

제2판의 3조건을 전부 교체한다. **모든 비교는 69-case × 3seed · posture_gate_v2 · worst-case 척도 하나로만 한다**(§13이 지적한 척도 혼합 금지).

| | 조건 | 근거 |
|---|---|---|
| **①** | S1 최종 정책의 **69-case×3seed worst-case points_70** > **S0.5에서 실측한 A017의 같은 척도 값** | 제2판의 50.199(7-case·1-seed·32env)는 W17이 "3중으로 얕다"고 판정 |
| **②** | 그리고 > **Pilot-01의 33.793**(동일 척도 기실측) | 유일하게 같은 척도로 존재하는 외부 기준선 |
| **③** | 후보·기준선 양쪽 **case `summary.json`의 `survival_proxy_source == posture_gate_v2`** (`TIER1_DECISION.json`의 최상위 `schema_version`은 3이므로 그것을 보지 않는다 — F7) | G-D109. **문서 규칙이 아니라 S0④에서 계약 테스트로 강제한다**(W20) |

**셋 중 하나라도 불만족 → A017을 제출한다. 점수 손실 0.** (S0에서 제출 세트가 이미 확정돼 있으므로 이 분기는 GPU를 추가로 쓰지 않는다.)

**폐기한 조건:** `terrain_levels ≥ 4.2503`. 이유는 F1(삭제된 객체를 잼) + F2(정보량 0: A017의 1000개 표본 중 그 값 이상은 iter 999 하나뿐이고, H1 실측에서는 iter 740에 통과) + W13(`terrain_levels`는 품질 지표가 아니라 3.5275에서 출발하는 경로 의존 상태변수). **다만 W13이 제시한 옳은 서술은 유지한다** — "A017만이 초기 지형 수준을 회복한 유일한 run이고 나머지 8건은 전부 출발점 아래에서 끝났다."

### 14-e. 실패 분기 (사전등록)

| # | 상황 | 조치 |
|---|---|---|
| **B1** | **S0.5에서 A017이 69-case에서 붕괴**(track_120/Chain-01의 전례: tier-1 +3.090 → 실측 2.308) | **S1을 취소한다.** A017 가중치에 5h를 걸 근거가 사라진다. 대신 (a) 동결 Pilot-01 장기화, 또는 (b) 원장이 사전등록한 G-A026을 실행한다. **0.5h가 6.4h를 구한 경우다.** |
| **B2** | S1 학습이 중간에 죽음 | 러너가 `PARTIAL`을 남긴다. 사다리 평가가 이미 돈 지점(1k/2k/3k/4k)의 산출물은 유효하므로 **가장 높은 사다리 지점의 정책**으로 판정한다. 체크포인트 복구가 필요하면 `_finalize.py --no-cleanup`(`:920`)로 재실행 — **cleanup 기본값이 `True`라 이 플래그 없이는 best 외 전부 삭제된다**(13-c(b)3). |
| **B3** | `play.py` export 실패 | `play.py:205-213`의 경고대로 `exported/policy.pt`가 **이전 실행 파일**일 수 있다. mtime과 sha256을 S1 산출물과 대조해 확인하고, 불일치면 재실행한다(W22). |
| **B4** | 시간 초과 조짐 | **학습을 자르지 않는다**(하드 스톱 폐기). 사다리 지점에서 이미 확보된 최고 정책으로 마감하고, 부족하면 A017을 제출한다. |
| **B5** | S0④(G-D107 해소)가 예상보다 오래 걸림 | **S1을 띄우지 않는다.** G-D107은 원장의 살아 있는 금지이며, 이를 무시한 실행은 판정 자체가 무효다(W19). |

### 14-f. 이 계획이 포기하는 것 (명시)

- **10,000 iter 완주.** H1 곡선 기준 5%p의 지형 이득을 포기하고 예비 3.1h를 산다.
- **`track_ang_vel_z_exp` 실측.** S0③에서 **정의 여부만** 확인하고(GPU 0), 값을 바꾼 학습은 하지 않는다. 파일 주석이 "0.75 = 현재 유효값이라 그대로 풀면 변화 없음"이라 적으므로 값 변경 실험이 필요하지만 예산이 없다. **E4의 잔여 1항은 이 라운드에서 열리지 않는다.**
- **두 번째 설정/시드.** 대안 B(4000×2)는 §13 F4에서 "기각 근거가 데이터와 반대"로 판정됐다(4000 iter는 총 이득의 92%로 포화 평탄부). 그럼에도 채택하지 않는 이유는 **S0.5에 0.5h를 먼저 쓰기로 했기 때문**이며, 이것은 "정보를 늘리는 두 방법 중 어느 쪽이 싼가"의 문제다 — B는 5h로 두 번째 표본을, S0.5는 **0.5h로 첫 표본의 신뢰도**를 산다. 후자가 10배 싸다.
- **`--resume` 재검토.** §13 F3은 정정 3이 과장이었음을 보였다 — from-scratch도 terrain 3.5275에서 출발해 A017 기준 **914 iter(0.87h)** 만에 회복하므로, resume이 추가로 잃는 것은 없고 가중치는 보존된다. 즉 resume은 **약 0.9h를 살 수 있다.** 채택하지 않는 이유는 (a) `_finalize.py:75-81`이 resume 시 best 선별 워밍업 임계값 버그를 문서화하고 있고, (b) README가 제출본은 from-scratch를 권하며, (c) 0.9h를 사려고 검증되지 않은 경로를 타는 것이 예비 3.1h 대비 이득이 작기 때문이다. **이것은 "구조적으로 불가"가 아니라 "값이 안 맞아서 안 쓴다"이며, 정정 3의 표현을 이 문단으로 정정한다.**

### 14-g. 이 계획이 아직 감사받지 않은 지점

§13이 제2판을 무너뜨린 것과 같은 방식으로 이 제3판을 공격해 주기 바란다. 특히:

1. **S0.5의 0.5h 추정이 맞는가.** 근거는 G-F76(G-A012 69-case×3seed 약 29분)과 G-F32(정책 하나 69-case 약 22분)뿐이다. A017은 Pilot-01보다 더 멀리 걷는 정책이라 에피소드가 길어져 비용이 더 클 수 있다. **[미측정]**
2. **`terrain_5k_v1` 러너가 실제로 무결한가.** "로컬 검증 완료"는 `WITHDRAWN_PRE_LAUNCH` 시점의 라벨이고, 그 뒤 엔진이 1.2.0 → 1.4로 바뀌었다. **G-F165가 지적한 "VERIFIED는 그때 검증됨이지 지금 유효함이 아니다"가 그대로 적용된다.** S1 전에 이 스크립트를 다시 검증해야 한다.
3. **H1 곡선으로 Go2의 길이를 정하는 것이 `AGENTS.md:250` 위반이 아닌가.** §13이 §3-d의 H1 인용을 바로 그 조항으로 반박했는데, 14-c는 다시 H1 데이터를 쓴다. **차이는 "reward 결론"이 아니라 "커리큘럼 포화 시점"이라는 것인데, 이 구분이 정당한지 나는 확신하지 못한다.** 이것이 이 계획의 가장 약한 고리다.
4. **S0④(G-D107 해소)를 1.0h로 잡은 것이 맞는가.** 엔진 수정 + 계약 테스트 2종(등등성, G-D113) 구현이다. 늘어나면 B5로 S1 전체가 취소된다 — **즉 이 계획은 학습이 아니라 코드 작업에 물려 있다.**
