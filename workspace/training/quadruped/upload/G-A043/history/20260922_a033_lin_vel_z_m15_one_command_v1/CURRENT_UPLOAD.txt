CURRENT GO2 UPLOAD — G-A043 (one command)

UPLOAD ONLY THIS ONE FILE
1. C:\dev\NConnect\workspace\training\quadruped\upload\G-A043\current\GO2_G_A043_a033_lin_vel_z_m15_one_command.zip
   SHA256 90661e80cb4eacac4428d30620e43c7abfa5a5a0832230b48fdfde1babf0b11c

한 파일·한 명령으로 1단계 → 서버 게이트 → 통과 시 2단계 → 결과 ZIP 하나.
절차는 같은 폴더의 GO2_G_A043_ONE_COMMAND_RUN_GUIDE.txt. 회차 ZIP은 이 파일 안에 이미 들어 있다.
서버 실행은 사용자 결정 사항이다.

RELEASE_ID 20260922_a033_lin_vel_z_m15_one_command_v1
STATUS ARTIFACT_VERIFIED
이전 판 1개는 ../history 에 보존한다(가장 최근 20260922_a033_lin_vel_z_m15_staged_v1), 모두 서버에 올린 적 없다.
권고 상태: 추론 사슬 INFORMATION_RUN — 보상 가중치 한 항만 바꾼다. 기준선 `-2.0`에서 `-1.5`로, 배포 파일이 스스로 적은 권장 대역 `추천 -5.0 ~ -0.5` 안이다. 단 `-2.0`은 Isaac Lab v2.3.1 base·Go2 rough·Go2 flat이 모두 쓰는 값이라 이 팔은 현재 맞춰져 있는 외부 기준에서 **떨어지는** 방향이며(이탈 사유는 사양 `external_reference.departure_reason`), 배포 파일 자신이 약한 쪽을 험지·계단에 불리하다고 적어 둔 방향이다. 그 반대 근거를 지우지 않고 `contradicting`에 싣고 절반 폭으로 시험한다.
