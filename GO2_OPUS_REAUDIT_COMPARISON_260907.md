# Go2 연속 실패 원인 — 3자 교차검증 최종 감사 (Opus 재감사 Phase III)

- 작성: 2026-09-07 (Asia/Seoul)
- 지시서: `OPUS_GO2_REAUDIT_PROMPT_260907.md`
- 대조 대상: Phase I 독립 감사(본 감사, 격리 컨텍스트) · `GO2_INDEPENDENT_AUDIT_CODEX_260907.md` · `GO2_DESIGN_REVIEW_260907.md`(§12~§14 포함 최신본)
- 판정 용어: `ARTIFACT_VERIFIED` / `VIDEO_OBSERVED` / `VIDEO_UNKNOWN` / `INTERNAL_GATE_PASS` / `INTERNAL_GATE_FAIL` / `INTERNAL_GATE_INCONCLUSIVE` / `OFFICIAL_RESULT`
- 본 문서의 모든 점수는 **내부 proxy**다. 공식 점수가 아니며 예선 통과를 보장하지 않는다.

## 0. 예선 기준 현재 위치

- **[예선 목표]** 시뮬 70점의 측정 타당성 회복. 현재는 70점 척도 자체가 자기 정책을 변별하지 못하는 상태이므로, 배점 확보 이전에 계측 복구가 선행 과제다.
- **[현재 단계]** **단계 1/6 — 기준선 실재성 검증.** 다음 행동을 막는 가장 이른 공백은 "동결 기준선 Default-01이 애초에 보행하지 않는 정책"이라는 사실이며, 이는 세 감사 중 Phase I만 발견했고 본 감사에서 원자료로 재확인됐다.
- **[확보]** ① A006~A025 전 실행의 evaluator 지문 전수 대조(12건 스캔 완료) ② Default-01·Chain-01 비보행 실측 ③ A017 판정 산술 16자리 검증 ④ G7≡G3 바이트 동일성 3 seed 전수 ⑤ G6 채점 필드 상수화 12/12 케이스 ⑥ Pilot-01 `33.793106/70`·Chain-01 `2.307745/70` 독립 재계산 일치.
- **[미확보]** Default-01의 `posture_gate_v2` 실측값, 공식 evaluator·`OFFICIAL_RESULT`, **현재 잔여 서버 과금시간**, 무효 판정 7건의 참 delta, PPO 실행간 분산(no-op control 0건), A015 이후 전 영상 `VIDEO_UNKNOWN`.
- **[이번 테스트]** 이번 감사는 실행이 아니라 판정이다. "A006~A025의 연속 FAIL 중 무엇이 정책 실패이고 무엇이 계측·운영 실패인가"를 원자료로 재판정한다.
- **[흐름]** A006~A025 회수 → Codex 독립 감사 → 기존 Opus 설계검토(§12~§14) → **본 3자 교차검증(현재)** → 통과 시 계측 복구 S1~S3(GPU 0) → 실패 시 계측 복구 우선순위만 재배열 → 최종 제출
- **[지금 할 일]** **새 학습을 승인하지 않는다.** GPU 0시간 항목(evaluator 지문 동등성 강제, G7 DR 분리, G6 upright 전환, 게이트를 인수→곱으로 교체, 잔여 시간 실측 기록)을 먼저 끝내고, 그다음 학습 없는 재판정(Default-01 v2 실측 + A017 3-seed)을 실행한다.
- **[보장하지 않음]** 본 문서의 어떤 수치도 공식 점수가 아니다. 계측을 고쳐도 정책이 좋아지지는 않는다. C1이 사실이라고 해서 기각된 후보들이 자동 복권되지는 않는다.

---

## 1. 집행 요약

### 1.1 한 문장

Go2 캠페인의 "연속 실패"는 대부분 **정책이 나빠서가 아니라, 걷지 못하는 정책을 기준선으로 삼고(C1), 후보와 기준선을 서로 다른 자로 재고(C2), 곱이 아니라 인수를 보는 게이트로 판정했기 때문에(C3)** 관측된 것이다. 다만 이것이 "모든 후보가 사실은 좋았다"는 뜻은 결코 아니다 — 같은 자로 잰 비교에서 실제로 파국적으로 무너진 후보가 3건 확정돼 있다(A015·A016·A018).

### 1.2 상위 원인 — 영향도 순

인과사슬은 지시서 §5.4 형식(`원자료 사실 → 잘못된 측정/판정 → 다음 실행 선택에 미친 영향 → 연속 실패처럼 관측된 결과`)을 따른다.

---

#### **C1 — 동결 기준선 Default-01은 보행 정책이 아니다.** 증거 강도 **높음**

**원자료 사실.** A006 동일 실행·동일 evaluator 안에서 두 arm의 실측 속도가 다음과 같다 (`workspace/_keep/go2_default_vs_pilot_v1/evaluation/{default,pilot}/cases/seed_101/*/summary.json#/speed_xy_mean`):

| case | Default-01 `speed_xy_mean` | Pilot-01 `speed_xy_mean` | Default `tracking_xy_rmse` |
|---|---:|---:|---:|
| `forward_nominal` | **0.0267 m/s** | 0.7424 m/s | 0.7369 |
| `forward_fast` | **0.0265 m/s** | 1.1736 m/s | 1.1852 |
| `rough_forward` | **0.0320 m/s** | 0.2413 m/s | 0.4979 |
| `left` | **0.0231 m/s** | 0.3034 m/s | 0.3533 |

Default-01의 RMSE는 명령 속도와 거의 같다 — 즉 **명령을 전혀 따르지 않고 제자리에 있다**. 시나리오 수준에서 G1 `tracking_proxy = 0.003619` 대 Pilot-01 `0.892505`다 (`.../default/SELF_EVAL_REPORT.json#/scenarios/G1`, `.../pilot/SELF_EVAL_REPORT.json#/scenarios/G1`). `ARTIFACT_VERIFIED`

그런데 같은 파일에서 Default-01의 G1 `survival_proxy = 1.0`이다. 이유는 v1 생존식이 `1 - terminated/num_envs`뿐이기 때문이다 — **가만히 서 있는 정책은 종료 이벤트가 없으므로 생존 100%다.**

Chain-01도 같은 병이다. `speed_xy_mean` 0.0259~0.0315로 Default와 사실상 동일한 행동이면서, `posture_gate_v2`로 재면 `survival_proxy = 0.0`이다 (`workspace/_keep/go2_chain01_baseline/evaluation/chain01/cases/seed_101/{forward_nominal,forward_fast,rough_forward}/summary.json`). **같은 비보행 행동이 v1에서는 생존 1.0, v2에서는 0.0으로 채점된다.** `ARTIFACT_VERIFIED`

**잘못된 측정/판정.** 캠페인은 `17.90699/70`이라는 Default-01 점수를 "정상 기준선"으로 받아들였다. 그 17.9점은 보행 능력이 아니라 **정지 상태가 v1 생존 게이트를 통과해 얻은 점수**다.

**다음 실행 선택에 미친 영향.** Default 계보 단일변수 실험 A007·A009·A010·A013·A024·A025와 Chain 계보 A020~A022, 합계 9건이 이 기준선(또는 그 파생)을 대조군으로 삼았다. 걷지 않는 정책과의 차이를 재는 것은 reward 효과 측정이 아니다.

**연속 실패처럼 관측된 결과.** Default 계보에서 나온 "+3.87", "+3.09", "+2.26" 같은 양수 delta는 reward가 좋았다는 뜻이 아니라 **바닥에서 조금 올라왔다**는 뜻일 수 있고, "−17.13", "−1.43" 같은 음수는 후보가 나빠졌다는 뜻이 아니라 **비교 대상이 성립하지 않는다**는 뜻이다. 어느 쪽도 다음 실험을 정당하게 안내하지 못했다.

**반증 가능 조건.** Default-01 checkpoint를 `posture_gate_v2`로 69-case 재측정했을 때 G1~G5 `survival_proxy`가 유의하게 0이 아니고 `speed_xy_mean`이 명령의 절반 이상이면 C1은 반증된다.

**영향받은 run.** A006, A007, A009, A010, A013, A020, A021, A022, A023, A024, A025.

**교정하지 않으면 재발하는 실패 형태.** 새 단일변수 실험이 계속 "정지 정책 대비 개선/악화"를 측정하고, 그 부호에 따라 다이얼을 채택·기각하게 된다.

**세 감사 중 발견자.** Phase I 단독. Codex와 기존 Opus 설계검토 모두 미발견.

---

#### **C2 — 후보와 기준선을 서로 다른 evaluator로 쟀다.** 증거 강도 **높음**

**원자료 사실.** `workspace/_keep/go2_g_a0*/`의 tier-1 실행 12건에 대해 양 arm의 `schema_version`/`survival_proxy_source`를 전수 스캔한 결과다. `ARTIFACT_VERIFIED`

| 실행 | baseline arm | candidate arm | 대칭성 |
|---|---|---|---|
| A010 (원, 260902) | `1 / None` | `1 / None` | 대칭 (v1) |
| A010 (재, 260906) | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A013 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A015 | `2 / posture_gate_v2` | `2 / posture_gate_v2` | 대칭 (v2) |
| A016 | `2 / posture_gate_v2` | `2 / posture_gate_v2` | 대칭 (v2) |
| A017 | `2 / posture_gate_v2` | `2 / posture_gate_v2` | 대칭 (v2) |
| A018 | `2 / posture_gate_v2` | `2 / posture_gate_v2` | 대칭 (v2) |
| A020 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A021 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A022 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A024 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |
| A025 | `1 / None` | `2 / posture_gate_v2` | **비대칭** |

**비대칭 7건 / 대칭 5건.** 원인은 러너가 캐시된 baseline을 복사하고 candidate만 새 evaluator로 계산하는 구조다 (`workspace/training/quadruped/server_run_go2_tuning_engine_v1.sh:334-350`; `tools/build_go2_tuning_engine.py:58-114`). 리포터에는 arm 지문 동등성 검사가 없다 (`workspace/training/quadruped/go2_tuning_eval_report.py:14-58`).

**잘못된 측정/판정.** 비대칭 방향이 한쪽으로 쏠려 있다. v1은 정지·낙상을 생존 1.0으로 세고 v2는 0.0으로 세므로, **baseline만 v1이면 baseline이 체계적으로 과대평가된다.** 따라서 이 7건의 음수 delta는 후보의 열화가 아니라 자의 차이일 수 있다.

**독립 재계산.** Codex와 Phase I이 서로 독립적으로 양 arm을 v1로 재채점한 결과가 6자리까지 일치했다.

| 실행 | 기록된 delta | 양 arm v1 재채점 delta |
|---|---:|---:|
| A010 (재) | −7.632522 | **+2.257160** |
| A013 | −1.427788 | **+3.654378** |
| A020 | −18.182232 | −0.221130 |
| A021 | −6.607936 | −0.351602 |
| A022 | −13.727549 | **+0.449132** |
| A024 | −17.132070 | **+3.461676** |

A010 재채점값 `+2.257160`이 260902 원 기록과 일치한다는 점이 이 재계산의 교차검증이다. **다만 이 v1 값들은 후보를 복권하는 근거가 아니다** — v1 자체가 C1이 보여준 대로 부적합한 자다. 이 표가 증명하는 것은 오직 **기존 음수 delta가 같은 자의 비교가 아니었다**는 것뿐이다.

**반증 가능 조건.** Default-01·Chain-01의 v2 실측이 나온 뒤 같은 자로 재계산한 delta가 여전히 크게 음수면, 이 7건은 계측 무효가 아니라 실제 열화로 확정된다.

**영향받은 run.** A010(재), A013, A020, A021, A022, A024, A025.

**교정하지 않으면 재발하는 실패 형태.** 기준선을 새로 만들 때마다 캐시 세대가 갈리고, 세대가 갈릴 때마다 후보가 부당하게 기각된다.

**세 감사 중 발견자.** Codex와 Phase I이 독립적으로 발견(수치까지 일치). 기존 Opus 설계검토는 §12에서 "evaluator 세대" 문제를 인지했으나 **전수 스캔으로 7건을 특정하지 못했고 부호 역전 재계산도 하지 않았다.**

---

#### **C3 — 안전 게이트가 곱이 아니라 인수를 본다.** 증거 강도 **높음**

**원자료 사실.** A017은 양 arm 모두 `posture_gate_v2`인 유효 비교다 (위 표). `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/reports/TIER1_DECISION.json`:

```
status  : INTERNAL_EARLY_KILL_FAIL
baseline: 46.4912409491015
candidate: 50.19915737233102
delta   : +3.70791642322952
failure_reasons: ["G4_survival_regressed_over_0.1"]

시나리오별 proxy delta:
  G1 +0.009905   G2 +0.041201   G3 +0.084472   G4 +0.015871
  G5 +0.109301   G6 -0.006756   G7 +0.103098
```

**7개 시나리오 중 6개의 `scenario_proxy`가 개선됐고, 유일하게 나빠진 G6조차 −0.0068이다. 그리고 후보를 죽인 G4는 `scenario_proxy`가 +0.015871로 개선된 시나리오다.** 게이트가 본 것은 G4의 `survival` 인수 −0.21875뿐이다. `ARTIFACT_VERIFIED`

**잘못된 측정/판정.** 공식형 시나리오 점수는 `survival × tracking`이라는 **곱**이다. 곱이 개선됐는데 인수 하나의 후퇴로 기각하는 것은 목적함수와 다른 것을 재는 것이다. `max_survival_regression = 0.10`이라는 임계값은 저장소 어디에도 유도 근거가 없다 (`workspace/training/quadruped/config/go2_self_eval_registry.json:42-50`).

**여기서 C1과 C3가 만난다.** 기준선이 정지 정책일수록 생존은 1.0에 붙는다. 그러면 **실제로 걷기 시작한 후보는 필연적으로 "생존 후퇴"로 보인다.** 생존 회귀 게이트는 구조적으로 보행을 처벌하는 방향으로 작동했다. 이는 두 개의 독립 결함이 아니라 하나의 병이 두 곳에서 나타난 것이다.

**다음 실행 선택에 미친 영향.** A017은 캠페인 전체에서 **동일 evaluator·양 arm 대칭 비교에서 총점이 개선된 유일한 후보**다. 이것이 `FAIL`로 기록되면서 track_lin 다이얼이 사실상 닫혔고, 후속은 A019(1.3 축소 재검증)를 취소한 채 다른 다이얼로 옮겨갔다.

**반증 가능 조건.** A017 checkpoint를 seed 202·303 및 69-case로 재평가했을 때 총점 우위가 사라지거나 G4 생존 후퇴가 더 커지면, 게이트의 보수성이 옳았던 것으로 판정된다.

**영향받은 run.** A017 (직접), A019 (취소로 파급), A009·A010(원) (동종의 target-only gate 오기각).

**교정하지 않으면 재발하는 실패 형태.** 총점을 올리는 후보가 계속 인수 단위 veto로 죽고, 캠페인은 "아무것도 통과하지 못한다"는 잘못된 학습을 반복한다.

**세 감사 중 발견자.** 세 감사 모두 발견. 다만 **인과 규모 주장은 기존 Opus 설계검토가 과대**했고(§1.3 참조), C1과의 연결은 본 감사가 처음 제시한다.

---

#### **C4 — 기준선을 4번 갈아치우면서 세대 간 다리를 놓지 않았다.** 증거 강도 **높음**

**원자료 사실.** `baseline_points_70`의 시계열: `17.537121`(A009) → `17.906992`(Default v1) → `46.491241`(Pilot tier-1 v2) → `18.610562`(Chain) → `17.132070`(Default 복귀). 같은 축의 이동이 아니라 **서로 다른 자로 잰 서로 다른 정책**이다.

**잘못된 측정/판정.** "12전 12패", "17회 0채택" 같은 요약은 이 네 계열의 합집합이다. 계열이 다르면 delta를 합산할 수 없다.

**다음 실행 선택에 미친 영향.** Chain-01은 A009·A010의 v1 양수 신호를 "검증된 승자"로 과대승급해 합성한 정책인데(`GO2_PROJECT_STATE.md:506-515`), 정작 Chain-01 자체의 v2 실측(A023, `2.307745/70`, 6/7 시나리오 생존 0)은 그 위에 세 실험(A020~A022)을 올린 **뒤에** 나왔다. 순서가 뒤집혔다.

**반증 가능 조건.** 네 기준선을 모두 동일 evaluator·동일 case/seed로 재측정했을 때 서열이 기존 서술과 같으면 C4의 영향은 축소된다.

**영향받은 run.** A009 이후 전부.

**교정하지 않으면 재발하는 실패 형태.** 기준선 교체 때마다 이전 실험 전체가 소급 무효가 되고, 누적 학습이 0이 된다.

**세 감사 중 발견자.** 기존 Opus 설계검토와 Phase I. Codex는 §3 표에서 개별적으로는 지적했으나 "4회 교체" 자체를 독립 원인으로 세우지 않았다.

---

#### **C5 — 7 case × 1 seed × worst-case 집계, no-op control 0건.** 증거 강도 **중간~높음**

**원자료 사실.** tier-1 판정은 seed 101 · 7 case뿐이다 (`workspace/training/quadruped/go2_tuning_eval_report.py:61-85`가 대표 승급에 3 seed를 요구하는 것과 대비된다). 그리고 **같은 reward로 seed만 바꿔 돌린 no-op control이 캠페인 전체에 0건**이다.

**잘못된 측정/판정.** PPO 실행간 분산이 얼마인지 모르는 상태에서 ±3점대 delta의 부호를 reward 효과로 읽었다. worst-case 집계는 단일 case의 꼬리에 총점을 종속시켜 분산을 더 키운다.

**다음 실행 선택에 미친 영향.** A007 `+3.87`, A009 `+3.09`, A010 `+2.26`, A017 `+3.71`이 모두 잡음 하한 미상인 채로 채택/기각 판단에 쓰였다.

**반증 가능 조건.** seed 43·44 no-op control의 총점 산포가 ±1.0/70 이내면, 위 delta들은 신호로 읽어도 된다.

**영향받은 run.** 전 tier-1 실행.

**교정하지 않으면 재발하는 실패 형태.** 잡음을 효과로 오독해 다이얼을 채택하거나, 실제 효과를 잡음으로 기각한다.

**세 감사 중 발견자.** 세 감사 모두. no-op control을 계획 단계로 승격한 것은 Phase I이 유일하다.

---

#### **C6 — 가중치 0.20이 무정보이거나 중복이다.** 증거 강도 **높음**

**원자료 사실 (a) — G7(가중 0.10)은 G3의 부분집합이다.** A012 Pilot-v2 full-suite에서 3 seed 전부 `dr_seed_<seed>/steps.csv`가 `rough_forward/steps.csv`와 SHA-256 바이트 동일이다. `ARTIFACT_VERIFIED`

```
f9e7680… seed_101/dr_seed_101/steps.csv == seed_101/rough_forward/steps.csv
df1f758… seed_202/dr_seed_202/steps.csv == seed_202/rough_forward/steps.csv
f262778… seed_303/dr_seed_303/steps.csv == seed_303/rough_forward/steps.csv
```

Codex는 이를 A006·A012·A023 세 스위트 전부에서 확인했다. **G7은 새 데이터를 만들지 않을 뿐 아니라, G3보다 높은 점수를 낸다** — G3는 `{rough_forward, rough_lateral} × 3seed` 6 case의 worst이고 G7은 `rough_forward × 3seed` 3 case의 worst라서 낮은 `rough_lateral`이 빠지기 때문이다. Pilot-01에서 G3 proxy `0.073386` 대 G7 proxy `0.484826`. 즉 **가중 0.10이 이미 있는 데이터를 유리한 쪽으로 다시 세고 있다.**

**원자료 사실 (b) — G6(가중 0.10)의 채점 필드가 상수다.** 리포터는 `recovery_rate`를 쓴다 (`workspace/training/quadruped/go2_fixed_eval_report.py:38`). Pilot-01의 push case 12/12 전부에서 그 값이 정확히 `1.0`이다. 반면 `recovery_rate_upright`는 0.9766~1.0으로 변별력이 있다. 수집기 주석 자신이 이렇게 쓴다 (`go2_eval_telemetry.py:286-291`):

> `recovery_rate` alone is not evidence of recovery: its quiet-window test (speed <= 0.15 m/s) is satisfied trivially by a robot that is lying on the ground and never moved. `recovery_rate_upright` … is the figure that may be quoted as recovery.

Chain-01 `push_pos_x`는 이 결함의 교과서적 사례다 — `recovery_rate = 1.0`, `recovery_rate_upright = 0.78125`, `survival_proxy = 0.5625`. **누워서 안 움직이는 로봇이 회복률 100%를 받는다.** `ARTIFACT_VERIFIED`

**영향 규모 — 본 감사의 독자 기여.** 두 결함을 고쳤을 때 실제 점수가 얼마나 바뀌는지 직접 재계산했다 (`go2_fixed_eval_report.build_policy()`를 런타임에 `_case_proxy`만 치환해 호출, 원본 모듈 무변경):

| 정책 | 현행 채점 | G6를 upright로 | + G7 제외·재가중 |
|---|---:|---:|---:|
| Pilot-01 (A012) | **33.793106** | 33.793106 | 33.777025 |
| Chain-01 (A023) | **2.307745** | 1.579102 | 1.754557 |

**해석은 양방향이다.** 계약 위반은 실재하고 반드시 고쳐야 하지만, **현재 서 있는 숫자에 미치는 영향은 Pilot-01에서 0.016/70, Chain-01에서 0.73/70에 불과하다.** 즉 C6은 계측 신뢰성 문제이지, 지금까지의 캠페인 결론을 뒤집는 크기는 아니다. Codex §5.7이 채점 인프라에 `REQUEST_CHANGES`를 준 것은 옳지만, 이 정정만으로 기각된 후보가 복권되지는 않는다.

**부수 확인.** 위 재계산에서 Pilot-01 `33.793106/70`과 Chain-01 `2.307745/70`이 원장 기록과 소수점 6자리까지 일치했다. `ARTIFACT_VERIFIED`

**반증 가능 조건.** 공식 evaluator가 실제로 `recovery_rate` 계열을 쓰고 DR을 G3와 같은 지형으로 돌린다면 C6은 결함이 아니다. 현재 로컬에 공식 evaluator가 없어 `[미확인]`.

**세 감사 중 발견자.** Codex 단독 발견(G6·G7 모두). Phase I은 G7을 독립 확인. **기존 Opus 설계검토는 둘 다 놓쳤다.**

---

### 1.3 기존 Opus 설계검토(§12~§14)의 자기 과대주장 정정

본 감사는 자신의 이전 결론에도 같은 잣대를 적용한다.

| 기존 주장 | 재판정 | 근거 |
|---|---|---|
| E1(생존 게이트 불일치)이 "0채택의 주원인" | **과대.** 확정 실사례는 A017 1건 | A015·A016·A018은 대칭 v2에서 총점 자체가 −30~−45로 붕괴했다. 이들은 게이트가 없어도 기각된다 |
| §12·§13 감사가 계측 결함을 규명했다 | **부분적.** **G7≡G3, G6 필드 오용, arm 지문 전수 비대칭, Default-01 비보행 — 네 건을 모두 놓쳤다** | 본 문서 C1·C2·C6 |
| §14 S1이 `terrain_5k_v1`로 5,000 iter 학습 | **승인 불가.** C1·C2 미해결 상태에서 GPU 6.4h 투입 | 아래 §8 |
| §14의 5,000 iter 근거를 H1 포화곡선에서 가져옴 | **규정 위반.** `AGENTS.md:250` | Go2 근거는 `quadruped_rewards.py:112-120`까지만 |

---

## 2. 감사 방법과 독립성 봉인

### 2.1 열람 순서

지시서 §2는 Phase I 종료까지 두 감사 문서의 열람을 금지한다. 본 감사는 이를 **컨텍스트 분리**로 집행했다. 본 세션은 `GO2_DESIGN_REVIEW_260907.md`를 이미 작성·보유한 오염된 컨텍스트이므로, Phase I을 스스로 수행하면 독립성 요건이 성립하지 않는다.

| 순서 | 주체 | 행위 | 산출물 |
|---:|---|---|---|
| 1 | 본 세션 | `OPUS_GO2_REAUDIT_PROMPT_260907.md` 열람 | — |
| 2 | 본 세션 | 격리 컨텍스트 감사관 1인 기동(병렬 없음). 금지 파일 2종 열람 차단을 명시 지시 | — |
| 3 | **격리 감사관** | 원자료만으로 Phase I 수행. 금지 문서 미열람 | `GO2_OPUS_REAUDIT_INDEPENDENT_260907.md` (98,690 B / 1,217 lines) |
| 4 | **격리 감사관** | 봉인 | `GO2_OPUS_REAUDIT_INDEPENDENT_260907.sha256` |
| 5 | 본 세션 | Phase I 대기 중 `GO2_INDEPENDENT_AUDIT_CODEX_260907.md` 열람 (격리 감사관에게는 금지된 문서) | — |
| 6 | 본 세션 | Codex 핵심 주장 3건을 원자료로 독립 재검증 | 본 문서 §1.2 C6, §3 |
| 7 | 본 세션 | Phase I 수령 후 그 핵심 주장(C1)을 원자료로 독립 재검증 | 본 문서 §1.2 C1 |
| 8 | 본 세션 | Phase II 대조 + Phase III 작성 | 본 문서 |

### 2.2 Phase I 봉인 해시

```
SHA256 = 43fd31a94af8c7ff397d121db3d78a52a036467dc3fa72aa799a19a9887909c9
파일   = C:\dev\Nconnect\GO2_OPUS_REAUDIT_INDEPENDENT_260907.md
크기   = 98,690 bytes / 1,217 lines
검증   = sha256sum -c OK
```

격리 감사관은 본문에 금지 문서 2종 미열람을 명시했고, 저장소를 읽기 전용으로 다뤘다고 보고했다. 재채점은 `go2_fixed_eval_report`를 import해 런타임에 `_case_proxy`만 치환·즉시 복원하는 방식으로 수행했으며 원본 모듈은 변경하지 않았다.

### 2.3 본 세션이 원자료로 직접 재검증한 항목

권위 채택 금지(지시서 §6) 원칙에 따라, 두 감사의 결론 중 판정에 결정적인 것만 골라 직접 확인했다.

| 대상 주장 | 출처 | 재검증 결과 |
|---|---|---|
| Default-01 비보행 | Phase I | **확인** — 표 §1.2 C1 |
| Chain-01 비보행 + v2 생존 0 | Phase I | **확인** |
| arm 지문 비대칭 7건 | Codex·Phase I | **확인** — 12건 전수 스캔, §1.2 C2 |
| G7 ≡ G3 바이트 동일 | Codex | **확인** — 3 seed 전부 |
| G6가 `recovery_rate` 채점 | Codex | **확인** — 12/12 케이스 값 1.0 상수 |
| `scenario_proxy ≠ survival×tracking` | Codex | **확인, 단 표현 정정 필요** — §3.2 |
| Pilot `33.793106` / Chain `2.307745` | 원장·Codex | **확인** — 독립 재계산 6자리 일치 |
| A017 판정 산술 | 세 감사 공통 | **확인** — 16자리 |

---

## 3. 공식형 채점·평가기 계약 진단

### 3.1 세 층위의 분리

지시서 §5.1이 요구한 3분할이다. **혼용 금지가 이 캠페인의 최대 서술 위험이다.**

| 층위 | 정의 | 현재 상태 |
|---|---|---|
| `FORMULA_PROXY_RESULT` | 공식형 곱셈·가중합 구조를 로컬 데이터에 적용한 내부 결과 | 산출됨. case 단위 `survival × tracking`은 구조적으로 일치 (`go2_fixed_eval_report.py:23-47`) |
| `CAMPAIGN_GATE_RESULT` | 캠페인이 추가한 안전·승급 gate 결과 | `min_total_points_delta 1.0`, `max_survival_regression 0.10`, `minimum_points_70 60.0`, survival 0.95 / tracking 0.70. **전부 내부 정책이며 공식 채점식에 없다** (`config/go2_self_eval_registry.json:42-50,60-70`) |
| `OFFICIAL_RESULT` | 실제 운영진 결과 | **전 실행 `[미측정]`.** 로컬에 공식 evaluator·공식 결과가 존재하지 않는다 |

**결론 1.** A006~A025 중 어떤 실행도 "공식식상 FAIL"이 아니다. 기록된 모든 `FAIL`은 `CAMPAIGN_GATE_RESULT`다. 반대로 어떤 실행도 "공식식상 합격"이 아니다.

### 3.2 산술 구조 — 곱셈은 맞지만 집계 순서가 미정이다

**Codex 주장 재판정.** Codex §5.0은 "시나리오 집계가 표시된 `survival × tracking`과 일치하지 않는다"며 Pilot G3에서 `0.5625 × 0.5904084 = 0.3321`인데 저장값이 `0.4403`이고 그 차이가 "G3 가중치 환산 약 1.515/70점"이라고 썼다. **부분동의 — 사실은 맞고 표현이 틀렸다.**

코드를 직접 읽으면 (`go2_fixed_eval_report.py:83-100`):

```python
worst     = min(valid, key=lambda item: item["proxy"]["scenario_proxy"])
survival  = min(item["proxy"]["survival_proxy"] for item in valid)
tracking  = min(item["proxy"]["tracking_proxy"] for item in valid)
proxy     = min(item["proxy"]["scenario_proxy"] for item in valid)
```

- **채점에 쓰이는 값은 `proxy`**, 즉 `min over cases (survival × tracking)`이다. 곱셈 구조는 case 수준에서 지켜지고 있다.
- `survival_proxy`·`tracking_proxy` 필드는 **각각 독립적으로 취한 최소값**이며, 서로 다른 case에서 올 수 있다. 그래서 두 필드를 곱해도 `scenario_proxy`가 나오지 않는다.

따라서 정확한 진술은 **"점수가 1.515점 부풀었다"가 아니라 다음 두 가지**다.

1. **집계 순서가 미정이다.** `min(a×b)`(현행)와 `min(a)×min(b)`(표시 필드가 암시하는 것)는 다른 값이며 G3 하나에서 `0.4403` 대 `0.3321`, 가중 환산 약 `1.515/70` 벌어진다. 공식 evaluator가 어느 쪽인지, 애초에 worst-case인지 평균인지 로컬 근거가 없다 (`config/go2_self_eval_registry.json:27-35`). `[미확인]`
2. **리포트가 자기모순적이다.** 검토자가 표시된 세 필드를 곱셈으로 재현할 수 없다. 이것은 제출 리포트의 신뢰성 문제다.

`ARTIFACT_VERIFIED` (`workspace/_keep/go2_default_vs_pilot_v1/evaluation/pilot/SELF_EVAL_REPORT.json#/scenarios/G3`: `survival_proxy 0.5625`, `tracking_proxy 0.5904083599115408`, `scenario_proxy 0.4403176288114788`, `worst_case {rough_lateral, seed 303}`)

### 3.3 생존 판정 — v1은 부적합, v2는 부분 준수

- **v1 (`termination_only_v1`)** 은 `1 - terminated/num_envs`뿐이다. C1이 보여준 대로 **정지·낙상 정책에 생존 1.0을 준다.** 기준선 채점에 쓰인 자로서 부적합하다.
- **v2 (`posture_gate_v2`)** 는 중력 투영과 지면 대비 높이를 결합해 0.5초 연속 비정상 자세를 낙상으로 센다. 방향은 옳다. 파라미터 `{grace_s 0.5, height_rel_min_m 0.18, hold_s 0.5, tilt_cos_max 0.5}`의 공식 calibration 근거는 없다. `[미확인]`
- **v2의 부분 준수.** 코드 주석은 두 채널을 요구한다고 쓰지만 실제 구현은 `measured = gravity_finite OR height_available`이고 height 부재 시 `height_ok = True`, 둘 다 없으면 `termination_only_v1`로 자동 fallback한다 (`go2_eval_telemetry.py:233-243,348-377`). 상위 계약은 두 채널을 요구하고 불가 시 `POSTURE_UNMEASURED`로 점수 사용을 금지한다 (`AGENTS.md:297-301`). **조용한 v1 퇴행 경로가 열려 있다.**

### 3.4 시나리오 구성의 정보량

| ID | 가중 | 상태 |
|---|---:|---|
| G1 평지 속도 추종 | 0.15 | 정상 |
| G2 방향별 속도 추종 | 0.15 | 정상 |
| G3 거친 지형 | 0.20 | 정상 |
| G4 경사 ±20° | 0.15 | 정상 |
| G5 계단 10~15cm | 0.15 | 정상 |
| **G6 밀침 회복** | **0.10** | **채점 필드 `recovery_rate`가 12/12 케이스에서 상수 1.0 — 변별력 0** |
| **G7 도메인 랜덤화** | **0.10** | **`steps.csv`가 G3의 `rough_forward`와 바이트 동일 — 독립 데이터 0** |

**가중치 1.00 중 0.20이 무정보이거나 중복이다.** (격리 감사관은 이를 "0.40"으로 보고했는데, 본 감사는 registry 가중치 원자료로 **0.20이 정확한 값**임을 확인한다. G3 자신의 0.20은 정상적으로 기능하므로 결함 가중에 포함되지 않는다.)

부수 확인: G7 DR 수정(`NCRC_EVAL_DR`)은 tuning-engine 러너에만 들어갔고 69-case 러너 3종에는 해당 문자열이 없다 — 즉 **full-suite에서만 G7이 무효**이고 tier-1 G7은 별도 확인이 필요하다.

### 3.5 스펙과 구현의 provenance 불일치

- registry는 tracking std를 env에서 읽는다고 명세하지만 (`config/go2_self_eval_registry.json:37-40`) 리포터는 `TRACKING_STD = 0.5`를 하드코딩한다 (`go2_fixed_eval_report.py:12,19-20`). 현재 env도 `std: 0.5`라 수치 차이는 없으나 계약이 깨져 있다.
- JSON Schema는 `engine_version: 1.2.0`만 허용하는데 런타임은 `1.3.0`이고 A025 spec도 `1.3.0`이다 (`config/go2_tuning_experiment_schema.json:6-63`; `go2_tuning_config.py:17,156-211`). **스키마 단독 검증은 현행 spec을 거부하면서 nested gate 타당성은 검증하지 못한다.**
- 리포터에 arm 지문 동등성 검사가 없다 (`go2_tuning_eval_report.py:14-58`) — C2의 코드적 원인.

### 3.6 강좌에 공식 채점식은 없다

지시서 §3-4가 요구한 전수 검색 결과를 Codex가 수행했고, `test/` 강좌에서 `survival_rate`·`tracking_score`·70점·G3~G7 정의가 **0건**이었다. 강좌가 직접 지지하는 것은 reward 의미, "한 번에 하나씩", 1,000 iter 예시와 영상 비교뿐이다. 채점 정의의 로컬 출처는 `workspace/PRELIM_RL_GUID.md:60-70,92-104` 단 하나이며, 이것이 운영진 원문과 동일한지는 `[미확인]`이다.

→ `config/go2_self_eval_registry.json:12-16`의 `course_or_guide_direct`는 출처를 뭉뚱그린 것이므로, G1~G7·가중치는 **guide direct**, 단일변수 지침만 **course direct**로 분리해야 한다.

---

## 4. A006~A025 실행별 전수 감사표

전 20개 ID가 각각 정확히 한 번 등장한다. `절대 /70`은 해당 실행 evaluator의 candidate 절대 proxy, `delta /70`은 기록된 값이다. `OFFICIAL_RESULT`는 전 행 `[미측정]`이므로 열에서 생략한다.

| ID | 유형 | 기준선 계보 | evaluator (base/cand) | 단일변수 | 절대 /70 | delta /70 | 캠페인 게이트 | 판정 유효성 | 최종 분류 |
|---|---|---|---|---|---:|---:|---|---|---|
| **A006** | 기준선 측정 (paired) | — | v1 / v1 | ✗ (4변수) | Def `17.906992` · Pil `41.979898` | — | 양쪽 `INTERNAL_GATE_FAIL` | **부분 유효** — 같은 v1 안의 상대 방향만. G7 무효 | 기준선 측정 |
| **A007** | 학습 (Default+`feet_air .01→.20`) | Default-01 | v1 / v1 | ✓ | `21.772582` | **+3.865590** | `INTERNAL_SCREEN_FAIL` (목표 G5 미달) | **유효 비교(같은 자), 단 기준선 비보행** | 운영 실패 — 양수 총점 신호를 목표 게이트로 폐기 |
| **A008** | 인프라 (crash/graceful shutdown hotfix) | — | — | — | — | — | — | N/A | 인프라 |
| **A009** | 학습 (Default+`track_lin 1.0→1.2`) | Default-01 | v1 / v1 | ✓ | `20.627405` | **+3.090285** | target G1 미달 FAIL | **유효 비교(같은 자), 단 기준선 비보행** | 운영 실패 — target-only 게이트 오기각 |
| **A010** | 학습 (Default+`lin_vel_z −3→−2`) ×2회 판정 | Default-01 | 원 v1/v1 · 재 **v1/v2** | ✓ | 원 `19.79…` · 재 `9.499548` | 원 **+2.257160** · 재 −7.632522 | 원 target FAIL · 재 FAIL | 원 유효 / **재 `EVALUATOR_MISMATCH`** | 계측 무효 (재판정분) |
| **A011** | **식별자 충돌** | — | — | — | — | — | — | **`LEDGER_IDENTITY_FAIL`** — 실제 artifact의 `work_id = G-A009` | 미실행 + 라벨 재사용 |
| **A012** | 기준선 측정 (Pilot-01 69-case, 학습 0) | Pilot-01 | — / v2 | — | **`33.793106`** (독립 재계산 일치) | — | `INTERNAL_GATE_FAIL` | **유효** — G1~G6 절대 v2 측정. G7 무효 | 기준선 측정 |
| **A013** | 학습 (Default+`flat_orientation 0→−1`) | Default-01 | **v1 / v2** | ✓ | `15.704282` | −1.427788 | FAIL | **`EVALUATOR_MISMATCH`** — 대칭 v1 재채점 `+3.654378` | 계측 무효 |
| **A014** | 취소 (`flat_orientation −2` 조건부 후속) | — | — | — | — | — | — | N/A | 미실행 |
| **A015** | 학습 (Pilot+`feet_air .20→.35`) | Pilot-01 | **v2 / v2** | ✓ | `16.369309` | **−30.121932** | 5 시나리오 생존 veto | **유효** | **실제 정책 실패 (확정)** |
| **A016** | 학습 (Pilot+`ang_vel −.05→−.15`) | Pilot-01 | **v2 / v2** | ✓ | `1.375697` | **−45.115544** | 7/7 생존 회귀 | **유효** | **실제 정책 실패 (확정)** |
| **A017** | 학습 (Pilot+`track_lin 1.2→1.4`) | Pilot-01 | **v2 / v2** | ✓ | **`50.199157`** | **+3.707916** | `INTERNAL_EARLY_KILL_FAIL` — `G4_survival_regressed_over_0.1` | **유효 비교, 게이트 판정은 부적합** — G4 proxy 자체는 **+0.015871** | 운영 실패 — 곱이 아닌 인수로 기각 |
| **A018** | 학습 (Pilot+`action_rate −.01→−.008`) | Pilot-01 | **v2 / v2** | ✓ | `2.092602` | **−44.398639** | 7/7 생존 회귀 | **유효** | **실제 정책 실패 (확정)** |
| **A019** | 취소 (`track_lin 1.3` 축소 재검증) | Pilot-01 | — | — | — | — | — | N/A | 미실행 — **정보가치 최상이었으나 미집행** |
| **A020** | 학습 (Chain+`lin_vel −3→−2`) | Chain-01 | **v1 / v2** | ✓ | `0.428330` | −18.182232 | FAIL | **`EVALUATOR_MISMATCH`** — 대칭 v1 `−0.221130` | 계측 무효 + 후보 절대 붕괴 |
| **A021** | 학습 (Chain+`ang_vel −.08→−.05`) | Chain-01 | **v1 / v2** | ✓ | `12.002626` | −6.607936 | FAIL | **`EVALUATOR_MISMATCH`** — 대칭 v1 `−0.351602` | 계측 무효 |
| **A022** | 학습 (Chain+`feet_air .01→.20`) | Chain-01 | **v1 / v2** | ✓ | `4.883013` | −13.727549 | FAIL | **`EVALUATOR_MISMATCH`** — 대칭 v1 **`+0.449132`** | 계측 무효 + 후보 절대 붕괴 |
| **A023** | 기준선 측정 (Chain-01 69-case, 학습 0) | Chain-01 | — / v2 | — | **`2.307745`** (독립 재계산 일치) | — | `INTERNAL_GATE_FAIL` — 6/7 생존 0 | **유효 측정, 순서 오류** — A020~A022 **뒤에** 나옴 | 기준선 측정 |
| **A024** | 학습 (Default+`ang_vel −.08→−.15`) | Default-01 | **v1 / v2** | ✓ | `0.0` | −17.132070 | 7/7 생존 회귀 | **`EVALUATOR_MISMATCH`** — 대칭 v1 `+3.461676` | 계측 무효 + 후보 절대 붕괴 |
| **A025** | **A013의 중복 실행** | Default-01 | **v1 / v2** | ✓ | `15.704282` | −1.427788 | FAIL | **중복** — model SHA `676cc1cb…b12e1c` 동일, 16자리 일치 | 중복 — 정보이득 0 |

### 4.1 분류 집계 (각 ID 1회)

| 분류 | 건수 | ID |
|---|---:|---|
| 기준선 측정 | 3 | A006, A012, A023 |
| 인프라 | 1 | A008 |
| 미실행·취소 | 3 | A011, A014, A019 |
| 중복 | 1 | A025 |
| **실제 정책 실패 (같은 자로 확정)** | **3** | A015, A016, A018 |
| **비교 무효 — 계측 실패** | **6** | A010, A013, A020, A021, A022, A024 |
| **운영 실패 — 유효 신호 오처리** | **3** | A007, A009, A017 |
| 합계 | **20** | A006~A025 |

**주.** 계측 무효 6건 중 A020·A022·A024는 후보의 **절대** v2 점수가 각각 `0.428`·`4.883`·`0.0`으로 파국적이다. 이 후보들이 나쁘다는 것은 별도 근거로 말할 수 있으나, **"그 reward 변경이 원인"이라는 인과는 대칭 기준선 없이 확정할 수 없다.** 계측 결함으로 후보를 복권하지 않고, 후보 붕괴로 계측 결함을 덮지 않는다 (지시서 §9).

### 4.2 지시서 §5.2가 요구한 개별 검증

1. **실행/미실행/중복/인프라 구분** — 위 표. 학습 실행 12건, 학습 없는 측정 3건, 인프라 1건, 미실행 3건, 중복 1건.
2. **arm 조건 동일성** — §1.2 C2 전수 스캔. 대칭 5 / 비대칭 7.
3. **절대와 delta의 결론 충돌** — A017이 대표 사례(절대 `50.199 < 60` 미달이나 delta `+3.708` 양수). A022는 반대 방향(절대 `4.883` 붕괴, 대칭 v1 delta `+0.449`).
4. **양의 총점 delta가 게이트로 거절된 사례** — **4건**: A007 `+3.865590`(목표 G5), A009 `+3.090285`(target G1), A010원 `+2.257160`(target G1), A017 `+3.707916`(G4 생존).
5. **같은 세대에서 실제 붕괴한 사례** — **3건**: A015 `−30.12`, A016 `−45.12`, A018 `−44.40`. 모두 양 arm v2.
6. **중복 계상** — A025가 A013을 재측정으로 계상. A011이 A009 결과를 재명명.
7. **1,000 iter · 단일 학습 seed · 단일 평가 seed로 확정** — 전 tier-1 실행 해당. C5.
8. **`model_best.pt` 선택 기준과 비교 iter** — `Train/mean_reward` 최대치 선택이며 최대 iter가 아니다 (`go2_task/_finalize.py:40-105`). 실제 비교 checkpoint는 iter 662~999로 서로 다르다. A017은 step 856 → `model_900.pt`이고 중간 checkpoint 10개는 삭제됐다 (`:108-134,178-180`).
9. **no-op control** — **0건.** C5.

---

## 5. H1 대비 Go2 연속 실패 원인 분석

### 5.0 전제 — H1은 성공 신화가 아니다

H1도 공식 결과가 없다. `H1_REWARD_EVIDENCE_MASTER.md:13`은 "운영진 evaluator 점수, 예선 통과 결과, H1~H3·H5 격리 정량값, 독립 seed"를 미확보로 명시하고, `:17`은 "영상 1세트나 단일 seed만으로 공식 survival_rate, 최적 reward, 예선 점수 또는 통과 가능성을 보장하지 않는다"고 못박는다. Run06조차 `INTERNAL_GATE_PASS`이며 `:99`는 "10k 결과가 좋아도 최종 승급이 아니다"라고 쓴다.

따라서 비교 가능한 것은 **실험 방법·증거 폐쇄성·평가 순서·반복 설계**뿐이다 (지시서 §3-9, `workspace/training/quadruped/AGENTS.md:8-22`). H1의 reward 값·임계값·학습 단가·시나리오 결론은 Go2 사실로 전이하지 않는다.

### 5.1 비교표

| 비교 축 | H1 원자료에서 확인된 사실 | Go2 원자료에서 확인된 사실 | 차이가 실패 연속성에 미친 영향 | 증거 강도 |
|---|---|---|---|---|
| **기준선 확정 시점·조건** | Run06(`model_9900`)을 **고정 evaluator 후보로 승급하고 reward 실험을 동결** (`H1_REWARD_EVIDENCE_MASTER.md:86-87`) | 기준선을 4회 교체: `17.537121` → `17.906992` → `46.491241` → `18.610562` → `17.132070`. 교체 시 세대 간 다리 없음 | **결정적.** Go2는 매 교체마다 이전 실험이 소급 무효가 됐다 (C4) | 높음 |
| **기준선이 실제로 과제를 수행하는가** | Run06은 학습지표 5/5 게이트 통과, 영상에서 요구 행동 관찰 (`:12`) | **Default-01은 `speed_xy_mean` 0.023~0.032 m/s로 보행하지 않는다.** 그럼에도 v1 생존 1.0 (C1) | **가장 결정적.** 대조군이 과제를 수행하지 못하면 어떤 delta도 reward 효과가 아니다 | 높음 |
| **한 번에 바꾼 reward 변수 수** | 단일 reward만 변경 (`:208`) | A006만 4변수, A007~A025는 단일변수 (spec `single_change` 확인) | 중립 — Go2도 A007부터는 지켰다 | 높음 |
| **screening iteration** | **3,000 iter** 고정 (`:208`) | **1,000 iter** 고정 | 중간. 로컬 근거가 상충하므로(§7 쟁점 6) 단독 원인으로 세울 수 없다 | 중간 |
| **장기 학습 승급 기준** | screening winner만 독립 seed 재검증 후 10,000 iter 후보 (`:232`) | 장기 승급 0건. A019(축소 재검증)조차 취소 | 중간 — Go2는 승급 경로 자체가 한 번도 작동하지 않았다 | 높음 |
| **학습 seed · 평가 seed 반복** | 단일 seed 한계를 **문서에 명시하고 Colas et al.을 인용** (`:73,:175`), 승급 조건에 독립 seed를 넣음 (`:232`) | 학습 seed 42 고정, 평가 seed 101 단일. 3 seed는 대표 승급 요건에만 있고 tier-1 판정에는 미적용 | **결정적.** Go2는 잡음 하한을 모른 채 부호를 읽었다 (C5) | 높음 |
| **evaluator 세대 고정** | evaluator를 **먼저 확장·고정한 뒤** screening (`:199`) | 캠페인 중간에 v1→v2 전환. 러너가 캐시 baseline을 복사해 **7건이 v1 대 v2** (C2) | **결정적** | 높음 |
| **자세 기반 생존 검증** | H4·H6·H7은 고정 telemetry 회수, H1~H3·H5는 영상 관찰 | v1은 자세를 보지 않음. v2는 두 채널 `OR` + 무측정 시 v1 자동 fallback (`go2_eval_telemetry.py:233-243,348-377`) | 높음 — v1 구간의 모든 생존 수치가 부적합 | 높음 |
| **checkpoint 선택·비교 iter 정합** | Run05/Run06 첫 3,000 iter가 TensorBoard 31개 태그 bit-identical (`:73`) | `Train/mean_reward` 최대치 선택이라 비교 checkpoint iter가 662~999로 제각각 (`go2_task/_finalize.py:40-105`) | 중간 — 세대 간 비교에 계통 오차 유입 | 높음 |
| **정책 평가 완료 후 다음 학습으로 넘어갔는가** | Run06 회수 완료 전 서버·tmux 종료 금지를 규칙화 (`:96-99`) | **A023(Chain-01 실측)이 A020~A022 뒤에 나왔다.** 기준선의 실체를 모른 채 그 위에 세 실험을 올림 | **결정적** — 순서 역전이 3건을 무효화 | 높음 |
| **시나리오 구성·지형 비중** | H1~H7, 밀침·경사 포함 | G1~G7, 지형 가중 G3 0.20 + G4 0.15 + G5 0.15 = 0.50 | 구조적 난도 차이는 있으나(§5.2 Q3) 현재 실패의 직접 원인은 아니다 | 중간 |
| **안전성 reward/levers 활성 상태** | — | `undesired_contacts: null`, `termination_penalty` 부재, `track_ang_vel_z_exp`는 env에 존재하나 reward 파일에서 주석 (`quadruped_rewards.py:85-94`) | 낮음 — 미탐색 다이얼은 1개뿐 | 높음 |
| **영상·telemetry·내부 정량·공식 결과 분리** | 4층 분리를 문서 규칙으로 강제 (`:159,:163`) | 문서 규칙은 동일하나, **A015 이후 전 실행 영상이 `VIDEO_UNKNOWN`** | 중간 — 붕괴 후보의 붕괴 양상을 눈으로 확인하지 못했다 | 중간 |
| **실패 실험이 다음 의사결정을 구분했는가** | Run02→04 yaw 개선을 Run05에서 재검증하고 경계 후퇴를 기록 (`:46`) | A017 양수 신호의 후속(A019)을 취소. A016 파국 뒤 같은 값을 Default 계보에서 재시도(A024) | 높음 — 실패로부터의 학습이 다음 실행에 반영되지 않았다 | 높음 |

### 5.2 지시서 §5.3의 6개 질문에 대한 답

**Q1. "Go2의 연속 FAIL" 중 몇 건이 무엇인가?**

§4.1 표 그대로다. 학습 실행 12건 기준으로:

- **실제 정책 실패 3건** (A015, A016, A018) — 양 arm v2 대칭, 총점 −30~−45. 게이트가 없어도 기각된다.
- **비교 무효·계측 실패 6건** (A010, A013, A020, A021, A022, A024) — arm 지문 비대칭. 이 중 A020·A022·A024는 후보 절대값도 붕괴했으나 **원인 귀속은 불가**.
- **운영 실패 3건** (A007, A009, A017) — 같은 자로 잰 양수 총점 신호를 목표/인수 게이트로 폐기.

여기에 미실행 3, 중복 1, 인프라 1, 기준선 측정 3을 더해 20 ID가 전부 분류된다.

**분류 기준.** ① 양 arm 지문이 같은가 → 아니면 계측 실패. ② 같다면 총점 delta 부호가 음인가 → 음이면 정책 실패. ③ 양수인데 게이트로 기각됐는가 → 운영 실패. C1(기준선 비보행)은 이 분류의 위에 겹쳐 있어, **A007·A009·A010원의 "유효 비교"는 자 자체가 같다는 뜻일 뿐 대조군이 타당하다는 뜻은 아니다.**

**Q2. H1이 잘 진행된 핵심 이유는 정책·reward 자체인가, 실험 설계와 증거 폐쇄성인가?**

**설계와 증거 폐쇄성이다.** 근거는 세 가지다. ① H1은 evaluator를 먼저 고정하고 screening을 시작했다(`:199,:208`) — Go2는 캠페인 중간에 evaluator를 바꿨다. ② H1은 기준선을 동결하고 reward 실험을 멈췄다(`:86-87`) — Go2는 4번 갈아치웠다. ③ H1은 단일 seed 한계를 문서에 적고 승급 조건에 독립 seed를 넣었다(`:73,:232`) — Go2는 tier-1 판정에 단일 seed를 그대로 썼다.

**반대 근거도 기록한다.** H1의 Run06 우위는 reward 튜닝이 아니라 **3k→10k 학습시간 확장**에서 나왔고, H1 문서 자신이 "reward 튜닝도, 독립 seed 반복도 아니다"라고 쓴다(`:73`). 즉 H1은 "좋은 reward를 찾았다"기보다 **"기준선을 하나 정해 오래 학습시키고 그 이상 건드리지 않았다"**에 가깝다. Go2에 전이 가능한 교훈은 reward 값이 아니라 **동결과 순서**다.

**Q3. Go2 시나리오 구조가 H1보다 어려웠다는 주장은 어디까지 확인되는가?**

로컬 규정으로 확인되는 것은 **가중치 구성**뿐이다. Go2는 지형 계열(G3 0.20 + G4 0.15 + G5 0.15)이 총 0.50이고, 여기에 worst-case 집계가 걸린다 — 한 case만 무너져도 시나리오 점수가 그 값으로 내려간다. 이는 구조적으로 가혹하다.

**그러나 이것을 현재 실패의 직접 원인으로 쓸 수 없다.** 같은 구조에서 Pilot-01이 `33.793106/70`을 냈고 A017이 tier-1 `50.199157/70`을 냈다. 구조가 유일 병목이면 이런 편차가 나오지 않는다. **구조적 난도는 상수이고, 캠페인 실패는 변수에서 나왔다.**

**Q4. H1 evaluator에도 생존 판정 한계가 있는가?**

있다. `H1_REWARD_EVIDENCE_MASTER.md:13`은 "H4·H6·H7은 영상만으로 조건 식별이 불가능하다"고 쓰고, `:144`는 H7 밀침을 "직접 측정 안 함"으로 기록한다. `:132`는 내부 proxy가 운영진 evaluator 입력값과 동일하다고 확인되지 않았다고 명시한다.

→ **H1을 비교 기준으로 쓸 수 있는 범위는 "절차와 순서"로 제한한다.** H1의 생존·시나리오 수치를 Go2 판정의 기준선이나 임계값 근거로 쓰는 것은 금지한다.

**Q5. reward 문제로 인과 확정 가능한 실행과 불가능한 실행**

| 인과 확정 가능 (3) | 근거 |
|---|---|
| **A015** `feet_air_time .20→.35` | 양 arm v2 대칭, `−30.121932`, 5 시나리오 생존 veto |
| **A016** `ang_vel_xy −.05→−.15` | 양 arm v2 대칭, `−45.115544`, 7/7 생존 회귀 |
| **A018** `action_rate −.01→−.008` | 양 arm v2 대칭, `−44.398639`, 7/7 생존 회귀 |

| 인과 확정 불가 (9) | 사유 |
|---|---|
| A007, A009, A010(원) | 자는 같으나 **대조군이 비보행 정책**(C1) |
| A010(재), A013, A020, A021, A022, A024 | **arm 지문 비대칭**(C2) |

**A017은 세 번째 범주다** — 자도 같고 대조군(Pilot-01)도 보행한다. 따라서 `track_lin_vel_xy 1.2→1.4`의 tier-1 총점 효과 `+3.707916`은 **인과적으로 유효한 유일한 양수 측정**이다. 다만 단일 seed·7 case이므로 확정이 아니라 **재검증 1순위**다.

**Q6. 평가기를 먼저 고치지 않고 새 reward 학습을 계속하면 어떤 오류가 반복되는가?**

네 가지가 결정론적으로 반복된다.

1. **기준선 캐시 세대가 갈려 후보가 부당 기각된다** (C2). 코드에 지문 동등성 검사가 없으므로 다음 실행에서도 그대로 일어난다.
2. **보행을 시작한 후보가 "생존 후퇴"로 기각된다** (C1+C3). 대조군이 정지 정책인 한 구조적으로 반복된다.
3. **총점을 올린 후보가 인수 veto로 죽는다** (C3). `max_survival_regression = 0.10`은 유도 근거가 없다.
4. **G6·G7 가중 0.20이 계속 무정보를 채점한다** (C6). 변별이 필요한 곳에서 점수가 움직이지 않는다.

---

## 6. 실제 정책 실패 / 측정 실패 / 운영 실패 분해

### 6.1 세 범주의 상호 관계

이 셋은 배타적이지 않고 **층을 이룬다.**

```
[운영 실패]  기준선을 4번 교체, 순서 역전(A023이 A020~A022 뒤), no-op control 0건,
             A019 취소, A024가 A016의 파국을 반복
      ↓ 이것이 만든 조건 위에서
[측정 실패]  비보행 정책을 기준선으로 채점(C1), arm 지문 비대칭 7건(C2),
             인수 veto(C3), G6·G7 무정보 0.20(C6)
      ↓ 이것이 가린 채로
[정책 실패]  A015·A016·A018 — 실제로 무너진 3건
```

**따라서 "연속 실패"의 표층은 운영이고, 심층은 측정이며, 진짜 정책 실패는 12건 중 3건이다.**

### 6.2 각 범주의 확정 사실과 미확정 부분

**A. 실제 정책 실패 — 3건 확정, 3건 후보 절대 붕괴(원인 미확정)**

확정 3건(A015·A016·A018)의 공통점은 **penalty 계열을 공격적으로 키웠다는 것**이다: `feet_air_time` 0.20→0.35(1.75배), `ang_vel_xy` −0.05→−0.15(3배), `action_rate` −0.01→−0.008. 이 중 A016은 3배 증폭이라 단일변수라도 폭이 과했다.

후보 절대 붕괴 3건(A020 `0.428`, A022 `4.883`, A024 `0.0`)은 **후보가 부적합하다는 사실은 남지만 그 원인이 reward 변경이라는 인과는 성립하지 않는다.** A024는 특히 A016과 동일한 `ang_vel −0.15` 값을 Default 계보에서 재시도한 것으로, 이미 파국이 확인된 값을 재투입한 운영 판단이 겹쳐 있다.

**B. 측정 실패 — 6건이 판정 무효, 그리고 그보다 큰 것**

건수로는 6건이지만, **C1은 건수로 셀 수 없는 종류의 실패다.** Default 계보 전체(A007·A009·A010·A013·A024·A025)와 Chain 계보 전체(A020~A022)가 비보행 대조군 위에 서 있다. 즉 12건의 학습 실행 중 **9건이 대조군 타당성 문제를 안고 있고**, 그중 6건은 자까지 달랐다.

**정직하게 남길 부분.** 계측 결함이 후보를 복권하지는 않는다. 대칭 v1 재계산이 `+3.65`(A013), `+3.46`(A024)을 내놓더라도 **v1 자체가 정지 정책에 생존 1.0을 주는 부적합한 자**이므로, 이 양수들은 "후보가 좋다"가 아니라 "기존 음수가 근거 없다"만 말한다.

**C. 운영 실패 — 3건의 오처리, 그리고 5개의 절차 결함**

오처리 3건(A007·A009·A017)에 더해 절차 수준에서:

1. **순서 역전** — Chain-01의 실체(A023, `2.307745/70`, 6/7 생존 0)가 그 위에 세운 세 실험 뒤에 측정됐다.
2. **과대승급** — Chain-01은 A009·A010의 **v1** 양수 신호를 "검증된 승자"로 취급해 합성한 정책이다 (`GO2_PROJECT_STATE.md:506-515`).
3. **정보가치 최상 실험의 취소** — A019(`track_lin 1.3`)는 A017의 유일한 양수 신호를 절반 폭으로 재검증하는 설계였고 취소됐다.
4. **파국의 재투입** — A024가 A016에서 이미 파국이 확인된 `ang_vel −0.15`를 계보만 바꿔 재시도했다.
5. **잔여 예산 원장 미갱신** — 마지막 raw 기록은 260903 "잔여 25시간"뿐이고, 이후 A013~A025 다수 실행의 비용이 차감되지 않았다 (`GO2_CAMPAIGN_SCHEDULE.md:228`). **현재 잔여 시간은 `[미확인]`이며, 어떤 계획도 25시간을 현재값으로 인용해서는 안 된다.**

### 6.3 "12전 12패"라는 서술의 해체

| 서술 | 재판정 |
|---|---|
| "Go2는 12번 실험해서 12번 다 실패했다" | **성립하지 않는다.** 12건 중 **같은 자·타당한 대조군으로 잰 유효 비교는 A015~A018 4건뿐**이고, 그중 A017은 총점이 개선됐다 |
| "reward 값이 나빴다" | 3건(A015·A016·A018)에 대해서만 참 |
| "Go2가 2족보다 어렵다" | 구조적 난도는 상수. 같은 구조에서 `2.31`부터 `50.20`까지 편차가 나왔다 |
| "evaluator를 고쳐서 다 무효가 됐다" | 6건 무효. 나머지 6건은 유효하며 그중 3건은 진짜 실패다 |

---

## 7. 세 감사의 쟁점별 대조

지시서 §6에 따라 **다수결을 쓰지 않는다.** 각 쟁점은 원자료로 재판정하며, 세 문서가 모두 같은 말을 해도 원자료가 다르면 원자료를 따른다.

`분류`는 Phase I 독립 결론 · Codex · 기존 Opus 셋 사이의 관계다.

| # | 쟁점 | Phase I 독립 결론 | Codex 감사 결론 | 기존 Opus 결론 (§12~§14) | 분류 | **원자료 기준 최종 판정** |
|---:|---|---|---|---|---|---|
| 1 | **Go2가 H1보다 연속 실패한 원인** | 기준선이 보행하지 않음(C1)이 최상위. 그 위에 계측·운영 | 계측 비대칭이 최상위. 기준선 실체는 미언급 | E1(생존 게이트)이 최상위 | **불일치 (3자 3설)** | **C1 > C2 > C3 순.** C1은 원자료로 확인(§1.2). C2는 7건 전수 확인. E1(=C3)은 실사례 1건뿐이므로 최상위 원인이 될 수 없다 |
| 2 | **evaluator 세대 비대칭의 영향 범위** | 고유 6건 + 중복 1건, 부호 역전 재계산 제시 | 6건 + 중복 1건, 동일 수치 | "evaluator 세대" 문제로 인지, **건수 특정도 재계산도 없음** | **부분동의 (Opus 미달)** | **비대칭 7건 확정** (A010재·A013·A020·A021·A022·A024·A025). 12건 전수 지문 스캔으로 재확인. 대칭 5건 |
| 3 | **절대 proxy와 delta/gate의 용도 분리** | 제출 후보는 절대, reward 인과는 delta | 동일 | §14에서 절대(69-case worst-case) 단일 척도로 통일 | **부분동의** | **Phase I·Codex가 옳다.** 두 척도는 서로 다른 질문에 답한다. §14가 절대 하나로 통일한 것은 reward 인과 판정 능력을 버리는 것이다 |
| 4 | **양수 총점인데 게이트로 기각된 사례** | 4건 (A007·A009·A010원·A017) | A009·A010·A017 명시 | A017 중심, "두 번째 사례"는 §12에서 자체 철회 | **부분동의** | **4건 확정.** A007 `+3.865590`(목표 G5), A009 `+3.090285`, A010원 `+2.257160`, A017 `+3.707916` |
| 5 | **같은 세대에서 실제 붕괴한 후보** | A015·A016·A018 확정 + A020·A022·A024 절대 붕괴 | 동일 | §12에서 인정 | **동의** | **확정 3건** (A015·A016·A018). A020·A022·A024는 절대 붕괴이나 인과 미확정 |
| 6 | **1,000 iter의 정당한 용도와 오용** | screening 허용, 확정 불가 | 동일. `PRELIM_RL_GUIDE.md:173-183`(1000~5000)과 `quadruped_rewards.py:112-120`(3000~5000)의 충돌을 지적 | E3에서 "3,000 하한 미달"로 주장했다가 §12에서 자체 철회 | **동의 (Opus는 자체 정정 후 합류)** | **1,000 iter는 screening에 허용, 확정에는 불충분.** 로컬 근거가 상충하므로 3,000을 운영진 단일 하한으로 확정할 수 없다 |
| 7 | **baseline 승급·교체 순서** | 4회 교체 + A023 순서 역전을 독립 원인(C4)으로 세움 | §3 표에서 개별 지적, 독립 원인으로는 미승격 | §12에서 부분 인정 | **부분동의 (Phase I이 가장 정확)** | **C4 확정.** 교체 4회 + Chain-01 실측이 그 위 세 실험 뒤에 나온 순서 역전 |
| 8 | **G3/G7 독립성** | G7≡G3 확인. **A006·A012·A023 세 스위트 전부**. 69-case 러너 3종에 `NCRC_EVAL_DR` 문자열 부재까지 확인 | G7≡G3 확인. 원인을 러너 분기로 특정 | **미발견** | **한쪽만 발견 (Opus 누락)** | **확정.** 본 감사가 A012 3 seed 전부 SHA-256 바이트 동일 재확인. **가중 0.10이 G3의 부분집합을 유리한 쪽으로 재계상** (Pilot G3 `0.073386` vs G7 `0.484826`) |
| 9 | **posture gate 논리** | v2가 v1보다 타당하나 부분 준수 | 두 채널 `OR` + 무측정 시 v1 fallback을 코드로 특정 | v2 도입 자체는 기록, `OR`/fallback 결함 **미발견** | **부분동의 (Codex가 가장 정확)** | **Codex가 옳다.** 계약은 두 채널 AND + `POSTURE_UNMEASURED`를 요구하는데(`AGENTS.md:297-301`) 구현은 `OR` + 조용한 v1 퇴행 (`go2_eval_telemetry.py:233-243,348-377`) |
| 10 | **G6 recovery 필드** | 전 push case `recovery_rate=1.0` 확인 | 리포터가 구형 `recovery_rate` 사용을 코드로 특정, Chain-01 반례 제시 | **미발견** | **한쪽만 발견 (Opus 누락)** | **확정.** Pilot-01 push 12/12 케이스 `recovery_rate = 1.0` 상수, `recovery_rate_upright`는 0.9766~1.0. 수집기 주석 자신이 `recovery_rate`를 회복 증거로 쓰지 말라고 명시 |
| 11 | **scenario aggregation** | 언급 없음 | "`scenario_proxy ≠ survival×tracking`, G3에서 약 1.515/70 차이" | **미발견** | **한쪽만 발견 — 단 표현 정정 필요** | **부분동의.** 채점되는 `scenario_proxy`는 `min(survival×tracking)`으로 곱셈 구조가 지켜진다. 문제는 ① **집계 순서 미정**(`min(a×b)` vs `min(a)×min(b)`, G3에서 `0.4403` vs `0.3321`) ② **리포트 자기모순**(표시 세 필드가 곱으로 재현 불가). "점수가 1.515 부풀었다"는 부정확 |
| 12 | **tracking std 하드코딩·schema 정합성** | 언급 없음 | registry는 env 참조 명세, 리포터는 `0.5` 하드코딩. JSON Schema `1.2.0` vs 런타임 `1.3.0` | **미발견** | **한쪽만 발견** | **확정.** 현재 수치 영향은 0(env도 `std: 0.5`)이나 계약이 깨져 있고, 스키마 단독 검증은 현행 spec을 거부하면서 nested gate는 검증하지 못한다 |
| 13 | **A011 식별자 · A025 중복** | A011 라벨 재사용 + 5k terrain WITHDRAWN 혼재. A025는 16자리 동일 | A011 실제 `work_id = G-A009`. A025 model SHA 동일 | **둘 다 미발견** | **한쪽만 발견 (Opus 누락)** | **확정.** A011은 별도 실행이 아니다 (`workspace/server_returns/train_260902-Go2_track_lin_vel_120_1000_g_a009/VERIFICATION.json#/work_id`). A025는 A013의 중복이며 정보이득 0 |
| 14 | **H1 비교의 충분성** | 12개 축 비교표 작성 | §2.6에서 전이 가능 범위만 규정, 비교표 없음 | §6에서 "4족이 2족보다 어려운가"를 다루나 **H1 수치를 근거로 인용해 `AGENTS.md:250` 위반**(§12에서 자체 삭제) | **부분동의** | **Phase I이 가장 충실.** 본 문서 §5.1이 12축 비교를 완성했고, H1 자체 한계(단일 seed, H7 미측정, 공식 결과 부재)를 §5.0·Q4에 명시해 비교 범위를 절차로 제한했다 |
| 15 | **최신 계획이 계측 복구보다 새 학습을 앞세우는가** | 앞세운다 → S1~S3 GPU 0 우선 | 앞세운다 → 순서 0~4 GPU 0 우선, 새 학습 HOLD | **§14 S1이 GPU 6.4h 학습을 3순위에 배치** | **불일치** | **§14는 승인 불가.** C1(기준선 비보행)·C2(지문 비대칭)가 미해결인 상태의 6.4h 학습은 판정 불가능한 결과를 생산한다. 계측 복구와 학습 없는 재판정이 선행 |

### 7.1 세 감사가 **모두** 놓친 것

| 항목 | 내용 | 근거 |
|---|---|---|
| **정정 효과의 크기 정량화** | G6·G7 결함을 고쳤을 때 실제 점수가 얼마나 바뀌는지 아무도 계산하지 않았다. 본 감사 결과 **Pilot-01 `33.793106`→`33.777025`(−0.016), Chain-01 `2.307745`→`1.754557`(−0.553)** — 계약 위반은 실재하나 **캠페인 결론을 뒤집는 크기가 아니다** | 본 문서 §1.2 C6 |
| **인용된 artifact 경로의 부재** | Codex는 Pilot-01·Chain-01 총점을 `.../evaluation/{pilot_v2,chain01}/SELF_EVAL_REPORT.json#...`로 인용했으나 **그 파일들은 존재하지 않는다**. 두 디렉터리에 69개 case `summary.json`은 있고 집계 리포트는 없다 — 총점은 분석 시점에 `build_policy()`로 재계산된 값이다 (`GO2_PROJECT_STATE.md:553`) | `ls workspace/_keep/go2_pilot_v2_baseline/reports/` (빈 디렉터리), `go2_chain01_baseline/reports/` (부재) |
| **C1과 C3의 연결** | 생존 게이트가 보행을 처벌하는 이유가 "기준선이 정지 정책이라 생존이 1.0에 붙어 있기 때문"이라는 것 | 본 문서 §1.2 C3 |

### 7.2 기존 Opus 설계검토(§12~§14)의 누락 총계

기존 문서는 계측 결함 중 **G3/G7 중복, G6 필드 오용, scenario 집계, tracking std 하드코딩, JSON Schema stale, posture 두 채널 `OR`/fallback, arm 지문 전수 비대칭, A011 식별자 충돌, Default-01 비보행 — 9건을 놓쳤다.** 반면 다른 두 감사가 놓쳤고 기존 문서만 잡아낸 것도 있다.

| 기존 Opus만 포착 (원자료 재확인됨) | 근거 |
|---|---|
| `TRAIN_RC != 0` 시 러너가 평가 전 `exit 3`으로 종료 — 하드 스톱 경로 불성립 | `server_run_go2_tuning_engine_v1.sh:107-121` |
| finalize 기본 cleanup이 best 외 checkpoint 삭제 → 중간 checkpoint 측정 불가 | `go2_task/_finalize.py:108-134,178-180` |
| `terrain_levels=4.2503`은 iter 999, 평가 정책은 `model_900` — 교차 게이트의 객체 불일치 | A017 `launcher.log:33276-33282` |
| 전 Go2 러너가 `NO_AUTO_SUBMIT=1`로 운영진 학습 이력 자동 백업을 껐고 A017에서 실제 skip | `server_run_go2_tuning_engine_v1.sh:107`; A017 `launcher.log:33292` |

**마지막 항목의 위상 정정.** 기존 문서는 이를 §14 S0의 최우선 항목(규정 제14조 위험)으로 승격했다. Codex는 "제출 자격을 막는지는 로컬로 확인 불가"라며 성능 게이트와 분리할 것을 요구한다. **원자료 기준 판정: Codex가 옳다.** 백업 skip은 `ARTIFACT_VERIFIED`이지만, 그것이 실격 사유인지는 운영진 원문·서버 상태 없이 판정할 수 없으므로 `SUBMISSION_HISTORY_UNCONFIRMED`이며 **성능 판정과 분리된 별도 외부 확인 항목**이다. 다만 확인 비용이 GPU 0이고 실패 시 손실이 전부이므로 **우선순위는 여전히 최상위**로 둔다.

---

## 8. 남은 GPU 예산 대비 교정 계획과 fallback

### 8.1 예산 사실 — 먼저 정직하게

- 규정상 팀 총 100시간이라는 저장소 정본이 있다 (`AGENTS.md:351-355`).
- 잔여 시간에 대한 **마지막 raw 기록은 260903의 "잔여 25시간"뿐**이다 (`GO2_CAMPAIGN_SCHEDULE.md:228`). 그 이후 A013~A025의 다수 실행 비용이 차감된 단일 원장이 없다.
- **따라서 현재 잔여 서버 과금시간은 `[미확인]`이다.** 본 계획은 25시간을 현재값으로 쓰지 않으며, 아래 시간은 전부 **상한**이다. S3에서 실측 잔여를 기록한 뒤에만 S6·S7 착수를 결정한다.
- 실측 단가: **1,000 iter ≈ 59분** (`GO2_PROJECT_STATE.md:360`), **69-case × 3 seed 평가 ≈ 29분** (`GO2_CAMPAIGN_SCHEDULE.md:292`).

### 8.2 사전등록 실행 계획

원칙: **새 학습을 기본값으로 두지 않는다.** 정보가치/비용 비가 높은 순서다. S1~S3은 GPU 0, S4~S5는 학습 0.

---

#### **S1 — evaluator 지문 동등성 강제** · GPU **0h** · 등급 **필수(차단)**

- **목적 / 직접 측정 시나리오** — C2 재발 차단. 특정 G 시나리오를 측정하지 않는 코드 작업.
- **입력 식별자** — `go2_tuning_eval_report.py`, `tools/build_go2_tuning_engine.py`, `server_run_go2_tuning_engine_v1.sh`.
- **변경값** — 리포터에 arm 지문 동등성 검사 추가: 각 case의 `schema_version`·`survival_proxy_source`·posture gate 파라미터가 양 arm에서 동일하지 않으면 점수 계산을 중단하고 `EVALUATOR_MISMATCH`를 낸다. `_default_baseline_payload()`/`_chain01_baseline_payload()`의 v1 캐시 경로를 제거한다.
- **고정값** — 채점식·가중치·게이트 임계값 불변.
- **seed·반복** — 해당 없음.
- **성공** — 비대칭 fixture가 실패하고 대칭 fixture만 통과. **실패** — 기존 대칭 실행(A015~A018) 재계산 결과가 기록값과 달라지면 회귀이므로 되돌린다. **`INCONCLUSIVE`** — 없음.
- **조기중단점** — 없음.
- **필수 artifact** — 계약 테스트 파일, A015~A018 재계산 일치 로그.
- **성공 시** → S2. **실패 시** → 이후 전 단계 HOLD.

---

#### **S2 — G1~G7 계측 회귀 테스트 5종** · GPU **0h** · 등급 **필수(차단)**

- **목적 / 직접 측정** — G3·G6·G7 및 전 시나리오 집계의 계약 고정.
- **입력 식별자** — `go2_eval_telemetry.py`, `go2_fixed_eval_report.py`, `config/go2_tuning_experiment_schema.json`, 69-case 러너 3종.
- **변경값 5건**
  1. **posture 두 채널 AND** — `measured = gravity_finite AND height_available`. 둘 중 하나라도 없으면 `POSTURE_UNMEASURED`로 점수 사용을 금지하고, **`termination_only_v1` 자동 fallback 경로를 삭제한다** (`AGENTS.md:297-301` 준수).
  2. **G6를 `recovery_rate_upright`로 전환** (`go2_fixed_eval_report.py:38`). `posture_measured != true`면 G6는 `SELF_ASSESSMENT_INCOMPLETE`.
  3. **G7 DR 분리** — 69-case 러너 3종에 `NCRC_EVAL_DR`를 넣고 realized randomization 값을 보존. **같은 seed의 G3 `rough_forward`와 G7 `dr_seed_*`의 `steps.csv` SHA-256이 같으면 실패하는 hash-negative 테스트**를 추가한다.
  4. **집계 순서 명문화** — `scenario_proxy = min(survival×tracking)`을 채점값으로 고정하고, 리포트의 `survival_proxy`/`tracking_proxy`를 **worst-case가 나온 그 case의 값**으로 바꿔 세 필드가 곱으로 재현되게 한다.
  5. **provenance 정합** — `TRACKING_STD` 하드코딩을 env 참조로 교체(현 값 `0.5` 동일), JSON Schema를 `1.3.0`으로 동기화.
- **seed·반복** — 기존 artifact 재채점으로 검증(신규 실행 없음).
- **성공** — 5개 테스트 통과 + Pilot-01·Chain-01 재채점값이 §1.2 C6 표(`33.777025`, `1.754557`)와 일치. **실패** — 불일치 시 원인 규명 전 진행 금지. **`INCONCLUSIVE`** — G7 DR 필드의 공식 정의가 없으므로 "G3와 다르다"까지만 검증하고 "공식 DR과 같다"는 주장하지 않는다.
- **조기중단점** — 없음.
- **필수 artifact** — 테스트 5종, 재채점 전후 비교표.
- **성공 시** → S3. **실패 시** → 해당 시나리오를 `SELF_ASSESSMENT_INCOMPLETE`로 두고 가중치에서 제외한 재가중 점수를 병기.

---

#### **S3 — 게이트 재정의 · 예산 실측 · 제출 무결성 확인** · GPU **0h** · 등급 **필수(차단)**

- **목적** — C3 재발 차단 + 계획 수립의 전제 확보.
- **변경값 3건**
  1. **veto를 인수에서 곱으로 이동** — early-kill 조건을 `scenario_proxy` delta 기준으로 재정의한다. `survival` 단독 후퇴는 **veto가 아니라 `INTERNAL_SAFETY_FLAG`로 병기**한다. `min_total_points_delta`는 공식식이 아니므로 이름을 `min_operational_effect_points_70`으로 바꾸고 내부 정책임을 명시한다. `max_survival_regression = 0.10`은 유도 근거가 없으므로 **파국적 자세 실패(예: 절대 `survival_proxy = 0`인 case 발생)에만 적용되는 조건으로 축소**한다.
  2. **잔여 서버 과금시간을 실측해 원장에 기록** — `AGENTS.md` R-5 미이행 해소. 이 값 없이는 S6·S7을 착수하지 않는다.
  3. **`NO_AUTO_SUBMIT` 처리** — 이후 실행에서 해제하고, 기존 A017 학습 이력의 서버 보유 여부를 운영진 대시보드 또는 공식 문의로 확인한다. **이는 성능 게이트와 분리된 제출 무결성 항목이며 현재 상태는 `SUBMISSION_HISTORY_UNCONFIRMED`다.**
- **성공** — 게이트 부호 테스트 통과(A017 재판정 시 `INTERNAL_EARLY_KILL_FAIL`이 아니라 `총점 개선 + G4 SAFETY_FLAG`로 기록됨) + 잔여 시간 원장 1행 갱신. **실패** — 잔여 시간을 확인할 수 없으면 S6·S7을 계획에서 제거하고 S4·S5만 집행한다.
- **필수 artifact** — 게이트 계약 테스트, 잔여 시간 원장 항목, 제출 이력 확인 기록.

---

#### **S4 — G-A026: Default-01의 `posture_gate_v2` 최초 실측** · GPU **0.5~0.9h** · 학습 **0** · 등급 **필수(기준선)**

- **목적** — **C1의 확정 또는 반증.** 캠페인 전체의 대조군이 유효한지를 결정하는 단일 실험이다.
- **직접 측정** — G1~G7 전부.
- **입력 식별자** — Default-01 checkpoint(iter·`model_best.pt` SHA256·`env.yaml` SHA256을 실행 전 기록). 학습 없음.
- **변경값** — 없음(평가 전용). **고정값** — S1~S3 적용 후의 evaluator, 69 case, seed 101·202·303.
- **seed·반복** — 평가 seed 3개. 학습 반복 없음.
- **성공 기준** — 69/69 완료, 양 posture 채널 존재, 전 case `survival_proxy_source = posture_gate_v2`, G3/G7 hash 상이.
- **판정 분기**
  - **C1 확정** — G1~G5 `survival_proxy`가 0에 가깝거나 `speed_xy_mean`이 명령의 절반 미만 → **Default-01을 기준선에서 영구 폐기하고, Default 계보 6건(A007·A009·A010·A013·A024·A025)의 판정을 `BASELINE_INVALID`로 일괄 표시한다.**
  - **C1 반증** — Default-01이 정상 보행 → C1을 철회하고 §1.2를 개정한다. 이 경우 A007·A009·A010원의 양수 delta는 유효 신호로 승격된다.
- **조기중단점** — posture 채널 부재 또는 DR 미분리 확인 시 `POSTURE_UNMEASURED`로 즉시 중단.
- **필수 artifact** — 69 case `summary.json`, `steps.csv`, 7종 영상, `SELF_EVAL_REPORT`, SHA256SUMS, 다운로드 tar.
- **fallback** — tier-1 7 case만이라도 확보. 그것도 실패하면 **Default 계보 전체를 판정 불가로 두고 Pilot 계보만으로 캠페인을 재구성한다.**

---

#### **S5 — A017 재판정: 69-case × 3 seed** · GPU **0.5~0.9h** · 학습 **0** · 등급 **필수(재판정)**

- **목적** — 캠페인의 **유일한 인과 유효 양수 신호**(`+3.707916`)가 넓은 척도와 반복 seed에서 살아남는지 확정.
- **직접 측정** — G1~G7, 특히 **G4(경사)의 생존·추종·곱**.
- **입력 식별자** — A017 candidate checkpoint(`model_900.pt`, step 856 선택본) + Pilot-01 동결본. 두 정책을 **같은 실행에서 같은 evaluator로** 잰다.
- **변경값** — 없음. **고정값** — `track_lin_vel_xy = 1.4`(A017 그대로), 나머지 Pilot 값 동일, seed 101·202·303.
- **seed·반복** — 평가 seed 3개, 정책 2개(A017 / Pilot-01).
- **성공** — A017의 69-case worst-case 총점이 **Pilot-01의 같은 척도 값(`33.793106` 재측정본)을 상회**하고, 3 seed 전부에서 delta 부호가 양수.
- **실패** — 총점 우위가 사라지거나 seed 간 부호가 뒤집힘 → `track_lin 1.4` 폐기, Pilot-01 유지.
- **`INCONCLUSIVE`** — 부호는 양수이나 크기가 S6에서 측정할 잡음 하한 이내 → S6 결과를 기다린다.
- **조기중단점** — G4에서 절대 `survival_proxy = 0`인 case가 나오면 즉시 중단하고 안전 위험으로 기록.
- **필수 artifact** — 양 정책 69 case telemetry, **G4 경사 영상 필수**(현재 A015 이후 전 실행 `VIDEO_UNKNOWN`), `SELF_EVAL_REPORT` 2본, 다운로드 tar.
- **성공 시** → S6. **실패 시** → S6로 가되 대상 후보를 Pilot-01 단독으로 바꾼다.

---

#### **S6 — no-op control: 잡음 하한 실측** · GPU **1.5~3.0h** · 등급 **강력 권장**

- **목적** — C5 해소. **reward를 전혀 바꾸지 않고 학습 seed만 바꾼 재학습**으로 PPO 실행간 분산을 추정한다. 이것이 없으면 어떤 delta도 해석할 수 없다.
- **직접 측정** — G1~G7 총점의 산포.
- **입력 식별자** — S5 승자의 reward 세트(A017 또는 Pilot-01). **reward 파일 무변경.**
- **변경값** — **학습 seed 42 → 43 (그리고 예산이 허용하면 44).** 그 외 전부 고정: 1,000 iter, 4096 envs, 동일 코드·evaluator.
- **seed·반복** — 학습 seed 1~2개 추가, 평가 seed 101·202·303.
- **성공** — 산포가 산출됨(성공/실패가 아니라 **측정**이다).
- **판정 용도** — 이후 모든 단일변수 delta는 **이 산포의 2배를 넘을 때만** 효과로 인정한다.
- **예상 비용** — 1,000 iter ≈ 59분 × seed 수 + 평가 ≈ 29분 × seed 수.
- **조기중단점** — 잔여 시간이 S7 최소분(≈3h)을 남기지 못하면 seed 43 하나로 축소.
- **fallback** — 예산 부족 시 S6를 건너뛰되, **이후 모든 결론에 "잡음 하한 미측정"을 명시**한다.

---

#### **S7 — 조건부 단일변수 3,000 iter 승급** · GPU **≈3.0h / 후보** · 등급 **조건부**

- **착수 조건 (AND)** — S1~S3 완료 · S4에서 유효 기준선 확정 · S5에서 승자 확정 · S6 산포 확보 · S3에서 잔여 시간 실측.
- **목적** — screening 척도(1,000 iter)에서 살아남은 다이얼 하나를 확정 척도로 올린다.
- **1순위 후보** — S5가 성공하면 **A019(`track_lin 1.3`)**. A017 `1.4`의 양수 신호를 절반 폭으로 재검증하는, 캠페인에서 가장 정보가치가 높았으나 미집행된 설계다.
- **변경값** — 단일 reward 1개. **고정값** — 학습 seed 42, 3,000 iter, evaluator 고정.
- **iteration 근거** — `quadruped_rewards.py:112-120`(3,000~5,000)과 `workspace/training/PRELIM_RL_GUIDE.md:173-183`(1,000~5,000)이 **동시에 지지하는 유일한 구간이 3,000~5,000**이므로 하단인 3,000을 택한다. **H1의 포화곡선은 근거로 쓰지 않는다** (`AGENTS.md:250`).
- **성공** — 69-case×3seed 총점이 동결 기준선을 S6 산포의 2배 이상 상회. **실패** — 미달 시 기준선 유지. **`INCONCLUSIVE`** — 산포 이내.
- **조기중단점** — 학습 중 NaN/crash. **하드 스톱은 계획하지 않는다** — `TRAIN_RC != 0`이면 러너가 평가 전 `exit 3`으로 끝나기 때문이다 (`server_run_go2_tuning_engine_v1.sh:107-121`).
- **fallback** — 잔여 시간이 3.0h 미만이면 S7을 포기하고 **S5 승자를 그대로 최종 제출 후보로 확정**한다.

### 8.3 계획 요약과 상한

| 단계 | GPU 상한 | 학습 | 차단성 |
|---|---:|---|---|
| S1 지문 동등성 | 0h | — | 필수·차단 |
| S2 계측 회귀 5종 | 0h | — | 필수·차단 |
| S3 게이트·예산·제출 무결성 | 0h | — | 필수·차단 |
| S4 Default-01 v2 실측 (G-A026) | 0.5~0.9h | 0 | 필수 |
| S5 A017 69-case×3seed | 0.5~0.9h | 0 | 필수 |
| S6 no-op control | 1.5~3.0h | 1~2회 | 강력 권장 |
| S7 단일변수 3,000 iter | 3.0h/후보 | 1회 | 조건부 |
| **필수 구간 소계** | **1.0~1.8h** | **학습 0** | — |

**핵심.** 캠페인의 가장 중요한 두 질문 — "기준선이 유효한가"(S4)와 "유일한 양수 신호가 진짜인가"(S5) — 은 **합계 2시간 미만, 학습 0회로 답할 수 있다.** 기존 §14가 3순위에 배치한 6.4시간 학습보다 먼저 해야 할 일이다.

---

## 9. 감사 중 확인 불가능했던 항목

| # | 항목 | 상태 | 근거 |
|---:|---|---|---|
| 1 | 운영진 공식 evaluator 코드·임계값 | `[미확인]` | 로컬 부재 (`config/go2_self_eval_registry.json:27-35`) |
| 2 | 전 실행의 공식 채점 결과 | **`[미측정]`** | A006~A025 전 행 `OFFICIAL_RESULT` 없음 |
| 3 | 공식 tracking 변환식·command grid·episode 수·terrain generator·push·DR·통과선 | `[미확인]` | 동상 |
| 4 | `workspace/PRELIM_RL_GUID.md`와 운영진 규정집 원문의 진본 동일성 | `[미확인]` | 규정집 PDF·공지 snapshot이 저장소에 없음 |
| 5 | **현재 잔여 서버 과금시간** | `[미확인]` | 마지막 raw는 260903 "잔여 25시간" (`GO2_CAMPAIGN_SCHEDULE.md:228`), 이후 차감 없음 |
| 6 | **Default-01의 `survival_proxy_v2`** | `[미측정]` | 기준선 캐시에 `steps.csv`가 없어 로컬 재채점 불가 → S4의 목적 |
| 7 | 계측 무효 6건의 참 delta | `[미확인]` | S4 완료 후 재계산 가능 |
| 8 | PPO 실행간 분산(잡음 하한) | `[미측정]` | no-op control 0건 → S6의 목적 |
| 9 | `posture_gate_v2` 파라미터 `{0.5, 0.18, 0.5, 0.5}`의 공식 calibration | `[미확인]` | 내부 설정 (`go2_eval_telemetry.py:83-86`) |
| 10 | `max_survival_regression = 0.10`의 유도 근거 | **저장소 전체에 없음** | `config/go2_self_eval_registry.json:42-50` |
| 11 | 공식 G7 DR의 필드·범위 | `[미확인]` | 현행 G7은 G3와 바이트 동일이라 사용 불가 |
| 12 | A015 이후 전 실행 영상 | **`VIDEO_UNKNOWN`** | 붕괴 양상의 시각적 확인 없음 |
| 13 | A017 학습 이력의 서버 보유 여부 | `SUBMISSION_HISTORY_UNCONFIRMED` | `NO_AUTO_SUBMIT=1` skip은 확인, 실격 여부는 미확인 |
| 14 | 외부 논문(Rudin et al. 등) 인용 정확성 | `[미확인]` | 로컬에 원문·버전 pin 없음 (`GO2_REWARD_EVIDENCE_MASTER.md:209-220,241-268`) |
| 15 | Pilot-01 4개 항의 개별 기여 | `[미확인]` | A006이 4변수 동시 변경이라 분리 불가 |
| 16 | reward 파일 주석 해제(`track_ang_vel_z_exp`)의 제출 적법성 | `[미확인]` | `quadruped_rewards.py:38`("줄 추가/삭제/주석 자유")과 `AGENTS.md:357-370`(값만) 충돌, 규정 원문 부재 |

---

## 10. 최종 판정

### 10.1 새 학습 승인 여부

> # `REQUEST_CHANGES`

**HOLD(근거 불충분)가 아닌 이유** — 무엇을 해야 하는지는 원자료로 충분히 결정됐다. 판정을 미룰 이유가 없다.
**APPROVE가 아닌 이유** — 다음 세 가지가 미해결인 상태에서 학습하면, 그 결과를 해석할 방법이 없다.

1. **대조군이 유효한지 모른다** (C1). Default-01은 `speed_xy_mean` 0.023~0.032 m/s로 보행하지 않으며, `posture_gate_v2`로 측정된 적이 한 번도 없다. → **S4**
2. **자가 양쪽에서 같은지 코드가 보장하지 않는다** (C2). 7건이 v1 대 v2로 판정됐고, 리포터에 지문 동등성 검사가 없어 다음 실행에서도 반복된다. → **S1**
3. **잡음 하한을 모른다** (C5). no-op control 0건 상태에서 ±3점대 delta의 부호를 reward 효과로 읽고 있다. → **S6**

### 10.2 조건부 승인 경로

**S1·S2·S3(GPU 0) + S4·S5(학습 0, 합계 2시간 미만)를 완료하면 `APPROVE`로 전환한다.** 그 시점에 승인되는 학습은 §8.2 S7 — 단일변수 1개, 3,000 iter, 고정 evaluator다.

### 10.3 즉시 집행 항목 (사용자 확인 없이 진행 가능한 GPU 0 작업)

1. S1 지문 동등성 계약 + 테스트
2. S2 계측 회귀 5종(posture AND / G6 upright / G7 hash-negative / 집계 순서 명문화 / provenance 정합)
3. S3 게이트 재정의 + **잔여 서버 시간 실측 기록** + `NO_AUTO_SUBMIT` 해제
4. 원장 반영 — Default 계보 6건에 `BASELINE_INVALID_PENDING_S4` 표시, A011을 `LEDGER_IDENTITY_FAIL`로 정정, A025를 중복으로 재분류, A017 판정을 "총점 개선 + G4 안전 플래그"로 해석 정정

### 10.4 이 감사가 보장하지 않는 것

- 본 문서의 어떤 수치도 **공식 점수가 아니다.** 전 실행 `OFFICIAL_RESULT`는 `[미측정]`이다.
- 계측을 고쳐도 **정책이 좋아지지는 않는다.** A015·A016·A018은 계측과 무관하게 실패다.
- C1이 확정되어도 **기각된 후보가 자동 복권되지 않는다.** 대칭 v1 재계산의 양수값은 "기존 음수가 근거 없다"만 말하며, v1 자체가 부적합한 자다.
- 현재 잔여 GPU 시간이 S4~S7을 감당하는지 **본 감사는 알지 못한다.** S3의 실측이 선행되어야 한다.

### 10.5 품질 게이트 자체 점검 (지시서 §8)

- [x] A006~A025가 모두 한 번씩 등장하며 미실행·중복이 별도 분류됨 (§4, 20/20)
- [x] 모든 핵심 결론에 파일 경로·줄 번호 또는 JSON pointer가 있음
- [x] H1 성공을 공식 결과로 과장하지 않음 (§5.0, Q4에서 H1 자체 한계 명시)
- [x] Go2 연속 실패를 reward 실패 하나로 환원하지 않음 (§6.1 3층 구조)
- [x] 같은 evaluator 비교와 다른 evaluator 비교를 섞지 않음 (§1.2 C2 전수 표)
- [x] 절대 proxy 개선과 안전 gate 실패를 동시에 기록 (§4 A017 행)
- [x] 실제 정책 붕괴 사례를 계측 문제 뒤에 숨기지 않음 (§6.2-A, A015·A016·A018 확정)
- [x] 공식 결과가 없는 항목은 `[미측정]` (§9)
- [x] 개선 계획이 evaluator 복구·기존 artifact 재판정을 새 GPU 학습보다 앞세움 (§8.2 S1~S5)
- [x] 기존 Opus 문서의 폐기된 §7과 최신 §14를 혼동하지 않음 (§7 쟁점 15에서 §14만 평가)
- [x] 최종 문서와 SHA256 파일 생성 (본 문서 및 동명 `.sha256`)
- [x] 다수결을 쓰지 않음 — 쟁점 1·11·15에서 다수와 다른 판정을 냄

---

*본 감사의 성공 조건은 어느 기존 문서와 같은 결론을 내는 것이 아니다. 동일한 원자료에서 재현 가능한 판정과 반증 가능한 인과 설명을 만드는 것이다. 위 모든 인과 주장에는 §1.2의 반증 가능 조건이 붙어 있으며, S4 하나가 C1을 확정하거나 무너뜨린다.*
