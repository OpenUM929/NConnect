# NConnect 프로젝트 사용자 이해 우선 지침

이 파일은 상위 OMX `AGENTS.md`를 대체하지 않고, 이 저장소의 예선 기획·분석·보고 응답에 추가로 적용한다.

## 예선 맥락 우선 출력 계약

사용자에게 H1·Go2 학습, 평가, 실행계획 또는 보고서를 보여줄 때는 수치·명령·상세 분석보다 먼저 **한눈에 보는 현재 위치**를 출력한다.

첫 화면에는 반드시 다음 항목이 있어야 한다.

1. **예선 목표** — 이번 작업이 시뮬레이션 70점, 설계 의도 20점, 리포트 품질 10점 중 어느 축을 위한 것인지 쓴다.
2. **현재 단계** — 아래 표준 단계 중 현재 위치를 하나로 표시한다.
3. **확보된 것** — 완료된 run, 검증된 행동, 보존된 artifact만 쓴다.
4. **미확보/차단** — 미측정 H1~H7, 영상, 로그, 리포트, 제출물 등 다음 단계로 가지 못하게 하는 공백을 쓴다.
5. **이번 테스트의 목적** — 어떤 예선 시나리오 또는 제출 게이트를 만족시키기 위한 테스트인지 쓴다.
6. **흐름도** — `완료 → 현재 → 통과 시 → 실패 시 → 최종 제출`을 한 줄 화살표로 표시한다.
7. **사용자가 지금 할 일** — 서버 실행, 영상 관찰, 파일 회수 등 즉시 행동 하나를 구체적으로 쓴다.
8. **이번 테스트가 보장하지 않는 것** — 학습 횟수, 단일 rollout 또는 학습 지표만으로 예선 점수·통과 가능성을 보장하지 않는다고 쓴다.

## 표준 예선 단계

| 단계 | 이름 | 완료 기준 |
|---:|---|---|
| 0 | 증거·artifact 정합성 | checkpoint, config, log, policy, 영상의 run 대응 확인 |
| 1 | 기본 행동 게이트 | H1~H4의 제자리·전진·좌우·회전 명령 행동 확인 |
| 2 | 짧은 학습 pilot | 1,000~5,000 iter에서 reward 성격과 부작용 확인 |
| 3 | 환경 적응 게이트 | H5~H7의 요철·경사·밀침 행동 확인 |
| 4 | 단계적 장기 학습 | 직전 단계 PASS 후 5,000 → 10,000 → 15,000처럼 승급 |
| 5 | 최종 평가·문서 | H1~H7 격리 근거, `env.yaml`↔리포트 일치, 경고·제출물 확인 |
| 6 | 제출 | `policy.pt`, `env.yaml`, 기술 개선 리포트 업로드 확인 |

단계 번호는 학습 run 번호가 아니다. 여러 단계가 동시에 일부 진행됐으면 **다음 행동을 막는 가장 이른 미완료 단계**를 현재 단계로 표시한다.

## 사용자용 첫 화면 형식

```markdown
## 0. 예선 기준 현재 위치
- [예선 목표] 이번 작업이 확보하려는 배점/시나리오:
- [현재 단계] 단계 N/6 — 이름:
- [확보] 검증 완료:
- [미확보] 다음 단계 차단 항목:
- [이번 테스트] 대상 시나리오와 판정 목적:
- [흐름] 완료 → **현재** → 통과 시 → 실패 시 → 최종 제출
- [지금 할 일] 사용자의 즉시 실행 항목:
- [보장하지 않음] 이번 결과만으로 말할 수 없는 것:
```

## 자력 검증 우선 (260830 신설)

**로컬에서 확인할 수 있는 것을 사용자나 다른 배우에게 묻지 않는다.** 질문은 실측을 끝낸
뒤 남은 것만 한다. 근거 없이 올린 질문은 사용자의 시간을 쓰는 비용이며, 되물음 한 번이
곧 한 턴이다.

1. **묻기 전에 연다.** 저장소 안 파일·로그·코드 주석·해시·가중치로 답이 나오는 질문은
   질문이 아니라 미실행 작업이다. 판단·권한·대회 공지처럼 저장소 밖에만 있는 사실만
   질문 대상이다.
1-b. **역할 분담이 답을 결정하는 질문을 하지 않는다.** 이 프로젝트의 롤은
   **사용자가 실행하고 메인 루프가 지시·검증·기록한다**이다. 따라서 "우리가 그 단계를
   지시했는가"는 사용자에게 물을 것이 아니라 **「진행」 원장에서 유도할 것**이다.

   > **⚠️ 정정 (260830 · 팀장 판정 Q4 수용).** 이 항목의 초판은
   > *"지시가 없었으면 행위도 없다"* 라고 적었다. **너무 강한 명제였다.**
   > 원장이 확정하는 것은 **우리 쪽 행위(지시·검증·기록)**뿐이고,
   > **사용자 쪽 행위는 확정하지 못한다** — 사용자는 지시 없이도 행동할 수 있고 실제로 그랬다
   > (서버 파일 업로드). F44를 「공식 제출 0회」로 단정한 것이 이 오류의 실증이며,
   > 올바른 표기는 **「원장상 업로드 지시 0건 · 실제 제출 여부 [미측정]」** 이다.
   >
   > **경계: 원장으로 유도할 수 있는 것 / 없는 것**
   > - 유도 가능 — 우리가 무엇을 지시했나, 무엇을 검증했나, 어떤 산출물이 언제 생겼나
   > - 유도 불가 — 사용자가 외부 시스템(대시보드·강좌 사이트)에서 무엇을 했나
   >
   > 후자는 `[미측정]`으로 두고 **회수 방법과 함께** 올린다. 단정도, 방치도 하지 않는다.
2. **자기 로그를 먼저 읽는다.** 외부 문서를 추정하기 전에 우리가 남긴 실행 로그를 grep한다.
   실증 — `F26`(학습=제출)은 2일간 캠페인 예산의 전제였는데, 반증이 우리 로그
   `reports/train_260828-02.log:111247`에 처음부터 찍혀 있었다:
   *"⚠️ 이것은 **예선 제출이 아닙니다** — 제출은 대회 웹사이트에서 policy.pt 를 직접 업로드하세요."*
3. **해시 불일치는 내용 불일치의 증거가 아니다.** 직렬화가 비결정적일 수 있다.
   실증 — `exported/policy.pt`는 같은 checkpoint에서 export해도 파일 해시가 매번 다르지만
   (4종 관측) 가중치는 동일하다. **동일성 판정은 가중치 비교로 한다**
   (`torch.jit.load(...).state_dict()` 텐서 대조). 해시는 무손상 증거로만 쓴다.
4. **식별자는 재현 가능한 것으로 적는다.** 문서에 정책을 특정할 때 비결정적 해시 대신
   `checkpoint의 iter + model_best.pt sha256`을 쓴다.
5. **질문은 근거와 함께 만든다.** 외부 배우에게 넘길 때는 이미 확정한 사실·재현 명령·
   판정 가능한 질문형을 함께 싣는다(REV_GUIDE §6-b·§6-d). "어떻게 할까요"는 질문이 아니다.

## 계획 우선·결정 즉시 기록 (260830 신설)

**전제가 바뀌면 명령이 아니라 계획을 먼저 낸다.** 260830에 전제 3개가 동시에 무너졌는데
(F26 자동제출 오독 · F44 제출 0회 · D23 H1 전용 확정) 메인 루프는 계획 재작성 없이
학습 명령 한 줄로 넘어갔다. 사용자가 세 번 연속 지적해야 멈췄다.

1. **사용자의 범위·기체·일정 결정은 발화 즉시 `CAMPAIGN_SCHEDULE.md`와
   `PROJECT_STATE.md` 결정표에 행으로 남긴다.** 실증: "2족만 한다"가 어느 원장에도 없어
   메인 루프가 Go2 착수를 3턴 반복 제안했다(D23). 기억이 아니라 원장이 정본이다.
2. **단발 명령으로 계획 변경을 대신하지 않는다.** 전제가 바뀌면 ① 무엇이 무너졌는지
   ② 남은 점수축과 각 축의 통제 가능 여부 ③ 새 단계표 ④ 그제서야 첫 명령, 순으로 낸다.
3. **새 학습을 제안하기 전에 기존 판정과 대조한다.** 실증: `track_ang_vel_z_exp` 인상안은
   D18이 "선속도 우세(Run 05)가 회전 우세(Run 04)보다 가중 +6.78 낫다"고 판정한 시소를
   **거꾸로 미는 것**이었다. 자기 판정을 조회하지 않은 제안은 제안이 아니다.
4. **통제 가능한 축을 통제 불가능한 축보다 먼저 쓴다.** 시뮬 70점은 운영진만 채점하므로
   우리 실험은 blind bet이지만, 의도분석 20 + 리포트 10은 우리가 직접 검증·확정할 수 있다.
   같은 시간이면 확정 이득이 blind 이득에 우선한다.
5. **판정 요청의 선지는 원장 원문과 대조한 뒤 만든다.** 판정자는 선지 밖을 볼 수 없으므로
   **틀린 선지는 반드시 틀린 판정을 낳는다.** 원칙 12(피측정자가 자를 소유하지 않는다)의
   역방향 위반 — 질문자가 선지를 만들어 판정을 유도하는 것 — 을 막는다.
   실증(260830): 팀장에게 낸 `Q3. (b) ▲ blocked — 공백 C(마감일) 미회수`는 메인 루프의 창작이었다.
   원장 원문(`PROJECT_STATE.md:871`)이 막은 것은 **① iter 상한** ② **공백 F**(50시간 해석)인데
   이를 **① 학습 시작 자체** ② **공백 C**(마감일)로 두 번 변형해 선지에 넣었다.
   팀장은 주어진 선지 중 골랐을 뿐이고 D28이 나왔다. 선지를 만들기 전에 **인용할 원문 줄을 연다.**
6. **단계표에는 「등급」이 있어야 한다 — `필수(제출요건)` / `개선` / `조사`.**
   **`필수` 행은 `blocked` 상태를 가질 수 없다.** 막히면 일정 조정이 아니라 **제출 불가**이므로
   `▲ 제출 불가 — 즉시 해소 대상`으로 격상한다. 등급이 없으면 제출 요건과 개선 작업이
   같은 형식으로 나란히 놓여 **둘 다 미룰 수 있는 것처럼 보인다**(실증: B2 = 제출 체크리스트 #3).
7. **벽시계 작업을 사람 시간 입력으로 막지 않는다.** 학습은 tmux로 걸어두면 사람 시간을 쓰지 않는다.
   달력·일정 미확인을 이유로 GPU 작업 착수를 막는 것은 두 예산의 혼용이며, 오히려
   **학습이 도는 동안 문서 축 작업을 병렬 배치**하는 것이 정본이 명령하는 동작이다
   (`prelim-campaign-manager.md` L91-96·L101).

## 표현 규칙

- `몇 회 학습`, `몇 iteration`, `예선 제출 가능`을 같은 뜻으로 쓰지 않는다.
- 학습 횟수를 제시할 때는 `최소 경로`, `1차 계획 예산`, `재평가 지점`을 구분한다.
- 각 테스트가 H1~H7 중 무엇을 직접 측정하고 무엇을 측정하지 않는지 표로 연결한다.
- 현재 단계와 무관한 장기 계획은 먼저 말하지 않는다. 사용자가 지금 해야 할 한 단계가 첫 화면에서 보이게 한다.
- 내부 에이전트 역할·워크플로 이름보다 사용자의 목적·현재 위치·다음 행동을 우선한다.
- 상세한 기존 출력 형식이 있더라도 `## 0. 예선 기준 현재 위치`를 앞에 추가한다.

## 증거 계층·판정 용어 강제 규칙 (260831 신설)

학습 정상 종료, 파일 무결성, 영상 관찰, 내부 evaluator, 운영진 평가는 서로 다른 증거다.
이들을 하나의 `PASS`로 합치지 않는다.

1. 모든 판정은 아래 네 계층 중 하나를 이름에 붙인다.
   - `ARTIFACT_VERIFIED`: checkpoint·config·policy·영상·로그의 대응과 무결성만 확인
   - `VIDEO_OBSERVED` / `VIDEO_UNKNOWN`: 화면에서 요구 행동을 관찰했거나 판독하지 못함
   - `INTERNAL_GATE_PASS|FAIL|INCONCLUSIVE`: 사전등록한 내부 기준으로만 판정
   - `OFFICIAL_RESULT`: 대시보드·운영진 evaluator에서 회수한 점수·통과 결과
2. **수식의 출처와 입력값의 출처를 분리한다.** `PRELIM_RL_GUIDE.md`의
   `survival_rate × tracking_score`는 공식 가이드의 채점 개념이다. 그러나 학습 로그의
   `base_contact`, 자체 MAE, recovery time, 내부 `W`는 그 공식 evaluator 입력값과 동일하다고
   확인된 적이 없으므로 `공식 survival`, `공식 tracking`, `공식 점수`로 바꾸어 쓰지 않는다.
3. bare `PASS`, `합격`, `제출 가능`, `통과 가능`은 금지한다. 어느 계층의 판정인지 붙이지 못하면
   `미확정` 또는 `미측정`으로 쓴다. `제출 후보`는 파일 준비 상태이며 성능 합격을 뜻하지 않는다.
4. H1~H7 표에는 각 시나리오마다 `영상`, `내부 정량`, `공식 결과` 세 열을 둔다.
   한 열의 긍정적 결과로 다른 열을 승격하지 않는다.
5. 과거 문서가 위 용어 규칙과 충돌하면 최신 artifact에 맞춘 정정 행을 `PROJECT_STATE.md`에
   추가하고, 사용자 보고에서는 정정된 최신 판정만 사용한다.
6. **부분 판정은 출품 성능점수에 넣지 않는다.** H1~H7 중 telemetry·생존·추종 중 하나라도
   빠진 시나리오는 `SELF_ASSESSMENT_INCOMPLETE`이며 점수 0으로 보수 처리한다. `VIDEO_OBSERVED`나
   일부 방향 성공만으로 시나리오 점수를 만들지 않는다.
7. 공식 결과는 대회 종료 뒤의 사후 확인일 수 있으므로 개선 계획의 선행조건으로 두지 않는다.
   제출 전 자체 evaluator가 H1~H7 전부를 재현하고 `시뮬 proxy /70`을 계산해야 한다.

## 제출 전 자체 70점 게이트 (260831 신설)

1. 자체 채점은 `시뮬레이션 proxy /70`, `설계 의도 자체감사 /20`, `리포트 자체감사 /10`,
   `총 자체예상 /100`을 별도 표시한다. 운영진 점수처럼 쓰지 않는다.
2. 공개된 공식 개념은 `survival_rate × tracking_score`까지이며 tracking의 정확한 변환식은
   비공개다. 따라서 내부식은 버전·근거·한계를 고정한다. 현재 v1은 Run06 `env.yaml`의
   `std=0.5`와 exponential tracking 형태를 사용한
   `tracking_proxy = exp(-(RMSE/0.5)^2)`이고, 좌우·회전·경사 쌍은 더 나쁜 방향을 채택한다.
3. 시나리오 내부 통과 기준은 `survival_proxy ≥ 0.95`와 `tracking_proxy ≥ 0.70`을 동시에
   만족하는 것이다. H1~H7 전부 측정되고 모든 시나리오가 이 기준을 통과하며 가중 시뮬
   proxy가 0.70 이상일 때만 `SELF_ASSESSMENT_PASS`를 쓴다.
4. 최종 제출 후보 승급은 **총 자체예상 70점 이상**을 최소조건으로 하고, evaluator 불확실성
   여유를 위해 목표는 75점 이상으로 둔다. 문서 30점은 파일 일치·근거 완결성 rubric을 실제로
   감사한 만큼만 더하며, 단순 파일 존재만으로 만점을 주지 않는다.
5. `SELF_ASSESSMENT_PASS`는 공식 합격이 아니다. 그러나 `INCOMPLETE`, 시나리오 FAIL 또는
   총 자체예상 70점 미만인 정책은 공식 제출로 약점을 확인하려 하지 말고 먼저 평가·튜닝한다.

## 서버 제한시간 우선 보고 규칙 (260831 신설)

1. 사용자가 서버 종료 가능 여부나 다운로드 완료를 묻는 순간에는 **첫 두 문장 안에**
   `서버 종료 가능/불가`와 아직 회수할 필수 파일을 먼저 말한다. 분석 과정은 그 뒤에 쓴다.
2. 서버가 켜져 있으면 로컬 문서 정리·장기 설명·서브에이전트 호출보다 TTL 결정을 먼저 한다.
3. 종료 가능 판정은 `학습 bundle`, `필수 영상`, `평가 telemetry`, 각 SHA/manifest의 로컬 도착 여부를
   작업별 사전등록 목록과 대조해 내린다. 불필요한 항목은 사전에 `VIDEO_NOT_REQUIRED`처럼 근거를 남긴다.
4. 서버가 초기화되는 전제에서, 다음 접속 전에 로컬에서 package·CRLF·`bash -n`·명령 한 줄·예상 시간·
   완료 표식·다운로드 경로를 모두 준비한다. 서버에서는 준비가 아니라 실행·회수만 한다.

## 학습 승인·진보 게이트 (260831 신설)

**iteration을 소비하는 것 자체는 진보가 아니다.** 결과가 다음 의사결정을 구분할 때만 학습을 승인한다.

1. 새 학습 전 `H1_REWARD_EVIDENCE_MASTER.md`에 다음을 사전등록한다:
   기준 policy 식별자, 단일 변경값, 유지값, 과학/공식 근거와 한계, 직접 측정 H1~H7,
   seed·반복, 성공·실패·INCONCLUSIVE 기준, 조기중단점, 필수 영상·telemetry·bundle, 결과별 다음 분기.
2. 공식 점수 개선이 목적이면 시나리오별 `survival proxy`와 `tracking proxy`를 **둘 다** 측정한다.
   tracking만 개선되거나 생존만 개선된 결과는 다른 인수가 비열등일 때만 후보로 승급한다.
3. reward screening은 한 번에 하나만 바꾸고 1,000~5,000 iter에서 수행한다. 장기 10,000~15,000 iter는
   screening 승자이면서 동일 evaluator·영상·부작용 게이트를 통과한 조합에만 사용한다.
4. 한 seed의 성공은 `exploratory`다. 장기 승급 전 가능한 범위에서 독립 seed와 고정 evaluator로
   재검증한다. 공식 결과는 제출 전에 이용할 수 있다고 확인된 경우에만 추가 근거로 쓴다.
5. 다음 후보의 우선순위는 `내부 H1~H7 자체 채점의 최대 감점 시나리오` →
   `생존·추종 중 약한 인수` → `미측정 reward의 정보가치` 순이다. 코드 주석의 추천 순서나
   사후 공식 결과를 기다리는 계획으로 실행 순서를 확정하지 않는다.
6. 학습 종료 후에는 자동으로 `artifact 검증 → 필수 영상 → 내부 정량 → 증거 계층 표 → 마스터·일정·상태 원장 갱신`
   을 한 작업으로 완료한다. 어느 단계가 빠지면 다음 장기 학습과 성능 승급은 `HOLD — 평가 미완료`다.

## 휘발성 서버 회수·병합 규칙 (260831 신설)

0. 서버 실행·다운로드·artifact 분석·파일 병합 작업을 시작하기 전에 **`ARTIFACT_MANAGEMENT.md`를 읽고 작업 ID를 등록한다.** 이 문서가 파일 목록, lifecycle, 작업 내역의 단일 진입점이다.
1. 서버의 `training` 전체를 내려받는 것은 **보험 snapshot**으로 허용한다.
2. 서버 snapshot을 로컬 정본 `workspace/training`에 통째로 덮어쓰지 않는다. 업로드 뒤 로컬에서 갱신된 원장·보고서·이력의 역행 위험이 있기 때문이다.
3. 서버 결과는 먼저 `workspace/server_returns/<RUN_ID>/`에 격리하고 SHA256, `STATUS.txt`, 종료 코드, source hash, checkpoint, tfevents를 검증한다.
4. 검증 후 새 run artifact만 선택 병합한다. 기존 run·원장·보고서를 삭제하거나 과거 사본으로 교체하지 않는다.
5. `/workspace/_keep`가 `training` 밖에 있으면 `*_DOWNLOAD.tar.gz`를 별도 다운로드하거나 전체 snapshot 생성 전에 `training/_server_returns/<RUN_ID>/`로 복사한다.
6. 작업 상태는 `PLANNED → RUNNING → RECEIVED → VERIFIED → MERGED → ANALYZED → REPORTED`로만 전진시킨다. 외부 실행은 사용자 제공 로그나 회수 파일이 없으면 `[미측정]`이다.
7. 파일 병합 전후에 `MERGE_PLAN.tsv`, `MERGE_RESULT.tsv`, `LOCAL_SHA256SUMS.txt`를 남기고 `ARTIFACT_MANAGEMENT.md`의 같은 작업 ID를 갱신한다.

## 학습 종료 후 영상 증거 게이트 (260831 신설)

**학습 완료는 평가 완료가 아니다.** 학습 프로세스가 정상 종료돼도 새 checkpoint의 행동을
영상으로 확인하지 않았다면 H1~H7 행동, survival, 부작용, 최종 후보 여부는 `[미측정]`이다.

1. **학습 명령을 내기 전에 영상 필요성을 사전 판정한다.** `필수 / 조건부 / 불필요` 중 하나와 근거, 대상 checkpoint, H1~H7 시나리오, 영상 수·길이·seed·명령, 저장 경로와 다운로드 파일을 `ARTIFACT_MANAGEMENT.md`의 같은 작업 ID에 기록한다.
2. **다음 경우 영상은 기본적으로 `필수`다.** reward·환경·정책·checkpoint가 달라진 run, 장기 학습, 후보 채택/폐기 판단, H1~H7·survival·tracking·안정성에 관한 주장을 만드는 평가다.
3. **`불필요`는 예외다.** checkpoint가 생성되지 않은 설치/문법 smoke test, 또는 동일 checkpoint tensor와 동일 evaluator 조건의 영상이 이미 검증된 경우에만 허용한다. 기존 영상의 경로·정책 식별자·조건 일치를 근거로 남긴다.
4. **학습 종료 직후 서버를 끄기 전에 영상 필요성을 다시 판정한다.** 사전 판정과 실제 최종 checkpoint가 달라졌는지, 학습 중 이상 징후가 새 시나리오를 요구하는지 확인한다. 판정 변경은 원장에 즉시 기록한다.
5. **영상이 필수면 생성·패키징·다운로드 확인 전 서버 종료를 지시하지 않는다.** 최소 확인 항목은 영상 파일 수, 각 시나리오 로그, checkpoint/policy 식별자, tar SHA256, 로컬 다운로드 경로다. 학습 bundle만 내려받은 상태는 `학습 artifact VERIFIED / 행동 증거 PENDING`으로 분리 보고한다.
6. 영상 생성 실패 시 실패 로그와 재현 가능한 checkpoint·source·config를 먼저 회수하고 `VIDEO_REQUIRED_NOT_ACQUIRED`로 기록한다. 이를 PASS나 평가 완료로 바꾸지 않으며, 다음 서버 세션의 첫 작업은 영상 복구다.
7. 서버 종료 보고에는 반드시 `영상 판정`, `생성 결과`, `다운로드 결과`, `로컬 검증 상태`, `미측정 H1~H7`을 한 줄씩 쓴다. 이 다섯 항목이 없으면 종료 가능 판정을 내리지 않는다.

## 저비용 artifact 검증 라우팅 (260831 신설)

1. 다운로드 파일 존재, SHA256, tar 안전성·가독성, 내부 manifest, 파일 수, 시나리오 목록, checkpoint SHA 대응처럼 **판정 규칙이 고정된 검사**는 `.codex/agents/artifact-verifier.md`에 맡긴다.
2. 이 역할은 `agent_type=explore`, `model=gpt-5.6-luna`, `reasoning_effort=low`로 호출한다. 입력은 `work_id`, `run_id`, tar·SHA 절대경로, 예상 model SHA, 예상 시나리오를 메인 팀장이 확정해서 준다.
3. 검증자는 `tools/verify_download_artifact.py`를 실행하고 JSON과 종료 코드만 보고한다. 파일 병합·삭제, 서버 종료, 행동 성능·reward·예선 통과 판정을 할 수 없다.
4. `PASS`는 package 무결성과 기대 구조 일치만 뜻한다. 메인 팀장은 이를 원본 로그·현재 단계와 결합해 서버 종료 및 다음 행동을 최종 판정한다.
5. 입력값 불명, model/evaluator identity 불명, tensor 의미 비교 필요, 로그와 manifest 충돌은 저비용 역할이 추측하지 않고 `ESCALATE`한다.

## 튜닝 근거 선행·반복 질문 방지 (260831 신설)

사용자가 보상값의 이유, 현재 만족 여부, 학습 지속 이유를 다시 물어야 했다면 보고 실패다.
H1 학습·튜닝·평가 답변과 실행계획은 다음 규칙을 의무 적용한다.

1. **항상 `H1_REWARD_EVIDENCE_MASTER.md`를 먼저 읽는다.** 새 artifact가 이 문서보다 최신이면 artifact를 우선하고 같은 턴에 마스터를 갱신한다. 과거 계획의 `완료`, `PASS`, `최종 후보`가 최신 실험 감사와 충돌하면 최신 근거로 즉시 철회·재분류한다.
2. **값의 출처와 값의 성능을 분리한다.** 배포 기본값·코드 권장범위·Isaac Lab upstream 값은 후보 출발 근거일 뿐 NCRC 최적값이나 만족 판정이 아니다.
3. **모든 reward를 `만족 / 부분 만족 / 미만족 / 미측정 / INCONCLUSIVE` 중 하나로 표시한다.** 각 행에 로컬 `파일:줄` 또는 artifact, 외부 공식문서/논문, 적용 한계, H1~H7 직접 측정 범위를 붙인다.
4. **5,000 iter 초과 학습 전에 8개 항목을 제시한다.** 현재 단계, 단일 변경, 통제 변수, 로컬 근거, 외부 근거, H1~H7 측정범위, 사전 판정 기준, seed/evaluator 반복과 안전 중단·회수점이다. 하나라도 없으면 `HOLD — 근거 불충분`이다.
5. **단일 seed·단일 rollout·평균 reward로 최적·수렴·확정 만족을 선언하지 않는다.** promising candidate는 원칙적으로 독립 seed 반복과 동일 evaluator를 거친다. 예산 때문에 못 하면 `exploratory` 또는 `PARTIAL`로만 기록한다.
6. **학습 중단/지속 판단에는 정보가치와 회수 안전성을 함께 쓴다.** 현재 iteration, 가장 가까운 보존 checkpoint, 중단 시 잃는 가설, bundle 완성 조건을 먼저 확인하고 명령을 낸다.
7. 사용자 첫 화면에 **왜 지금 이 작업을 하는지**, **무엇이 만족/미만족/미측정인지**, **결과가 어느 의사결정을 바꾸는지**를 마스터 링크와 함께 표시한다.

## Go2 별도 캠페인 라우팅 (260901 신설)

1. H1 캠페인의 과거 결정과 완료 상태는 보존한다. Go2 작업은 `GO2_PROJECT_STATE.md`,
   `GO2_CAMPAIGN_SCHEDULE.md`, `GO2_REWARD_EVIDENCE_MASTER.md`와
   `workspace/training/quadruped/AGENTS.md`를 정본으로 사용하는 **별도 캠페인**이다.
2. Go2 작업에서는 H1 시나리오·임계값·학습 단가·reward 결론을 복사해 사실처럼 쓰지 않는다.
   공통으로 재사용할 수 있는 것은 증거 계층, 단일변수 실험, 독립 seed, artifact·영상·회수 규칙이다.
3. Go2 G1~G7의 정본은 `workspace/training/quadruped/config/go2_self_eval_registry.json`이다.
   기존 `server_run_Go2_videos.sh`는 H1형 시나리오 매핑이므로 `LEGACY_INVALID_MAPPING`이며,
   공식 Go2 시나리오 커버리지·자체 점수의 근거로 사용하지 않는다.
4. Go2의 현재 1차 결과 `train_260831-Go2_5var_1000`은 실질 4개 reward를 동시에 바꾼
   `MULTIVARIABLE_EXPLORATORY_BASELINE`이다. 각 reward의 인과효과나 G1~G7 성능을 확정하지 않는다.
5. Go2 튜닝·평가 전에는 `GO2_REWARD_EVIDENCE_MASTER.md`를 먼저 읽는다. 기존 정책을 올바른
   G1~G7 evaluator로 측정하기 전에는 새 학습을 제안하지 않는다.
6. Go2 전용 역할은 `go2-campaign-manager`, `go2-test-planner`, `go2-report-writer`,
   `go2-evaluation-auditor`다. 다운로드 SHA·tar·manifest 검사는 공용 `artifact-verifier`를 재사용한다.
7. 사용자가 260901 제공한 1라운드 제출 화면을 현재 제출 계약으로 사용한다:
   **로봇 유형 Go2 선택 + `policy.pt` + `.yaml` + 기술 개선 리포트 30~200자**.
   `report.html`, 영상, 내부 장문 보고서는 검증 근거이지 업로드 3종이 아니다.

## 공식 규정 정본 (260903 신설 — 예선 규정집 v1.0 / 시행 2026-08-17)

**이 절이 채점·제출·예산에 관한 최상위 정본이다.** 강좌(`test/`), 내부 원장, 과거 추정치가
이 절과 충돌하면 이 절이 이긴다. 강좌에는 채점 기준이 존재하지 않는다(본문 전수 검색:
`G1`~`G7`·`시나리오`·`survival_rate`·`70점`·`tracking_score` 각 0건). 채점 개념의 출처는
`workspace/PRELIM_RL_GUID.md`와 이 규정집이며, 강좌를 채점 근거로 인용하지 않는다.

### R-1. 배점과 합산 (제3·6·10·11조)

| 항목 | 정본 |
|---|---|
| 총점 | **200점 = H1 100점 + Go2 100점** |
| 로봇 1대 내역 | 시뮬레이션 **70** + 설계 의도 분석 **20** + 리포트 품질 **10** |
| 라운드 | 총 7라운드. **한 라운드에 한 로봇만** 제출 가능 |
| 합산 방식 | 전 라운드를 통틀어 **로봇 유형별 최고점만** 뽑아 합산 |
| 상한 | **한 로봇만 계속 제출하면 100점을 넘을 수 없다** |
| 동점 처리 | ① 자동 평가 ② 의도 분석 ③ 제출 속도 |

> **R-1 파생 규칙 — 두 기체 모두 제출하지 않으면 상한이 절반이다.**
> 따라서 "H1이냐 Go2냐"는 잘못된 질문이다. 정본 질문은 **"남은 시간을 어느 기체의
> 한계 점수에 쓰는가"**이며, 이미 제출 가능한 정책이 있는 기체는 **재학습이 아니라 제출**이
> 최우선 행동이다. 미제출 정책의 점수는 92점이든 0점으로 집계된다.

### R-2. 채점식과 생존의 정의 (제8조)

```
시나리오 점수 = 생존율 × 추종 점수        (0 ~ 1)
  · 생존율   = 넘어지지 않고 완주한 비율
  · 추종 점수 = 명령 속도를 정확히 따라간 정도 (오차 0 → 1.0)
종합 점수 = Σ(시나리오 점수 × 가중치) → 최대 1.0 → ×70점
```

**규정 원문은 "넘어지지 않고 완주한 비율"이지 "종료 이벤트가 발생하지 않은 비율"이 아니다.**
`terminated` 플래그만으로 생존을 계산하는 evaluator는 **규정 위반 지표**이며, 배를 깔고
누운 채 완주한 로봇을 생존으로 집계한다. 내부 생존 proxy는 반드시 자세 게이트
(중력 투영 + 지면 대비 높이)를 포함하고 `survival_proxy_source`를 함께 기록한다.
자세 채널이 없어 게이트를 적용하지 못한 결과는 `POSTURE_UNMEASURED`이며 점수 근거로 쓰지 않는다.

### R-3. 평가 시나리오 (제7조 — 공식 표)

**H1 (휴머노이드)**

| ID | 시나리오 | 지형 | 가중치 |
|---|---|---|---:|
| H1 | 제자리 균형 | 평지 | 0.15 |
| H2 | 전진 속도 추종 | 평지 | 0.20 |
| H3 | 좌우 속도 추종 | 평지 | 0.10 |
| H4 | 회전 복합 추종 | 평지 | 0.10 |
| H5 | 약한 요철 | 요철 | 0.15 |
| H6 | 완만한 경사 ±10° | 경사 | 0.15 |
| H7 | 밀침 회복 | 평지 + 충격 | 0.15 |

> **R-3a. H1 평가에는 계단도 박스 단차도 없다.** 평지 계열이 **0.70**, 요철 0.15,
> 완만한 경사(±10°) 0.15가 전부다. 학습 지형은 계단 33% + 박스 단차 33%로 구성되지만
> 그것은 **본선 과제**이고 예선 70점에는 **직접 기여하지 않는다**(제7조 본선 장애물 안내).
> 따라서 H1의 reward 투자 우선순위는 평지 추종·자세 안정·밀침 회복이며,
> 등반력을 위한 reward 변경은 예선 점수 기준으로 **기대이득 0**으로 평가한다.

**Go2 (사족)**

| ID | 시나리오 | 지형 | 가중치 |
|---|---|---|---:|
| G1 | 전진 속도 추종 | 평지 | 0.15 |
| G2 | 전방위 속도 추종 | 평지 | 0.15 |
| G3 | 거친 지형 | 험지 | 0.20 |
| G4 | 경사 ±20° | 경사 | 0.15 |
| G5 | 계단 10~15cm | 계단 | 0.15 |
| G6 | 밀침 회복 | 평지 + 충격 | 0.10 |
| G7 | 도메인 랜덤화 | 험지 + DR | 0.10 |

> **R-3b. "사족(Go2)의 단차 등반은 요구되지 않는다"(제7조).** G5가 요구하는 계단은
> **10~15cm 한 대역**이다. 따라서 학습 커리큘럼의 terrain level을 최대치까지 올리는 것은
> 목표가 아니며, **G5 대역을 통과하는 최소 level**을 목표로 잡는다. terrain level 자체를
> 성능 목표로 사전등록하지 않는다 — 목표는 언제나 G1~G7 시나리오 점수다.
> 평지 계열(G1+G2+G6)이 **0.40**으로 단일 최대 블록이라는 점도 함께 반영한다.

### R-4. 제출물 (제4·5조)

제출 **3종**: `policy.pt` · `env.yaml` · **기술 개선 리포트**.
`model_best.pt`와 `report.html`은 **제출물이 아니다**(규정 명문). 과거 문서의
"model_best.pt = 예선 채점용"은 학습 로그 문구를 오독한 것이므로 인용하지 않는다.

리포트는 **본인 작성·한국어 권장·비속어와 허위 기재 금지**. 의도↔결과 불일치는
**감점 요소일 뿐 부정행위가 아니고**, 의도를 고의로 허위 기재하는 것이 부정행위다(제14조).
따라서 리포트에는 실패한 실험과 철회한 가설을 **그대로 쓴다**. 숨기는 쪽이 위험하다.

### R-5. 예산 (제2조)

팀 **총 100시간**, 팀원 합산이며 개인별 배정이 아니다. 동시 접속 불가.
학습은 벽시계 시간이므로 tmux로 걸고 사람 시간은 문서 축에 병렬 배치한다.
잔여 시간은 `PROJECT_STATE.md` 예산 원장에 매 작업 후 갱신한다.

### R-6. 수정 허용 범위와 환경 무결성 (제2·12·14조)

1. **참가자가 바꾸는 것은 `humanoid_rewards.py` / `quadruped_rewards.py`의 값(가중치)뿐이다.**
   `go2_task/`·`h1_task/`·`train.py`·`play.py`·그 외 코드는 수정 대상이 아니다.
2. **제14조는 "제공된 서버에 에이전트·도구를 설치하거나 외부 서비스를 연동하는 등
   학습 환경의 구성을 변경하는 행위"를 부정행위로 규정한다.** 서버는 초기 설정 그대로 쓴다.
   본인 PC에서 LLM·코딩 보조 도구를 쓰는 것은 제한되지 않는다.
3. **판정 기준 — 학습 경로와 평가 경로를 분리한다.**
   - 제출물을 만드는 **학습 경로**(`train.py`, `*_task/`, `play.py`, reward 파일 이외의 코드)는
     **무조건 배포 원본 그대로** 실행한다. 여기에 손을 대면 제14조 대조 조항
     ("제출물과 서버 학습 이력을 대조한다")에서 이력 불일치가 발생할 수 있다.
   - 학습이 끝난 정책을 **읽기만 하는 내부 계측**은 제출물의 생성 경로가 아니다.
     그러나 안전 여유를 위해 계측도 배포 파일을 수정하지 않고 별도 모듈로 분리하며,
     제출 리포트에 계측 존재와 목적을 밝힌다.
4. **작업 전 `git status`로 배포 파일의 수정 여부를 확인한다.** 배포 파일이 수정된 상태에서
   생성된 정책은 `SUBMISSION_INTEGRITY_AT_RISK`로 표시하고, 원본 복원 후 재학습한
   정책으로 교체하기 전에는 제출 후보로 승급하지 않는다.
5. 학습은 **시작 순간의 설정을 스냅샷**한다(제12조). 학습 중 파일 수정은 진행 중인 학습에
   반영되지 않으므로, 실행 중 수정으로 실험 조건이 바뀌었다고 기록하지 않는다.

### R-7. 참고자료 사용 (제15조)

논문·기술문서·공개 저장소(Isaac Lab 포함)의 보상 설정을 읽고 적용하는 것은 **허용**이다.
금지는 타인 정책(`.pt`)·보상 설정을 그대로 제출하는 것, 대리 참가, 의도의 허위 기재다.
**"평가는 값의 출처가 아니라 의도와 결과의 일치를 본다."** 따라서 상류 기본값을 채택할 때도
왜 그 값을 택했는지 우리 실험 근거로 설명할 수 있어야 한다.
