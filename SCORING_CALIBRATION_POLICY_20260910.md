# 내부 채점·보정 참고점수 병기 계약 v2

## 사용자 결정 — 2026-09-10
기존 채점은 없애지 않는다. 기존 점수와 변경된 점수를 모두 제공한다. 이 결정은 H1과 Go2 양쪽에 적용한다.

## 적용 방식
1. **기존 점수(raw)**: 기존 수식·배점·seed·최악 case 집계·안전성 임계값을 그대로 보존한다. 과거 결과 파일을 재작성하지 않는다.
2. **보정 참고점수(v2)**: raw × H1 공식/내부 총점 관측 비율. 현재 계수는 `57.45 / 65.73163359451405 = 0.8740084014098619`다. H1 입력 출처와 hash는 `workspace/calibration/h1_official_20260910/H1_OFFICIAL_CALIBRATION.json:evidence`.
3. **공식 점수**: 사용자 전사 H1 결과는 별도 열, Go2는 미측정. 보정값을 공식 점수로 바꾸어 표시하지 않는다.
4. **기존/보정 delta**를 각각 보여 준다. 생존·추종 최저값과 미달 case는 보정과 무관하게 모두 보존한다.
5. 시나리오마다 계수를 따로 맞춰 공식 점수와 완전히 같게 만드는 방식은 진단 자료만 남기고 채택하지 않는다. 한 정책 7개 시나리오는 독립 정책 표본 7개가 아니다.

## 효력과 한계
- v2는 **예측 보고 방식 업데이트**이며 공식 규정이나 학습 환경 변경이 아니다. reward·train.py·task·play.py를 바꾸지 않는다.
- H1 공식 제출본과 Run06의 대응이 아직 미확인이다. 현재 오차는 조건부 비교다. 동일 정책 확인 전에도 두 수치는 볼 수 있지만, 예측 정확도 개선이라고 주장하지 않는다.
- H1 H5/H6의 지면 대비 자세 미측정 이력도 남아 있다(`H1_REWARD_EVIDENCE_MASTER.md:§12-b`). 과거 raw65.73은 비교 대상이지 현행 전범위 안전성 검증값으로 승격하지 않는다.
- Go2 v2는 **H1 비율 이전 민감도**다. 신뢰구간·하한·검증된 예상점수가 아니다. 계수는 양수라 이번 Go2 총점 순위를 바꾸지 않으며 계단 0점도 그대로 남는다.
- `validated_prediction=false`, `used_for_decisions=false`; 기존 내부 gate를 완화하거나 후보를 승급시키지 않는다.

## 이후 정밀화 검증
공식 결과마다 기체·라운드·제출 policy 식별자와 그 정책의 동결 내부 평가를 한 쌍으로 저장한다. 첫 표본으로 만든 계수를 다음 **독립 정책/제출 표본을 보기 전에 고정**하고, 기존/보정의 시나리오 MAE·총점 절대오차·약점 순위를 비교한다. 보정의 오차 감소가 실제 외부 표본에서 확인된 범위만 예측기로 채택한다. 생존과 추종이 분리 제공되지 않으면 어느 인수가 원인인지 추정해 임계값을 고치지 않는다.

## 구현
- H1: `tools/build_h1_official_calibration.py`
- Go2 병기: `tools/build_go2_calibration_sensitivity.py`
- Go2 실제 산출물: `workspace/server_returns/G-A027/audit_20260910/H1_RATIO_SENSITIVITY.json`
- 이번 수치·한계·판정: `H1_OFFICIAL_CALIBRATION_20260910.md`, `GO2_RESULT_AUDIT_G-A027_20260910.md`
