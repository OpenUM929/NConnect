# H1 학습 사전 예측 ↔ 실제 결과 비교 보고서 — `train_260828-03` (실행 완료)

> **문서 역할:** Run 03의 사전 계획(§1~§6)과 실제 결과(§7~§10)를 함께 담은 내부 보고서다. 최종 제출 적합성을 주장하지 않는다.
>
> **§1~§3, §5, §6은 실행 전에 작성된 원문이며 결과를 본 뒤 수정하지 않았다.** §4는 실행 상태만 갱신했고, §7 이후는 결과 확인 후 추가한 절이다.

## 1. 변경 범위

| 항목 | Run 02 | Run 03 계획 |
|---|---:|---:|
| 변경 변수 | `termination_penalty=-50.0` | `termination_penalty=-90.0` |
| 나머지 reward 및 환경 | Run 02 설정 | 변경하지 않음 |
| seed / environments / iterations | 42 / 4,096 / 3,000 | 동일하게 유지 |

변경폭은 권장 범위 `-50 ~ -400` 안에서 `-50`의 2배 절댓값으로 정했다. `-400`으로 한 번에 이동하지 않아, 낙상 벌점 강화의 추가 효과와 부작용을 분리해 관찰한다.

## 2. 근거와 한계

Run 02에서 `termination_penalty=-50.0`은 Run 01 대비 마지막 `base_contact`를 0.9244에서 0.2979로 낮추고, `time_out`을 0.0756에서 0.7024로 높였다. 그러나 `error_vel_xy`는 0.3493에서 0.3596으로, `error_vel_yaw`는 0.2367에서 0.9341로 악화됐다.

따라서 `-90.0`은 **낙상률을 추가로 낮출 수 있다는 가설**을 검증하는 값이며, 최적값이라는 증거는 아직 없다. Run 02 영상 평가는 미완료이므로 실제 보행 품질·부들거림·회전 실패는 미측정이다.

## 3. 실행 전 사전 예측과 판정 기준

| 항목 | 사전 예측 | 판정 기준 |
|---|---|---|
| 낙상 종료 (`base_contact`) | 감소 또는 유지 | Run 02의 0.2979 미만이면 개선, 이상이면 미개선 |
| 시간 종료 (`time_out`) | 유지 또는 증가 | Run 02의 0.7024 이상 |
| terrain progression | 유지 또는 증가 | Run 02의 5.6356 이상 |
| 명령 추종 오차 | 추가 악화 위험 | `error_vel_xy`, `error_vel_yaw`를 Run 02와 함께 기록; 둘 다 악화하면 부작용으로 판정 |
| 실제 보행 | 미예측 확정 불가 | 동일한 1,000-step 영상에서 낙상·제자리 버티기·부들거림·불필요 회전을 별도 관찰 |

최고 `Train/mean_reward`는 termination penalty의 크기 자체가 달라 직접 비교가 왜곡될 수 있으므로, Run 03의 주 판정 지표로 사용하지 않는다.

## 4. 실행 상태

**실행 완료 — 2026-08-28 20:36:33 시작, 3,000 iteration 종료.** `log_dir = logs/rsl_rl/humanoid/2026-08-28_20-36-33`. 결과는 §7 이후에 기록한다.

⚠️ 실행 전 원문은 다음과 같았다: "계획 등록됨 — 아직 미실행. Run 02의 `play_video.mp4`는 Run 01 artifact로 확인되어, Run 02 실제 영상 평가는 별도로 필요하다." **§6.4의 실행 게이트(Run 02 play 선행)를 충족하지 않은 채 실행됐다.** 상세는 `eval_260828-03_add.md` §6.

## 5. 근거 경로

- Run 02 결과 비교: `reports/train_260828-02_add.md`
- Run 02 평가 상태: `reports/eval_260828-02_add.md`
- 누적 실험 이력: `reports/experiment_history.csv`

## 6. 정량 선정 방법과 불확실성

### 6.1 탐색용 용량-반응 모형

이번 내부 실험 목표는 Run 02의 29.79%에서 약 절반인 **15% 이하**로 종료율을 낮추는 것이다. 이는 대회 공식 합격선이 아니라, 낙상 감소와 이미 악화된 yaw 추종 사이에서 과도한 벌점 증가를 피하기 위해 사전 등록한 실험 목표다.

학습 종료 시의 `base_contact` 비율을 `p`, 벌점의 절댓값을 `λ=|termination_penalty|`로 두고, 현재 두 관측값만을 사용한 **탐색용** logit 반응곡선을 다음과 같이 둔다.

`logit(p) = α + β log(λ)`,  `logit(p) = ln(p / (1-p))`

- Run 01: `λ=5`, `p=0.9244`, `logit(p)=2.504`
- Run 02: `λ=50`, `p=0.2979`, `logit(p)=-0.857`
- 두 점에서의 기울기 추정: `β=(-0.857-2.504)/(ln(50)-ln(5))=-1.460`

이 모형에서 사전 목표를 `base_contact=15%`로 두면 `λ≈91.2`가 계산된다. 설정값은 이를 반올림한 `-90.0`으로 고정한다. 이 값의 모형상 예상 `base_contact`는 약 15.3%다.

참고 후보점은 다음과 같다.

| 목표 `base_contact` | 모형상 `λ` | 해석 |
|---:|---:|---|
| 20% | 약 72 | 낙상 감소와 추종 보존 사이의 보수적 후보 |
| 15% | 약 91 | **선택값 `-90.0`의 근거** |
| 10% | 약 125 | 더 강한 생존 제약 후보 |

### 6.2 이 모형이 최적값 증명이 아닌 이유

이 추정은 서로 다른 벌점의 **두 번, 동일 seed 한 번씩**의 학습 결과만 사용한다. 실제 PPO 학습 과정은 비선형이며, `base_contact`는 학습 중 종료 비율이지 고정 시나리오 평가의 낙상 확률이 아니다. 또한 Run 02에서는 yaw 오차가 크게 악화됐다. 따라서 위 수치는 다음 실험의 사전 후보를 정하는 데만 사용하며, `-90`이 최적이라는 결론으로 사용하지 않는다.

### 6.3 제약 다목적 판정

다음 run의 선택 문제는 단일 보상 최대화가 아니라 다음 제약 문제로 다룬다.

`minimize p_fall(λ)`

subject to `error_vel_xy`와 `error_vel_yaw`가 허용 범위 안에 있고, 동일 1,000-step 영상에서 소극적 정지·불필요 회전·낙상이 없어야 한다.

현재 목표 낙상률과 허용 추종 오차의 공식 대회 임계값은 확보되지 않았다. 그러므로 Run 03은 Run 02 대비 낙상 지표가 개선돼도 두 추종 오차가 모두 더 악화되거나 영상에서 회전 실패가 확인되면 채택하지 않는다. 이 조건을 만족하는 후보들만 Pareto 후보로 남긴다.

### 6.4 실행 게이트

**Run 03 값 `-90`은 사전 등록 후보이나, 아직 실행 승인값은 아니다.** Run 02의 동일 조건 1,000-step play 평가로 실제 보행·회전 행동을 먼저 확인한다. 그 평가에서 yaw 위험이 실제로 확인되면, 더 큰 종료 벌점 대신 추종 보상 또는 별도의 보상 항을 다루는 새 가설을 테스트 기획자가 설계해야 한다.

---

## 7. 실제 결과 (실행 후 추가)

### 7.1 비교 조건 검증

| 항목 | Run 02 | Run 03 | 상태 |
|---|---|---|---|
| 원본 run | `2026-08-28_09-48-44` | `2026-08-28_20-36-33` | 확인됨 |
| seed | 42 | 42 | 동일 |
| `num_envs` | 4,096 | 4,096 | 동일 |
| 종료 iteration | 2,999 | 2,999 | 동일 |
| `params/agent.yaml` | — | — | **전체 줄 비교 결과 완전 동일** |
| `params/env.yaml` | — | — | **`log_dir`과 `termination_penalty` 두 줄만 상이** |

[사실] 설정 파일 전체 줄 비교(개행문자 차이 제외)에서 실험 변수 차이는 `termination_penalty: -50.0 → -90.0` 하나다. **단일 변수 비교 조건을 충족한다.** 다만 seed 하나, 실행 1회이므로 반복 재현성은 확인되지 않았다.

### 7.2 학습 지표 (마지막 iteration, step 2,999)

| 지표 | Run 02 | Run 03 | 변화 |
|---|---:|---:|---:|
| `Episode_Termination/base_contact` | 0.2979 | 0.2785 | -0.0194 (-6.51%) |
| `Episode_Termination/time_out` | 0.7024 | 0.7215 | +0.0191 (+2.72%) |
| `Curriculum/terrain_levels` | 5.6356 | 5.6682 | +0.0326 (+0.58%) |
| `Metrics/base_velocity/error_vel_xy` | 0.3596 | 0.4182 | +0.0586 (**+16.30% 악화**) |
| `Metrics/base_velocity/error_vel_yaw` | 0.9341 | 1.7672 | +0.8331 (**+89.19% 악화**) |
| `Policy/mean_std` | 1.3107 | 1.5473 | +0.2366 (+18.05%) |
| `Loss/entropy` | 23.0600 | 28.3315 | +5.27 (+22.86%) |
| `Train/mean_episode_length` | 775.09 | 732.71 | -42.38 (-5.47%) |
| `Episode_Reward/track_lin_vel_xy_exp` | 0.2972 | 0.2843 | -4.34% |
| `Episode_Reward/track_ang_vel_z_exp` | 0.1584 | 0.0897 | **-43.37%** |
| `Episode_Reward/action_rate_l2` | -0.0584 | -0.0757 | 악화 |
| `Episode_Reward/feet_slide` | -0.0450 | -0.0483 | 악화 |
| 최고 `Train/mean_reward` | 3.8434 (iter 2,776) | 0.3257 (iter 2,516) | §3 규정에 따라 판정에 사용하지 않음 |

`termination_penalty` 절댓값이 달라 보상 총합의 스케일이 바뀌므로, 최고 `Train/mean_reward`의 하락은 §3에서 사전에 규정한 대로 성능 판정 근거로 사용하지 않는다.

### 7.3 영상 관찰

[사실] Run 03 checkpoint로 play가 2회 실행됐고 영상 2건이 보존됐다. 사용자 관찰: **"제자리에 잘 서 있기는 한데 왼쪽으로 돌고 있어."** 상세와 한계는 `eval_260828-03_add.md`.

## 8. 사전 예측 ↔ 실제 결과 대조

§3에 학습 전 등록된 기준과 대조한다. 결과를 본 뒤 기준을 변경하지 않았다.

| 사전 예측·기준 | 실제 결과 | 충족 여부 |
|---|---|---|
| `base_contact < 0.2979` | 0.2785 | 충족 |
| `time_out >= 0.7024` | 0.7215 | 충족 |
| `terrain_levels >= 5.6356` | 5.6682 | 충족 |
| 추종 오차 Run 02와 병기; 둘 다 악화하면 부작용 | `error_vel_xy` +16.30%, `error_vel_yaw` +89.19% — **둘 다 악화** | 부작용 확정 |
| 영상에서 회전 실패 없음 | **명령과 무관한 지속 좌회전 확인** | 미충족 |

### 판정

**FAIL — 채택하지 않음.**

§6.3의 사전 등록 조건("낙상 지표가 개선돼도 두 추종 오차가 모두 더 악화되거나 영상에서 회전 실패가 확인되면 채택하지 않는다")을 **두 가지 모두** 충족한다. 낙상 3개 지표가 예측 방향대로 움직였다는 사실은 기록하되, 채택 근거로 삼지 않는다.

## 9. §6.1 용량-반응 모형의 반증

§6.1 모형은 `λ=90`에서 `base_contact ≈ 15.3%`를 예측했다. **실측은 27.85%다.**

### 9.1 국소 기울기 재추정

| 구간 | `λ` 변화 | `logit(p)` 변화 | 기울기 `β` |
|---|---|---|---:|
| Run 01 → Run 02 | 5 → 50 | +2.504 → -0.857 | -1.460 (§6.1에서 사용한 값) |
| Run 02 → Run 03 | 50 → 90 | -0.857 → -0.952 | **-0.162** |

[사실] 실제 국소 기울기는 §6.1이 가정한 값의 약 **1/9**이다. 즉 반응곡선은 단일 로그-선형이 아니며, `-50` 부근에서 이미 **포화**했다.

### 9.2 남은 후보점의 폐기

§6.1이 제시한 후보(`λ≈125`로 10%, `-400`)를 실측 국소 기울기로 재계산한다.

| `λ` | §6.1 모형 예측 | 국소 기울기 재추정 |
|---:|---:|---:|
| 90 | 15.3% | 27.85% (실측) |
| 400 (권장 범위 상한) | 약 3% | **약 23.3%** |
| 15% 도달에 필요한 `λ` | 약 91 | **약 11,000 (권장 범위 밖)** |

[추론] 허용 범위의 상한인 `-400`까지 밀어도 `base_contact`는 약 23%에 머물 것으로 추정된다. **`termination_penalty` 축만으로는 사전 목표 15%에 도달할 수 없다.** §6.1의 남은 후보점(`-125`, `-400`)을 모두 폐기한다.

### 9.3 절대 효과 크기 비교

| 구간 | `base_contact` 변화 |
|---|---:|
| `-5 → -50` | 0.9244 → 0.2979 (**-62.65%p**) |
| `-50 → -90` | 0.2979 → 0.2785 (**-1.94%p**) |

[추론] 두 번째 구간의 효과는 첫 구간의 약 1/32이며, `humanoid_rewards.py:124`가 기록한 cudnn 비결정성에 의한 run 간 변동과 구분되지 않을 수 있다. 반복 seed 실행이 없으므로 이 -1.94%p가 실제 효과인지 잡음인지 **확정할 수 없다.**

## 10. 결론과 다음 handoff

### 확정된 사실

- `termination_penalty` 축은 `-50` 이후 낙상 감소 효과가 포화했다.
- 벌점을 더 키우면 두 추종 오차와 탐색 std, action smoothness가 함께 악화된다.
- Run 03은 사전 등록 기준상 FAIL이며, **기준선은 Run 02(`-50.0`)로 유지한다.**
- `error_vel_yaw`는 `ang_vel_z` 명령 최대 절댓값(1.0 rad/s)을 넘어섰다. 회전 행동이 명령과 결합되지 않았다는 신호다.

### 아직 미측정

- 반복 seed에서의 재현성
- Run 02 checkpoint의 영상 대조군 (Run 03 좌회전이 `-90`에서 새로 생긴 것인지 판별 불가)
- H1~H7 시나리오별 격리 평가
- 수렴 이후 성능 — 세 run 모두 종료 시 `mean_std`가 1.0 이상으로 학습 부족 상태다

### 다음 handoff

낙상 벌점 축을 종료하고 **회전 추종 보상 축**으로 전환한다. `track_ang_vel_z_exp`를 권장 범위 하한 0.5에서 올리되, 기준선은 Run 02(`termination_penalty=-50.0`)로 복귀시켜 단일 변수 비교를 유지한다. 사전 등록: `reports/train_260828-04_add.md`.

