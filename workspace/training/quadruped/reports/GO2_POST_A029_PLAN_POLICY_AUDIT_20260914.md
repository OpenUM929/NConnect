# Go2 튜닝 계획·정책 일관성 감사 (2026-09-14)

대상:
- 병행 계획 `upload/plan/GO2_POST_A029_TUNING_PLAN_20260914.md`(GO2-REPLAN-A029, 이하 "계획")
- 시행 계획 `upload/plan/GO2_AUDIT_REMEDIATION_PLAN_20260914.md` §3-C(G-A030 설계, 이하 "내 C")
- A·B 시행 후 튜닝 정책 문서(기체 `AGENTS.md`, `GO2_NOW.md`, 원장 §1-a)

G-A030 파일(사양 JSON·패키지)은 존재하지 않는다. C는 보류 중이므로 감사 대상은 설계 문서다.
서버 실행·코드 수정 없음. 읽기 전용 감사다.

## 0. 예선 기준 현재 위치
- [예선 목표] Go2 시뮬레이션 70점 축. G3·G5 생존 손실 회복.
- [현재 단계] 2/6 짧은 학습 pilot. C 재검토 중.
- [확보] 계획 수치 재현, 계측 지문 일치(evaluator `353614…0d84`, registry `8d8c34ca…9ba6`), 엔진 평가 범위 확인.
- [미확보] 잔여 GPU 실측, 69case 후보 평가 수단.
- [이번 테스트] 문서·코드 대조와 계획의 Python 재현만 수행. 학습 없음.
- [흐름] 계획 감사 → **정책 적용 감사** → C 재설계 결정 → G-A030 패키지
- [지금 할 일] §4 권고 중 C 재설계 방향(측정 경로)을 사용자가 결정.
- [보장하지 않음] 이 감사는 후보 성능을 판정하지 않는다.

## 1. 판정

| 대상 | 판정 | 요지 |
|---|---|---|
| 계획 | **정책 부합 — 경미 1건** | 값 근거·사전등록·report-first·다이얼 이력 규칙을 지킨다. reward 상태 한 줄이 §1-a와 다르다(F4) |
| 내 C | **불일치 — 이대로 시행하면 사전등록 기준을 잴 수 없다** | 계획 §5 기준을 채택해 놓고, 그 기준을 측정하지 못하는 엔진으로 패키지를 만들도록 설계했다(F1·F2). 대안 인용 오기(F3), 시간 과소추정(F11) |
| 정책 적용 | **부분 일관** | A·B 잔존 충돌 4건(F6~F9). 내가 넣은 재시도 금지 문구가 캠페인 감사 M5를 스스로 위반한다(F5) |

## 2. 계획이 지킨 것 (검증함)

| 정책 | 계획의 이행 | 확인 |
|---|---|---|
| 기체 AGENTS §4-1 단일변수 | A017에서 `flat_orientation_l2 0→-1.0`만 변경 | §4 |
| §4-2 기본값·중간값·배수 금지 | "권장범위 중간·기본값 복사 아님", upstream 0.0은 추천 근거 아님 명시 | §4 |
| §4-3 사전등록 | 목적·표적 G3·부작용 G4/G5·성공/실패/INCONCLUSIVE·seed42·1000iter·500iter 보존점·영상·bundle | §4·§5 |
| §4-4 survival×tracking 분해 | 손실을 S/T로 분해, 학습 reward로 점수 만들지 않음 | §3 |
| §4-7 단일 seed exploratory | seed43 대조 재현 전 장기 승급 금지 | §5 |
| report-first(09-14 개정) | A017 `MISSING — 복구 불가` 1회 기록, 차단하지 않음. Pilot HTML 직접 열람 `READ_UNMATCHED` | §2 |
| §9-a report.html 보존 | 평가 전 `_keep/<명칭>/exported/report.html` 보존 | §5 |
| 다이얼 이력(§1-a) | flat은 보행 기준선 "미탐색" → 재시도 아님. A018 유효 실패로 action 완화 제외 | §1·§4 |
| 유효 비교 정의 | 기준선 A017은 보행, 69case posture_gate_v2 | §3 |

수치 재현(이번에 직접 실행):

- rough_lateral 종료 17/14/17, 종료 전 q 평균 .688471/.627949/.660327, 중앙값 .771251/.657381/.717148. 계획 표와 일치한다.
- 민감도 70×.20×.75736×.10 = 1.0603. 일치한다.
- G3 최저 곱 case는 seed303(S .4688·T .7574). 계획의 .46875/.75736과 일치한다.

계측 지문:

- 현재 `go2_eval_telemetry.py`의 SHA는 `353614…0d84`로, A027 identity와 같다.
- registry `8d8c34ca…9ba6`도 같다.
- 엔진 `go2_tuning_eval_report.py:28-42`는 `comparison_blockers()`로 지문이 비대칭인 비교를 차단한다.
- 따라서 계획 §5-4("새로운 evaluator면 양쪽 재평가")는 현재 상태에서 "A027 기준선 재사용 가능"으로 풀린다. C1 편집 대상인 `go2_tuning_config.py`는 evaluator 파일이 아니다.

## 3. 결함

### 치명

**F1. 계획의 표적 case를 엔진이 평가하지 않는다 (내 C의 설계 결함).**
- 엔진 `CANONICAL_CASES`(`go2_tuning_config.py:125-133`)의 G3는 `rough_forward`, G5는 `stairs_15_up`이다.
- tier-1은 seed101 7case이고, representative 단계를 합쳐도 21case다(`server_run_go2_tuning_engine_v1.sh:390-407`).
- A027 A017 값으로 보면 엔진이 보는 case는 이미 거의 생존한다:

  | case | seed101 | seed202 | seed303 | 엔진 평가 |
  |---|---:|---:|---:|---|
  | G3 rough_forward S | .906 | .969 | 1.000 | ✓ |
  | G3 rough_lateral S | .469 | .563 | .469 | **✗** |
  | G5 stairs_15_up S | .719 | — | — | ✓ |
  | G5 stairs_15_down S | .000 | — | — | **✗** |

- 계획의 성공 기준("G3 최저 case 생존 +.10", rough_lateral base-contact 17/14/17 감소)과 필수 부작용 판독(G5 하강)은 엔진 출력에 없다.
- 영상도 맞지 않는다. `validate_experiment()`는 G1 `forward_fast` 1개만 허용한다(`go2_tuning_config.py:247-251`). 계획은 24개를 요구한다.
- 69case 러너(`server_run_go2_a017_full_suite.sh`)는 A017·Pilot 고정본이라 새 후보를 받지 못한다.
- 결과: 내 C1·C3대로 발행하면 계획 §5 규칙상 **INCONCLUSIVE(필수 telemetry·영상 누락)**가 확정된 학습에 GPU를 쓴다.

**F2. 엔진의 조기 종료가 계획의 판정 규칙과 충돌한다.**
- 엔진은 tier-1 7case가 `INTERNAL_EARLY_KILL_PASS`(총 +1.0/70, 시나리오 생존 후퇴 ≤.10)가 아니면 즉시 끝낸다(`server_run_go2_tuning_engine_v1.sh:399-403`).
- flat −1이 rough_lateral만 개선하고 rough_forward 7case 총합이 +1.0에 못 미치면, 표적은 측정되지 않은 채 "실패"로 끝난다.
- 계획의 case별 비열등 기준(생존 ≤1/32, 추적 ≤.02)은 사양 스키마(`additionalProperties: false`)의 gates로 표현할 수 없다.

### 중대

**F3. 내 C2가 계획의 대안을 잘못 인용했다.**
- C2는 "차순위(그 계획 §4 대안과 동일): `ang_vel_xy_l2` 완화, `lin_vel_z_l2`"라고 적었다.
- 계획 §4의 실제 대안은 다음과 같다: action 완화 제외, track 추가 인상 후순위, ang_xy −.06(강화) 보류, feet/lin_z 동시 변경 안 함.
- "ang_xy 완화"는 계획에 없는 내용이다.

**F4. 계획의 reward 상태 한 줄이 정본 §1-a와 다르다.**
- 계획 §4는 "feet/lin_z/ang_xy의 개별 효과 INCONCLUSIVE"라고 적었다.
- §1-a는 feet를 "기각(상향 0.35, A015 유효)", ang_xy를 "기각(강화, A016 유효)"으로 적는다.
- 계획이 §1-a보다 먼저 쓰였고, 이후 갱신되지 않았다.

**F5. 내가 넣은 재시도 금지 문구가 증거보다 강하다(캠페인 감사 M5 재발).**
- 문제 문구는 두 곳에 있다.
  - `GO2_NOW.md:56` "재시도 금지: `action_rate_l2` 완화, `feet_air_time` 0.35, `ang_vel_xy_l2` 강화"
  - 시행 계획 A2 초안 "강화 금지·완화 금지"
- 근거가 된 A016은 −.15(3배) 한 값, A018은 −.008 한 값이다. 둘 다 학습 seed 42 하나다.
- 이 결과로 **방향 전체**를 금지하는 것은 캠페인 감사가 비판한 "단일 seed로 완전히 기각"(M5)과 같은 과잉 일반화다.
- 이 점에서는 계획의 "ang_xy −.06 보류"가 더 정확하다.

### 보통 — A·B 잔존 충돌

| # | 위치 | 내용 | 충돌 대상 |
|---|---|---|---|
| F6 | 기체 `AGENTS.md:118` §8 | "run 결과·원장·**200자** 제출문: go2-report-writer" | 같은 파일 §7(500자), 루트 R-4a. 검사 3은 §7만 봐서 놓쳤다 |
| F7 | 기체 `AGENTS.md:55-56` §3 | "현재 단계·다음 행동은 `GO2_PROJECT_STATE.md` 최신 행과 `upload/README.md`에서 읽는다" | 같은 파일 §1 "`GO2_NOW.md`가 이긴다" |
| F8 | 기체 `AGENTS.md:71` §4-5 | "Pilot-01과 Default-01을 동일 evaluator에서 비교하기 전에는 특정 reward를 만족·개선 원인으로 쓰지 않는다" | Default-01은 폐기됐다(G-D113). 문자 그대로 읽으면 원장의 track "채택"과 계획의 "부분 만족"을 금지한다 |
| F9 | 기체 `AGENTS.md:184-196` REPORT-FIRST 블록 | "2026-09-13 … NEXT … **새 학습 HOLD**"에 SUPERSEDED 표시가 없다 | 같은 블록 4항의 09-14 개정, 루트 튜닝 요청 산출물 계약 |

### 경미

- **F10.** 계획 파일 자체에 보류 표시가 없다. §6 NEXT는 여전히 "실행 패키지 구현"이다. 원장과 `GO2_NOW.md`에는 보류가 적혀 있으므로 우선순위 규칙상 해소는 되지만, 계획만 연 독자는 모른다.
- **F11. 시간 견적이 없거나 과소하다.**
  - 계획은 실행시간 견적을 두지 않았다. 기체 AGENTS §9는 서버를 켜기 전에 예상시간을 닫도록 요구한다.
  - 내 C4의 "약 1~1.5h"에는 후보 69case 평가가 빠져 있다. A027은 두 arm 69case에 약 1시간 45분이 걸렸다(원장 914행).
  - 계획의 총 예산은 학습 3회에 69case 평가 3회 이상이다.
  - 잔여 GPU는 `[미측정]`이다(9/8 15h − A027 약 1.75h − A028 미기록). 루트 계약의 차단 사유 ③을 판정하려면 실측이 먼저다.

## 4. 권고 (C 재검토 입력, 미집행)

1. **측정 경로를 먼저 정한다(F1·F2).** 둘 중 하나를 고른다. 어느 쪽이든 엔진·러너 작업이며 배포 학습 코드는 건드리지 않는다(R-6 무관).
   - (가) 엔진 1.6.0에서 다음을 지원한다.
     - 사양별 평가 case 목록: G3 rough_lateral, G5 stairs_15_down 포함
     - 후보 69case 평가
     - 영상 목록
     - 이 실험의 tier-1 조기 종료를 "평지 붕괴" 같은 명백한 파국에만 걸리게 바꾸는 것
   - (나) 두 단계로 나눈다.
     - 1단계: 엔진으로 학습하되 조기 종료를 끈다.
     - 2단계: A027 러너를 후보를 인자로 받도록 일반화해 69case와 24영상을 잰다.
   - 계획 §5의 case별 비열등 기준은 판정 스크립트에 넣는다.
2. 내 C2의 차순위 문장을 계획 원문으로 고친다(F3).
3. 재시도 금지 문구를 "같은 값 재시도 금지. 같은 방향 다른 크기는 새 근거와 §1-a 인용 필요"로 바꾼다. 대상은 `GO2_NOW.md`와 시행 계획 A2이다(F5).
4. F6~F9를 고치고 검사를 넓힌다.
   - 검사 3: 기체 AGENTS 전체에서 "200자 제출문"을 금지한다.
   - 검사 2: §3의 라우팅 문구를 확인한다.
   - REPORT-FIRST 블록은 계약 테스트 문자열을 지키기 위해 지우지 않고 SUPERSEDED 한 줄만 붙인다.
5. 계획 §4의 reward 상태 한 줄을 §1-a에 맞춰 고치고, 계획 머리에 보류 표시를 붙인다(F4·F10).
6. 시간 견적은 A017 원 학습 시간과 A027 arm당 평가 시간으로 계산하고, 서버에 접속하면 잔여 GPU를 먼저 실측한다(F11).

## 5. 검증 명령

```
python - < (계획 §6 "재현" 코드)            # 17/14/17, .688/.628/.660 재현
sha256sum workspace/training/quadruped/go2_eval_telemetry.py   # 353614…0d84
sha256sum workspace/training/quadruped/config/go2_self_eval_registry.json  # 8d8c34ca…9ba6
grep -n "200자\|최신 행\|Default-01을 동일" workspace/training/quadruped/AGENTS.md
```

## 6. 메인 튜닝 담당의 재판정·의견 (2026-09-14 사용자 요청 추가)

> 이 절은 위 독립 감사 원문을 수정하지 않고 덧붙인 **피감사자의 답변**이다. 독립 감사의 추가 판정이나 결함 수정 완료를 뜻하지 않는다. 사용자 요청은 직전 설명과 의견의 문서 기록이며, C 실행 승인·새 패키지 발행 요청으로 해석하지 않는다. 세션 경로: STAY.

### 6-1. 현재 보정하려는 행동

- 보존 중인 안은 A017에서 `flat_orientation_l2 0.0→-1.0`만 바꾸는 탐색이다. 아직 새 학습을 실행하지 않았다.
- **직접 보정 대상:** 거친 지형에서 횡이동할 때 몸통이 수평 대비 크게 기울어지는 행동.
- **기대 결과(가설):** G3 `rough_lateral`의 몸통 접촉 종료 감소와 이동 지속. 따라서 안정성·낙상 감소를 겨냥한 실험인 것은 맞다.
- **관측 근거:** 기존 계획의 첫 종료 전 0.5초 기울기 지표와 접촉 종료 17/14/17개(/32, 평가 seed 101/202/303). 직전 판단에서는 본 감사의 수치 재현 기록을 확인했으며 원 CSV를 다시 계산한 것은 아니다.
- **미확정:** 큰 기울기가 실패의 원인인지, 접촉·미끄러짐 이후의 결과인지 구분되지 않았다. 접촉 종료를 곧바로 모든 실제 낙상의 판독으로 쓰지 않는다.
- **부작용:** 경사·계단에서 필요한 기울임까지 억제하여 정체하거나 추종이 나빠질 수 있다. G5 낮은 높이 proxy와 `stairs_down` 라벨만으로 실제 전도·하강을 확정하지 않는다.

### 6-2. 우선순위에 대한 정정

사용자 지적을 수용한다. 정책은 **낙상을 완치한 뒤에야 다른 행동을 개선한다**가 아니다. 기존 사용자 결정은 **기대 가중 이득·실험 비용·원인 확실성을 함께 비교**하는 것이다.

근거: `upload/plan/GO2_G_A029_DIFFICULTY_SCREENING.md:7-9`의 사용자 결정 기록. 이 인용은 해당 우선순위 원칙만 재사용하며, 같은 역사 문서의 폐기된 report 누락 HOLD를 되살리지 않는다.

- 최대 감점 시나리오와 약한 인수는 중요한 후보 선정 근거지만, 낙상 개선을 무조건 첫 작업으로 고정하는 뜻은 아니다.
- G3 선택 이유는 큰 손실과 G5보다 해석이 명확한 접촉 종료 신호였다. 그러나 **다른 행동 개선 후보보다 비용 대비 유리하다는 비교는 끝내지 않았다.**
- 계획의 `+1.06/70`은 tracking 고정·survival +.10 달성 가정의 민감도이며 달성 확률을 반영한 기대 이득이 아니다.
- **재판정:** `flat -1`은 근거 있는 탐색 후보로 보존하되, 최우선 튜닝으로 확정하지 않는다. 정책 부합과 후보 간 우선순위 입증은 별개다.

### 6-3. 감사 항목별 의견

| 항목 | 메인 의견 | 근거·범위 |
|---|---|---|
| F1·F2 | 수용, 확신 높음 | 직전 판단에서 `go2_tuning_config.py:125-133,225,249-250`과 `server_run_go2_tuning_engine_v1.sh:390-407`을 직접 열었다. G3 전진/G5 상승 고정, G1 영상 고정, tier-1 종료가 목표 횡이동·필수 평가를 누락시킬 수 있다. 현재 시행 설계로 실험 판정 가능하다고 볼 수 없다. |
| F3 | 수용 | 계획에 없는 `ang_xy 완화`를 원 계획과 같은 대안으로 인용하면 안 된다. |
| F4 | 표기 분리 필요 | A015의 feet .35 및 A016의 ang_xy -.15 변경은 유효 기각 이력이다. 반면 현재 유지값 feet .2/lin_z -2/ang_xy -.05의 개별 인과효과는 미확정이다. 전자를 누락하거나 후자를 확정 효과로 바꾸지 않는다. |
| F5 | 수용 | 한 값·한 학습 seed의 실패로 강화/완화 방향 전체를 금지할 수 없다. 과거 실패값을 무시한 재제안도 정당화되지 않으며, 새 후보는 기존 실패와 차별 근거가 필요하다. |
| F6~F10 | 수용 | 제출문 상한·현재 상태 정본·폐기 기준선·옛 HOLD·보류 표기의 충돌이 남았다. 원문 보존과 최신 적용 상태의 명시를 구분해야 한다. |
| F11 | 수용 | 학습만이 아니라 평가·영상·압축·회수와 여유시간을 포함해야 한다. 잔여 GPU 미측정을 회수 가능으로 간주하지 않는다. |

### 6-4. 자기 진단과 현재 결론

1. **시험할 만한 가설을 지금 가장 먼저 해야 하는 튜닝으로 너무 빨리 올렸다.** 손실과 관측 신호는 제시했으나 대안 간 기대 이득·비용 비교가 부족했다.
2. **사전등록 기준을 실제 실행 경로가 측정할 수 있는지 확인하지 않았다.** 문서의 판정 기준이 상세하다는 사실은 구현 준비의 증거가 아니다.
3. **감사의 결론에 동의한다:** 실험 계획은 대체로 정책에 부합하지만 시행 설계 C는 그대로 실행하면 안 된다. 다만 '정책 부합'은 '최우선 후보 선정 입증'까지 포함하지 않는다.
4. 현재 `flat -1` 후보와 기존 감사·review 자료는 보존한다. **우선순위 확정·실행 준비 완료로 취급하지 않으며 C 보류를 해제하지 않는다.** 측정 경로 (가)/(나) 중 하나를 이번 기록으로 확정하거나 구현하지 않았다.
5. 이 추가 절은 판단 기록이다. 코드·정책·평가 도구·기존 실행 ZIP 변경, 새로운 학습/성능 판정, F1~F11 수정 완료를 주장하지 않는다.

## 7. 감사자의 답변 검토 (2026-09-14)

판정: **답변 타당. 감사 원문의 오류 2건을 바로잡았고, 과대 주장은 없다.** §0~5는 작성 당시 원문과 바이트 단위로 같다(세션 기록과 대조).

### 7-1. 원자료로 확인한 답변 주장

| 주장 | 확인 |
|---|---|
| 우선순위 결정 인용 `GO2_G_A029_DIFFICULTY_SCREENING.md:7-9` | 원문과 일치. 원장 `GO2_PROJECT_STATE.md:1707`, `GO2_REWARD_EVIDENCE_MASTER.md:406`에도 같은 결정이 기록돼 있다 |
| F1 근거 `go2_tuning_config.py:125-133,225,249-250` | 줄 번호·내용 일치 |
| 0.5초 기울기 지표, 기울기 원인/결과 미확정, G5 높이 판정 한계 | 계획 69·81·83-84행과 일치 |
| `+1.06/70`은 민감도이지 기대 이득이 아니다 | 계획 147행이 "민감도"로 적는다. **감사자의 채팅 요약이 "기대 이득"이라고 잘못 불렀다** |

### 7-2. 감사 원문 정정

- **F4 철회.** 정본 원장 §3(`GO2_REWARD_EVIDENCE_MASTER.md:77`, 09-14 정정판)은 "A017에 남은 Pilot 조합(feet .2·lin_z -2·ang_xy -.05)의 개별 인과효과 = INCONCLUSIVE"라고 적는다. 계획 116행과 같은 말이다. §1-a의 "기각"은 바꾼 값(0.35, -0.15)의 이력이라 충돌하지 않는다. 답변의 "표기 분리"가 맞다. 남는 것은 계획 116행이 A015·A016 기각 이력을 함께 적지 않았다는 참고 수준의 누락뿐이다.
- **§1의 "계획: 정책 부합" 판정을 한정한다.** §2 대조표에 사용자 우선순위 결정(기대 가중 이득·비용·원인 확실성 비교)이 빠져 있었다. 계획은 G5(10.50)가 아니라 G3(9.03)을 고르면서 원인 확실성 근거는 적었다(계획 85행). 그러나 G4·G7이나 다른 행동 개선 후보와의 이득·비용 비교는 없다. 정정 판정: **"형식 정책 부합, 후보 우선순위 입증은 미완."**
- **내 C도 같은 결함을 가진다.** 시행 계획 C1은 비교 없이 flat -1을 G-A030으로 고정했다. 답변 6-4의 1번 자기 진단은 내 설계에도 해당한다.

### 7-3. 새 결함

**F12(중대). 정본 규칙끼리 우선순위 원칙이 충돌한다.**
- 아래 셋은 여전히 "최대 감점 시나리오 우선"을 규칙으로 적는다.
  - 루트 `AGENTS.md:222-224`
  - `GO2_REWARD_EVIDENCE_MASTER.md` §5 1~3항(95-97행)
  - `.codex/agents/go2-test-planner.md:45`
- 사용자 결정(이득·비용·원인 확실성 비교)은 G-A029 기록 절에 "검토 정책"으로만 남아 있다. 규칙 문서로 옮겨지지 않았다.
- 결과적으로 계획이 G3를 고른 것은 사용자 결정에는 맞고, 규칙 문서의 문자 해석(§5 1항 → G5)과는 다르다. 다음 계획자도 같은 모순을 만난다.

### 7-4. 답변의 공백

- **재판정이 효력 문서에 없다.** "flat -1은 최우선으로 확정하지 않는다"는 이 보고서에만 있다. 우선하는 `GO2_NOW.md`는 13·50행에서 G-A030을 flat -1로 적고 있다. 사용자 결정 없이는 고치지 않는다는 답변 태도는 맞다. 다만 반영하기 전까지 다음 담당자는 여전히 flat -1을 다음 패키지로 읽는다.
- **순서.** 우선순위가 열렸으므로 C 재검토 순서는 "후보 비교(이득·비용·원인 확실성) → 측정 경로 (가)/(나)"가 맞다. (가)의 엔진 case 목록·69case 평가 확장은 후보와 무관하게 필요하므로 순서와 상관없이 쓸 수 있다.

### 7-5. 시행 기록 (2026-09-14 사용자 지시 "진행하고 기록")

범위는 F12 해소와 재판정의 `GO2_NOW.md` 반영이다. F3·F5~F11은 이번 범위가 아니어서 손대지 않았다. 코드·엔진·배포 학습 파일·registry도 바꾸지 않았다.

| 파일 | 변경 |
|---|---|
| `GO2_REWARD_EVIDENCE_MASTER.md` §5 1~3항 | "최대 감점 시나리오 → 약한 인수 → reward 하나"를 다음으로 바꿨다: 감점·약한 인수 표는 입력, 후보별 기대 가중 이득·실험 비용·원인 확실성 비교, 비교표와 선택 이유를 계획서에 기록, 민감도≠기대 이득 |
| 루트 `AGENTS.md` 학습 승인 게이트 5항 | 휴머노이드와 공용인 규칙이라 본문은 두고 Go2 조항 3줄을 추가했다 |
| `.codex/agents/go2-test-planner.md:45` | "최대 감점 G 시나리오의 약한 인수 하나로 정한다"를 비교표 기준으로 바꿨다 |
| `GO2_NOW.md` §0·§4 | 흐름·지금 할 일을 "후보 비교(원장 §5)"로 바꿨다. "G-A030 후보 미확정" 항과 재검토 입력(이 보고서 §6·§7)을 추가했다 |
| 병행 계획 머리 | "미확정 탐색 후보, §6 NEXT 효력 없음" 상태 표시를 추가했다(본문 미수정, F10 부분 해소) |
| 시행 계획 C2 머리 | "flat -1 채택 효력 정지, 비교 대상 후보로 보존" 표시를 추가했다(본문 미수정) |
| `tools/test_go2_canonical_consistency.py` | 검사 7을 신설했다. 원장 §5에 "원인 확실성"이 있는지, 옛 규칙 문구가 되돌아오지 않았는지, 네 문서가 결정 ID `G-D-PRIORITY-20260914`를 인용하는지 확인한다 |

검증 결과:
- Go2 테스트 35개 통과(검사 7 추가).
- `GO2_CAMPAIGN_CONTRACT_OK`.

남은 문제:
- `GO2_NOW.md`는 65줄로 B1의 60줄 상한을 넘는다. (정정: "시행 전 63줄"은 독립 검증하지 않은 주장이라 철회한다. 파일이 git 미추적이라 이전 판을 대조할 수 없다.) → §7-6에서 해소.
- F3(C2 차순위 오인용)은 C2 본문에 그대로 남아 있다.

### 7-6. 사용자 감사 반영 (2026-09-14)

사용자 감사는 F12 해소와 재판정 반영을 인정하고 공백 3건을 지적했다. 세 건 모두 받아들여 아래처럼 고쳤다.

| 지적 | 조치 |
|---|---|
| 보통: "기대 가중 이득" 산정 방식이 정의되지 않았고, 사용자가 승인한 사실로 취급될 위험이 있다 | 원장 §5 3항에 두 가지를 추가했다: 달성 확률·효과 크기 근거가 없으면 기대 가중 이득은 수치 대신 `미추정`으로 적는다. 산정식은 사용자 승인 사항이 아니며, G-D-PRIORITY-20260914가 정한 것은 비교 항목까지다 |
| 보통: 검사 7은 문자열 몇 개만 봐서 규칙 문장이 지워져도 통과한다 | 원장 §5의 핵심 문구 8개(자동 1순위 부정, 이득·비용·원인 확실성, 낙상 선행조건 금지, 민감도, 미추정, 산정식 미승인)와 루트 AGENTS Go2 조항 2개, test-planner "자동 1순위가 아니다"를 필수로 걸었다. 문구 검사일 뿐이며, 뜻을 바꾼 재서술은 잡지 못한다고 주석에 적었다 |
| 낮음: `GO2_NOW.md`가 65줄로 B1 미충족이다 | 재검토 입력 목록과 조회용 문서 목록을 줄여 60줄 이하로 만들었다. 이제 검사 8이 상한을 강제한다 |

검증 결과:
- Go2 테스트 36개 통과(검사 8 추가).
- `GO2_CAMPAIGN_CONTRACT_OK`.
- `GO2_NOW.md`는 58줄이다.

정정: §7-5 보고의 "고친 8개 파일 모두 한글 무손상"은 틀렸다.
- 이번 변경분에는 `???`가 없다.
- 두 원장에는 이전부터 손상된 줄이 있다.
  - `GO2_REWARD_EVIDENCE_MASTER.md` 419~425행(G-A029 절)
  - `GO2_PROJECT_STATE.md` §11 11줄(HEAD에 이미 있음), 1716~1722행(G-A029 절)
- G-A029 절은 이전 튜닝 담당의 미커밋 작성분이다. 같은 손상문이 `PLANNER_BRIEF.md`와 `GO2_G_A029_DIFFICULTY_SCREENING.md`에도 있다.
- 한글이 `?`로 바뀌어 원문을 복원할 수 없어 손대지 않았다. 복원하거나 재작성할지는 사용자가 결정한다.
- 2026-09-14 정정: 복원하지 않는다는 결정은 시행 계획 A4에서 이미 내려져 있었다. 원장 두 곳의 G-A029 절도 이미 격리돼 있었다. 표시가 없던 나머지 구간은 G-P-A029-REVISION-20260914 R5에서 격리했다.
