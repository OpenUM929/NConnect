# G-A016 검증 기록 — `ang_vel_xy_l2` −0.05 → −0.15 (260904)

## 1. 업로드 대상

| 파일 | 크기 | SHA-256 |
|---|---:|---|
| `go2_tuning_engine_v1_3.zip` | 12,781,997 B | `dfbe47aecb5584ad07583caea726d23a372764b22e12962e0cbd76b268877b1a` |
| `G_A016_pilot_ang_vel_xy_m015.json` | 2,633 B | `0eadb9a7a72dbbaaf3618faf7e07482ba0309054bc3bfaa9fccc149882e11f76` |

엔진은 G-A015에서 실행된 것과 **바이트 동일**하다. 재빌드해도 SHA가 같다(결정론적 빌드,
`FIXED_TIMESTAMP` 고정). 따라서 이번 회차에 새로 검증할 대상은 실험 사양뿐이다.
엔진 자체의 검증 기록은 `go2_tuning_engine_v1_3.VERIFICATION.md`에 있다.

## 2. 실행한 검증

| 항목 | 결과 |
|---|---|
| 사양 스키마 검증 | `{"sha256": "0eadb9a7…e11f76", "status": "VALID", "work_id": "G-A016"}` |
| 배포 ZIP 추출본으로 materialize | `{"status": "MATERIALIZED", "work_id": "G-A016"}` |
| 후보 arm 렌더 결과 | `ang_vel_xy_l2 = -0.15`, 나머지 5개는 Pilot-01 값 그대로 |
| 기준선 arm 렌더 결과 | `ang_vel_xy_l2 = -0.05`, 나머지 5개 동일 → 변경 1개 |
| 기준선 checkpoint | `sha256 c4d78adf…f89af8d` = 동결 Pilot-01 |
| 계약 테스트 | `python -m unittest discover -s tools` → `Ran 50 tests` `OK` |
| 엔진 SHA 재확인(테스트가 재빌드) | `dfbe47ae…877b1a`, 50 멤버 — 업로드본과 동일 |

## 3. 이번에 추가한 계약

- `test_g_a016_is_exactly_one_reward_change` — 단일 변수 불변식.
- `test_g_a016_keeps_feet_air_time_at_the_measured_ceiling` — `feet_air_time`은 양 arm 모두 0.20 고정.
- `test_the_retired_g_a015_spec_still_validates` — 폐기된 사양도 여전히 스키마를 통과한다(회귀 방지).
- `test_measured_g_a015_collapse_is_killed_on_both_clauses` — G-A015의 **실측 7시나리오 수치**를
  그대로 게이트에 재생해 `−30.12/70`과 총점·생존 두 절 동시 발화를 재현한다.

## 4. 증거 경계

`ang_vel_xy_l2`를 −0.15로 강화하면 계단(G5) 생존이 오른다는 것은 **가설**이다. 측정된
사실은 다음 셋뿐이다.

1. Pilot-01 tier-1(seed 101)에서 G5 생존 `0.71875`, G4 `1.0`, G3 `0.8125`이고 69-case
   기준 가중 실점 순위는 G3 `12.97` > G5 `10.50` > G4 `4.95`다.
2. `ang_vel_xy_l2`는 Pilot-01 기준선에서 **한 번도 측정된 적이 없는** 다이얼이며,
   지금까지의 어떤 측정 결과와도 모순되지 않는다.
3. −0.15는 참가자 파일이 명시한 권장 구간 `-0.3 ~ -0.02` 안이다.

가설이 틀렸을 경우의 다음 수는 사양의 `branch.on_tier1_fail`에 실행 전 기록돼 있다.
