# 예선 캠페인 기획서 — H1 단독 100점

> **지위: 이 캠페인의 기획 정본.** 260830 확정.
> **소유자: 메인 루프(Claude Code).** Codex/OMX 배우는 읽기 전용이며,
> 이견은 `CAMPAIGN_SCHEDULE.md` §5 `[Codex 회신]`에 행을 덧붙인다.
>
> **문서 역할 분담 — 중복 기재 금지**
> | 문서 | 담당 | 충돌 시 |
> |---|---|---|
> | **`CAMPAIGN_PLAN.md`** (이 문서) | **기획** — 목표·전제·단계별 기대값·수용기준·리스크 | — |
> | `CAMPAIGN_SCHEDULE.md` | 일정·단계표 state·진행 원장 | 상태값은 SCHEDULE이 이긴다 |
> | `PROJECT_STATE.md` | 사실(F) · 결정(D) 원장 | **사실·결정은 STATE가 이긴다** |
> | `reports/EVIDENCE_INDEX_S0.md` | 의도↔영상 증거 인벤토리 | 증거는 이 문서가 이긴다 |
>
> **동반 갱신 목록 (CLAUDE.md 원칙 10)** — 이 문서를 개정하면 같은 작업에서 점검한다:
> `.codex/agents/prelim-campaign-manager.md` §확정된 캠페인 방향 · `CAMPAIGN_SCHEDULE.md` §3 단계표 ·
> `PROJECT_STATE.md` §방향 결정.

## 1. 목표와 확정 범위

**50시간을 써서 `workspace/training/humanoid`(H1) 하나를 예선 통과 수준까지 만든다.**

| 항목 | 값 | 근거 |
|---|---|---|
| 대상 기체 | **H1 (2족) 단독** | D23 사용자 확정 |
| 만점 | **100점** | 로봇 1대분. 상한이 아니라 **확정된 목표** |
| 제외 | Go2 · `workspace/training/quadruped/` 일체 | D23. 판정 근거로 인용 금지 |
| 제출처 | `robotics.connect.or.kr` 팀 대시보드 | `PRELIM_RL_GUID.md:27` |
| 제출 단위 | 로봇 유형 택1(H1) + `policy.pt` + `env.yaml` + 리포트 | `COURSE_ANALYSIS_260829.md` §2 |

**재제출은 무위험이다.** 규정집 제10조 — 전 라운드 통틀어 **각 로봇의 최고 점수만** 남는다.
더 나쁜 제출을 해도 기존 최고점이 내려가지 않는다 → **정정본·개선본 제출은 순이득이거나 0.**

## 2. 채점 구조와 축 배분

```
시나리오 점수 = survival_rate × tracking_score        (0 ~ 1)
종합 점수     = Σ(시나리오 점수 × 가중치)              (최대 1.0)
```

| 축 | 배점 | 채점 주체 | **우리 통제** | 전략 |
|---|---:|---|---|---|
| 시뮬레이션 | **70** | 운영진 단독. 참가자는 점수를 미리 볼 수 없다(`PRELIM_RL_GUID.md:117`) | **blind** | 측정 근거가 있을 때만 투자 |
| 설계 의도 분석 | **20** | 리포트 ↔ `env.yaml` ↔ 영상 일치 | **직접 검증 가능** | 확정 이득 — 우선 |
| 리포트 품질 | **10** | 설명의 명료·타당성 | **직접 검증 가능** | 확정 이득 — 우선 |

> **핵심 전략 (D26)** — 같은 사람 시간이면 **확정 이득이 blind 이득에 우선한다.**
> 단, 학습은 벽시계 예산이라 사람 시간을 쓰지 않으므로 **둘은 경쟁하지 않고 병렬로 돈다**(D29·L101).

### 평가 시나리오 가중치 (H1)

| # | 시나리오 | 지형 | 가중 | 걸리는 지표 |
|---|---|---|---:|---|
| H1 | 제자리 균형 | 평지 | 0.15 | (zero-command) |
| H2 | 전진 속도 | 평지 | **0.20** | `error_vel_xy` |
| H3 | 좌우 속도 | 평지 | 0.10 | `error_vel_xy` |
| H4 | 회전 복합 | 평지 | 0.10 | **`error_vel_yaw` — 유일** |
| H5 | 약한 요철 2~10cm | 요철 | 0.15 | `error_vel_xy` |
| H6 | 완만한 경사 ±10° | 경사 | 0.15 | `error_vel_xy` |
| H7 | 밀침 회복 | 충격 | 0.15 | (zero-command) |

**유도되는 산술 — 모든 보상 판정의 기준선**

- 비제로 명령 = **0.70** · 직진 계열(`error_vel_xy`) = **0.60** · 회전(`error_vel_yaw`) = **0.10**
- → **선속도 추종이 회전 추종보다 6배 무겁다.**
- 가중 개선도 `W = 0.60 × Δxy% + 0.10 × Δyaw%` — 보상 변경안은 이 식과 대조한 뒤에만 제안한다.

> ⚠️ **평가 지형 ≠ 학습 지형.** 학습은 계단 5~23cm·박스 15~50cm를 포함하지만
> 평가에는 **계단·박스가 없다**(평지 0.70 · 요철 0.15 · 경사 0.15).
> → `terrain_levels`는 **예선 점수축이 아니다**(D5). 수렴 지표로만 쓰고 부작용 게이트로 쓰지 않는다.
> → 학습 로그의 `base_contact`를 예선 `survival_rate`로 인용하지 않는다.

## 3. 전제 — 무너진 것과 살아있는 것

### 무너진 전제 (전부 260830 정정)

| 전제 | 실제 | 영향 |
|---|---|---|
| ~~학습 횟수 = 제출 횟수~~ (F26) | 자동 전송은 **운영 백업**. zip에 `policy.pt`가 없다 | **run 수 제약 소멸.** GPU·사람 시간만이 제약 |
| ~~Go2 병행으로 200점~~ (D11) | H1 전용 확정 | 상한 100. Go2 배우 신설 불요 |
| ~~보상 노브 탐색 계속~~ | 시소 양 끝 측정 완료 | **남은 시뮬 레버 = 수렴 하나**(D24) |
| ~~공식 제출 0회 확정~~ (F44) | 원장은 **우리 쪽 행위**만 확정한다 | 제출 이력은 `[미측정]` |
| ~~장기 학습은 마감일 대기~~ (D28) | **제출 요건이다.** 벽시계 작업을 사람 시간으로 막았다 | **즉시 착수**(D29) |

### 살아있는 전제 (실측 확정)

| ID | 사실 |
|---|---|
| 단가 | **0.725 s/iter** — 3000=36분 · 10000=2h01m · **15000=3h01m** |
| F20 | 3000 iter에서 정체 없음 → 장기 학습 유효 |
| F40 | Run 05는 3000에서 **미수렴** — `error_vel_yaw` −0.1063/1k 하강 중 · `terrain_levels` 5.5646/9.0 |
| F41 | `_finalize.py`가 best 외 `model_*.pt` **전량 삭제**. `--no-cleanup`은 도달 불가 |
| F42 | best 선정 = `Train/mean_reward` **argmax**(잡음 계열). Run 05의 최대값은 +3.04σ |
| F43 | policy export는 **바이트 비결정적**. 정책 동일성 판정에 sha256을 쓰면 안 된다 |
| F25·D13 | 서버는 세션마다 초기화. 모든 실행안의 1번은 `bash _bootstrap/restore.sh` |
| — | `--resume`은 **지형 커리큘럼 레벨을 복원하지 못한다** → from scratch |

## 4. 현재 단계 기획

### 4-a. 등급 규율 (D30)

`필수` = 제출 요건 · `개선` = 점수 상향 · `조사` = 정보 회수.
**`필수` 행은 `blocked` 상태를 가질 수 없다** — 막히면 일정 조정이 아니라 **제출 불가**이므로
`▲ 제출 불가 — 즉시 해소 대상`으로 격상한다.

### 4-b. 병렬 3레인

세 레인은 **서로를 막지 않는다.** 벽시계 레인과 사람 시간 레인이 분리돼 있기 때문이다.

```
[GPU 레인]   B2 학습 15000 iter (3h01m) ──┬── B3 체크포인트 구출(동시)
                                          └── C1 재촬영 → C2 리포트 확정 → C3 재제출
[문서 레인]  A1 리포트 정정 7건 ── A2 증거 인벤토리(done) ── A3 중간 재제출
[조사 레인]  B1 마감일 · U2 제출이력
```

| # | 등급 | 단계 | 소유자 | 비용 | 산출물 |
|---|---|---|---|---|---|
| **B2** | 필수 | 최종 장기 학습 15000 iter | 사용자 | GPU 3h01m | `logs/.../events.*` · `model_best.pt` · `env.yaml` |
| **B3** | 필수 | 중간 체크포인트 구출 | 사용자 | 0분 | `_keep/ckpt/model_*.pt` |
| **A1** | 개선 | 리포트 정정 7건 | `humanoid-report-writer` | 0분 | `TECH_REPORT_H1.md` · `experiment_history.csv` |
| A2 | 개선 | 증거 인벤토리 | 메인 루프 | 0분 | `reports/EVIDENCE_INDEX_S0.md` **(done)** |
| A3 | 개선 | 정정본 중간 재제출 | 사용자 | 0분 | 대시보드 기록 |
| **B1** | 조사 | 마감일(공백 C) | 사용자 | 0분 | 「10000 vs 15000」과 C단계 일정의 입력 |
| U2 | 조사 | 제출 이력 | 사용자 | 0분 | 로봇 유형·횟수·시각 |
| C1 | 필수 | 최종 정책 7시나리오 재촬영 **+ 명령 override 기록** | 사용자 | 0분 | mp4 7종 + log |
| C2 | 필수 | 리포트 최종 확정 | `humanoid-report-writer` | 0분 | `TECH_REPORT_H1.md` v2 |
| C3 | 필수 | 최종 재제출 | 사용자 | 0분 | 대시보드 기록 |

## 5. B2 사전 등록 — 수용기준 (원칙 12)

> **이 절은 학습 결과를 보기 전에 작성됐다.** Run 05는 `사전 등록 없음 — 사후 기록`으로
> 절차 위반이 기록됐고(`experiment_history.csv`), 그 결과 판정 기준을 결과에 맞춰 만들었다.
> **원칙 12-a: 수용기준의 타당성은 실행 레인의 판단 대상이 아니다.** 만족 불가능한 기준을
> 발견하면 우회하지 말고 결정요청으로 올린다. **원칙 12-c: 이 절의 수정은 two-key다** —
> 메인 루프 단독으로 성립하지 않으며, 수정되면 그 자로 내린 판정은 전부 stale이다.

### 5-a. 변경 내용

| 항목 | Run 05 | **B2 (Run 06)** |
|---|---|---|
| 변경 변수 | — | **`max_iterations` 3000 → 15000** |
| 보상 9개 | Run 05 설정 | **전부 동일** (`restore.sh` check 통과분) |
| `--resume` | — | **없음 — from scratch** |
| `num_envs` / seed | 4096 / 42 | 동일 |

**단일 변수다.** 보상을 건드리지 않으므로 "이 결과가 수렴 때문인가 보상 때문인가"가 교락되지 않는다.

### 5-b. 이 실험이 답하는 질문

**"Run 05의 회전 열위(statue 0.94배)는 보상 설정의 최종 성격인가, 학습 중간 상태인가?"**

F40이 근거다 — `error_vel_yaw`가 −0.1063/1k로 **아직 하강 중**이고 `terrain_levels`가 5.5646/9.0으로
**미포화**다. 지금 노브를 고르면 후자를 전자로 오진한다.

### 5-c. 측정 방법 — 고정

- **구간:** 마지막 500 iter 평균 = **iter 14500~14999**.
  Run 05를 iter 2500~2999 평균으로 잰 것과 **같은 추정량**이다.
- **정규화 의무:** `error_vel_*`는 원값 단독으로 판정하지 않는다.
  **`/step = 원값 / mean_episode_length × 500`** (`max_command_step = 10.0s / 0.02s`).
  이유 — 이 지표는 매 step `|Δ|/500`을 누적해 episode 종료 시 로그하므로 **오래 사는 run일수록 크게 찍힌다.**
  장기 학습은 생존이 늘어 `ep_len`이 커질 것이므로, **원값 비교는 개선을 악화로 뒤집는다.**
- **측정 명령** (PC · `workspace/training/humanoid/`):
  ```
  python ../../../tools/tfcurve.py window Run05 Run06
  ```
  ※ `tools/tfcurve.py`의 `RUNS`에 `"Run06": "<로그 디렉터리 타임스탬프>"` 등록 선행.

### 5-d. 기준선 — Run 05 (iter 2500~2999 평균, 실측)

| 지표 | 원값 | **/step** | statue 대비 |
|---|---:|---:|---:|
| `error_vel_xy` | 0.3385 | **0.2219** | 0.31x |
| `error_vel_yaw` | 0.9613 | **0.6303** | **0.94x — 경계선** |
| `mean_episode_length` | 762.61 | — | — |
| `base_contact` | 0.3020 | — | — |
| `Policy/mean_std` | 1.6641 | — | — |
| `terrain_levels` | 5.5646 | — | (9.0 만점) |

statue(명령 무시하고 서 있기) 기준선: **`xy 0.7165` · `yaw 0.6681`**.
추종이 이 값보다 나쁘면 **명령을 따르는 것보다 서 있는 편이 낫다**는 뜻이다.

### 5-e. 주 판정 — 가중 개선도

예선 가중을 그대로 쓴다. `Δ% = (Run05 − B2) / Run05 × 100` (양수 = 개선), **전부 /step 값**.

```
W = 0.60 × Δxy%  +  0.10 × Δyaw%
```

| 판정 | 조건 | 의미 |
|---|---|---|
| **성공** | `W ≥ +5.0` | 수렴이 실제 이득을 냈다. 이 정책을 제출 후보로 확정 |
| **부분** | `0 < W < +5.0` | 이득은 있으나 작다. 15000이 상한인지 재검토 |
| **실패** | `W ≤ 0` | 3000에서 이미 실질 수렴. **학습량은 더 이상 레버가 아니다** |

> **선형 외삽을 쓰지 않는 이유** — F40의 −0.1063/1k를 12k 구간에 곧이곧대로 적용하면
> `0.9613 − 1.2756 = −0.314`로 **음수**가 된다. 오차 지표는 0 아래로 갈 수 없으므로
> 이 기울기는 단기 국소값이다. **장기 목표치를 외삽으로 만들지 않는다.**
> 대신 절대 기준(statue 배율)과 상대 기준(가중 개선도)을 함께 쓴다.

### 5-f. 부작용 게이트 — 4종 전부 통과해야 「성공」 인정

하나라도 걸리면 주 판정이 성공이어도 **`조건부`** 로 강등하고 원인 분석을 선행한다.

| # | 지표 | 기준 | 근거 |
|---|---|---|---|
| G1 | `mean_episode_length` | **≥ 762.61** | 생존 시간 감소 금지. 줄면 정규화 이득이 허상 |
| G2 | `base_contact` | **≤ 0.3200** | Run 05 0.3020 + 여유 6%. **예선 survival이 아니라 학습 지형 낙상률**임에 유의 |
| G3 | `Policy/mean_std` | **≤ 1.7500** | Run 05 1.6641. 탐색이 안 닫히면 미수렴 신호 |
| G4 | `error_vel_xy` /step | **≤ 0.2219** | 직진 계열 0.60이 걸린 축. **악화 불가** |

**수렴 지표 (판정 아님 — 기록만):** `terrain_levels`.
D5에 따라 예선 점수축이 아니므로 **FAIL 근거로 쓰지 않는다.** 5.5646 → 어디까지 가는지만 남긴다.

### 5-g. 절대 기준 — statue 배율 (참고 판정)

| 축 | Run 05 | **B2 목표** |
|---|---:|---|
| `xy` /statue | 0.31x | 유지 또는 개선 |
| `yaw` /statue | **0.94x** | **≤ 0.75x** — 회전축이 statue를 의미있게 이기는 선. Run 04가 0.63x로 도달한 바 있어 **달성 가능한 값**이다 |

### 5-h. 제출 체크포인트 선정 — 사전 규정

**F42: `model_best.pt`는 `Train/mean_reward` argmax로 뽑히며 이는 잡음 계열이다.**
따라서 다음을 **모두 확보한 뒤** 별도 판정한다. 자동 선정을 그대로 받지 않는다.

- [ ] `model_best.pt` (finalize 선정) — `torch.load(...)['iter']`로 실제 iter 기록
- [ ] 최종 iter 근처 체크포인트 (`_keep/ckpt/model_15000.pt` 또는 최근접)
- [ ] 두 정책의 `iter` + `model_best.pt` sha256 기록 (**`policy.pt` 해시는 식별자로 쓰지 않는다** — F43)

## 6. 조정 지점 — 학습 중 판정

**첫 판정은 36분 뒤다.** 3시간을 다 기다리지 않는다.

| 시점 | 경과 | 보는 것 | 이 값이 주는 것 |
|---|---|---|---|
| **iter 3000** | 36분 | Run 05의 iter 3000과 직접 대조 | **run 간 재현 잡음 하한**(D22). `schedule: adaptive`라 `max_iterations`는 앞 3000 궤적에 개입하지 않는다 → **같은 설정 두 run의 차이 = 순수 잡음.** 이 값보다 작은 과거 run 간 차이는 전부 무의미했다는 뜻이다 |
| **iter 5000** | 60분 | `error_vel_yaw` /step 하강 지속 여부 | F40 가설의 1차 검증. 여기서 평평하면 15000까지 갈 이유가 약해진다 |
| **iter 10000** | 2h01m | `terrain_levels` 포화 · G1~G4 | 가이드 최소선(10000) 도달. **이 시점 체크포인트는 그 자체로 제출 가능**하다 |
| **iter 15000** | 3h01m | §5-e 주 판정 + §5-f 게이트 | 최종 |

**중단 조건** — 아래 중 하나라도 관측되면 즉시 보고하고 판정을 다시 받는다.
- `base_contact`가 0.3500을 넘어 상승 추세
- `mean_episode_length`가 700 아래로 하락
- `Policy/mean_std`가 상승 전환 (탐색 발산)

## 7. 리스크

| 리스크 | 확률 | 영향 | 대응 |
|---|---|---|---|
| **세션 초기화로 3시간 소실**(F25) | 중 | 큼 | B3 구출기가 10분마다 `_keep/ckpt/`로 복사. **추가로 iter 10000 시점에 PC로 1회 내려받는다** |
| `finalize`가 중간본 전량 삭제(F41) | **확실** | 중 | B3 구출기 — 선택이 아니라 필수(D21) |
| `model_best.pt`가 잡음 첨두(F42) | 중 | 중 | §5-h — 두 후보 확보 후 별도 판정 |
| 원값 비교로 개선을 악화로 오독(D6) | **높음** | 큼 | §5-c 정규화 의무. `ep_len` 병기 없는 표는 미완성 |
| 마감일이 촉박(공백 C) | [미측정] | 중 | iter 10000 체크포인트가 이미 제출 가능. **15000을 못 채워도 제출은 성립한다** |
| B2가 실패 판정(`W ≤ 0`) | 중 | 중 | 학습량이 레버가 아님이 확정될 뿐 손실 아님. 30점 축은 병렬로 이미 진행됨 |

## 8. 알려진 결손 — 추측으로 메우지 않는다

| ID | 결손 | 영향 | 회수 |
|---|---|---|---|
| **강좌 11강** | 「로봇 학습의 목표」가 로컬 19건에 **없다** | **"몇 점이면 통과인가"를 이 캠페인은 모른다** | 강좌 사이트 |
| **공백 C** | 예선 마감일 | C단계 일정 · 「10000 vs 15000」 | 대시보드 |
| **U2** | H1 제출 이력 | 신규 제출/재제출 게이트 선택 | 대시보드 |
| **공백 F** | "50시간"이 GPU 벽시계인지 사람 시간인지 | **잔여율 계산 불가**(D9). 비용은 절대량으로만 말한다 | 사용자 |
| **공백 G** | 남은 사람 시간 | 축 배분의 분모 | 사용자 |
| **통과 커트라인** | 미공개 | **영구 `[미측정]`** | 회수 불가 |
| **S0 명령 미기록** | S0 로그에 override·지형 덤프가 없다 | 「평가 조건」이 로그로 재현되지 않는다 | **C1에서 해소** |

**내릴 수 있는 판정 / 없는 판정**
- 가능 — "이 축이 미확보다" · "이 변경이 가중 산술상 유리/불리하다" · "이 게이트가 열렸다"
- 불가 — "통과 가능하다" · "몇 점이다" · "이 정도면 충분하다"

## 9. 폐기된 계획 — S0~S3 매핑

원칙 3(append-only)에 따라 이전 계획을 지우지 않고 대응 관계를 남긴다.

| 구 단계 | 내용 | 현재 |
|---|---|---|
| S0 | 예선 7시나리오 근사 진단 play | **완료** → `reports/S0b/` `S0c/` `S0d/` · A2 인벤토리 |
| S1 | 기술 개선 리포트 초안 (+ ~~Go2 병행~~) | **완료**(초안) → A1이 정정 중. Go2는 D23 폐기 |
| S2 | 결함 축 1개 단일 변경 (36분 × 2~3 run) | **종료** — D24. 시소 양 끝 측정 완료, 잔여 노브는 tracking 무관 |
| S3 | 최종 장기 학습 (2~3시간) | **= 현재 B2.** ~~"S2 완료 후"~~ 차단 조건은 D24가 해소했고, D28이 새로 붙인 마감일 의존은 **D29로 철회** |

> **S3가 계획에서 사라졌던 경로 (260830 근본원인 분석 · D31)**
>
> S3의 원래 차단 조건은 *"보상 배분이 정리될 것"*(`PROJECT_STATE.md:748-749`)이었고 정당했다.
> **D24가 그 조건을 해소한 순간 S3는 즉시 실행 가능해야 했다.** 그런데 메인 루프가 A/B/C로
> 재작성하면서 `차단 조건 = 마감일`을 새로 붙였다. 출처는 `PROJECT_STATE.md:871`인데,
> 원문이 막은 것은 **① iter 상한 ② 공백 F**였고 이를 **① 시작 자체 ② 공백 C**로 두 번 변형했다.
> 그 변형된 전제가 팀장 판정 요청 Q3의 **선지에 들어가** D28을 낳았다.
> → 재발 방지: `AGENTS.md` 「계획 우선」 5·6·7항 · D30 등급 열 · D31 선지 대조 의무.
