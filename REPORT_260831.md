# 예선 준비 현황·기획자 제안 판정 보고서 — 260831

## 0. 예선 기준 현재 위치

- [예선 목표] 시뮬레이션 70점 후보의 H1~H7 실제 행동과 설계 의도 20점·리포트 10점의 영상 증거를 확보한다.
- [현재 단계] **단계 1/6 — H4 정량 기본 행동 게이트.** H4 yaw 추종량이 가장 이른 미완료 증거다.
- [확보] Run06 10,000 iter·model_9900·학습/영상 artifact 정합, 사전등록 수치 게이트 5/5 PASS, H1~H3·H5 시각 PASS.
- [미확보] H4 양방향 yaw 추종량, H6 tracking·termination, H7 밀침 회복시간, 독립 seed.
- [이번 테스트] 추가 학습 없이 Run06 고정 정책을 32환경에서 H4·H6·H7 계측한다.
- [흐름] Run06 학습·영상·곡선 분석 완료 → **고정 evaluator 실행** → 정량 판정 → env·리포트·policy 일치 → 제출.
- [지금 할 일] 서버를 켜지 않고 `run06_fixed_eval_package.zip`의 실행 지시를 기다린다.
- [보장하지 않음] 영상 1세트나 단일 seed만으로 공식 survival_rate, 최적 reward, 예선 점수 또는 통과 가능성을 보장하지 않는다.

## 1. 대회 개요 — 로컬 가이드 정본

> **260831 최신 운영 판정:** Run06은 약 30분이 남은 시점에서 중단하지 않고 10,000 iter를 자연 완료한다. 완료 뒤 `*_DOWNLOAD.tar.gz`와 전체 `training` 보험 snapshot을 회수하되, 로컬 정본에는 통째로 덮어쓰지 않고 `workspace/server_returns/<RUN_ID>/`에서 검증 후 선택 병합한다.
>
> **운영 정본:** 이후 모든 서버 작업은 `ARTIFACT_MANAGEMENT.md`에 작업 ID를 먼저 등록한다. 이 문서가 파일 목록, 회수·검증·병합 lifecycle, 작업 내역의 단일 진입점이며 현재 Run06은 `A260831-02 RUNNING`, 결과 회수는 `A260831-03 결과 대기`다.

- 제출물은 `policy.pt`, `env.yaml`, 기술 개선 리포트 3종이다(`workspace/PRELIM_RL_GUID.md:21-28`).
- H1은 H1~H7 격리 시나리오에서 평가되며 가중 합은 1.0이다(`:43-58`).
- 공식 시나리오 점수는 `survival_rate × tracking_score`, 시뮬레이션 70점·설계 의도 20점·리포트 품질 10점이다(`:92-104`).
- 참가자는 운영진 평가 전 공식 점수를 확인할 수 없으므로, 로컬에서는 고정 조건 play와 artifact·리포트 일치로 준비도를 판정한다(`:117-119`).
- 짧은 실험은 1000~5000 iter, 최종본만 10000~15000 iter가 가이드 흐름이다(`:168-194`).

## 2. 현재 확보와 공백

| 영역 | 현재 근거 | 판정 |
|---|---|---|
| Run 이력 | Run 01~05 로그·tfevents·CSV | 확보 |
| Run 05 artifact | checkpoint iter 2700 + `policy.pt` 가중치 일치, env 정합 | 확보 |
| H1~H4 | 고정 명령 영상에서 직립·방향 행동 관찰 | 기본 게이트 완료 |
| H5 | 요철 영상 있으나 실제 요철 노출 약 44% | 미완료 |
| H6 | ±10° 지형 노출은 확인, 오르막/내리막 방향 격리 안 됨 | 미완료 |
| H7 | 단일 밀침 표본, 회복시간 미측정 | 미완료 |
| 계단·양발 보행 | 계단 증거 0건, 한 발 깡총 여부 명시 판정 없음 | 미완료 |
| 장기 학습 | 현재 서버 tmux/process/Run06·07 log 없음 | **현재 세션 미실행** |
| 리포트 | `TECH_REPORT_H1.md` v1 존재, A1 정정 7건 미완 | 미완료 |
| 제출 | 원장상 업로드 지시 0건 | 실제 여부 [미측정] |

## 3. 팀 교차검증 결과

| 관점 | 판정 | 핵심 |
|---|---|---|
| 분석가 | **부분 채택** | “시소 양 끝”은 오류. survival 누락 지적은 타당하나 `base_contact`는 공식 survival이 아님 |
| 기획 검토 | **조건부 승인** | `lin=2.0`은 screening 후보이나 control·반복·격리 평가 없이 장기 승급 불가 |
| 스케줄러 | **현재 단계 3/6** | 단계 4는 원장상 선행 착수됐지만 로컬 artifact가 없고 단계 3 제출 게이트가 미완 |
| 메인 팀장 | **D32** | D24 부분 철회. 제시 명령 그대로는 실행 보류, 기존 run 상태·artifact 확인이 먼저 |

## 4. 기획자 주장별 판정

### 4-1. “시소 양 끝을 측정했다”

**오류로 채택한다.** Run 04/05는 `lin=0.5/1.0`, `ang=1.0`이고 코드 추천 lin 범위는 `0.5~2.5`다. 두 점은 파라미터 범위의 양 끝이 아니다.

다만 `0.5→1.0`에서 정규화 xy 오차가 19.73% 개선되고 yaw가 50.64% 악화해, 관측된 결과가 직진↔회전 tradeoff의 서로 다른 쪽에 놓였다는 좁은 비유는 성립한다. `0.60×19.73−0.10×50.64=+6.78`도 재현됐다.

**한계:** +6.78은 0.5보다 1.0이 우세했다는 방향성 proxy다. 1.0→2.0에서도 단조 개선된다는 증거는 아니다.

### 4-2. “survival을 빠뜨렸다”

**부분 채택한다.** 공식식이 곱이므로 tracking만으로 잔여 노브를 배제한 D24는 불완전했다. `flat_orientation_l2`, `termination_penalty`, `feet_air_time`은 안정·낙상에 영향을 줄 가능성이 있다.

그러나 현재 `base_contact≈0.302`는 계단·박스를 포함한 혼합 학습지형 termination 집계다. 공식 H1~H7 survival이 아니다. “survival 10% 개선이 전 시나리오에 곱해진다”는 모든 시나리오에서 survival이 균일 개선되고 tracking이 불변이라는 조건에서만 맞다.

### 4-3. “가이드 2~4를 건너뛰었다”

**일부 오류다.** `feet_air_time=0.75`, `flat_orientation_l2=-0.3`은 미실행이지만 `termination_penalty`는 Run 02·03에서 -5→-50→-90으로 이미 시험했다. 또한 0.75는 코드 주석상 한 발 깡총 위험을 체험·확인하는 값이지 자동 채택값이 아니다.

## 5. 제안된 36분 Run 판정

**판정: 현재 명령 그대로는 실행 보류. 수정 후 screening으로만 조건부 승인.**

차단 사유:

1. 동일 설정 control 재실행이 없어 `lin=2.0` 효과와 run noise를 분리하지 못한다.
2. `W≥3`은 근거 없이 기존 사전등록 `W≥5`를 바꿨고, `0<W<3` 분기가 없다.
3. W는 survival을 포함하지 않고 공식 tracking_score 계산식도 아니다.
4. 단일 실패는 2.0 미채택만 뜻하며 1.5·2.5 또는 비단조 반응까지 “lin 축 소진”으로 만들 수 없다.
5. 제시 명령은 restore 후 백업하므로 restore 직전 artifact를 잃는다. 고정 `_keep` 파일명도 재실행 시 덮어쓴다.
6. 기존 B2가 실제 실행 중이면 `tmux new -As train`은 동시 run·finalize 최신 run 선택 충돌을 만들 수 있다.
7. 학습만으로 `policy.pt`는 새 정책으로 갱신되지 않아 checkpoint/env/policy 불일치가 생긴다.
8. 과거 무효 `train_260830-06` 식별자가 있어 새 RUN_ID가 필요하다.

조건부 승인 요건:

- 활성 학습 없음 확인
- 새 RUN_ID
- restore 전 timestamp 백업
- `NO_AUTO_SUBMIT=1`
- 고유 tmux 세션
- 같은 조건 `lin=1.0` control + `lin=2.0` treatment
- 최소한 동일 window의 tfevents와 checkpoint 보존
- H1~H7 고정 suite에서 scenario별 완주/termination과 tracking을 분리 기록
- `W≤0`은 2.0 기각, `0<W<임계`는 INCONCLUSIVE, 임계 이상이어도 survival 비열등성 통과 후에만 장기 후보

## 6. 표준 단계 모니터링

| 단계 | 상태 | 통과 증거 | 실패 시 분기 |
|---:|---|---|---|
| **0 artifact 정합성** | **완료(로컬 Run 05 정본)** | iter 2700 + policy weight match + env 일치 | 매 세션 스크립트가 restore·hash 재검사 |
| 1 기본 행동 | 완료 | H1~H4 영상 | 명령/정책 정합 재검증 |
| 2 짧은 pilot | 완료 | Run 01~05 | 새 pilot은 D32 게이트 |
| 3 환경 적응 | 완료(근사 행동) | H5/H6 1 episode 무낙상·H7 반복 밀침 무낙상 | 정량 한계는 단계 5로 이관 |
| **4 장기 학습** | **현재·패키지 준비 완료** | 10k/15k artifact | 서버 업로드 후 Run 06 실행 |
| 5 최종 평가·문서 | 대기 | 최종 H1~H7·env·리포트 일치 | 수치·영상 재촬영 |
| 6 제출 | [미측정] | 대시보드 업로드 기록 | 누락 artifact 즉시 해소 |

## 7. 역할과 산출물 인계

- **사용자:** 서버 명령 실행, 결과 다운로드, `workspace/training` 업데이트
- **메인 팀장:** 실행 전 판정, 안전한 단일 명령 확정, 로컬 artifact 검증, 원장 갱신
- **스케줄러:** 단계·등급·병렬 레인·제출 게이트 모니터링
- **기획자:** 단일변수 가설·control·사전 기준·명령 작성
- **분석가/보고서 작성자:** 실행 후 예측↔실제 비교, Evidence/Inference/Unknown 구분, 리포트 갱신

모든 회수 묶음은 `train log`, `events`, `params/env.yaml`, `model_best.pt`, 보존 checkpoint, `policy.pt`(play 후), H1~H7 영상, RUN_ID·iter·checkpoint sha256을 포함한다. 정책 동일성은 `policy.pt` 파일 해시가 아니라 가중치 대조로 판정한다.

## 8. 지금 실행할 서버 확인 명령 — 읽기 전용

```bash
cd /workspace/training/humanoid || exit 1
echo '=== tmux ==='; tmux ls 2>&1 || true
echo '=== train processes ==='; pgrep -af 'train.py|isaaclab.sh' || true
echo '=== keep logs ==='; ls -lh /workspace/_keep/train_260830-0{6,7}.log 2>&1 || true
echo '=== run07 tail ==='; tail -n 40 /workspace/_keep/train_260830-07.log 2>&1 || true
echo '=== rewards (read-only) ==='; grep -n 'track_lin_vel_xy_exp\|track_ang_vel_z_exp' humanoid_rewards.py
```

이 출력은 새 학습 승인이 아니라 **현재 외부 상태 회수**다. 활성 B2가 확인되면 새 pilot을 시작하지 않고 checkpoint·events·env부터 보존한다.

### 260831 서버 회수 결과

`tmux` 없음, 학습 process 없음, Run 06/07 log 없음, reward는 `lin=0.5`, `ang=0.5`였다.
따라서 현재 세션은 B2 실행 중이 아니라 **Run 05조차 복원되지 않은 초기 상태**다(F45·D33).

사용자 재확인(F46): 서버는 접속할 때마다 초기화되므로 이는 정상적인 새 세션 상태다. 캠페인 진척은
로컬 `C:\dev\Nconnect\workspace\training` artifact가 보존하며, 서버는 실행 후 반드시 bundle을 내려받아야 한다.

로컬 재인벤토리(F47): H1 기존 데이터는 190파일·466,878,876B이며 tfevents 5, `.pt` 34,
영상 37, 로그 22를 포함한다. 이후 실험은 이 자료로 답할 수 없는 항목만 편성한다(D35).

## 9. 다음 실행 — Run 05 복원

```bash
cd /workspace/training/humanoid || exit 1
STAMP=$(date +%Y%m%d_%H%M%S)
KEEP=/workspace/_keep/${STAMP}_pre_restore
mkdir -p "$KEEP"
for f in humanoid_rewards.py exported/model_best.pt exported/env.yaml exported/policy.pt; do
  [ -f "$f" ] && cp -a "$f" "$KEEP"/
done
sha256sum "$KEEP"/* 2>/dev/null | tee "$KEEP/SHA256SUMS.txt" || true
bash _bootstrap/restore.sh
echo '=== restored rewards ==='
grep -n 'track_lin_vel_xy_exp\|track_ang_vel_z_exp\|feet_air_time\|termination_penalty\|flat_orientation_l2' humanoid_rewards.py
echo "PRE_RESTORE_BACKUP=$KEEP"
```

`restore.sh`의 모든 `[OK]`와 마지막 `[DONE]`이 필요하다. 이 복원은 `model_best.pt`, `env.yaml`,
reward 코드를 Run 05로 맞추지만 `policy.pt`는 복원하지 않는다. 따라서 복원 직후 `policy.pt`는
제출 artifact로 사용하지 않고, 선택 checkpoint를 `play.py`로 export한 뒤 가중치 정합을 다시 검사한다.

## 10. 최신 분석 결론 — Run06 후보 승급 (260831)

Run06은 reward 튜닝 run이 아니라 Run05 reward를 고정한 10,000 iter 학습시간 실험이다.
마지막 500 iter 비교에서 xy 오차 9.71%, yaw 오차 16.61%, base_contact 14.76%, mean_std 4.39%가
개선됐고 episode length는 7.25% 증가했다. `W=+7.487`과 비열등성 게이트를 모두 통과했다.

따라서 `model_9900.pt`를 고정 evaluator 후보로 승급한다. 그러나 단일 seed이며 영상은 H4 yaw,
H6 경사, H7 밀침 회복을 정량하지 못한다. 다음 작업은 reward 수정이나 재학습이 아니라
`run06_fixed_eval_package.zip`을 이용한 H4·H6·H7 telemetry 회수다.

관련 과학적 한계는 PPO 자체가 reward 합을 공식 점수의 선형 레버로 보장하지 않는다는 점과,
단일 seed 비교가 일반화를 확정하지 못한다는 점이다. 근거: PPO 논문
https://arxiv.org/abs/1707.06347, Henderson et al.
https://arxiv.org/abs/1709.06560, Isaac Lab termination/command manager API
https://isaac-sim.github.io/IsaacLab/main/source/api/lab/isaaclab.managers.html.
