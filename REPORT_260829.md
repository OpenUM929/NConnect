---
doc: 예선 준비 현황 보고서 (H1 휴머노이드)
date: 260829
owner: main loop (Claude Code) — 배타 작성자
sources: PROJECT_STATE.md · reports/experiment_history.csv · reports/S0_260829/train_260829-05.log · reports/S0b/*
status: 사용자 보고용
---

# 예선 준비 현황 보고서 — 260829

> 이 문서는 **메인 루프가 실측한 값만** 담는다. 재계산 근거는 각 절 하단에 명시했다.
> 배우 소유 문서(`reports/PLANNER_BRIEF.md` · `reports/train_260829-05_add.md`)는 인용만 하고 수정하지 않았다.

---

## 0. 한 장 요약 (260830 갱신)

> 이 절은 **최신 상태로 유지**한다. 260829 초판의 값은 아래 「갱신 이력」에 남긴다 —
> 지운 것이 아니라 옮긴 것이다.

| 항목 | 현재 |
|---|---|
| 학습 run | **5회** (Run 01~05), 전부 3000 iter |
| GPU 벽시계 누적 | **약 3.0시간** (0.725 s/iter × 3000 × 5) |
| 채점 시나리오 커버리지 | **1.00 / 1.00** — 7종 전부 1회씩 육안 확인 (§2-1) |
| 튜닝된 보상 노브 | **9개 중 3개** (6개는 배포 기본값 그대로) |
| 낙상률(학습 지형) | **30.2%** — Run 02 이후 4개 run 동안 변화 없음 |
| 작업 기준선 | **Run 05** (예선 우세는 미확정 — D18 ⚠️정정) |
| 다음 학습 | **보상 불변 · 5000 iter · from scratch** (D20) — 노브 변경 아님 |
| 제출 | **0건** (Run 05는 `NO_AUTO_SUBMIT=1`로 차단) |
| 리포트(10점 축) | **미작성** |
| Go2 (합산 100점 축) | **run 0회 · 담당 배우 0종** |

**가장 중요한 발견 (260830)**: 5 run 전부의 **학습 곡선 원본이 PC에 있었다**
(`events.out.tfevents.*` × 5, 각 6.0 MB, 31 태그 × 3000 iter). `play` 로그에 수치가 없다는
사실(F28)이 「수치가 없다」로 잘못 일반화돼 있었다. 로컬 파서(`tools/tfcurve.py`)로 읽으니
**Run 05는 3000 iter에서 절반만 수렴한 상태**였다 — 낙상률·에피소드 길이·탐색 std는 평평한데
`error_vel_yaw`는 여전히 **−0.106/1k로 내려가는 중**이고 지형 커리큘럼은 **5.56/9.0으로
미포화**다. 그래서 다음 학습은 **보상을 바꾸지 않고 5000 iter를 돈다**(D20). 근거는 §4-E.

**갱신 이력**

- **260830** — 커버리지 `0.80 → 1.00`(S0c-1 완료, §2-1 ⚠️블록) · Run 05 교환 폭
  `직진+6%/회전−27% → 직진 오차 −21.2%/회전 오차 +47.9%`(마지막 500 iter 평균으로 재측정, §4-E) ·
  기준선 표기를 「확정」에서 「작업 기준선」으로 하향 · 다음 학습을 노브 변경에서 5000 iter로 교체.
- **260829** — `PROJECT_STATE.md` F29 표의 "Run 04" 열은 실제로는 **Run 02의 값**이었다
  (낙상률 29.8% · 보상 3.84). 내가 잘못 옮겨 적었다. 정정표는 §4-D.

---

## 1. 예선 준비 과정 (시간순)

### 1단계 — 규칙 회수 (260829 오전)
강좌 HTML 19건에서 본문 301,702자를 추출하고 `PRELIM_RL_GUID.md`에서 채점 공식을 확보했다.

- `시나리오 점수 = survival_rate × tracking_score`, `종합 = Σ(시나리오 × 가중치)` (F5)
- 로봇당 100점 = **시뮬 70 + 설계의도분석 20 + 리포트품질 10** (F6)
- **H1 + Go2 합산 최대 200점. 한 기체만 내면 100점 상한** (F7)
- 제출 3종 = `policy.pt` + `env.yaml` + 기술 개선 리포트 (F8)

여기서 확정된 사실 하나가 이후 모든 판단을 바꿨다 — **시뮬 70점은 전체의 70%가 아니라
한 기체 안에서의 70%**다. 두 기체를 내면 시뮬 축은 140점이 되고, 한 기체만 내면 리포트를
완벽하게 써도 100점이 천장이다.

### 2단계 — 4 run 이력의 재검증 (260829)
`experiment_history.csv`에 기록된 Run 01~04를 tfevents 원본으로 되짚었다. 여기서 **기록된
추세 자체가 틀렸다**는 것이 드러났다.

- **F18** — `error_vel_*` metric은 생존 시간에 오염돼 있다. IsaacLab은 매 step
  `|Δ|/max_command_step`을 누적해 reset 때 로그하므로, **일찍 죽은 run은 오차가 작게 찍힌다.**
  Run 01은 332.6 step만 살았고, 그래서 오차가 실제보다 좋아 보였다.
- **F19** — 그 착시가 `PLANNER_BRIEF.md`의 "4 run 연속 단조 악화"라는 서술을 만들었고,
  그 서술이 다음 실험의 근거였다. 보정하면 Run 01→02는 **57% 개선**이다.

### 3단계 — 측정 대상이 잘못돼 있었다는 발견 (F22 · F24)
가이드 §2가 H1 학습 지형표에 **"평가 시나리오 아님 — 등반은 여기서 배웁니다"** 라고
명시하고 있었다. 학습 지형과 예선 평가 지형은 별개다.

| | 학습 지형 (커리큘럼 0~9) | 예선 평가 7종 |
|---|---|---|
| 계단 5~23cm | 33% | **없음** |
| 박스 단차 15~50cm | 33% | **없음** |
| 경사 | 0~22° | H6 ±10° (0.15) |
| 요철 | 2~10cm, 11% | H5 2~10cm (0.15) |
| 평지 | — | **H1·H2·H3·H4·H7 = 가중 0.70** |

→ 4 run 동안 우리가 본 수치는 전부 **커리큘럼 지형 학습 통계**였고, 예선 7 시나리오 중
**측정된 것은 0개**였다(F24). 낙상률 31%도 계단·박스가 섞인 난이도 5.4 지형의 값이지
예선 `survival_rate`가 아니다.

### 4단계 — 방향 전환: 학습을 멈추고 측정을 먼저 (D8)
1회 실험 비용이 36분으로 확정된 반면 점수축 커버리지는 0.15/1.00이었다. 병목은 학습량이
아니라 **어느 축이 고장났는지 모른다는 것**이었고, 학습을 한 번 더 도는 것은 그 병목을
건드리지 않는다. S0(진단) → S1(리포트 초안) → S2(단일 변경) → S3(장기 학습) 순으로 재편했다.

### 5단계 — 서버 휘발 대응 (F25 · D13)
서버가 세션마다 초기화된다는 제약이 **저장소 어디에도 기록돼 있지 않았고**(정본 8건 전수
grep 0건), 그래서 S0 실행계획이 그것을 무시한 채 작성됐었다. `_bootstrap/` + `restore.sh` +
zip 체계를 만들고 **검출력을 실증**했다 — 정상 exit 0 / 체크포인트 1바이트 손상 `[BLOCKED]`
exit 1 / 보상값 변조 `[BLOCKED]` exit 1.

### 6단계 — Run 05 (S2 후보 1 단독 검증)
`track_lin_vel_xy_exp` 0.5 → 1.0. 3000 iter, exit=0, 미제출. 결과 분석은 §4.

### 7단계 — S0 근사 진단 6종 (F27 · F30)
**여기서 방법이 하나 바뀌었다.** 오전에는 "이 서버는 학습해야만 영상이 나온다"고 판단했는데,
그 근거였던 `exported/model_best.pt 없음` 경고는 학습 필수여서가 아니라 **세션 초기화로 폴더가
비어 있었기 때문**이었다(F25). 원인과 증상을 뒤바꾼 오판이다. 체크포인트를 업로드하면
**학습 0분으로 재생이 성립한다**(F27).

그 방법으로 6종을 찍었다. 도중에 크래시 1건이 있었다 — `terrain_type=plane`을 강제하면
지형 커리큘럼이 존재하지 않는 `terrain_levels` 속성을 참조해 죽는다. `env.curriculum.terrain_levels=null`을
함께 주는 것이 해법이고, 재실행 로그의 `Curriculum Manager: contains 0 active terms`로 확인했다.

---

## 2. 현재 위치 — 점수축 커버리지

### 2-1. 채점 시나리오 (F30)

| 시나리오 | 가중 | 상태 | 근거 |
|---|---:|---|---|
| H1 제자리 균형 | 0.15 | **측정** | `S0b/H1_stand.mp4` — 직립, 지령 마커 길이 0 |
| H2 전진 속도 | 0.20 | **측정** | `S0b/H2_forward.mp4` — 보행, 파랑·초록 화살표 정렬 |
| H3 좌우 속도 | 0.10 | **측정** | `S0b/H3_left.mp4` · `H3_right.mp4` — 측방 보행 |
| H4 회전 복합 | 0.10 | **측정** | `S0b/H4_turn.mp4` — 선회 보행, 직립 |
| H5 약한 요철 | 0.15 | **미측정** | 지형 세기 단위 미확인 |
| H6 완만한 경사 | 0.15 | **미측정** | 지형 세기 단위 미확인 |
| H7 밀침 회복 | 0.15 | **측정** | `S0b/H7_push.mp4` — 4초 간격 ±0.5 m/s 4회 후에도 직립 |

**커버리지 0.80 / 1.00.** 6개 로그 전부 `Traceback`·`Error executing job`·`AttributeError`·🔴 = 0건.

> **⚠️ 260829 정정 — 커버리지는 0.80이 아니라 0.70이다.** 측정된 시나리오는 H1·H2·H3·H4·H7이고
> 0.15+0.20+0.10+0.10+0.15 = **0.70**이다. 내 산술 오류다. 미측정분 H5·H6 = 0.30이며
> 0.70 + 0.30 = 1.00으로 닫힌다. H5·H6 완료 시 1.00 / 1.00. 아래 §5-1의 "0.80 → 1.00" 도
> **"0.70 → 1.00"** 으로 읽는다.

**명령 override가 실제로 걸렸다는 증거**: 로그에는 명령값이 한 줄도 찍히지 않으므로(F28)
영상 마커가 유일한 직접 증거다. 파랑 = 지령 속도, 초록 = 실제 속도이며, **지령을 0으로 준
H1·H7에서만 파란 마커가 점(길이 0)이고 초록 화살표가 사라진다.** H2·H3·H4는 둘 다 뻗어 있다.

> **판정 한계 — 이 수치를 생존율로 인용하지 않는다.** 20초(1000 step) 영상에서 8프레임,
> H4·H7은 4프레임 추가 확대만 표본했다. 표본 시점에 넘어진 개체는 없었으나
> **표본 사이의 낙상·리셋은 검출하지 못한다.** 말할 수 있는 것은 "표본 시점 전부 직립"까지다.

> **⚠️ 260829 갱신 — H5·H6 측정 완료. 커버리지 0.70 → 1.00** (F37, 학습 0분·제출 0회)
>
> | 시나리오 | 산출물 | 지형 실측 | 25프레임 직립 |
> |---|---|---|---|
> | H5 약한 요철 | `S0c/H5_rough.mp4` (9,291,697 B) | `random_rough` 100%, `noise_range=[0.02,0.10]` m, 난이도 1.0, 6×4 타일 | **25/25** |
> | H6 경사 A | `S0c/H6_up.mp4` (6,074,136 B) | `hf_pyramid_slope_inv` 100%, `slope_range=[0.17632698,…]` = 10° | **25/25** |
> | H6 경사 B | `S0c/H6_down.mp4` (6,406,601 B) | `hf_pyramid_slope` 100%, 동일 세기 | **25/25** |
>
> 표본은 40프레임 간격 25장(n=0,40,…,960)이며 `video_length: 1000` = `episode_length_s 20.0`
> ÷ `dt 0.02` 이므로 **1 에피소드 전량을 덮는다**. 세 run 모두 리셋 없음.
> 지형 적용 증거: 평지 run(`S0b/H2_forward.mp4`)과 H6 두 run의 n=400 프레임을 회색조·동일
> 대비(`eq=contrast=3.0:brightness=-0.55`)로 비교하면 평지는 균일, H6은 대형 면 단위 밝기
> 편차 + 높이장 계단 무늬가 나타난다.
>
> **한계 3건 — 아래를 떼고 인용하지 않는다.**
> 1. **낙상 0 ≠ `survival_rate`.** `play.py`는 지표를 하나도 찍지 않으므로(F28) 근거는
>    `num_envs=4` 중 **추적 env 1개**의 **1 에피소드**, **25프레임 표본**이다.
> 2. **H5는 후반이 요철이 아니다.** n≈440(≈8.8 s)에 요철 필드를 벗어나 평지 border로 나간다
>    — 요철 노출 **약 44%**. `num_rows` 1→6으로 필드는 8 m→48 m가 됐으나
>    `curriculum=false`의 랜덤 배치로 시작 타일이 가장자리였다.
> 3. **H6의 up/down 라벨은 신뢰할 수 없다.** 로봇은 타일 중심에서 출발하므로 `pyramid_slope`
>    (중심이 봉우리)는 내리막부터, `_inv`(중심이 웅덩이)는 오르막부터 시작한다 — 내가 붙인
>    이름과 반대다. 타일을 넘으면 둘이 섞인다. 따라서 **"±10° 경사에 노출됐고 넘어지지 않았다"
>    까지만** 말할 수 있고, 오르막·내리막을 분리 측정한 것이 아니다.
>
> 커버리지가 1.00이 됐다는 것은 **7종 전부를 한 번씩 눈으로 봤다**는 뜻이지
> 생존율·추종점수를 측정했다는 뜻이 아니다. §2-2의 70점 축은 여전히 미측정이다.

### 2-2. 100점 배분 대비 현재

| 축 | 배점 | 현재 |
|---|---:|---|
| 시뮬 성능 | 70 | 정책 있음. **점수는 확인 불가**(F10 — 운영진 일괄 평가) |
| 설계의도 분석 | 20 | **증거 확보됨**(S0 영상 6종). 문서화 미완 — 획득이 아니라 준비(D10) |
| 리포트 품질 | 10 | **미작성** |
| Go2 (별도 100) | 100 | **미개방** — run 0회, 담당 배우 0종 |

F9가 결정적이다 — 의도분석 20점은 **"리포트에 적은 의도대로 로봇이 실제로 그렇게 행동하는가"** 다.
S0 영상 6종은 정확히 그 증거이고, 이것을 4 run 동안 한 번도 갖고 있지 않았다.

---

## 3. 튜닝 현황

### 3-1. 보상 노브 9개 — 무엇을 건드렸고 무엇을 안 건드렸나

`humanoid_rewards.py` 현재 상태(= Run 05 상태) 대 배포 기본값.
근거: `train_260829-05.log` `[report] 보상 변화 (기본값 대비)` 3줄 — 나머지 6개는 기본값이라는 뜻.

| # | 노브 | 기본값 | 현재 | 권장 범위 | 탐색 이력 |
|---|---|---:|---:|---|---|
| ① | `track_lin_vel_xy_exp` | 0.5 | **1.0** | 0.5 ~ 2.5 | Run 05에서 1회 (§4-A) |
| ② | `track_ang_vel_z_exp` | 0.5 | **1.0** | 0.5 ~ 2.0 | Run 04에서 1회 → PASS |
| ③ | `feet_air_time` | 0.2 | 0.2 | 0.1 ~ 0.5 | **미탐색** |
| ④ | `termination_penalty` | -5 | **-50** | -400 ~ -50 | Run 02 채택 · Run 03(-90) 기각 → **축 포화** |
| ⑤ | `flat_orientation_l2` | -1.5 | -1.5 | -3.0 ~ -0.2 | **미탐색** |
| ⑥ | `ang_vel_xy_l2` | -0.05 | -0.05 | -0.3 ~ -0.02 | **미탐색** |
| ⑦ | `joint_deviation_hip` | -0.2 | -0.2 | -0.5 ~ -0.1 | **미탐색** |
| ⑧ | `joint_deviation_torso` | -0.1 | -0.1 | -0.3 ~ -0.02 | **미탐색** |
| ⑨ | `action_rate_l2` | -0.0005 | -0.0005 | -0.008 ~ -0.0005 | **미탐색** |

**9개 중 3개 탐색, 6개 미탐색.** 탐색한 3개 중 ④는 포화가 실증됐고(-5→-50이 -62.65%p,
-50→-90이 -1.94%p), ①②는 서로 상쇄한다(§4-A). 즉 **실질적으로 남아 있는 자유도는 대부분
아직 안 건드린 6개 쪽에 있다.**

### 3-2. run 이력 — 사전 등록 대비 결과

| Run | 변경 | 사전 예측 | 결과 | 판정 |
|---|---|---|---|---|
| 01 | baseline | **없음** | base_contact 0.9244 | FAIL (비교 불가) |
| 02 | ④ -5 → -50 | 낙상↓ | 0.9244 → 0.2979 | PARTIAL (영상 미측정) |
| 03 | ④ -50 → -90 | 낙상 15%까지 | 0.2785 (-1.9%p) · yaw 오차 +89% | **FAIL — 축 포화 · 롤백** |
| 04 | ② 0.5 → 1.0 | yaw 오차 ≤0.80 | yaw 0.6230 · 부작용 게이트 4개 통과 | **PASS** (영상 미측정) |
| 05 | ① 0.5 → 1.0 | 낙상 개선 | 낙상 무변화 · **회전 -27%** | **교환** (§4-A) |

사전 예측을 등록하고 그것으로 판정한 run은 03·04·05뿐이다. 01·02는 사후 서술이다.

### 3-3. 보상 예산이 실제로 어디에 쓰이고 있나 (Run 05 실측)

`train_260829-05.log` 마지막 50 iteration 평균. `Episode_Reward/<항목>` ÷ 가중치 =
**상한 대비 성취율**(F21이 세운 방식과 동일).

| 항목 | Episode_Reward | 가중치 | 상한 대비 |
|---|---:|---:|---:|
| `track_lin_vel_xy_exp` | 0.6191 | 1.0 | **61.9%** |
| `track_ang_vel_z_exp` | 0.3207 | 1.0 | **32.1%** |
| `feet_air_time` | 0.0063 | 0.2 | **3.2%** |
| `flat_orientation_l2` | -0.0148 | -1.5 | 1.0% (벌점 거의 안 맞음) |

**`feet_air_time` 3.2%** — 발 들기 축이 사실상 꺼져 있다. 이 로봇은 발을 거의 안 든다.
F15가 Go2에 대해 "올려야 잘 걷기 시작"이라고 기록한 것과 같은 상태이고, H1에서는 **한 번도
건드린 적이 없다.**

`flat_orientation_l2` 벌점이 -0.0148밖에 안 걸린다는 것은 **몸통이 이미 충분히 곧다**는 뜻이다.
이 축을 더 조여도 얻을 것이 없다.

> **⚠️ 260829 정정 — 위 표의 "상한 대비"는 두 군데가 틀렸다.** 지적 접수(외부 검토).
>
> **(1) 분모에 생존 보정이 빠졌다.** IsaacLab은 `Episode_Reward/<항목>`을
> `Σ(r × w × dt) ÷ max_episode_length_s`로 찍는다. 즉 상한은 `w × (항별 per-step 상한) ×
> (실제 ep_len ÷ 최대 ep_len)`이지 `w`가 아니다. Run 05의 생존 비율은 756.9/1000 = **0.7569**다.
>
> **(2) `feet_air_time`의 per-step 상한은 1.0이 아니라 `threshold`다.** 함수는
> `feet_air_time_positive_biped`이고 `exported/env.yaml:903`에 **`threshold: 0.4`** 로 고정돼 있다.
> raw 보상은 0.4 s에서 잘리며, 추가로 **한 발 지지**(single stance)와 **|명령| > 0.1** 두 게이트를
> 통과할 때만 적립된다. `_exp` 계열(직진·회전)만 per-step 상한이 1.0이다.
>
> **정정표 (Run 05):**
>
> | 항목 | Episode_Reward | per-step 상한 | 가중치 | 실제 상한 | 상한 대비 |
> |---|---:|---:|---:|---:|---:|
> | `track_lin_vel_xy_exp` | 0.6191 | 1.0 | 1.0 | 0.7569 | ~~61.9%~~ → **81.8%** |
> | `track_ang_vel_z_exp` | 0.3207 | 1.0 | 1.0 | 0.7569 | ~~32.1%~~ → **42.4%** |
> | `feet_air_time` | 0.0063 | **0.4** | 0.2 | 0.0606 | ~~3.2%~~ → **10.4%** |
>
> **`feet_air_time`에 대한 서술도 함께 정정한다.** "축이 사실상 꺼져 있다 / 발을 거의 안 든다"는
> **과장이다.** 실측이 말하는 것은 여기까지다 — 한 발 지지 구간의 평균 체공/접지 시간이
> **0.0416 s**로, 상한 0.4 s의 10.4% 수준이다(0.0063 ÷ (0.2 × 0.7569)). 이 값이 낮은 원인이
> ⓐ 실제로 발을 낮게 끄는 것인지 ⓑ 한 발 지지 게이트를 자주 못 통과하는 것인지는
> **이 수치로 구분되지 않는다.** 구분하려면 영상에서 발 높이·한발 지지·끌림을 직접 봐야 한다.
> 따라서 §5-2의 `feet_air_time` 후보는 **근거가 약해졌고, 영상 확인이 선행 조건이다.**
>
> `flat_orientation_l2` 관련 서술은 영향받지 않는다(벌점 항이고 절대값이 작다는 사실만 쓴다).

---

## 4. 분석

### 4-A. Run 05는 실패가 아니라 교환이었다 — 두 추종 항은 시소다

F29는 Run 05를 "낙상률 변화 없음 / 보상 비교 불가"로만 기록했다. 그것은 사실이지만
**결과의 본체가 아니다.** 항별 보상과 추종 오차를 함께 보면 정반대의 그림이 나온다.

**증거 1 — 항별 성취율** (Run 04는 F21, Run 05는 이번 실측. 둘 다 마지막 50 iter 평균)

| 항목 | Run 04 | Run 05 | 변화 |
|---|---:|---:|---:|
| 직진 성취율 `track_lin_vel_xy_exp` ÷ 가중치 | 0.5828 | 0.6191 | **+6.2%** |
| 회전 성취율 `track_ang_vel_z_exp` ÷ 가중치 | 0.4407 | 0.3207 | **-27.2%** |

> 생존 보정은 필요 없다 — `Mean episode length`가 Run 04 767.2 · Run 05 757.0으로 **1.3% 차이**다.
> 두 run은 같은 생존 수준에서 비교되고 있다.

**증거 2 — 추종 오차** (F18 방식으로 생존시간 보정: `raw × 500 / ep_len`)

| Run | 가중치 (직진:회전) | ep_len | xy/step | statue 대비 | yaw/step | statue 대비 |
|---|---|---:|---:|---:|---:|---:|
| 01 | 0.5 : 0.5 | 332.6 | 0.5359 | 0.75x | 0.3580 | 0.54x |
| 02 | 0.5 : 0.5 | 780.1 | 0.2327 | 0.32x | 0.6145 | 0.92x |
| 03 | 0.5 : 0.5 | 791.2 | 0.2667 | 0.37x | 1.1571 | **1.73x** |
| 04 | 0.5 : **1.0** | 767.2 | 0.2730 | 0.38x | **0.4190** | **0.63x** |
| 05 | **1.0** : 1.0 | 757.0 | **0.2243** | **0.31x** | 0.6255 | 0.94x |

`statue` = 명령을 무시하고 서 있기만 하는 로봇의 이론 오차(xy 0.7165 · yaw 0.6681, 2×10⁷ 샘플 수치적분).

**두 증거가 같은 말을 한다:**

1. **직진은 5 run 중 최고**가 됐다. xy/step 0.2243은 statue의 0.31배로, Run 02(0.32x)를
   근소하게 넘어선 최고 기록이다.
2. **회전은 Run 04의 성과가 통째로 사라졌다.** yaw/step 0.4190 → 0.6255. statue 대비
   0.63x → 0.94x, 즉 **회전 가중치를 올리기 전인 Run 02(0.92x) 수준으로 되돌아갔다.**
   회전 가중치는 여전히 1.0인데도 그렇다.
3. 따라서 효과를 낸 것은 **가중치의 절대값이 아니라 두 항의 비율**이다.
   Run 04는 직진:회전 = 1:2 → 회전이 이겼다. Run 05는 1:1 → 직진이 이겼다.

**교환의 손익 — 예선 배점으로 환산하면 대체로 본전, 근소하게 손해다.**

| | 대응 시나리오 | 가중 합 | 성취율 변화 | 가중 곱 |
|---|---|---:|---:|---:|
| 직진 | H2(0.20) + H3(0.10) | 0.30 | +0.0363 | **+0.0109** |
| 회전 | H4(0.10) | 0.10 | -0.1200 | **-0.0120** |

(H4는 `vx + wz` **복합** 명령이므로 회전 축만 걸린 것이 아니다 — 위 환산은 회전 손실을
과대평가한 보수적 계산이다. 그래도 결론은 "본전 부근"이다.)

> **⚠️ 260829 정정 — 위 환산표는 예선 점수 추정치가 아니다.** 지적 접수(외부 검토).
> 공식 점수는 시나리오마다 `survival_rate × tracking_score`이고(F5 · `PRELIM_RL_GUID.md`),
> **평가 지형에서** 산출된다. 위 표가 곱한 것은 **커리큘럼 혼합 지형의 학습 내부 보상 성취율**이다.
> 지형도 다르고 함수도 다르므로 `+0.0109` · `-0.0120`을 예선 점수로 읽어서는 안 된다.
> 이 표가 지지하는 것은 **부호와 상대 크기의 방향성**(직진 소폭 이득 · 회전 큰 손실 · 합계는
> 대략 상쇄)까지이며, 그 이상은 미측정이다. 아래 §4-A의 세 결론과 "저울을 반대로 기울였다"는
> 판정 자체는 이 정정에 영향받지 않는다.
>
> **성취율을 §3-3 정정(생존 보정)으로 다시 계산해도 시소는 그대로다:**
>
> | 항목 | Run 04 | Run 05 | 변화(상대) |
> |---|---:|---:|---:|
> | 직진 상한 대비 | 76.0% | 81.8% | **+7.7%** |
> | 회전 상한 대비 | 57.4% | 42.4% | **-26.3%** |
>
> (보정 전 값 +6.2% / -27.2%와 비교해 부호·크기 모두 유지된다. §4-A의 결론은 정규화 방식에
> 대해 **견고하다**.)

> **⚠️ 260829 정정 — 위 3번("효과를 낸 것은 비율이다")은 [사실]이 아니라 [추론]이다.**
> 지적 접수(외부 검토). Run 04 → Run 05에서 실제로 바뀐 값은 `track_lin_vel_xy_exp` **하나**이므로,
> 이 한 쌍만으로는 "비율이 원인"과 "직진 가중치 절대값이 원인"을 구분할 수 없다.
>
> 다만 `termination_penalty = -50`으로 고정된 **세 run**을 함께 놓으면 비율 가설만 살아남는다:
>
> | Run | 직진 w | 회전 w | 비율(직진÷회전) | yaw statue 대비 |
> |---|---:|---:|---:|---:|
> | 02 | 0.5 | 0.5 | 1.0 | 0.92x |
> | 04 | 0.5 | **1.0** | **0.5** | **0.63x** |
> | 05 | **1.0** | 1.0 | 1.0 | 0.94x |
>
> 회전 가중치 절대값은 예측에 실패한다(0.5 → 0.92x, 1.0 → 0.63x, 1.0 → 0.94x).
> 직진 가중치 절대값도 실패한다(0.5 → 0.92x, 0.5 → 0.63x). **비율만 세 점을 모두 설명한다.**
>
> 그래도 [사실]로 올리지 않는 이유는 세 가지다 — (a) run당 seed 1개이고 교재가 명시한 대로
> cudnn 비결정성이 있다 (b) Run 03은 비율 1.0인데 yaw 1.73x로, `termination_penalty`도 이 축을
> 크게 움직인다는 뜻이다 — 즉 **비율이 유일한 구동자는 아니다** (c) 비율을 직접 조작한
> 확인 실험(예: 직진 1.5 : 회전 1.0)을 아직 돌리지 않았다. §5-2 후보 3번이 그 검정이다.

> **이것이 Run 05의 진짜 결과다.** "낙상률이 안 좋아졌다"가 아니라
> **"36분을 써서 저울을 반대로 기울였고 총합은 그대로였다"** 이다.

### 4-B. 낙상률은 4개 run · 3개 노브 동안 못 움직였다

| Run | 변경한 노브 | `Episode_Termination/base_contact` |
|---|---|---:|
| 01 | — | 0.9244 |
| 02 | ④ termination_penalty | **0.2979** ← 여기서 한 번 크게 떨어지고 |
| 03 | ④ termination_penalty | 0.2785 |
| 04 | ② track_ang_vel_z_exp | 0.3071 |
| 05 | ① track_lin_vel_xy_exp | 0.3050 |

**Run 02 이후 4개 run, 3개 서로 다른 노브를 건드리는 동안 28~31% 대에 고정돼 있다.**
이것은 우연이 아니라 **지금까지 시도한 노브들이 낙상 축을 건드리지 못한다**는 뜻이다.

다만 여기에 F22가 붙는다 — **이 30%는 계단 23cm·박스 50cm가 섞인 난이도 5.5 지형의 낙상률**이고,
예선 평가 지형의 70%는 평지다. S0 영상 6종에서 표본 시점 낙상이 0건이었던 것과 모순되지 않는다.

**따라서 낙상률 30%를 예선 목표로 쫓는 것은 잘못된 과녁일 가능성이 높다.**
다만 `survival_rate`는 예선 점수식에서 **곱해지는 항**(F5)이므로, 평지 생존이 실제로 높다는
확증이 필요하다. 현재 그 확증은 **8프레임 표본**뿐이다(§2-1의 판정 한계).

### 4-C. 5개 run 전부 잘못된 지형에서 측정됐다

이것이 가장 구조적인 문제다.

- 우리가 가진 모든 수치(`error_vel_*`, `base_contact`, `terrain_levels`, 항별 보상)는
  **커리큘럼 지형**에서 나왔다. 난이도 5.5 = 계단·박스가 섞인 구간이다.
- 예선 평가는 **평지 0.70 + 요철 0.15 + 경사 0.15**다(F22).
- **평지에서의 추종 오차·생존율은 5 run 동안 단 한 번도 측정된 적이 없다.**

그리고 F28에 따라 **지금 도구로는 측정할 수 없다** — `play.py`는 성능 수치를 한 줄도
찍지 않는다. 영상 마커의 파랑↔초록 화살표 정렬이 유일한 추종 정보이고, 그것은 정성적이다.

> 즉 우리는 **평가받는 지형이 아닌 곳에서 잰 값으로 튜닝하고 있다.** 이것이 손해라는 증거는
> 없지만(전이 효과 미측정), **측정 대상이 아니었다는 것은 확정 사실이다.**

> **⚠️ 260829 정정 — 제목 "전부 잘못된 지형"은 과한 표현이다.** 지적 접수(외부 검토).
> 학습 지형에는 요철·경사가 실제로 들어 있다. `exported/env.yaml:263-355` 실측 비중(합 0.90을
> 정규화):
>
> | sub_terrain | 설정 proportion | 정규화 비중 | 범위 |
> |---|---:|---:|---|
> | `boxes` | 0.30 | 33.3% | height 0.15~0.50 m |
> | `pyramid_stairs` + `_inv` | 0.15 + 0.15 | 33.3% | step 0.05~0.23 m |
> | `hf_pyramid_slope` + `_inv` | 0.10 + 0.10 | **22.2%** | slope 0.0~0.4 |
> | `random_rough` | 0.10 | **11.1%** | noise 0.02~0.10 m |
>
> 즉 예선 H5(요철 0.15)·H6(경사 0.15)에 대응하는 지형은 학습 분포 안에 **33.3%** 로 존재하며,
> 예선의 "약한 요철·완만한 경사(±10° = slope 0.176)"는 학습 범위(경사 0~0.4)의 **아래쪽 끝**이다.
> 분포 밖(out-of-distribution)이 아니다.
>
> **정확한 진술은 이것이다 — "평가 분포와 다른 혼합 지형의 집계값만 갖고 있다."** 구체적으로:
> (a) **평지는 학습 분포에 0%** 인데 예선 가중은 **0.70**이다 — 이것만은 원문 그대로 유효하다.
> (b) 요철·경사는 있지만 **강도가 예선보다 높은 쪽에 치우쳐** 있다.
> (c) 모든 수치는 **시나리오별로 분해되지 않은 혼합 집계값**이라 H5·H6 각각의 성능을 말하지 못한다.
> 더 어려운 지형에서 배운 것이 예선에 **손해인지 강건성 전이로 이득인지는 여전히 미측정**이며,
> 이 정정은 §5-2 후보 1번(지형 분포 이동)의 **근거를 약화시킨다** — "과녁이 틀렸다"가 아니라
> "과녁이 겹치되 중심이 어긋나 있다"가 실제 상태다.

> **⚠️ 260829 추가 — S0c가 (b)에 부분 답을 줬다 (F37).** 위 (b)의 미측정 항목("더 어려운
> 지형에서 배운 것이 예선에 손해인가 이득인가")에 대해, Run 05 정책을 예선급 지형에 직접
> 올려 본 결과 **요철(0.02~0.10 m)·경사(±10°) 3종 모두에서 1 에피소드 낙상 0**이었다.
> 최소한 **"어려운 지형 학습이 예선급 지형에서 붕괴를 일으키지는 않는다"** 는 것이 관측됐고,
> 이는 후보 1번(지형 분포를 예선 쪽으로 이동)의 근거를 **한 번 더** 약화시킨다 — 옮겨서
> 고칠 낙상이 애초에 관측되지 않았다.
>
> 단, 이것은 **낙상 축**에만 해당한다. 예선 점수는 `survival_rate × tracking_score`이고
> S0c는 `tracking_score`를 **전혀 측정하지 못했다**(F28 — `play.py` 지표 0건). 지형 이동이
> 추종 정확도에 주는 영향은 여전히 미측정이며, 그 축의 판정에는 학습이 필요하다.
> 그리고 (a)(평지 0% 학습 vs 가중 0.70)는 **그대로 열려 있다** — S0c는 평지를 재지 않았다.

### 4-D. 정정 — F29 표의 "Run 04" 열은 Run 02의 값이었다

`PROJECT_STATE.md` F29에 내가 이렇게 적었다:

| 지표 | Run 04 | Run 05 | 판정 |
|------|--------|--------|------|
| 낙상률 | 29.8% | 30.4% | 변화 없음 |
| 최고보상 | 3.84 | 12.09 | 비교 불가 |

**29.8%와 3.84는 Run 02의 값이다.** `experiment_history.csv` Run 02 행의
`base_contact=0.2979` · `reward=3.8434`가 그것이고, Run 04 행은
`base_contact=0.3071` · `reward_best=9.0870`이다. 내가 열을 잘못 옮겼다.

**정정표:**

| 지표 | Run 04 | Run 05 | 판정 |
|---|---:|---:|---|
| 낙상률 `base_contact` | 0.3071 | 0.3050 | 변화 없음 (-0.21%p, 잡음 범위) |
| 탐색 std | 1.4191 | 1.672 | **악화** (+17.8%) |
| 최고보상 | 9.087 | 12.089 | **비교 불가** (가중치 변경) |
| 지형 난이도 | 5.3926 | 5.5399 | +2.7% |

결론("Run 05는 낙상률을 개선하지 못했다")은 **바뀌지 않는다.** 30.71% → 30.50%도 무변화다.
바뀌는 것은 근거 수치의 출처뿐이다. 다만 std 악화 폭이 원래 적은 것보다 작다
(1.311 → 1.672가 아니라 1.419 → 1.672).

### 4-E. 학습 곡선을 처음으로 직접 읽었다 — Run 05는 3000에서 절반만 수렴했다 (260830)

**전제가 하나 틀려 있었다.** F28의 「`play` 로그에 성능 수치가 한 줄도 없다」는 사실이다.
그러나 그것이 「수치가 없다」로 일반화돼, 지금까지의 모든 판정이 **run 종료 시점의 요약값
몇 개**와 **mp4 육안 판정**에만 의존했다. 실제로는 5 run 전부의 tensorboard 원본이 PC에 있다.

| 항목 | 실측 |
|---|---|
| 파일 | `workspace/training/humanoid/logs/rsl_rl/humanoid/<run>/events.out.tfevents.*` |
| 개수·크기 | **5개** (Run 01~05), 각 약 **6.0 MB** |
| 내용 | **31개 태그 × 3000 iteration** — 추종 오차·낙상·지형 레벨·손실·std 전부 |
| 도구 | `tools/tfcurve.py` (tensorboard 미설치 환경용 자체 파서) |
| 재현 | `cd workspace/training/humanoid && python ../../../tools/tfcurve.py window Run04 Run05` |
| 파서 검증 | 출력 `final` 열 = `experiment_history.csv` Run 04 행 **5개 값 전부 일치** (0.4102 · 0.6230 · 0.3071 · 5.3926 · 1.4191) |
| 비용 | **GPU 0분 · 서버 접속 0회** |

**Run 04 vs Run 05** (마지막 500 iter 평균 / 마지막 1000 iter 기울기 per 1k):

| 지표 | R04 | R05 | Δ | R04 기울기 | R05 기울기 | 수렴? |
|---|---:|---:|---:|---:|---:|---|
| `error_vel_xy` | 0.4296 | 0.3385 | **−21.2%** | −0.0372 | −0.0099 | 거의 |
| `error_vel_yaw` | 0.6501 | 0.9613 | **+47.9%** | −0.0498 | **−0.1063** | **아니오** |
| `base_contact` | 0.3073 | 0.3020 | −1.7% | −0.0198 | −0.0025 | 예 |
| `terrain_levels` | 5.2830 | 5.5646 | +5.3% | +0.4917 | +0.3279 | **아니오 (최대 9)** |
| `mean_std` | 1.4163 | 1.6641 | +17.5% | +0.0023 | **−0.0135** | 예 |
| `mean_episode_length` | 776.9 | 762.6 | −1.8% | +6.00 | −2.30 | 예 |

**세 가지가 바뀐다.**

**① 교환의 폭이 훨씬 크다.** §4-A는 성취율 기준으로 「직진 +6% / 회전 −27%」였다.
마지막 500 iter 평균(단일 종점보다 잡음이 작은 추정)으로 다시 재면
**직진 오차 −21.2% · 회전 오차 +47.9%** 다. 시소라는 결론은 그대로이고 진폭만 커진다.

**② 기준선 결론은 살아남지만 「확정」은 아니다.** 예선에서 명령이 0이 아닌 가중 0.70 중
회전이 걸리는 것은 H4 0.10뿐이다. 역전에 필요한 각속도 절대 가중은
`w × 47.9 = (0.70−w) × 21.2` → `w = 0.2147`, 즉 비제로 가중의 **30.7%** 인데 실제는 **14.3%** 다.
여유는 오히려 커졌다. **다만 이 계산은 미공개 예선 `tracking_score` 식에 학습 지형 집계값을
대입한 [추론]이므로, Run 05는 「예선 확정 우위」가 아니라 「다음 검증의 작업 기준선」이다.**

**③ 다음 학습은 노브 변경이 아니다.** `error_vel_yaw`는 아직 **−0.106/1k**로 내려가는 중이고
이는 Run 04(−0.0498/1k)의 **2.1배 속도**다. 지형 커리큘럼은 5.56인데 **최대는 9**
(`env.yaml:257 num_rows: 10`)이고 여전히 +0.33/1k로 오르는 중이다. 즉 **Run 05가 회전을
잃은 것이 보상 설정의 최종 성격인지, 아직 따라잡는 중인 중간 상태인지 구분되지 않는다.**
이 상태에서 노브를 고르면 후자를 전자로 오진할 수 있다.
→ **다음 학습: Run 05 보상 그대로 · 5000 iter · from scratch · `NO_AUTO_SUBMIT=1`** (D20).

> **`mean_std` 1.672에 대한 주의.** 이 값을 「미수렴 위험 신호」로 읽지 않는다.
> Run 05 안에서 std는 iter 1000 부근에 안착한 뒤 마지막 1000 iter 기울기가 **−0.0135/1k**,
> 즉 **내려가는 중**이다. Run 04도 +0.0023/1k로 평평하다. 두 값의 차이는 수렴도 차이가 아니라
> **보상 설정이 다른 두 정책의 평형 잡음 수준 차이**다. 진짜 미수렴 증거는 std가 아니라
> `error_vel_yaw` 기울기와 `terrain_levels` 미포화다.

> **비교 자체의 교란 1건.** `terrain_levels`가 두 run에서 서로 다른 속도로 오른다
> (0.49 vs 0.33 /1k). Run 04의 오차는 **더 빨리 어려워지는 지형**에서 측정된 값이므로,
> Run 05의 직진 우위는 다소 과대평가, 회전 열위는 다소 과소평가돼 있다. 방향은 유지되지만
> 마진은 위 표보다 좁다.

> **실행 시 걸리는 함정 1건 (사전 검출).** `h1_task/_finalize.py:111-140`이 학습 종료 시
> `model_*.pt`를 **best 1개만 남기고 전부 삭제**한다(`train.py:146`이 `finalize()`를 인자 없이
> 호출하므로 `--no-cleanup`을 끼울 수 없다). 실증: 로컬 5개 run 폴더에 `.pt`가 각 1개뿐인데
> `save_interval: 100`이므로 원래는 run당 30개다. **따라서 "3000과 5000 체크포인트를 비교한다"는
> 계획은 그대로는 실패한다** — 학습 도중에 `model_3000.pt`를 별도 폴더로 복사해야 한다.
> 단 **수치 비교는 `.pt` 없이 tfevents만으로 완결된다**(삭제 대상이 아니다).

---

## 5. 다음 수

### 5-1. 즉시 (학습 0분)

| # | 할 일 | 이유 |
|---|---|---|
| 1 | **H5·H6 명령 확정 후 실행** — 커버리지 0.80 → 1.00 | 남은 유일한 미측정 점수축 0.30. 서버에서 `slope_range` 단위(라디안 vs 비율)를 먼저 조회해야 한다 |
| 2 | **기술 개선 리포트 초안**(S1) | 10점 축 전체 + 20점 축의 문서화. S0 영상 6종이 F9가 요구하는 "의도대로 행동하는가"의 증거다 |
| 3 | **Go2 착수 승인 요청**(D11) | 유효 제출 1건의 한계 가치 = **100점 축 개방**. H1 reward 미세조정 어느 것보다 크다 |

### 5-2. S2 후보 재선정 — 근거와 함께

Run 05로 후보 1번(`track_lin_vel_xy_exp` 단독 상향)이 소진됐다. §3-1·§4를 근거로 한 재선정:

| 순위 | 후보 | 근거 | 위험 |
|---|---|---|---|
| **1** | **학습 지형 분포를 예선 쪽으로 이동** (Hydra override로 `sub_terrains` proportion 조정) | §4-C. 5 run 전부 평가받지 않을 지형에서 측정됐다. 예선은 평지 0.70인데 학습은 계단·박스 66%다. **노브 하나가 아니라 과녁 자체를 옮기는 수** | 본선(계단·박스) 성능 희생. 단 현 목표는 예선 통과(D8) |
| **2** | `feet_air_time` 0.2 → 0.35 | §3-3. 성취율 **3.2%** — 축이 꺼져 있다. 9개 중 미탐색이고 F15가 Go2에서 같은 축의 효과를 기록 | 0.3 이상에서 한 발 깡총(reward hacking) 위험 — F17. **영상 확인 필수** |
| **3** | 직진:회전 비율을 1.5:1로 | §4-A. 효과를 내는 것은 절대값이 아니라 비율이고, 예선 배점은 직진 계열 0.30 대 회전 0.10 | 이미 두 번 시소를 왕복했다. 세 번째도 교환일 가능성 |
| — | 낙상률 축 | §4-B. 4 run 동안 3개 노브가 못 움직였고, 그나마 **틀린 지형의 낙상률**이다 | **추천하지 않음** |

**추천은 1번**이다. 2·3번은 "이미 재고 있는 자 위에서 눈금을 옮기는" 수이고, 1번은
**자를 예선 쪽으로 옮기는** 수다. §4-C가 말하는 구조적 문제를 유일하게 건드린다.

단, 1번은 **단일 변수 원칙에 걸린다** — 지형 분포는 노브 하나가 아니다. 이 실험을 하려면
사전에 "무엇을 단일 변수로 볼 것인가"를 확정해야 하고, 그 판단은 기획자(`humanoid-test-planner`)
소관이며 수용기준의 개정 권한은 사용자와 메인 루프에 있다(D7 · 원칙 12).

> **⚠️ 260829 정정 — 위 순위표는 §4-C 정정으로 근거가 바뀌었다.** 지적 접수(외부 검토).
> 1번(지형 분포 이동)의 근거였던 "5 run 전부 잘못된 지형"이 "요철 11.1% · 경사 22.2%는 이미
> 학습 중"으로 축소됐고, 2번(`feet_air_time`)의 근거였던 "성취율 3.2% = 축이 꺼짐"이
> "10.4% + 원인 미구분"으로 축소됐다. **두 후보 모두 지금 상태로는 S2를 열 근거가 부족하다.**
>
> **선행되어야 할 것은 학습이 아니라 판정이다** — 현재 우리는 Run 04와 Run 05 중 **어느 쪽을
> 기준선으로 삼을지조차 확정하지 않았다.** 두 정책은 시소의 양 끝이고 총합은 본전 부근이므로
> (§4-A), 어느 쪽 위에서 S2를 돌리느냐가 S2 결과보다 먼저 정해져야 한다.
>
> 따라서 순서를 이렇게 바꾼다 — **S0c(학습 0분)를 먼저 돌린다:**
>
> | 순서 | 내용 | GPU 학습 |
> |---|---|---|
> | **S0c-1** | Run 05 × H5·H6 3종 play — 커버리지 0.70 → 1.00 | **0분** |
> | **S0c-2** | Run 04 × 7 시나리오 play — Run 05와 **쌍 비교** | **0분** |
> | **S0c-3** | 두 정책 중 기준선 채택 판정 + `feet_air_time` 원인 ⓐ/ⓑ 영상 판별 | **0분** |
> | S2 | 채택된 기준선 위에서 단일 변경 1회 | 36분 |
>
> S0c-3이 §3-3 정정이 남긴 질문(발을 안 드는 것인가, 한 발 지지를 못 하는 것인가)에
> 답하고, 그 답이 2번 후보의 채택 여부를 결정한다. **영상 외에 답할 수단이 없다**(F28).

> **⚠️ 260829 결과 — S0c 전량 완료. 기준선 = Run 05 (D18). 단, S0c-2는 판별 정보 0이었다.**
>
> **S0c-2 결과(F38)** — Run 04를 4 시나리오(H5·H6×2·H2)에 재생한 결과 **25/25 프레임 직립,
> 낙상 0** 으로 Run 05와 **완전 동률**이었다. 학습 로그의 `base_contact`도 0.3071 vs 0.3050으로
> 이미 동률이었으므로, **낙상 축은 두 정책을 가르지 못한다**는 것이 두 경로에서 확인됐다.
> 즉 S0c-2는 예정된 산출을 내지 못했고, 사용자 시간 약 6분을 쓰고 판별 정보 0을 반환했다.
> → **D19 신설: 새 측정을 편성하기 전에 기존 지표가 이미 그 답을 갖고 있는지 먼저 계산한다.**
>
> **추종 축을 영상으로 읽으려던 시도는 실패했다.** 지령(파랑)·실측(초록) 마커 길이 차로
> 추종 오차를 읽으려 했으나, 처음 커 보였던 차이는 **두 run의 진행 방향이 달라 생긴 카메라
> 투영 차이**였다. 같은 시나리오·같은 초기 프레임(H6_up, n=40~160)으로 통제하면 차이가
> 육안 판별되지 않는다. **마커 길이는 추종 지표가 아니다.**
>
> **그래서 기준선은 추종 수치가 정한다 (D18).** 예선에서 명령이 0이 아닌 가중은 **0.70**
> (H2 0.20 · H3 0.10 · H4 0.10 · H5 0.15 · H6 0.15)이고, 이 중 **회전이 걸리는 것은 H4 0.10뿐**이다.
>
> | 계산 | 식 | 결과 |
> |---|---|---|
> | H4를 반반(선/각)으로 | `0.65 × (+7.7) + 0.05 × (−26.3)` | **+3.69** (Run 05) |
> | H4를 **전부 각속도**로 | `0.60 × (+7.7) + 0.10 × (−26.3)` | **+1.99** (Run 05) |
> | 역전에 필요한 각속도 지분 | `7.7 / 26.3 = 0.293` → 0.70 중 0.1585 | **22.6%** 필요, 실제 **14.3%** |
>
> 어느 배분을 써도 Run 05가 이기고 경계도 아니다. 다만 근거는 **학습 지형 집계 성취율**이고
> [추론] 등급이며(F35), 예선 `tracking_score` 식은 미제공이다 — 새 측정이 충돌하면 재판정한다.
>
> **`feet_air_time` ⓐ/ⓑ 판별은 못 했다.** 25프레임 표본으로는 발 높이·한발 지지를 구분할 수
> 없다. 이 질문은 **미해소로 남긴다**(고속 구간 확대 관찰이 필요).
>
> **부수 확인(F39)** — Run 04 → Run 05의 변경은 **`track_lin_vel_xy_exp` 0.5 → 1.0 단 하나**였다
> (가중 9개 전수 대조). 나머지 8개 동일. 단 `experiment_history.csv`에 **Run 05 행이 없다** —
> 다음 학습 전에 사전 등록을 채워야 한다.

### 5-3. 순서와 비용

| 단계 | 내용 | GPU |
|---|---|---|
| S0 잔여 | H5·H6 | **0분** |
| S1 | 리포트 초안 + Go2 착수(병행) | **0분** |
| S2 | 위 후보 중 1개, 단일 변경 | 36분 × 2~3 |
| S3 | 최종 장기 학습 10,000~15,000 iter | 2~3시간 |

누적 GPU 벽시계 예상 **약 8시간**(현재 3.0 + S2 1.8 + S3 3.0).

---

## 6. 미해소 · 리스크

### 사용자에게 회수해야 할 것 (진행이 여기서 막힌다)

| # | 공백 | 왜 막히나 |
|---|---|---|
| **F** | **"학습 시간 50시간"이 GPU 벽시계인가 사람 시간인가** | S3의 iter 상한과 Go2 일정을 확정할 수 없다. 사람 시간 해석이면 서버 재업로드 의식(F25)이 예산에 직접 들어간다 |
| **G** | 남은 사람 시간 실측 | 소진량을 기록한 적이 없다 |
| **H** | **제출 횟수 제한** | F26 — `train.py`가 학습마다 자동 제출한다. **학습 횟수 = 제출 횟수.** 제한이 있으면 S2의 run 수가 직접 묶인다 |
| **C** | 예선 마감일 | 50시간 배치가 여기 걸린다. 회수처: 대시보드 |
| **D11** | **Go2 배우 신설 승인** | 승인 없이 대리 생성하지 않는다. 100점 축이 계속 닫혀 있다 |

### 기술 리스크

| 리스크 | 현황 |
|---|---|
| 평지 성능이 실제로 어떤지 정량으로 모른다 | §4-C. `play.py`가 수치를 안 찍는다(F28). 표본 8프레임이 전부 |
| `survival_rate`가 곱셈 항이다 | F5. 평지 생존이 낮으면 추종 점수를 아무리 올려도 곱해서 깎인다 |
| 자동 제출 | F26. `NO_AUTO_SUBMIT=1`을 빠뜨린 학습은 그대로 순위표에 올라간다(D14) |
| 서버 휘발 | F25. 매 세션 `bash _bootstrap/restore.sh`가 1번 단계(D13). 빠지면 BLOCKED |
| 한 기체만 제출 | F7. 100점 천장. 리포트를 만점 받아도 마찬가지 |

---

## 부록 — 재현 명령

이 문서의 §3-3 · §4-A 수치는 아래로 재계산된다.

```bash
cd /c/dev/NConnect/workspace/training/humanoid/reports/S0_260829
python - <<'PY'
import re
keys = ["Episode_Reward/track_lin_vel_xy_exp","Episode_Reward/track_ang_vel_z_exp",
        "Episode_Reward/feet_air_time","Curriculum/terrain_levels",
        "Metrics/base_velocity/error_vel_xy","Metrics/base_velocity/error_vel_yaw",
        "Episode_Termination/base_contact","Mean reward","Mean episode length"]
vals={k:[] for k in keys}
pat=re.compile(r'^\s*([A-Za-z_/ ]+?):\s*(-?\d+\.?\d*)\s*$')
for line in open("train_260829-05.log",encoding="utf-8",errors="replace"):
    m=pat.match(line.rstrip())
    if m and m.group(1).strip() in vals:
        vals[m.group(1).strip()].append(float(m.group(2)))
for k in keys:
    last=vals[k][-50:]
    print(f"{k:45s} last50avg={sum(last)/len(last):.4f}")
PY
```

기대 출력 (실측):

```
Episode_Reward/track_lin_vel_xy_exp           last50avg=0.6191
Episode_Reward/track_ang_vel_z_exp            last50avg=0.3207
Episode_Reward/feet_air_time                  last50avg=0.0063
Curriculum/terrain_levels                     last50avg=5.5399
Metrics/base_velocity/error_vel_xy            last50avg=0.3395
Metrics/base_velocity/error_vel_yaw           last50avg=0.9468
Episode_Termination/base_contact              last50avg=0.3050
Mean reward                                   last50avg=10.0870
Mean episode length                           last50avg=756.9486
```

생존시간 보정: `xy/step = 0.3395 × 500 / 756.9486 = 0.2243` · `yaw/step = 0.9468 × 500 / 756.9486 = 0.6255`
