---
target: NAVER CONNECT ROBOTICS CUP 2026 · 예선 통과
source: C:\dev\NConnect\test (강좌 HTML 19건, 본문 301,702자 실측)
date: 260829
status: analysis-only — 계획 수립 전 입력
---

# 강좌 분석 — NAVER CONNECT ROBOTICS GUIDE BOOK

## 1. 입력 실측

로컬 사본 19개 파일. **목차상 22강(00~21) 중 3건이 로컬에 없다.**

| 챕터 | 강 | 로컬 |
|---|---|---|
| 01 탄생 — 시뮬레이션 환경 접속 | 00 들어가며 · 01 강화학습과 시뮬레이션 기초 · 02 Isaac Sim 처음 열기 · 03 중력 만져보기 · 04 로봇 불러오기 | **00·01 없음**, 02~04 있음 |
| 02 각성 — 로봇을 움직이고 감각 달기 | 05 관절 · 06 지형 · 07 카메라 · 08 라이다 · 09 키보드 조작 | 5/5 |
| 03 진화 — 스스로 걷는 법 | 10 Isaac Lab·강화학습 원리 · **11 로봇 학습의 목표** · 12 학습 준비 · 13 학습 하기 · 14 보상 함수 설계·조정 · 15 학습 관찰·진단 · 16 policy.pt 준비 | **11 없음**, 나머지 6/7 |
| 04 초월 — 자연어 명령 | 17 자연어→로봇 명령 · 18 믿을 수 있는 명령 · 19 생각하는 로봇 · 20 여기까지 · 21 부록·본선 연습 | 5/5 |

**11강 결손이 가장 아프다.** 제목이 「로봇 학습의 목표」이고 위치가 진화 챕터 한복판이라,
예선이 요구하는 성능 목표가 적혀 있을 가능성이 가장 높은 강이다. 추측으로 메우지 않는다.

## 2. 예선이 실제로 요구하는 것 (16강 실측)

```
학습 → model_best.pt 자동 선정 → play.py 실행 → policy.pt 생성 → 예선 제출
```

제출처는 `robotics.connect.or.kr` 대시보드. 제출물 4종:

| # | 항목 | 출처 |
|---|---|---|
| 1 | 로봇 유형 선택 — **H1(휴머노이드) 또는 Go2(사족)** | 대시보드에서 택1 |
| 2 | `policy.pt` | `play.py` 실행 산출. actor 가중치만 |
| 3 | `env.yaml` | 학습 산출 |
| 4 | **전략 및 변경 사항 서술** (보상 설계 의도, 바꾼 항목) | 직접 작성 |

학습 자동 제출 로그는 `model_best.pt + env.yaml + report.html`을 묶어 보내고,
로그 문구는 `★ model_best.pt (예선 채점용 RSL-RL checkpoint)`다.

**.pt 3종 구분 (16강 표)**

| 파일 | 내용 | 쓰임 |
|---|---|---|
| `model_XXXXX.pt` | 가중치 + 옵티마이저 + iteration | N번마다 자동 체크포인트 |
| `model_best.pt` | 최고 성능 체크포인트 | **대회 시스템이 자동 선정**, resume 가능 |
| `policy.pt` | actor 가중치만 | `play.py` 산출. 예선 제출 + 초월 챕터 |

체크포인트를 사람이 고를 필요가 없다 — 시스템이 `model_best.pt`를 자동으로 뽑는다.

## 3. 범위 판정 — 예선에 필요한 것은 챕터 3뿐이다

예선 산출물 `policy.pt`는 **16강(진화 챕터의 마지막 강)** 에서 완성된다.

| 챕터 | 예선 기여 |
|---|---|
| 01 탄생 | **전제** — 서버·Isaac Sim 접속. 없으면 아무것도 못 함 |
| 02 각성 (05~09) | **예선에 직접 기여 없음.** 관절·지형·카메라·라이다·키보드는 감각/조작이지 정책 학습이 아니다 |
| 03 진화 (10~16) | **예선 그 자체** |
| 04 초월 (17~21) | **본선용.** LLM·전략문은 예선 제출물에 포함되지 않는다 |

50시간 예산에서 각성 챕터와 초월 챕터는 예선 통과의 필수 경로가 아니다.
단 06강(지형)은 험지 정책을 노릴 경우 간접 연결이 있다(21강 "험지를 노리기로 했다면
사전에 험지에서 많이 학습시켜 두면 된다").

## 4. 시간을 먹는 것 (13강 실측)

- **본격 학습 = 몇십 분 ~ 몇 시간** (`--num_envs 4096 --max_iterations 1000 --headless`)
- 그 전에 **smoke test 10 iter 필수** — "처음부터 몇 시간짜리 학습을 실행하면 런타임 에러나
  오타 하나 때문에 학습이 진작 중단돼 있던 것을 뒤늦게 발견"
- **tmux 안에서 실행** — 터미널을 닫거나 접속이 끊겨도 학습이 계속 돈다

> **일정 설계의 핵심**: 학습 시간은 **벽시계 시간이지 사람 시간이 아니다.**
> tmux로 걸어두면 사람은 그동안 다른 것을 한다. 50시간을 직렬로 계산하면 안 된다.

**서버 휘발성** — "클라우드 서버(GPU 박스)를 종료하면 그 서버에 있던 학습 내용은 모두 사라진다."
`exported/`는 반드시 로컬로 내려받는다. 16강에 서버가 꺼진 뒤의 복구 경로(대시보드 다운로드 →
`exported/` 재생성 → `model_best.pt` 업로드 → `play.py`)가 별도로 있다.

## 5. 로봇 선택 — 이 분석의 최대 소득

12강에 결정적 실측이 있다.

> **기본 보상으로 1000 iter 학습한 H1은 "제대로 서 있지 못하고 계속 뒤로 넘어진다."**
> 안정적으로 서면서 걷게 만드는 것은 **14강 보상 조정의 몫**으로 넘어간다.

즉 **H1은 기본값 + 1000 iter로 제출 가능한 정책이 나오지 않는다.** 보상 함수 9종
(`track_lin_vel_xy_exp 0.5` · `track_ang_vel_z_exp 0.5` · `feet_air_time 0.2` ·
`flat_orientation_l2 -1.5` · `termination_penalty -5.0` · `joint_deviation_hip -0.2` ·
`joint_deviation_torso -0.1` · `ang_vel_xy_l2 -0.05` · `action_rate_l2 -0.0005`)을
튜닝하는 반복 사이클이 추가로 필요하다.

Go2(사족)는 같은 파이프라인을 `Quadruped-v0`로 돌리며, 강좌는 Go2를 "스스로 해보기"로 배치했다.
사족보행은 정적 안정성이 있어 휴머노이드보다 기본 보상으로 걷게 만들기가 쉽다.

→ **50시간·예선 통과가 목적이면 Go2가 기본 선택이다.** H1은 보상 튜닝 반복을 감당할
예산이 있을 때의 선택지다. (본선 관점에서는 21강이 "사족=빠름 / 휴머노이드=험지"로 적어,
H1이 박스언덕을 오를 수 있는 유일한 기체라는 상반된 이점이 있다.)

## 6. 본선 규칙 (예선 목표가 아니라 방향 참고용, 21강)

| 항목 | 내용 |
|---|---|
| 방식 | 1:1 토너먼트, 같은 맵 동시 큐브 수집 |
| 시간 | 180초 |
| 점수 | 큐브 **+100** / 낙상 **−50** / 차량 충돌 **−30** |
| 입력 | 정책(`.pt`) + 전략문(`strategy.txt`, 300자 이내) |
| 지형 표준 용어 | 평지 · 단상 · 컨테이너 · 박스언덕(40cm, 휴머노이드만) · 박스미로 · 자동차 |

낙상 −50이 큐브 +100의 절반이므로 **넘어지지 않는 정책의 가치가 매우 크다.**
이것은 예선 단계의 보상 설계(`termination_penalty -5.0`, `flat_orientation_l2 -1.5`)와
같은 방향이다 — 예선을 안정성 위주로 학습시키면 본선에도 그대로 쓰인다.

## 7. 확인되지 않은 것 (추측하지 않고 남긴다)

| # | 미확인 | 왜 중요한가 |
|---|---|---|
| U1 | **예선 채점 기준·통과선** | 강좌 19건에 `심사`·`규정` 0건, 통과 임계값 서술 없음. 목표 성능을 모르면 학습을 언제 멈출지 정할 수 없다 |
| U2 | **예선 마감일** | `마감` 0건. 50시간을 어디에 배치할지가 이것에 걸린다 |
| U3 | **11강 「로봇 학습의 목표」** | 로컬 결손. U1의 답이 여기 있을 가능성이 가장 높다 |
| U4 | 00·01강 | 로컬 결손. 전제 지식이라 영향은 작다 |
| U5 | 서버 사용 시간/횟수 제한 | 학습이 벽시계 시간을 먹으므로 제한이 있으면 계획이 바뀐다 |

U1~U3은 **대시보드(`robotics.connect.or.kr`)와 11강 원문에서 회수 가능**하다.

## 8. 계획 수립에 넘길 확정 사실

1. 예선 산출물은 `policy.pt` + `env.yaml` + 전략 서술이며 16강에서 완성된다.
2. 필수 경로는 **챕터 01(전제) → 챕터 03(10~16)**. 챕터 02·04는 예선 비필수.
3. 학습은 벽시계 시간이고 tmux로 비동기다 — 사람 시간과 겹칠 수 있다.
4. smoke test(10 iter) 없이 장시간 학습을 걸지 않는다.
5. `exported/`는 서버 종료 전 반드시 로컬 다운로드.
6. **H1은 기본 보상 1000 iter로 걷지 못한다(강좌 실측).** Go2가 저비용 경로.
7. 낙상 페널티가 크므로 안정성 우선 보상 설계가 예선·본선 양쪽에 유리하다.
