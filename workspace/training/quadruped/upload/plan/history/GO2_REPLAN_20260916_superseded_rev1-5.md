# Go2 재계획 — 2026-09-16

이전 판의 "사용자 결정 대기" 목록(G-A036 비권고 · arm3 단독 · `track` 1.25/1.30/1.35)은 **폐기한다.**
그 권고들은 아래 §1의 사실이 확인되기 전에 쓰였다. 이 문서가 대체한다.

확신도 표시: [확인] 산출물·코드에서 직접 읽음 · [추정] 계산 또는 추론 · [모름].

## 1. 이번 정비에서 바뀐 사실

| # | 사실 | 확신도 | 근거 |
|---|---|---|---|
| F1 | **G-A035 패키지는 서버에서 시작되지 않는다.** 안내문 명령이 러너 외곽 경로를 타고, 외곽 경로는 `server_run_go2_candidate_staged.sh --inner`를 띄우는데 ZIP에 그 파일이 없다. `[STARTED]`를 찍고 tmux는 즉시 죽는다. 외곽 경로는 G-A033 때도 캠페인 러너가 `--inner`로 우회해서 서버에서 실행된 적이 없다 | [확인] | ZIP `516076b7…` 안 `.sh`는 `iter_pinned.sh` 하나 · 러너 173행 · 캠페인 러너 224행 |
| F2 | **G-A035가 시작돼도 후보를 자기 자신과 비교한다.** 기준선 라벨이 `candidate`라 서버 게이트와 로컬 검증기가 후보 폴더를 "같은 회차에서 재측정한 기준선"으로 읽는다 | [확인] | 스펙 `baseline.label` · 빌더 116행 · 게이트 163행 · 검증기 241행 |
| F2b | **G-A035 스펙에 `evaluation.sentinel_tolerance`가 없었다.** 서버 게이트가 표지 비교 첫 줄에서 KeyError → 판정 JSON 없음 → UNDECIDED → 2단계가 돌지 않는다. 로컬 검증기는 G-A035를 인자로 받지도 않았다 | [확인] | 스펙 키 비교 · `verify_go2_basic_motion_harvest.py` argparse |
| F2c | 계획 §15-5의 "러너가 `model_900.pt` SHA를 기록한다(공짜 부수 측정)"는 거짓이다. 러너는 iter 1450과 finalize 선택본만 남긴다 | [확인] | 러너 `snapshot_checkpoint` |
| F3 | G-A035 안내문의 "러너가 `TARGET_PASS_FULL_STAGE_REQUIRED`를 출력한다"는 거짓이다. 그 문자열은 로컬 검증기만 낸다. 안내문은 명령 두 개를 요구한다(한 명령 규칙 위반) | [확인] | 러너 grep 0건 · 빌더 341행 |
| F4 | **학습 15회 중 12회는 70점 축에 점이 없다.** 69case·현 계측(v2) 점수는 Pilot-01·A017·G-A033 셋뿐이다. 나머지는 7case tier1(11) · 10case(2) · 낙상 미검출 계측 69case(3) | [확인] | `reports/runs/LEDGER.csv` |
| F5 | 그래서 G-A036(재평가)을 "철회한 §8에 봉사한다"며 비권고한 것은 틀렸다. 그 회차는 12개 정책을 처음으로 70점 축에 올린다 | [확인] | F4 |
| F6 | 재평가 빌더의 기준점 `4.2503·4.7087`은 iter 999 값이었다(주석은 "iter900"). @900 실측은 3.4268·4.4937. 산출물에서 읽게 고쳤고 arm 순서가 바뀌었다(A033@700: 3→6번, A031: 7→10번) | [확인] | `reports/runs/TERRAIN_AT_PIN.csv` · 새 ZIP `e660411c…` 재빌드 2회 동일 |
| F7 | 재평가 arm은 평가 iteration이 700·800·900·999로 섞여 있다. arm끼리의 점수 차는 보상 차이와 학습량 차이가 섞인다 | [확인] | `arms.json` |
| F8 | 산문 숫자 게이트는 소수 2자리 이하에서 사실상 검사하지 않는다(무작위 숫자 통과율 1자리 98.9% · 2자리 55.0%). 측정 문서 숫자의 약 70%가 그 자릿수다 | [확인] | `tools/go2_claim_check.py` 실행 출력 |
| F9 | 학습 흔들림 0(재학습 2쌍 불일치 0) — n=1은 정확히 n=1이다. 일반화는 사지 못한다 | [확인] | `GO2_NOW.md` §0 [회차 실측] |

## 2. 계획

순서는 **의존**으로 정했다. 비용 순이 아니다.

### P0 — G-A035 패키지 수리 (로컬) — **완료 2026-09-16**

결과: `upload/G-A035/current/GO2_G_A035_a033_iter1500_one_command.zip` — 아래 1~4 전부 반영, 재빌드 바이트 동일.
관문 `tools/test_go2_training_length_campaign_contract.py` 12건: 서버 게이트·로컬 검증기를 가짜 수확물로 실제 실행한다.
v1 결함(라벨 `candidate`, `sentinel_tolerance` 누락)을 심으면 깨지고, 무수정 대조군은 통과한다.
러너·게이트는 G-A033 캠페인 ZIP 안의 것과 바이트 동일하다(서버 완주한 코드만 싣는다).

1. 기준선 라벨을 이 회차 이름(`g_a033`)으로 바꾸고, 저장본에서 그 기준선이 들어 있는 폴더 이름은 따로 적었다(`stored_label: candidate`). **서버 게이트는 수정하지 않았다** — 배포된 A031/A032 페어 패키지에 같은 파일이 실려 있다. 대신 저장 요약을 `stored_baseline/evaluation/g_a033`으로 싣는다. 로컬 검증기(패키지에 안 실림)만 `stored_label`을 읽고, 없으면 `label`을 쓴다. A031·A032·A033 계약 72건 통과.
2. G-A033이 서버에서 실제로 완주한 캠페인 러너(`server_run_go2_campaign.sh`, 무수정)로 감싼다. 1단계 → 서버 게이트 → 통과 시 2단계가 자동이다. **파일 하나·명령 하나·결과 하나.**
3. 기계 관문 두 개를 추가한다. (a) 출하되는 러너가 `bash X`로 부르는 모든 스크립트가 ZIP 안에 있다. (b) 기준선 라벨은 `candidate`일 수 없다.
4. 기존 G-A035 ZIP `516076b7…`은 `history/`로 내리고 **폐기**로 표시한다. 서버에 올린 적 없다.

### P1 — G-A035 서버 실행 (사용자 결정)

- 질문: 1000 iter에서 멈춘 커리큘럼(10단계 중 @900 3.39~4.50)을 1500까지 올리면 도달 레벨이 오르는가, 멈추는가.
- 검출력: 1차 관측량은 도달 레벨이고 학습 흔들림이 0이라 잡음에 묻히지 않는다 [확인 F9]. 70점 축 변화는 waiver(계획 §15-5).
- 비용 [추정]: 1단계 약 115분, 통과 시 2단계 약 55분.
- ~~**권고: 실행.**~~ **2026-09-16 3차 정정: 비권고(§5).** 70점 축 기대 이득이 미추정이다. 그 근거였던 §13-4는 기준선이 섞인 표였다(R2).

### P2 — G-A036 재평가 (사용자 결정, P1 결과 뒤)

- 질문: 이미 학습된 15개 정책이 70점 축에서 몇 점인가. 점 3개 → 18개.
- 왜 P1 뒤인가 [추정]: 1500 iter가 순위를 바꾸면 1000 iter 체크포인트로 만든 다이얼 모델은 다음 후보 선택에 쓰이지 못한다. P1이 순위를 안 바꾸면 G-A036의 가치는 그대로다. 반대 방향 의존은 약하다.
- 비용 [추정]: 6.8시간, `GO2_REEVAL_BUDGET_MIN`으로 끊을 수 있고 먼 점부터 채워진다.
- 한계: F7(iteration 혼재) · seed 42 하나.
- ~~**권고: P1 결과를 본 뒤 실행.** 결정에 직접 닿는 arm은 A031이다~~ **2026-09-16 정정(§4 R1·R3):** A031은 A017(`track` 1.4) 위의 변경이라 현 기준선 G-A033(`track` 1.5)의 `feet_air_time` 판단에 직접 닿지 않는다. 재평가 15개 중 현 기준선 위에서 학습된 정책은 0개다. **권고 강등: P1 뒤 재검토(자동 실행 권고 아님).**

### 보류 · 폐기

| 항목 | 판단 | 이유 |
|---|---|---|
| `track` 1.25 / 1.30 / 1.35 | **보류** | "문턱" 주장이 7case tier1 점수 위에 서 있다. G-A036이 70점 축 점을 주기 전에는 문턱의 위치를 70점 축에서 말할 수 없다. R-6 보상 변경이라 실행 시 사용자 결정 |
| arm3 단독(A033@700, 27분) | **폐기** | G-A035가 같은 질문("학습을 더 하면 오르는가")에 학습량만 바뀐 설계로 답한다. @700 대 @900은 커리큘럼 깊이와 학습량이 섞인다 |
| G-A030 (`flat_orientation` −1.0) | **폐기 권고** | 기준 arm이 A017이라 현 기준선과 맞지 않고, MASTER §1-b D-1이 방향 자체에 반대한다. 정식 폐기는 사용자 결정 |

### 배경 작업 (로컬, 서버와 무관)

- `GO2_PROJECT_STATE.md` 51 · MASTER 37건의 근거 없는 소수. 맹점 숫자(F8)는 파일별 상한으로 묶였다(`BLIND_CEILING`).
- `experiment_history.csv` 예측 원장 16행 미기록.

## 4. 재검토 — 사실·Isaac Lab·종합 보고서 대조 (2026-09-16 2차)

| # | 발견 | 확신도 | 근거 |
|---|---|---|---|
| R1 | 동결 기준선은 **G-A033**이다(G-D-BASELINE-A033-20260916). 사용자 답변에서 "기준선 하나(A017 계열)"라고 쓴 것은 틀렸다. 같은 답의 "A017 계열 (1.4)" 묶음에 `track` 1.5인 G-A033을 넣은 것도 틀렸다 | [확인] | `GO2_NOW.md` §1 · LEDGER `track_lin_vel_xy_exp` 열 |
| R2 | 계획 §13-4 여력표는 제목이 "A017 기준선"이고 G3 행이 A017 값(4.97)이다. G4·G7 행은 G-A033 값이다. **기준선이 섞인 표다.** 현 기준선 G3는 4.227, 손실 9.77(§15-1) | [확인] | 종합 §7 시나리오표 |
| R3 | 재평가(P2) 15개 정책 중 G-A033 위에서 학습된 것은 0개다. P2는 "다이얼→70점" 고리를 추정하는 데 쓰이지만, 현 기준선에서 다음 다이얼을 고르는 근거는 되지 않는다 | [확인] | `arms.json` · LEDGER |
| R4 | Isaac Lab v2.3.1 Go2 rough 대비 G-A033 reward는 10항 중 9항 일치, 이탈은 `feet_air_time` 0.2(IL 0.01) 하나다. P1(1500 iter)은 학습 길이를 IL 값에 맞춘다. 남는 reward 밖 차이는 명령 범위(x −1~2, y ±0.6)로 과제 env가 정한 값이다 | [확인] | `tools/go2_external_reference_diff.py` G-A033 `env.yaml` 실행 출력 · MASTER §1-b A·B |
| R5 | `feet_air_time` 0.01의 유일한 측정(A031)은 A017 위, 1000 iter, 표적 10case였다. G-A033(`track` 1.5) 위에서의 효과는 측정된 적이 없다 | [확인] | MASTER §1-b D · 계획 §14-2 |
| R6 | 테스트는 이미 G-A033을 본다: `test_go2_canonical_consistency.py`의 `BASELINE_TRAINING`은 G-A033 경로다. 남은 A017 참조는 A027 평가 경로 등 과거 기록용이다 | [확인] | 같은 파일 22~24행 |

**권고 갱신**
- ~~P1 G-A035: **유지(실행 권고).** 기준선이 G-A033이고, IL 대비 남은 reward 밖 차이 중 학습 길이를 맞춘다.~~ → §5에서 비권고로 정정. "IL 값에 맞춘다"는 MASTER §1-b 규칙 3이 금지한 근거다.
- P2 G-A036: **강등.** R3. P1 결과를 본 뒤 필요성을 다시 판단한다.
- P1 다음 후보 [추정]: G-A033(또는 P1 결과에 따라 1500 iter) + `feet_air_time` 0.2→0.01. 적용하면 reward 10항이 IL Go2 rough와 전부 같아진다. **검출력은 미추정**이다(R5: 현 기준선 위 측정 0건). 그래서 권고가 아니라 설계 대상이며, 사양에 waiver 또는 검출력 근거가 있어야 한다. → §5에서 비권고로 정정(waiver로 권고하지 않는다).

## 5. 권고 전면 갱신 — 기준선 G-A033 기준, 규칙 적용 (2026-09-16 3차)

적용 규칙: **70점 축 기대 이득이 검출 한계 2.53/70보다 크다는 추정이 없으면 권하지 않는다**(no-low-detectability). waiver는 권고 근거가 될 수 없다. "Isaac Lab 값이니 옳다"도 근거가 아니다(MASTER §1-b 규칙 3).

| 후보 | 70점 기대 이득 | 판정 | 이유 |
|---|---|---|---|
| P1 G-A035 (1000→1500 iter) | [모름] | **비권고** | 1차 관측량이 지형 레벨이다. 레벨→70점 환산은 점 3개라 추정 불가(F4). "아직 오르는 중"은 이미 산출물에 있다: G-A033 레벨 @800 4.1259 → @900 4.4937 → @999 4.7087 (`TERRAIN_AT_PIN.csv`) |
| P2 G-A036 (재평가 15개) | 해당 없음(점수 이득 회차 아님) | **비권고** | 현 기준선 위 정책 0개(R3) · iteration 혼재(F7) |
| G-A033 + `feet_air_time` 0.01 | [모름] | **비권고** | 현 기준선 위 측정 0건(R5). 유일한 측정 A031은 A017 위·10case·표적 −.022 |
| `track` 1.25~1.35 | [모름] | **비권고** | A017 계열 문턱 질문이다. 현 기준선은 1.5 |
| G-A030 `flat_orientation` −1.0 | [모름] | **비권고(폐기 권고 유지)** | 기준 arm A017 · MASTER §1-b D-1 반대 |

**현재 권고할 서버 회차는 0개다.** 패키지 G-A035는 보존한다(실행 가능성은 검증됨, 권고와 별개).
서버를 쓴다면 그것은 "점수 이득 미추정 탐색"을 사는 사용자 결정이며, 이 문서는 그것을 권하지 않는다.

## 6. §5 재점검 — 판정은 유지, 이유 셋이 틀렸다 (2026-09-16 4차)

| # | §5(및 사용자 답변)에서 쓴 것 | 사실 | 확신도 · 근거 |
|---|---|---|---|
| C1 | "P1이 재는 것은 지형 레벨" | P1은 통과 시 **69case 전수, 평가 seed 3개**로 70점 축을 잰다(iter 1450) | [확인] 스펙 `evaluation.case_count` 69 · `seeds` 101/202/303 |
| C2 | "'후보 0개' 근거는 기준선이 섞인 §13-4뿐이라 무효" | 계획 §15-1은 G-A033 값(G5 10.46 · G3 9.77 · 두 case 합 20.2)으로 다시 계산했다. §13-4의 섞임은 사실이지만 이 결론을 무효로 만들지 않는다 | [확인] 계획 §15-1 |
| C3 | "'IL 길이에 맞춘다'는 규칙 3 위반" | 재계획 한 줄 요약은 그렇게 읽히지만, 계획 §15-2는 원인 진단이다(0.15 m 계단을 학습한 회차가 없음). 규칙 2의 정당한 사용이다 | [확인] MASTER §1-b 규칙 2·3 · 계획 §15-2 |
| C4 | "레벨이 오르는 중이라는 건 이미 알아 새 정보가 적다" | 모르는 것은 **어디서 멈추는지**와 **`stairs_15_down` 추종이 풀리는지**다. 과장이었다 | [추정] |
| C5 | P2 비권고 이유 "점수 이득 회차가 아님" | 측정 회차라 검출력 규칙을 잘못 적용했다. 맞는 이유는 R3(현 기준선 위 정책 0개)·F7(iteration 혼재)이다 | [확인] |
| C6 | 비권고 후 재설계 없음 | 규칙은 "비권고 + 재설계"다. 재설계를 하지 않았다 | [확인] 규칙 원문 |

**판정 유지:** P1의 70점 축 기대 이득은 여전히 [모름]이다. 여력 상한(G5 생존만 +1.55, G3 9.77)은 민감도지 추정치가 아니다(§13-3 3항). 그래서 규칙상 **점수 이득 회차로는 권하지 않는다.**

**재설계 결과 (C6):** 현재 데이터(70점 축 정책 3개)로는 어떤 후보도 기대 이득을 추정할 수 없다. 규칙을 그대로 두면 **모든 서버 회차가 영구 차단된다.** 이것은 규칙과 목표의 충돌이고, 풀려면 사용자 결정이 필요하다:
- (가) 탐색 회차를 허용하지 않는다 → 서버 0회, 제출은 G-A033.
- (나) "점수 이득 미추정" 탐색 회차를 **1회에 한해** 명시적으로 산다 → 후보 중 **P1이 1순위** [추정]: 사용자 승인된 비보상 변경 · 가장 큰 구멍 20.2/70의 원인(학습 부족인가, 그 지형을 못 걷는가)을 겨냥 · 학습 흔들림 0이라 한 번에 답 · 패키지 종단 검증 완료.

기준선 G-A033 자체 [확인]: 평가 체크포인트 SHA `ccd60e19…6044` = `model_best.pt` = `model_iter900.pt` = `identity.json` = `GO2_NOW.md` 검사 필드. 선택 근거의 약점: 총점 +2.764의 95% 구간 하한 +0.047, 2단계 FAIL 기록 유지, `rough_lateral` 생존 −.09.

## 7. G-A033 기준 보고서 재검토 — 계획의 구멍 (2026-09-16 5차)

방법: 결과 보고서 19~24·LEDGER·원산출물(`_keep`)과 Isaac Lab v2.3.1 원 코드(`terrain_generator.py`·`terrain_importer.py`·`curriculums.py`·Go2 `rsl_rl_ppo_cfg.py`)를 직접 대조했다.

| # | 구멍 | 확신도 | 근거 |
|---|---|---|---|
| H1 | **"학습 흔들림 0"은 seed가 같은 재학습의 결정론이지, 학습 seed 간 흔들림이 아니다.** 재학습 2쌍은 둘 다 seed 42였다(checkpoint SHA 동일). `_keep` 전 회차의 학습 seed는 42 하나다. seed 간 흔들림 표본은 0건이다. P1의 "잡음에 묻히지 않는다"(§2)와 §6 (나)의 "한 번에 답"은 이 오독 위에 서 있었다 | [확인] | A010 v1/v2 `model_900.pt` `dc131ae5…`, A013/A025 `676cc1cb…`(학습 시각 다름) · 계획 §12-6 (1) |
| H2 | **"0.15 m 계단을 학습한 회차가 없다"(계획 §14·§15-2)는 성립하지 않는다.** 로그의 terrain 값은 전체 로봇의 **평균** 레벨이다. 시작 레벨은 0~`max_init_terrain_level` 5에서 균등하게 뽑히고, 5단 계단 높이는 0.14~0.158 m다. 최상단을 풀면 무작위 레벨로 보낸다. 평균 4.49는 "최고 0.14 m"가 아니다 | [확인] 코드·설정 / 로봇별 분포 [모름] | IL `curriculums.py` 반환 `torch.mean(terrain_levels)` · `terrain_importer.py` `randint(0, max_init_level+1)` · `terrain_generator.py` `difficulty=(row+U)/num_rows` · G-A033 `env.yaml` |
| H3 | **P1의 1차 관측량(평균 레벨)은 P1의 질문에 답하지 못한다.** 평균은 계단·경사·박스·험지 5종 열을 합친 값이다. 계단 열이 어디까지 갔는지 분리할 수 없다. "학습 부족인가, 못 걷는가"는 평균 레벨로 갈리지 않는다 | [확인] | H2 · `sub_terrains` 5종 비율 |
| H4 | **G-A035 출하 안내문에 거짓 약속 둘.** ① "`model_900.pt` SHA로 결정론 확인"(F2c에서 이미 거짓으로 확인된 문장이 ZIP 안 README·experiment.json에 남아 있다). ② "회차 간 차이 1.1이라 잡음에 묻히지 않는다" — 1.1은 보상이 다른 회차 사이의 차이지 잡음이 아니다 | [확인] | `current` ZIP 안 `go2_g_a035/README.txt`·`experiment.json` `notes.replicate_check` |
| H5 | **기준선 승급 +2.764는 G4 한 시나리오(+3.65)에서 나왔다.** 나머지 G1·G2·G3·G6은 합 −1.47이다. 게다가 비교 시점 iter 900에서 A017은 커리큘럼이 급상승하는 중이었다(@800 2.49 → @900 3.43 → @999 4.25). G-A033은 완만했다(4.13 → 4.49 → 4.71). 이득이 "더 나은 정책"인지 "커리큘럼이 더 빨랐을 뿐"인지 갈리지 않는다. P1은 G-A033만 늘리므로 이것을 가리지 못한다 | [확인] 수치 / 해석 [추정] | `BASELINE_MARGIN.csv` · `TERRAIN_AT_PIN.csv` · `LOCAL_VERIFY_G_A033.json` verdict FAIL(비열등 위반 3건) |
| H6 | **모든 판단은 iter 900~1000에서 내렸다. 배포 파일은 제출본을 5000~15000 iter로 안내한다.** 배포 `agent_cfg.py`의 기본값도 5000이다. Go2의 iteration-대-점수 자료는 0건이다(설계검토 W21, 5k 패키지 G-A011 미실행). (가) "G-A033(iter 900) 제출"도 이 구멍 위에 있다 | [확인] | `quadruped_rewards.py` 추천 순서 4번 · `go2_task/agent_cfg.py:20` · `GO2_CAMPAIGN_SCHEDULE.md:285` |
| H7 | **Isaac Lab 대비 reward 밖 차이는 명령 범위만이 아니다(R4 불완전).** `entropy_coef` 0.008 vs IL 0.01. 학습 길이 1000 vs 1500도 다르다. MASTER §1-b 표 B에 `entropy_coef` 행이 없다 | [확인] | G-A033 `params/agent.yaml` · IL v2.3.1 `UnitreeGo2RoughPPORunnerCfg` |
| H8 | 보고서 22(G-A033)에 "69 case 전수가 아니다 · 같은 축에 놓을 수 없다" 경고가 붙어 있다. 후보는 69 case다. 생성기가 **어느 arm이든** 부분 평가면 경고를 붙이고, arm 이름을 적지 않는다(표지 arm 5 case 때문) | [확인] | `tools/build_go2_run_reports.py` 78~81행 |
| H9 | LEDGER 2행 `pilot` arm의 reward 열이 Default 값(1.0 · 0.01 · −3)으로 적혀 있다. Pilot은 1.2 · 0.2 · −2 · −0.05다 | [확인] | `LEDGER.csv` · 보고서 10 표 |

**판정에 미치는 영향**
- 서버 권고 0개는 **유지**한다. H1~H6은 이득 추정을 더 어렵게 할 뿐, 쉽게 만들지 않는다.
- §6 (나)의 "P1 1순위" 근거 넷 중 둘이 무너졌다. ① 흔들림 0 → H1. ② 20.2 구멍의 원인을 가린다 → H2·H3. 남는 근거는 비보상 변경·사용자 승인, 그리고 판독기 실행 검증이다(단, 안내문 거짓 H4). **그래서 "P1 1순위"를 철회한다. 순위는 [모름]이다.**
- (가)도 공짜가 아니다: H5·H6.
- 기록만 했다. 문서 수정(계획 §14·§15-2, MASTER 표 B, 생성기, LEDGER, ZIP 안내문)은 하지 않았다. ZIP 수정은 재빌드와 종단 테스트가 필요하다.

## 3. 이 계획이 보장하지 않는 것

- P1·P2 어느 것도 점수 상승을 보장하지 않는다. 보장되는 것은 관측뿐이다.
- 잔여 GPU "약 30h"는 사용자 보고이고 실측이 아니다 [모름]. P1+P2 합계는 약 9.6시간이다 [추정].
