# 기획자 브리핑 — H1 실험 누적 요약

## 0. 예선 기준 현재 위치

- [예선 목표][사실] 이번 갱신은 시뮬레이션 70점 축의 H1~H7 근사 진단 증거와 설계 의도 20점·리포트 품질 10점 축의 문서 정합성을 확보한다.
- [현재 단계][사실] 단계 0/6 — 증거·artifact 정합성이다.
- [확보][사실] Run 05 학습 로그, 31태그×3000 iter tfevents, Run 05 `env.yaml`, S0b·S0c·S0d mp4 14개와 log 14개, Run 05 best checkpoint를 보존했다.
- [미확보][미측정] 같은 checkpoint를 사용한 정량 지표·영상 대응, H1~H7 공식 evaluator 수치, 권한 있는 기획자가 사전등록한 다음 run이 없다.
- [이번 테스트][사실] 이번 작업은 새 학습이 아니라 Run 05·S0 증거를 기획 문서와 원장에 맞추고, 다음 학습 전에 막아야 할 정책-지표 불일치를 드러내는 문서 게이트다.
- [흐름][추론] Run 05·S0 완료 → **현재 증거·원장 정합성** → 통과 시 새 수렴 pilot 사전등록 → 실패 시 checkpoint·지표 대응부터 복구 → H1~H7 최종 평가·문서 → 제출이다.
- [지금 할 일][사실] 서버 학습은 실행하지 않고, 다음 기획자는 이 브리핑의 §5 미측정과 §7 handoff를 입력으로 새 run을 사전등록한다.
- [보장하지 않음][미측정] 이번 문서 갱신만으로 H1~H7 공식 점수, 예선 통과 가능성, 최종 제출 가능 여부를 말할 수 없다.

> [사실] 이 문서는 `humanoid-report-writer`가 소유하며, 다음 실험 기획자가 읽는 누적 요약본이다.
> [사실] 개별 run 분석의 정본은 `train_<RUN_ID>_add.md`·`eval_<RUN_ID>_add.md`, 기계 대사의 정본은 `experiment_history.csv`다.
> [사실] 이 문서는 새 예선 점수나 통과 가능성을 만들지 않고, 원문 증거에서 확인된 값·한계·절차 상태만 압축한다.

- [사실] 최종 갱신은 260830이며, Run 05와 S0b·S0c·S0d 및 F40~F42를 반영했다.
- [사실] 완료된 학습 run은 5개이며, 다음 검증의 작업 기준선은 **Run 05**다.
- [추론] Run 05의 예선 우위는 미공개 공식 evaluator와 수렴 정책 비교가 없어 확정할 수 없다.

---

## 1. 현재 기준선 — 다음 실험이 비교할 대상

### Run 05 (`train_260829-05`)

| 지표 | Run 05 마지막 500 iter 평균 | 좋은 방향 | 근거와 한계 |
|---|---:|:---:|---|
| `error_vel_xy` | **0.3385** | ↓ | [사실] `tools/tfcurve.py window Run04 Run05`; 혼합 커리큘럼 지형의 학습 통계다. |
| `error_vel_yaw` | **0.9613** | ↓ | [사실] 마지막 1000 iter 기울기는 **-0.10627/1k**로 하강 중이다. |
| `base_contact` | **0.3020** | ↓ | [사실] 혼합 커리큘럼 지형의 종료 집계이며 예선 평지 생존율이 아니다. |
| `terrain_levels` | **5.5646 / 9.0** | ↑ | [사실] 미포화 상태이며 예선 점수축으로 사용하지 않는다. |
| `Policy/mean_std` | **1.6641** | — | [사실] 마지막 1000 iter 기울기는 **-0.01345/1k**다. |
| `Train/mean_episode_length` | **762.6120** | — | [사실] `error_vel_*` 비교의 생존시간 교란을 함께 확인하는 보조값이다. |

- [사실] 고정 비교 조건은 `task=Humanoid-v0`, `seed=42`, `num_envs=4096`, `max_iterations=3000`, `NO_AUTO_SUBMIT=1`이다.
- [사실] Run 05 보상은 `track_lin_vel_xy_exp=1.0`, `track_ang_vel_z_exp=1.0`, `termination_penalty=-50.0`, `feet_air_time=0.2`, `flat_orientation_l2=-1.5`, `ang_vel_xy_l2=-0.05`, `joint_deviation_hip=-0.2`, `joint_deviation_torso=-0.1`, `action_rate_l2=-0.0005`다.
- [사실] `exported/report.html`은 낙상률과 탐색 std 경고 2건을 표시한다.
- [사실] 로컬에는 Run 01~05 tfevents 5개가 있고, `cd workspace/training/humanoid && python ../../../tools/tfcurve.py list`는 각 run의 **31태그×3000 iter**를 출력한다.
- [사실] `python ../../../tools/tfcurve.py window Run04 Run05`의 Run 04 final 5개 값은 CSV의 `0.4102 / 0.6230 / 0.3071 / 5.3926 / 1.4191`과 5/5 일치한다.
- [사실] `exported/model_best.pt`와 play에 사용된 Run 05 체크포인트는 `model_2700.pt`이며, `Train/mean_reward` 최대가 기록된 tfevents step은 2738이다.
- [사실] 위 표의 정량값은 iter 2500~2999 평균이므로 영상 정책과 정량 비교 정책의 시점이 일치하지 않는다.

---

## 2. run별 예측 ↔ 결과 누적

| Run | 변경 변수 | old→new | 사전 예측 | 실제 | 판정 |
|---|---|---|---|---|---|
| 01 | — | — | [사실] 사전 예측 없음 | [사실] `base_contact=0.9244`, `terrain=0.0892`, `error_vel_yaw=0.2367` | [사실] **FAIL**(비교 불가) |
| 02 | `termination_penalty` | -5.0→-50.0 | [사실] 생존 개선·추종 유지 | [사실] 생존 개선, `error_vel_yaw=0.9341`로 추종 미충족 | [사실] **PARTIAL** |
| 03 | `termination_penalty` | -50.0→-90.0 | [사실] 생존 추가 개선·추종 감시 | [사실] 생존 소폭 개선, 두 추종 오차 악화 | [사실] **FAIL** |
| 04 | `track_ang_vel_z_exp` | 0.5→1.0 | [사실] `error_vel_yaw≤0.80` 및 부작용 4게이트 | [사실] `error_vel_yaw=0.6230`, 당시 등록한 4게이트 통과 | [사실] **PASS**(학습 지표 기준) |
| 05 | `track_lin_vel_xy_exp` | 0.5→1.0 | [사실] 사전 예측·수용 기준 없음 | [사실] 마지막 500 평균에서 xy 오차 -21.2%, yaw 오차 +47.9%, `mean_std` +17.5%의 교환 | [사실] **INCONCLUSIVE**(사전 기준 없음; 사후 비교만 가능) |

- [사실] Run 04의 기존 `terrain_levels≥5.0` 게이트는 이후 예선 점수와 무관한 stale 기준으로 폐기됐다.
- [사실] Run 05의 `Train/mean_reward` 최대 12.089는 마지막 500 평균 10.159보다 +3.04σ 높은 단일 스파이크다.
- [추론] `reward_best`의 run 간 대소를 정책 품질 순위로 사용하면 잡음의 최댓값을 성능으로 오인할 위험이 크다.

---

## 3. 축 상태 대장 — 기획자가 가장 먼저 볼 표

| reward 축 | 상태 | 근거 | 기획 시사점 |
|---|---|---|---|
| `termination_penalty` | **포화·탐색 종료** | [사실] -5→-50에서 큰 생존 개선 뒤 -50→-90의 추가 이득이 급감했다. | [사실] **-50.0 유지**하며 -125·-400 후보는 재제안하지 않는다. |
| `track_ang_vel_z_exp` | **부분 탐색** | [사실] 0.5→1.0에서 Run 04 학습 지표 PASS였으나, Run 05에서는 yaw 오차가 다시 커졌고 3000 iter에서 하강 중이다. | [추론] 1.0의 정상상태 효과는 미확정이므로 수렴도 분리 전 추가 인상을 기획하지 않는다. |
| `track_lin_vel_xy_exp` | **부분 탐색** | [사실] 0.5→1.0 단일 변경인 Run 05에서 xy 오차가 개선되고 yaw 오차가 악화했으나 사전 기준이 없고 정책이 미수렴이다. | [추론] 현재값 1.0을 작업 기준으로 유지하되, 다음 단일 노브 변경의 근거로 바로 사용하지 않는다. |
| `feet_air_time` | **미탐색** | [사실] 현재값 0.2이며 Run 05 마지막 500 평균은 0.0062다. | [미측정] 낮은 값이 발 높이 부족인지 한발 지지·명령 게이트 미충족인지는 분리되지 않았다. |
| `flat_orientation_l2` | **미탐색** | [사실] 현재값 -1.5다. | [미측정] H1·H6 안정성에 대한 단일 변수 효과가 없다. |
| `ang_vel_xy_l2` | **미탐색** | [사실] 현재값 -0.05다. | [미측정] 흔들림과 `mean_std`에 대한 단일 변수 효과가 없다. |
| `joint_deviation_hip` | **미탐색** | [사실] 현재값 -0.2다. | [미측정] 보행·생존 부작용을 포함한 단일 변수 효과가 없다. |
| `joint_deviation_torso` | **미탐색** | [사실] 현재값 -0.1다. | [미측정] 자세·회전 부작용을 포함한 단일 변수 효과가 없다. |
| `action_rate_l2` | **미탐색** | [사실] 현재값 -0.0005다. | [미측정] 동작 매끄러움·추종 손실의 단일 변수 효과가 없다. |

---

## 4. 예측 정확도(메타)

| 분류 | 내용 |
|---|---|
| 맞은 것 | [사실] Run 02의 `termination_penalty` 방향 예측과 Run 04의 yaw 개선 방향·등록 게이트는 해당 run의 사전 기준에서 성립했다. |
| 틀린 것 | [사실] Run 02의 추종 유지 예측은 빗나갔고, Run 03은 같은 축의 추가 강화가 선형 개선을 주지 않음을 보였다. |
| 정정한 것 | [사실] `error_vel_xy`의 Run 01→04 단조 악화 서술은 생존시간 정규화 누락 오류였으며, 정규화값은 0.5359→0.2327→0.2667→0.2730이다. |
| 새로 배운 것 | [사실] Run 05는 사전 예측이 없어 PASS/PARTIAL을 붙일 수 없고, 3000 종점만으로 reward 축의 정상상태를 판정할 수도 없다. |
| 새로 배운 것 | [사실] 자동 best 체크포인트와 종료 지표 시점이 다르므로, 영상·제출 정책과 정량 정책의 checkpoint를 이름과 hash로 맞춰야 한다. |

- [추론] 다음 사이클의 정보 가치는 새 reward 값을 즉시 고르는 것보다 같은 보상에서 수렴도·run 간 재현 잡음·checkpoint 정합성을 먼저 분리할 때 더 크다.

---

## 5. 미측정 목록과 예선 시나리오 커버리지

### 예선 시나리오 커버리지

- [사실] 아래 육안 관찰은 `PROJECT_STATE.md` F30·F37·F38에 보존된 사용자 관찰과 동결 mp4에서 이관했으며, 이번 문서 작업에서 새 영상 판정을 만들지 않았다.
- [사실] 동결 인벤토리는 S0b 6개·S0c 4개·S0d 4개로 mp4 **14개**이며, F42의 “영상 13종” 표현과 파일 개수가 달라 이 문서는 실제 파일 수를 사용한다.

| 시나리오 | 가중치 | 상태 | 확보한 근거 | 공식 평가에서 남은 공백 |
|---|---:|---|---|---|
| H1 제자리 균형 | 0.15 | **근사 진단으로 확인** | [사실] S0b 단일 영상 표본에서 직립과 0 명령 마커를 확인했다. | [미측정] 독립 rollout 생존율과 공식 초기조건 결과가 없다. |
| H2 전진 | 0.20 | **근사 진단으로 확인** | [사실] S0b에서 전진 보행을 관찰했고 S0d Run 04 대조 영상도 있다. | [미측정] 평지 tracking 수치와 정책 간 정량 차이는 측정되지 않았다. |
| H3 좌우 | 0.10 | **근사 진단으로 확인** | [사실] S0b에서 좌·우 명령별 측방 보행을 관찰했다. | [미측정] 독립 rollout과 좌우 tracking 수치가 없다. |
| H4 회전 복합 | 0.10 | **근사 진단으로 확인** | [사실] S0b에서 선속도+yaw 복합 선회 보행을 관찰했다. | [미측정] 공식 복합 명령 분포와 정량 tracking이 없다. |
| H5 요철 | 0.15 | **근사 진단으로 확인** | [사실] S0c 추적 env 1개의 1에피소드·25프레임 표본은 직립이었으나 요철 노출은 약 44%였다. | [미측정] 전 구간 요철 독립 rollout 생존·tracking이 없다. |
| H6 ±10° 경사 | 0.15 | **근사 진단으로 확인** | [사실] S0c 두 영상에서 ±10° 세기 경사에 노출된 추적 env의 25프레임 표본이 직립이었다. | [미측정] 오르막·내리막 라벨이 분리되지 않았고 독립 rollout 수치가 없다. |
| H7 밀침 회복 | 0.15 | **근사 진단으로 확인** | [사실] S0b 표본에서 4회 밀침 뒤 직립을 관찰했다. | [미측정] 회복 시간·편향·독립 rollout 성공률이 없다. |
| 합계 | **1.00 / 1.00** | **근사 진단 파일 커버리지** | [사실] H1~H7에 대응하는 mp4와 로그가 존재한다. | [미측정] 이 합계는 공식 점수나 시나리오 PASS 합계가 아니다. |

### 다음 사이클의 미측정 목록

| # | 항목 | 다음 기획에 미치는 영향 |
|---|---|---|
| M1 | [미측정] 같은 보상 설정의 3000→5000 iter 수렴 곡선과 동일 설정 run 간 재현 잡음 | [추론] Run 05의 yaw 열위가 reward 성격인지 학습 중간 상태인지 분리하지 못한다. |
| M2 | [미측정] iter 3000·5000 checkpoint의 별도 보존과 해당 checkpoint 전용 지표·영상 대응 | [사실] 현재 자동 finalize는 best 1개만 남겨 정책-지표 정합성을 깨뜨릴 수 있다. |
| M3 | [미측정] H1~H7 공식 evaluator 설정·정량식·독립 rollout 분포 | [사실] 현재 S0는 근사 진단이며 예선 점수·통과 가능성을 산출할 수 없다. |
| M4 | [미측정] H5 전 구간 요철 노출과 H6 오르막·내리막 격리 결과 | [사실] 현 영상은 H5 노출 44%, H6 방향 혼합 한계를 가진다. |
| M5 | [미측정] Run 05의 학습 전 예측·수용 기준 | [사실] 사후 복구로 채울 수 없으므로 Run 05 판정은 INCONCLUSIVE로 유지한다. |
| M6 | [미측정] 최종 정책의 `policy.pt`·`env.yaml`·checkpoint·H1~H7 영상·기술 리포트 상호 일치 | [사실] 단계 5 최종 평가·문서 게이트를 아직 통과하지 않았다. |

---

## 6. 절차 위반 기록 — 재발 방지 대상

| 대상 | 위반 또는 구조적 결함 | 재발 방지 게이트 |
|---|---|---|
| Run 01 | [사실] 사전 예측 없이 실행했다. | [사실] 학습 전에 CSV 사전등록 행이 없으면 실행하지 않는다. |
| Run 03 | [사실] Run 02 play 선행 게이트를 지키지 않았다. | [사실] 선행 play·artifact 확인 완료 전 후속 학습을 열지 않는다. |
| Run 04 | [사실] 사전등록 문서를 학습 시작 후 작성했다. | [사실] 파일 timestamp와 학습 시작시각을 대조한다. |
| Run 05 | [사실] 사전 예측·수용 기준 없이 실행했고 CSV 행은 사후 복구됐다. | [사실] 사후 결과에 PASS/PARTIAL을 붙이거나 사전 예측처럼 소급 기록하지 않는다. |
| Run 06 초안 | [사실] 메인 루프가 소유 배우 승인 없이 CSV 사전등록 행을 작성했고 근거가 철회됐다. | [사실] 해당 행은 삭제하며, 다음 계획은 권한 있는 기획자가 새로 사전등록한다. |
| 체크포인트 보존 | [사실] `finalize()`는 reward argmax 근처 best 1개만 남기고 중간 `.pt`를 삭제한다. | [사실] 비교할 iter의 `.pt`를 finalize 전에 별도 경로로 복사하고 SHA-256을 남긴다. |

- [사실] `h1_task/_finalize.py:111-140`은 best 1개를 제외한 `model_*.pt`를 삭제하고, `train.py:146`은 `finalize()`를 인자 없이 호출하므로 학습 경로에서 cleanup을 끌 수 없다.
- [사실] `save_interval=100`인데 로컬 Run 01~05 폴더에는 `model_*.pt`가 각각 1개만 남아 F41의 삭제 동작이 실증됐다.

---

## 7. 다음 기획에 넘기는 제약(handoff)

1. [사실] 비교 시작점은 Run 05 **작업 기준선**이며, 이를 예선 우위가 확정된 정책으로 쓰지 않는다.
2. [사실] `termination_penalty=-50.0`을 유지하고 폐기된 -125·-400 축을 재제안하지 않는다.
3. [사실] 삭제된 `train_260830-06` CSV 행을 실행 지시로 사용하지 않는다.
4. [추론] 다음 학습을 편성한다면 권한 있는 기획자가 Run 05 보상 9개를 유지한 from-scratch 수렴 pilot을 새 run ID로 사전등록하고, 성공 기준·부작용 게이트를 다시 검토해야 한다.
5. [사실] 비교할 iter의 checkpoint를 학습 중 별도 보존하고, 수치·영상·policy가 같은 checkpoint를 가리키는지 hash로 확인한다.
6. [사실] run 비교는 `reward_best`가 아니라 동일 window의 tfevents 지표·기울기·`mean_episode_length`를 사용한다.
7. [사실] H1~H7은 근사 진단 파일이 모두 있으나 공식 시나리오 점수·통과 가능성은 전건 미측정이다.
8. [사실] `TECH_REPORT_H1.md`의 소유자는 `humanoid-report-writer`로 정하되, 현재 초안은 최종 정책·artifact 정합성과 사용자 사실 확인 전에는 제출 완성본으로 승인하지 않는다.
9. [사실] 다음 run은 학습 시작 전 `experiment_history.csv`에 등록되고 소유 배우가 채택한 계획 문서가 있어야 한다.

---

## 갱신 이력

- [사실] 260829에 Run 01~04 누적 요약 구조를 만들었다.
- [사실] 260830에 Run 05 사후 복구, S0b·S0c·S0d 근사 진단, F40 tfevents 곡선, F41 checkpoint 삭제, F42 정책-지표 시점 불일치를 반영했다.
- [사실] 260830에 `error_vel_xy` 단조 악화 오류와 stale `terrain_levels` 예선 게이트를 제거했다.
