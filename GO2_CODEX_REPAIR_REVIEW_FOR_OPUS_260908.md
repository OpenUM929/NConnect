# Go2 채점 수리·A017 재평가 패키지 검토 — Opus 공유용

작성일: 2026-09-08 · 작성: Codex · 대상: engine 1.5.0 수정과 G-A027 패키지

## 0. 예선 기준 현재 위치
- [예선 목표] 시뮬레이션 70점의 내부 측정 신뢰성과 설계 의도 20점·리포트 품질 10점의 근거 정합성.
- [현재 단계] 단계 0/6 — 증거·artifact 정합성. 이는 이번 검토의 위치이며 캠페인 전체 성능 완료 선언이 아니다.
- [확보] 직전 코드 검토에서 A017 delta 재현, 계약 테스트 2종 성공, 패키지 SHA 일치 확인(§2).
- [미확보] 완전한 평가 지문, 이전 기준선 재사용 동등성, 제출 승급 조건 정합성.
- [이번 테스트] 수리의 실제 집행 범위와 남은 결함을 Opus가 원자료로 검증하도록 한다.
- [흐름] 기존 코드 검토 → **공유 검토** → 근거 합의 시 보완 검증 → 반증 시 판단 정정 → 최종 제출 검토.
- [지금 할 일] 아래 §7의 요청문으로 Opus에 교차검증을 전달한다. 이 문서는 서버 실행 지시가 아니다.
- [보장하지 않음] 테스트 성공·내부 개선은 공식 점수, 최종 자체평가 성공, 제출 자격을 보장하지 않는다.

## 1. 결론과 검토 경계

**A017의 추가 평가 방향에는 동의한다. 그러나 현재 패키지를 수정 완료·실행 준비 완료로 승인하지 않는다.**

이는 새 학습을 요구하는 결론이 아니다. 기존 자료 재사용과 판정 계약을 먼저 검증하자는 검토 의견이다. 근거는 §3의 코드 결함과 §4의 승급·실행 계약 공백이다.

- **확인 사실**: 코드·JSON·실행 출력으로 직접 확인한 것.
- **해석**: 확인 사실에서 도출한 판단. 다른 설명을 배제하지 않는다.
- **미확인**: 이번 검토로 결정할 수 없는 것.
- **제안**: 아직 구현하거나 실행하지 않은 개선안. 공식 규정으로 인용하지 않는다.

본 문서는 직전 읽기 전용 검토를 문서화한다. 전체 A006~A025 재감사를 새로 완료했다는 의미가 아니다. 기존 AI 감사의 결론을 증거로 대신하지 않았으며, 코드·로컬 실행 자료를 우선했다. 이 공유 문서를 읽은 뒤의 Opus 작업은 **교차검증**이지 새로운 맹검 독립 감사가 아니다.

경로는 모두 `C:\dev\Nconnect` 기준이다. 아래에서 `Q/`는 `workspace/training/quadruped/`를 뜻한다. 원본 수정으로 줄 번호가 이동하면 §8의 해시와 함수·필드명을 함께 대조한다.

## 2. 확인한 긍정적 결과

### 2.1 A017의 상대 개선은 재현된다 — 증거 강도 높음

원본 `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/reports/TIER1_DECISION.json:2–6,33–35,55`:

| 필드 | 원본 값 |
|---|---:|
| `baseline_points_70` | 46.4912409491015 |
| `candidate_points_70` | 50.19915737233102 |
| `candidate_minus_baseline_points_70` | 3.70791642322952 |
| `failure_reasons[0]` | `G4_survival_regressed_over_0.1` |
| `scenario_deltas.G4.survival` | -0.21875 |
| `status` | `INTERNAL_EARLY_KILL_FAIL` |

같은 작업 폴더의 `meta/tier1_registry.json`, 실험 명세와 `evaluation/{baseline_tier1,candidate}`를 현재 `build_policy`·`tier1_decision`으로 재계산하면 delta `3.7079164232295128`, `INTERNAL_EARLY_KILL_PASS`가 나온다. 두 정책의 `build_policy.status`는 각각 여전히 `INTERNAL_GATE_FAIL`이다. **조기기각 게이트 성공과 절대 안정성 게이트 성공은 다르다.**

“절대 점수가 없다”는 표현도 한정해야 한다. 위 tier-1 절대값은 이미 있다. 없는 것은 동일한 전체 69건 조건에서의 A017 점수다. 근거: 위 JSON과 `Q/server_run_go2_a017_full_suite.sh:250–280`의 전체 평가 범위.

### 2.2 구현 및 검증에서 확인된 것

- G6는 `recovery_rate_upright`를 우선한다. 다만 legacy fallback이 남는다(§3.3). 근거: `Q/go2_fixed_eval_report.py:43–57`.
- G7의 `NCRC_EVAL_DR=1`은 실제 소비된다. 러너가 전달하고 `Q/go2_task/env_cfg.py:134–147`이 마찰·반발·질량·관절 초기화 범위를 설정한다. 근거: `Q/server_run_go2_a017_full_suite.sh:212–215`. 공식 DR 조건과 같다는 뜻은 아니다.
- 직전 검토에서 `python -B tools/test_go2_scoring_repair_contract.py`와 `python -B tools/test_go2_a017_full_suite_contract.py`가 성공했다. 후자는 최초 `bash` PATH 부재로 중단됐고 Git Bash 경로를 추가한 뒤 성공했다. `bash -n`도 이 테스트에서 확인했다.
- `git diff --cached --check`는 직전 검토에서 오류 없이 끝났다. 이것은 시뮬레이터 실행 검증이 아니다.
- 패키지 `Q/go2_a017_full_suite.zip`의 SHA256은 제공된 `ee6f222f3013d5a1b685dd6ab0e112f2371a06d6133cd211b4f2fdde5eb582bd`와 일치했다.
- 전체 테스트 10종을 재실행한 것은 아니다. Isaac Sim 서버 평가·영상 행동 검증은 이번 검토에서 실행하지 않았다.

## 3. 남은 코드 결함 — Opus 재검증 요청

### R1. 지문이 모두 없어도 동일 측정으로 취급 — 높음

**확인 사실:** `Q/go2_fixed_eval_report.py:249–257`은 양쪽 값을 비교만 한다. `instrument_mismatch({}, {}) == []`를 직접 재현했다. `:161–175`는 누락 필드를 문자열 `None`·`null`로 집합화한다.

**영향 해석:** 정보 부재가 동일성의 증거로 취급된다. 현재 지문은 평가기 SHA, 케이스 명령·DR 조건도 포함하지 않는다. 따라서 “두 팔이 같은 자”라는 현재 테스트 문구는 검사한 제한된 필드의 일치로 축소해야 한다.

**제안/완료 기준:** 지문 누락·미인식·혼합 버전을 명시적으로 차단하고 평가기/측정 계약 revision 및 케이스별 조건을 비교한다. 양쪽 누락, 한쪽 누락, 같은 schema지만 다른 평가기, 케이스 조건 불일치 회귀 테스트가 필요하다. 정책 SHA는 팔별 신원 확인용이지 서로 같아야 할 값은 아니다.

### R2. 무효 비교에서도 delta를 계산·출력 — 높음

**확인 사실:** `Q/go2_tuning_eval_report.py:17–36`은 지문 검사 전에 delta를 계산한다. `:76–99`에서 무효 status와 비교 수치를 함께 반환한다.

**판정:** “다른 자면 아예 점수를 계산하지 않는다”는 설명과 구현이 다르다. 이것만으로 status 차단이 작동하지 않는다는 뜻은 아니지만, 숫자가 원장·후속 판단으로 재유입될 위험은 남는다.

**제안/완료 기준:** 비교 유효성 검사를 먼저 하고 무효 delta는 `null` 또는 미출력한다. 원측정·팔별 진단 수치는 보존하되 비교 불가로 분리한다. 테스트는 status뿐 아니라 비교값 부재도 검증한다.

### R3. G6 legacy 상태가 후속 승급에서 차단되지 않음 — 높음

**확인 사실:** `Q/go2_fixed_eval_report.py:52–57,197–209`는 legacy 회복률을 사용하고 `SELF_ASSESSMENT_LEGACY_METRIC`으로 낮춘다. 그러나 `Q/go2_tuning_eval_report.py:34–43,104–124`에는 legacy/불완전 측정 상태를 일관되게 배제하는 계약이 없다.

**영향 해석:** 조건을 만족하는 legacy 자료가 하위 계층에서 통과할 수 있는 경로다. 실제 A017이 이 경로로 잘못 승급됐다는 주장은 하지 않는다.

**제안/완료 기준:** tier-1은 완전하고 비교 가능한 측정인지 먼저 검사한다. 여기서 절대 성능 `INTERNAL_GATE_PASS`까지 요구하면 불완전한 후보를 개선하는 스크리닝을 막으므로 **측정 유효성과 성능 기준을 분리**한다. 최종 승급에서는 완전성·upright 회복·전체 성능 게이트·영상 근거를 함께 요구한다. 양쪽 legacy·결측·혼합 자료의 우회 테스트를 추가한다.

### R4. Pilot G1~G6 재사용 동등성 미증명 — 높음

**확인 사실:** `Q/go2_eval_telemetry.py:238–245,353–384`는 자세 두 채널 필수화·fallback 제거를 적용하지만 출력 `schema_version`은 2다. `Q/server_run_go2_a017_full_suite.sh:327–352`는 G7만 갱신하고 나머지 66건은 영향 없다고 서술한다.

**판정:** G7 변경은 확인됐지만 **변경 범위가 G7뿐이라는 전제는 성립하지 않는다.** 그렇다고 기존 66건이 모두 실제로 잘못됐다고 단정할 근거도 없다.

**제안/완료 기준:** 먼저 로컬 원시 자세 채널·결측·회복 정의·실행 조건을 대조해 케이스별 재사용 가능성을 증명한다. 증명 가능한 케이스는 재사용하고, 불가능한 범위만 같은 평가기로 재측정한다. 전체 69건 기준 재평가는 안전한 대안이나 GPU 비용을 먼저 산정한다. 새 schema/revision과 source SHA로 의미 변경을 식별한다.

## 4. 설명·운영 계약의 정정 필요 항목

### R5. 실패 원인을 채점 하나로 환원하지 말 것

**확인 사실:** 같은 재판정에서 A015 `-30.12193163995342`, A016 `-46.08137617493539`, A018 `-44.94136866407548`로 상대 하락이 남았다. 근거는 각 `workspace/_keep/go2_g_a015_pilot_feet_air_time_035/`, `go2_g_a016_pilot_ang_vel_xy_m015/`, `go2_g_a018_pilot_action_rate_m008/` 아래 `meta/tier1_registry.json` 및 `evaluation/{baseline_tier1,candidate}`이며, 대응 원장 표는 `GO2_PROJECT_STATE.md:763–766`이다.

**해석:** 측정 오류와 측정된 후보 성능 저하는 함께 존재한다. “우리는 학습에 실패한 것이 아니다”보다는 **“채점 결함이 일부 기각을 왜곡했고, 동일 측정에서도 성능 저하가 남는다”**가 근거에 맞다.

A017의 기준은 보행하는 Pilot-01이다(`tools/test_go2_a017_full_suite_contract.py`의 `[5]` 및 위 A017 양팔 원자료). 따라서 A017을 정지 기준선과 생존 게이트가 반드시 한 원인이라는 직접 증거로 삼을 수 없다. 또한 가만히 있는 기준은 진단·대조군으로는 유용할 수 있다. “승급 탐색 기준으로 부적합”과 “해당 비교의 모든 정보가 무의미”는 구별해야 한다.

H1은 가만히 서 있기 어려워 문제가 드러나지 않았다는 설명은 **미확인 가설**이다. H1 기준 정책 행동, evaluator·게이트 변화 시점, 동일 조건 반복을 대조하기 전에는 인과 결론으로 쓰지 않는다.

### R6. '유효 4전 1승'은 새 게이트의 제한된 재분류

`GO2_PROJECT_STATE.md:759–775` 표는 11건인데 서술은 12전과 비교한다. A010의 표 지문은 `None / None`으로, 표 자체도 “7건 모두 지문 비대칭”을 뒷받침하지 않는다.

또한 원본 A010·A013 명세 `meta/G_A*.json`의 `evaluation.gates`에는 `min_total_points_delta`가 없다. 이를 현재 `tier1_decision`에 직접 전달하면 `KeyError`가 난다(`Q/go2_tuning_eval_report.py:49`). 두 건까지 같은 조건으로 재판정하려면 **명시적인 legacy 명세 migration과 적용값 기록**이 필요하다. 임의 기본값으로 결과를 맞추지 않는다.

권장 표현은 **“검토 표의 11건 중 현재 로직에서 비교 차단되지 않은 4건이 있고, 새 조기기각 게이트 성공은 A017 1건이다”**이다. 다만 R1~R4가 남아 있으므로 이 4건의 측정 유효성을 포괄적으로 인증한 표현은 피한다. 무효 판정 이유도 기준선 부적합·측정 지문 불일치·식별정보 부족으로 각각 나눈다.

### R7. 제출 승급 조건 누락 — 실행 전 해소

`tools/build_go2_a017_full_suite_package.py:230–238`은 기준 초과와 생존 0.95만으로 제출 정책 승급을 예고한다. 반면 `Q/config/go2_self_eval_registry.json:47–52,68–71`에는 추종 0.70, 가중 합계, 문서 포함 총점, 별도 승급 기준이 있다. 어느 기준이 screening·최종 승급에 적용되는지 정합성을 명시해야 한다.

**제안:** 상대 개선이면 다음 평가 후보로 분류하고, 제출 후보는 별도의 완전한 rubric으로 판정한다. 평가 seed 3개는 독립 학습 seed 3개가 아니다. 후보가 기준 이하였다는 이유만으로 reward 방향을 “상향 소진”하는 문구도 단일 학습 결과의 일반화를 피하도록 고친다. 근거: 같은 builder `:237–238`의 소진 선언.

### R8. 시간·회수 안전성 과장 — 실행 전 해소

- `Q/server_run_go2_a017_full_suite.sh:250–280`의 69건에는 seed 반복이 이미 포함된다. 후보 69건 + Pilot G7 3건 + 영상 7개이지 후보 207건이 아니다.
- 같은 파일 `:80`의 시간은 `ESTIMATE`이며 전체 실행 강제 상한을 집행하는 코드가 아니다. 신규 실측 소요시간은 미확인이다.
- 같은 파일 `:74`는 기본 재실행에서 기존 KEEP·결과 ZIP을 삭제한다. 따라서 builder `:205`의 “cannot damage anything”은 과장이다.
- 같은 파일 `:199–205`의 resume 지문은 evaluator SHA·평가 step 수·DR 환경값을 포함하지 않는다. `:287–290`의 영상 재사용도 파일 존재만 본다.

**제안:** 작업 ID별 결과 보존, 기존 파일 발견 시 무손실 중단, 전체 실행 deadline과 부분 회수, 내용 기반 resume 검증이 필요하다. 테스트는 문자열 존재 확인 외에 서로 다른 설정을 같은 결과로 건너뛰지 않는지 검증한다.

## 5. 향후 진행안 — 제안이며 서버 실행 승인 아님

등급: 아래 '조사'·'개선'은 내부 검증 작업이다. 공식 제출요건과 혼동하지 않는다. 다음 서버 평가의 필수 안전 조건은 실제 실행 전에 충족해야 한다.

| 순서 | 등급 | 작업 | 비용·시간 박스 | 완료 증거 | 실패 시 대안 |
|---|---|---|---|---|---|
| 1 | 조사 | R1~R8 수용/반증 및 원자료 버전 고정 | GPU 0; 로컬 작업 | 지적별 파일·필드·재현 결과 | 의견 차이는 미확인으로 남기고 관련 승급만 보류 |
| 2 | 개선 | 지문·결측·legacy·무효 delta·resume 계약 수정 | GPU 0; 소요시간 미측정 | 부정 사례가 먼저 실패하고 수정 후 성공하는 회귀 테스트 | 수정 범위를 줄이고 아직 검증되지 않은 자동 승급 차단 |
| 3 | 조사 | Pilot 기존 66건 재사용 가능성 감사 | GPU 0 우선 | 케이스별 채널/조건/재계산 대조표 | 필요한 케이스만 재측정, 판별 불가 시 기준 전체 재평가 |
| 4 | 개선 | G-A027 패키지·회수·시간 계약 재검증 | GPU 0 | 새 SHA, bash 문법, archive 내용 비교, 보존·실패 회수 테스트 | 서버 실행 보류; 기존 결과·후보는 보존 |
| 5 | 조사 | A017 및 필요한 기준 케이스 동등 평가 | 잔여 시간 실측 후 산정; 고정 1시간 보장 금지 | 동일 계약 telemetry·필수 영상·manifest | deadline 도달 시 부분 회수, `SELF_ASSESSMENT_INCOMPLETE` |
| 6 | 개선 | 전체 점수·영상·제출 rubric으로 재판정 | GPU 추가 0이 목표 | 상대 개선/절대 게이트/영상/공식 결과 별도 표 | 기준 유지도 성능 승인과 분리; 추가 실험은 원인 구분 가능한 것만 사전등록 |

시간 박스 산정 제안: `평가 케이스 수 × 최신 보수적 케이스 소요시간 + 영상 + 시작/패키징 + 회수 여유`. 잔여 예산과 이 추정을 비교해 숫자를 동결한 뒤 실행한다. 현재 문서에서 없는 잔여 GPU 시간을 창작하지 않는다. GPU 단계의 timeout 값·회수 예약시간이 미정인 상태는 실행 준비 완료가 아니다.

## 6. 미확인·이번 검토의 한계

1. H1 대비 Go2 난항의 인과 기여율 — 이번 자료만으로 확정 불가(R5).
2. 공식 evaluator의 세부 추종 변환식·DR 분포와 실제 공식 점수 — 이번 로컬 검증 범위 밖.
3. 학습 이력 백업 skip의 제출 자격 영향 — 운영진 확인 미회수. 코드 기본값 복구와 과거 이력 복구는 다른 일이다.
4. 현재 잔여 GPU 시간·신규 패키지 실제 소요시간 — 실측 없음. 원장에도 미확인으로 기재(`GO2_PROJECT_STATE.md:749–753`).
5. 기존 Pilot 66건의 실제 의미 동등성 — 코드 변경만으로 유효/무효 어느 쪽도 확정하지 않음(R4).
6. A017 전체 평가 점수·영상 행동 — 새 서버 실행 결과 없음(§2.1).
7. '11곳 수정', '모든 실행 백업 복구', '10종 전부 성공' 전체 주장 — 이번 검토는 전수 인증하지 않음. 확인 범위는 §2.

## 7. Opus에 전달할 요청문

> `GO2_CODEX_REPAIR_REVIEW_FOR_OPUS_260908.md`를 교차검증하라. Codex 결론에 맞추지 말고 R1~R8을 각각 **수용 / 부분수용 / 반증 / 미확인**으로 판정하라. 파일 경로·줄 또는 JSON 필드·재현 출력으로 답하고, 문서의 주장 자체를 증거로 삼지 마라.
>
> 먼저 현재 파일 해시를 §8과 비교하라. 변경됐다면 '검토 당시 결함'과 '이후 수정으로 해소됨'을 분리하라. 우선 읽기 전용으로 답하고, 구현할 경우 검증과 구현 단계를 분리해 변경 목록·부정 회귀 테스트·실행 출력을 남겨라. 이 요청은 서버 실행이나 제출 승인이 아니다.
>
> 특히 다음을 답하라.
> 1. 누락 지문끼리 비교해도 측정 무효가 되는가? 같은 schema의 다른 수집기와 DR 조건을 구분하는가?
> 2. 무효 비교의 delta가 downstream에 남지 않는가? legacy G6·결측이 승급을 우회하지 않는가?
> 3. Pilot 66건 재사용은 어떤 원시 채널·조건 증거로 정당화되는가? 재측정 최소 범위를 명시하라.
> 4. A017은 보행 기준선 비교인데 왜 정지 기준선 함정의 직접 사례로 설명했는가? 실제 성능 저하 사례와 측정 결함을 나눠 설명하라.
> 5. 12건·11건·4건의 분모, A010/A013 legacy gates migration, 각 무효 사유를 명시하라.
> 6. 상대 개선과 제출 승급을 구분하고 registry의 모든 적용 기준·영상·문서 감사를 반영했는가?
> 7. 실제 작업 수·시간 상한·기존 결과 보존·resume 조건을 코드로 보장하는가?
> 8. H1과의 인과 비교는 확정/해석/미확인을 나눴는가? 반증에 필요한 최소 추가 증거는 무엇인가?
>
> 산출물은 루트 `GO2_OPUS_RESPONSE_TO_CODEX_REPAIR_REVIEW_260908.md`로 작성하라. 표 열은 `ID / 판정 / 원자료 / 재현 / 수정 여부 / 남은 위험`으로 한다. 마지막에 서버 실행 준비 여부와 차단 항목을 쓰되, 테스트 성공을 공식 합격으로 바꾸지 마라. 승인되지 않은 GPU 실행·제출·기존 artifact 삭제는 하지 마라.

## 8. 문서화 시점 source SHA256

아래 해시는 무손상·버전 식별용이며 policy tensor 동등성 증거가 아니다.

| 파일 | SHA256 |
|---|---|
| `Q/go2_fixed_eval_report.py` | `a022557b9f27ab459c30e2910021e01f9d562dae2377ab178d4dae19dc428595` |
| `Q/go2_tuning_eval_report.py` | `b09e53079790024ccb64e362df8339a462c525bf7fc26815efeb2df2f3e51d29` |
| `Q/go2_eval_telemetry.py` | `34edd178bfabee64d1c6e8b950cda54738a005abc10e108331c1944380763eb7` |
| `Q/server_run_go2_a017_full_suite.sh` | `e4cba9bae2258dbbe769542df3445ae83af4cd6d843a15bf91a694e54b4a63a1` |
| `tools/build_go2_a017_full_suite_package.py` | `f316079d61f52ab8a73ca8c88799a3f3fc6cb5bfe038bde0f29671d732629c83` |
| `Q/config/go2_self_eval_registry.json` | `8d8c34caf66813e2c18070fb9a85ed7c843c379cb3a5ffb3d0a73923b9349ba6` |
| `tools/test_go2_a017_full_suite_contract.py` | `8b98b2712e8182f60626f95feb9771651ded2bd894988230bd331a9f34466678` |
| `tools/test_go2_scoring_repair_contract.py` | `62cf31022c791517a923772817bd2efb4ab4206815ee516a0ff996c5e11cef1c` |
| `GO2_PROJECT_STATE.md` | `b30c01d7670916bc4323e0ef640090a879139be041bef4dc3bbc224079fd9018` |

본 문서 추가 외 코드·패키지·실행 원장·기존 감사 문서는 수정하지 않았다. 커밋·서버 실행·제출도 하지 않았다.
