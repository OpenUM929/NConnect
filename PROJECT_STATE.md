---
project: NAVER CONNECT ROBOTICS CUP 2026 — 예선 통과
budget: 사람 시간 50시간 이내 (학습 벽시계 시간은 별도)
owner: main loop (Claude Code) — 배타 작성자
manager: .codex/agents/prelim-campaign-manager.md
status: active
updated: 260829
---

# 프로젝트 상태 — 로보틱스 예선

> 세션 간 연속성의 정본. compact·세션 교체 후 **여기부터 읽는다.**
> 서브에이전트는 cold start이므로 상태를 들고 있지 못한다. 파일이 든다.

## 배우

| 배우 | 역할 | 범위 | 정의 |
|---|---|---|---|
| `prelim-campaign-manager` | 점수·예산·순서 관리, 기체/축 배분, 제출 판정 | 캠페인 전체 | `.codex/agents/prelim-campaign-manager.md` |
| `humanoid-test-planner` | 다음 실험 기획 (단일변수·사전예측·비용게이트) | **H1 전용** | `.codex/agents/humanoid-test-planner.md` |
| `humanoid-report-writer` | run 후 예측↔결과 대조, 이력 갱신 | **H1 전용** | `.codex/agents/humanoid-report-writer.md` |
| — | **Go2 담당 없음** | — | **공백** |

**실행은 사용자가 한다.** 서버 튜닝 → `C:\dev\NConnect\workspace\training`에 자료 투입.
관리자도 두 배우도 학습을 실행하지 않는다.

## 고정 사실 (재조사 금지)

### 자료 위치
| # | 사실 |
|---|---|
| F1 | 강좌 = `C:\dev\NConnect\test` (강의 HTML 19건). 목차 22강 중 **00·01·11강 없음** |
| F2 | 강좌 분석서 = `COURSE_ANALYSIS_260829.md` |
| F3 | 예선 가이드 = `workspace/PRELIM_RL_GUID.md` — **채점 공식 정본** |
| F4 | 작업 폴더 = `workspace/training/{humanoid,quadruped}` · 이력 = `humanoid/reports/experiment_history.csv` |

### 채점 (PRELIM_RL_GUID §2)
| # | 사실 |
|---|---|
| F5 | `시나리오 점수 = survival_rate × tracking_score`, `종합 = Σ(시나리오×가중치)`, 최대 1.0 |
| F6 | **로봇당 100점 = 시뮬 70 + 설계의도분석 20 + 리포트품질 10** |
| F7 | **H1+Go2 합산 최대 200점. 한 기체만 내면 100점 상한** (규정집 제10조). 각 로봇 최고점만 집계 |
| F8 | 제출 3종 = `policy.pt` + `env.yaml` + **기술 개선 리포트**. 팀 대시보드 직접 업로드 |
| F9 | 의도분석 20점 = **리포트에 적은 의도대로 로봇이 실제로 그렇게 행동하는가**. 허위기재는 부정행위(제14조)지만 **의도-결과 불일치 자체는 감점일 뿐** |
| F10 | 참가자는 점수를 미리 확인할 수 없다. 운영진 일괄 평가 |

### 작업 규율
| # | 사실 |
|---|---|
| F11 | 실험 run 1000~5000 iter (성격이 드러남) · **최종본만 10000~15000 iter** |
| F12 | 학습은 시작 순간 설정 **스냅샷** — 도중 파일 변경 무반영 (규정집 제12조) |
| F13 | 학습은 벽시계 시간 · tmux 비동기 — 사람 시간과 겹친다. **직렬로 더하지 않는다** |
| F14 | 서버 종료 시 내용 소실 → `exported/` 로컬 다운로드 필수 |
| F15 | **Go2 기본 `feet_air_time`=0.01로 매우 낮음 — "올려야 잘 걷기 시작"** (저비용 첫 개선축, §4) |
| F16 | H1은 기본 보상 1000 iter로 걷지 못함 (강좌 12강) |
| F17 | 트레이드오프: 발 들기 과하면 **한 발 깡총 → survival 붕괴** |

## H1 실험 이력 (4 run 완료)

| Run | 변경 | base_contact | error_vel_yaw | terrain | 판정 |
|---|---|---|---|---|---|
| 01 | baseline | 0.9244 | 0.2367 | 0.0892 | FAIL (사전예측 없음) |
| 02 | `termination_penalty` -5→-50 | 0.2979 | 0.9341 | 5.6356 | PARTIAL |
| 03 | -50→-90 | 0.2785 | 1.7672 | 5.6682 | FAIL — **축 포화 확인**, Run 02로 롤백 |
| 04 | `track_ang_vel_z_exp` 0.5→1.0 | 0.3071 | **0.6230** | 5.3926 | **PASS** (영상 미측정) |

현재 기준선 = Run 04. `std=1.4191`, `report.html` 경고 2건 유지.

### F18 — 추종오차 metric은 생존시간에 오염돼 있다 (260829 메인루프 실측)

IsaacLab `UniformVelocityCommand._update_metrics`는 매 step `|Δ|/max_command_step`을
누적하고 env reset 때 로그한다. `resampling_time_range=(10.0,10.0)`, `step_dt=0.02s`
→ `max_command_step=500`. `episode_length_s=20.0` → 1000 step. 따라서 **일찍 죽은 run은
error 값이 그만큼 작게 찍힌다.** (근거: `exported/env_2026*.yaml` 1113~1134행 · 21·84·845행)

tfevents 직접 파싱으로 회수한 `Train/mean_episode_length` (마지막 50 iter 평균):

| Run | ep_len | err_xy 원값 | **/step** | err_yaw 원값 | **/step** |
|---|---:|---:|---:|---:|---:|
| 01 | **332.6** | 0.3565 | **0.5359** | 0.2382 | **0.3580** |
| 02 | 780.1 | 0.3631 | 0.2327 | 0.9588 | 0.6145 |
| 03 | 791.2 | 0.4221 | 0.2667 | 1.8312 | **1.1571** |
| 04 | 767.2 | 0.4188 | 0.2730 | 0.6430 | 0.4190 |

**do-nothing 기준선**(명령 무시하고 서 있는 로봇) = `xy 0.7165` · `yaw 0.6681`.
`heading_command=true`·`rel_heading_envs=1.0`·`stiffness=0.5`·`rel_standing_envs=0.02` 반영
수치적분(2×10^7 샘플). → statue 대비 배율:

| Run | xy | yaw |
|---|---:|---:|
| 01 | 0.75x | 0.54x |
| 02 | 0.32x | **0.92x** (statue 수준) |
| 03 | 0.37x | **1.73x** (statue보다 나쁨) |
| 04 | 0.38x | 0.63x |

- **회전은 Run 04에서 처음으로 statue보다 의미있게 좋아졌다.** Run 03의 1.73x는 사용자
  영상 관찰("제자리에서 왼쪽으로 돌고 있어")의 정량 확인이다.
- **직진은 4 run 모두 statue보다 낫다** — 전혀 못 걷는 상태는 아니다.

### F19 — `PLANNER_BRIEF.md` §3·handoff#4의 "error_vel_xy 4 run 연속 단조 악화"는 오류

원값 0.3493→0.3596→0.4182→0.4102는 Run 01의 짧은 생존(332.6 step)이 만든 착시다.
보정값은 **0.5359 → 0.2327 → 0.2667 → 0.2730** — Run 01→02는 57% 개선이다.
실제 악화는 02→04 구간(+17%)뿐이며 이는 `track_ang_vel_z_exp` 인상의 대가로 설명된다.
이 틀린 추세가 "H2 가중 0.20 최대이므로 `track_lin_vel_xy_exp`로 가라"는 handoff 근거였다.
**정정 권한은 `humanoid-report-writer`에 있다 — 메인 루프는 이 파일을 고치지 않는다.**

### F20 — 3000 iter에서 정체 없음 (장기 학습 유효)

300-iter 창 추세(Run 04): `/step_xy` 0.2891→0.2793→0.2748, `/step_yaw` 0.4402→0.4229→0.4178,
`base_contact` 0.3190→0.3097→0.3046, `terrain` 5.046→5.192→5.335 — 전 지표 단조 개선 중.
Run 02의 `std`는 iter 1650 부근 1.3466 정점 후 1.3129까지 하강 → 탐색 감쇠가 시작됐다.

### F21 — 보상 배분이 예선 배점과 어긋나 있다 (Run 04 종료 시점)

| 항목 | Run02 | Run04 | 절대값 비중 | 가중치 | 학생 수정 |
|---|---:|---:|---:|---:|---|
| `track_ang_vel_z_exp` | 0.1650 | **0.4407** | 40.2% | 1.0 | 가능 |
| `track_lin_vel_xy_exp` | 0.3114 | 0.2914 | 26.6% | 0.5 | 가능 |
| `joint_deviation_arms` | -0.0835 | -0.0981 | 9.0% | — | **불가(파일에 없음)** |
| `feet_air_time` | 0.0089 | **0.0081** | **0.7%** | 0.2 | 가능 |
| `flat_orientation_l2` | -0.0102 | -0.0125 | 1.1% | -1.5 | 가능 |

- 회전이 직진보다 1.51배 큰 보상을 낸다. 예선 배점은 **H2 전진이 0.20으로 최대**다.
- 단, 항별 상한이 다르다(가중치 = 상한). **상한 대비로는 직진 58.3%, 회전 44.1%** —
  "직진이 못 배웠다"가 아니라 "직진에 걸린 보상 예산이 회전의 절반"이다.
  `track_lin_vel_xy_exp=0.5`는 권장범위 0.5~2.5의 **하한**이며 4 run 동안 한 번도 안 건드렸다.
- `feet_air_time` 0.7% — 발 들기 축이 사실상 꺼져 있다.

### F22 — **예선 평가 지형 ≠ 학습 지형** (260829 · `PRELIM_RL_GUID.md` §2 명문)

가이드 §2가 H1 학습 지형 표에 **"평가 시나리오 아님 — 등반은 여기서 배웁니다"** 라고
명시하고 있다. 두 지형은 별개다.

| | 학습 지형 (커리큘럼 0~9) | 예선 평가 7종 |
|---|---|---|
| 계단 5~23cm | 33% | **없음** |
| 박스 단차 15~50cm | 33% | **없음** |
| 경사 | 0~22° | H6 ±10° (0.15) |
| 요철 | 2~10cm, 11% | H5 2~10cm (0.15) |
| 평지 | — | **H1·H2·H3·H4·H7 = 가중 0.70** |

**따라서:**
1. `terrain_levels`는 **예선 점수축이 아니다** — 본선 단차 대비축이다. 예선 통과가 목표인
   구간에서 이 축에 게이트를 걸면 예선 점수와 무관한 이유로 개선안이 차단된다.
2. `base_contact = 0.3071`(낙상 31%)은 **계단 23cm·박스 50cm가 섞인 난이도 5.4 지형에서의
   낙상률**이다. 예선의 `survival_rate`(평지 70% 비중)와 같은 값이 아니며, 평지 survival은
   **미측정**이다. 31%를 예선 낙상률로 인용하지 않는다.
3. 예선 대비 관점에서 학습 지형은 **과잉 난이도**다. 이것이 손해라는 뜻은 아니다(전이 효과는
   미측정) — 다만 **측정 대상이 아니었다**는 것은 확정 사실이다.

### F23 — 학습 비용 실측 (260829 · `reports/train_260828-02.log`)

`Iteration time` 3000행 평균 **0.725 s/iter** (마지막 200행 0.724 — 안정).

| iter | 소요 | 비고 |
|---:|---|---|
| 1,000 | 12분 | |
| 3,000 | **36분** | 지금까지 전 run |
| 5,000 | 60분 | |
| 10,000 | **2시간 01분** | 최종본 하한 |
| 15,000 | **3시간 01분** | 최종본 상한 |

→ **GPU 벽시계 50시간이라면 약 248,000 iter.** 4 run이 쓴 **GPU 벽시계 시간은 약 2.4시간**이다.

> **⚠️ 260829 정정 (사이클 1 판정 지적).** 이 절의 초판은 "예산의 4.8% 사용, 95% 잔여"라고
> 적었다. **이것은 D3(사람 시간 ≠ 벽시계 시간) 위반이다.** 사용자가 말한 "학습 시간 50시간"이
> GPU 벽시계인지 사람 시간(강좌 학습·영상 관찰·문서 작성 포함)인지는 **미확인(공백 F)** 이며,
> 2.4h를 50h로 나눈 비율은 **어느 쪽 예산에도 검증 없이 적용할 수 없다**. 확정 사실은
> 「단가 0.725 s/iter」와 「GPU 벽시계 누적 2.4h」 둘뿐이다. 이하 잔여율 표현은 폐기한다.

확정되는 것: **1회 실험의 GPU 비용은 36분이고 최종 장기 학습은 2~3시간이다.** 이 규모는
어느 해석에서도 "한 축을 확인하기 위해 감당 못 할 비용"이 아니다 — S0~S3 순서는 두 해석
모두에서 성립한다(사람 시간 해석이면 S0·S1이 사람 시간을 쓰므로 **더욱** 먼저 확정해야 한다).
`humanoid-test-planner`의 "45분 이상 = 비용 게이트"는 3000 iter(36분)를 아슬아슬하게 통과시키고
5000 iter(60분)를 막는 값인데, 이 경계는 실측 없이 설정된 것이다.

### F24 — **예선 7 시나리오 중 측정된 것은 0개** (260829)

| 시나리오 | 가중 | 지형 | 확인 상태 |
|---|---:|---|---|
| H1 제자리 균형 | 0.15 | 평지 | **행동만 확인**(사용자 관찰: 서 있음). 격리 점수 미측정 |
| H2 전진 속도 | **0.20** | 평지 | **미확인** — 전진 명령에 전진하는지 본 적 없음 |
| H3 좌우 속도 | 0.10 | 평지 | **미확인** |
| H4 회전 복합 | 0.10 | 평지 | 학습 통계만(statue 0.63x). 격리 행동 미확인 |
| H5 약한 요철 | 0.15 | 요철 | **미확인** |
| H6 완만한 경사 | 0.15 | 경사 | **미확인** |
| H7 밀침 회복 | 0.15 | 평지+충격 | **미확인** — `NCRC_PLAY_PUSH` 기능이 있는데 4 run 동안 한 번도 안 씀 |

**행동이 확인된 가중 = 0.15 / 1.00.** 나머지 0.85는 전부 미확인이다.
4 run 동안 우리가 본 수치는 전부 **커리큘럼 지형 학습 통계**이지 예선 시나리오 점수가 아니다.

### F25 — **서버는 세션마다 초기화된다** (260829 · 실측)

`root@da-perfect38`에서 `play.py` 실행 시 `exported/model_best.pt` 없음으로 즉시 종료.
4 run의 산출물은 **사용자 PC(이 저장소)에만** 존재한다. 12강 PART 5가 이미 경고하고 있다 —
*"클라우드 서버(GPU 박스)를 종료하면 그 서버에 있던 학습 내용은 모두 사라져요."*

**이 제약은 사용자가 구두로 알려줬으나 이 저장소 어디에도 기록돼 있지 않았고, 그래서 260829
S0 실행계획이 그것을 무시한 채 작성됐다** (`grep 초기화|리셋|휘발` → 8개 정본 전체 0건).
정본에 없는 제약은 다음 세션에 소멸한다 — 이 항목이 그 증거다.

| 휘발되는 것 | 복구 방법 |
|---|---|
| `exported/model_best.pt` (Run 04, sha256 `c785cc36…e28c7`) | `bootstrap_run04.zip` |
| `humanoid_rewards.py` 튜닝값 (`track_ang_vel_z_exp 1.0` · `termination_penalty -50.0`) | 같은 zip |
| `reports/`, `logs/` | 이 저장소가 정본 (서버 복구 불필요) |
| 강좌 배포 파일 (`train.py`·`play.py`·`h1_task/`) | 서버 이미지가 복원 — 업로드 불필요 |

**세션 소멸 시 잃는 것은 GPU 시간이 아니라 사람 시간이다** — 매 세션 업로드·검증 의식이 붙는다.
공백 F(50시간의 정의)가 사람 시간 해석이면 이 비용이 예산에 직접 들어간다.

### F26 — `train.py`는 매 학습마다 대회 서버로 자동 제출한다 (260829 · 실측)

`train.py:162-163` — *"리포트 report.html 생성 + 제출 zip 전송 모두 finalize()가 자동 수행.
플래그 불필요."* 12강 실행결과에 `[submit] ✅ 제출 성공 (HTTP 200)` 확인.
차단 스위치는 환경변수 **`NO_AUTO_SUBMIT=1`**.

→ **학습 횟수 = 제출 횟수다.** S2(2~3 run)·S3(1 run) 계획은 제출 4회를 뜻한다.
**제출 횟수 제한 여부는 미확인(공백 H)** 이며, 제한이 있다면 S2의 run 수가 곧바로 제약된다.

> **⚠️ 정정 (260830 · Codex 검토 §4 지적 → 메인 루프 코드 실측 확인)**
>
> **위 결론은 틀렸다. 자동 전송되는 것은 대회 제출이 아니라 운영 서버 자동 백업이다.**
>
> `go2_task/_finalize.py:841-849` (h1_task 미러) docstring 원문:
> *"exported/ 결과물을 zip으로 묶어 운영 서버에 **자동 백업**(학습 종료 시).
> ⚠️ **예선 제출이 아니다.** 공식 제출은 참가자가 대회 웹사이트에서 policy.pt·env.yaml·
> 코멘트를 직접 업로드하는 것. 이 기능은 학습이 몇 시간 걸리므로 서버가 꺼지거나
> 컨테이너가 재생성돼도 학습 이력이 남도록 제공하는 편의 장치다."*
> `_finalize.py:191` 주석도 동일: *"학습 결과 자동 **백업** (zip → 운영 서버) — 예선 제출과 별개(편의 장치)."*
>
> 뒷받침 2건:
> - zip 내용물은 `model_best.pt` + `env.yaml` + `report.html`이다(`_finalize.py:869-878`).
>   **공식 제출물 `policy.pt`가 들어 있지 않다.**
> - `PRELIM_RL_GUID.md:198` 제출 전 체크리스트 — *"제출 3종(policy.pt · env.yaml · 리포트)을
>   **팀 대시보드에 업로드**했나요?"* 업로드는 사람이 한다.
>
> 따라서:
> - **학습 횟수 ≠ 공식 제출 횟수.** 진단 학습이 순위표에 올라가지 않는다.
> - **학습만으로는 제출물이 만들어지지 않는다** — `policy.pt`는 `play.py`가 만든다(D17).
> - `NO_AUTO_SUBMIT=1`의 의미는 "제출 차단"이 아니라 "외부 백업 전송 차단"이다.
>   운영 서버로 산출물이 나가는 것을 원치 않는 진단 run에는 계속 붙인다.
>
> 이 오독이 캠페인에 끼친 영향: 공백 H(제출 횟수 제한)가 S2의 run 수를 제약한다는 전제가
> **성립하지 않는다.** run 예산의 제약은 제출 한도가 아니라 GPU·사람 시간뿐이다.
> `prelim-campaign-manager.md:271-274`도 같은 오독을 담고 있어 정정 대상이다(동반 갱신).

### F27 — **체크포인트를 올리면 학습 없이 play가 된다** (260829 · 실측 · F25/D13 보완)

세션 초기화 후 `bootstrap_run05.zip` 업로드 → `restore.sh` → `play.py` 실행 로그에
`[INFO]: Loading model checkpoint from: /workspace/training/humanoid/exported/model_best.pt`
(`reports/S0b/H1_stand.log`). **학습 0분으로 영상·재생이 성립한다.**
`play.py` docstring 원문이 이를 명시한다 — *"pt 를 올리거나 타임스탬프 백업을 `model_best.pt` 로
덮어써도 된다."*

> 260829 오전에 "이 서버는 학습해야만 영상이 나온다"고 판단했던 근거는
> `exported/model_best.pt 없음` 경고였는데, 그건 **세션 초기화로 폴더가 비어 있었기 때문**(F25)이지
> 학습이 필수여서가 아니었다. 원인과 증상을 뒤바꾼 오판이며, 이 오판 때문에 Run 05 학습(36분)이
> 영상 확보 목적으로 정당화됐다. Run 05 자체는 S2 실험으로서 유효하므로 낭비는 아니지만,
> **앞으로 시나리오 영상 확보에 학습을 편성하지 않는다.**

→ 재생 전제 조건 2가지: `exported/model_best.pt` **와** `humanoid_rewards.py` 값이 함께 복구돼야
한다. `env_cfg._apply_common_overrides()`가 매 실행 reward dict를 읽어 환경을 구성하므로,
값이 다르면 다른 환경에서 재생된다. `restore.sh`가 둘 다 검사한다.

### F28 — **play 로그에는 성능 수치가 한 줄도 없다** (260829 · 실측)

`reports/S0b/H1_stand.log`(17,800 bytes)에 `error_vel` · `Episode_*` · `mean_episode_length` ·
보상 요약 **전부 0건**. `play.py`는 설정 표와 로딩 경로만 찍고 종료한다.

→ **S0 시나리오 판정의 유일한 증거는 mp4 육안 확인이다.** 자동 게이트를 걸 수치가 없으므로,
S0 수용기준을 수치 임계로 적으면 그 자체가 만족 불가능한 자다(원칙 12-a). S0의 수용기준은
「시나리오별 mp4 존재 + 로그 내 Traceback 0건 + 사용자 육안 판정(넘어짐/방향)」으로 적어야 한다.

### F29 — Run 05 결과 (260829 · `reports/S0_260829/train_260829-05.log`)

단일 변수 `track_lin_vel_xy_exp` 0.5 → 1.0. 3000 iter, exit=0, `NO_AUTO_SUBMIT=1`로 미제출.

| 지표 | Run 04 | Run 05 | 판정 |
|------|--------|--------|------|
| 낙상률 | 29.8% | 30.4% | **변화 없음** (+0.6pp) |
| 탐색 std | 1.311 | 1.672 | 악화 |
| 최고보상 | 3.84 | 12.09 | **비교 불가** |

> **⚠️ 260829 정정 — 위 표의 "Run 04" 열은 실제로 Run 02의 값이다.** 메인 루프가 열을 잘못
> 옮겼다. `experiment_history.csv` Run 02 행이 `base_contact=0.2979`(29.8%)·`reward=3.8434`이고,
> Run 04 행은 `base_contact=0.3071`·`reward_best=9.0870`·`std=1.4191`이다. 정정표는 F31.
> **결론(「Run 05는 낙상률을 개선하지 못했다」)은 바뀌지 않는다** — 30.71%→30.50%도 무변화다.
> 바뀌는 것은 std 악화 폭뿐이다(1.311→1.672가 아니라 **1.419→1.672**, +17.8%).

> 보상 12.09는 개선이 아니다. 추종 가중치를 2배로 올렸으므로 동일한 걸음도 값이 커진다.
> **가중치가 다른 run끼리 보상 절대값 비교 금지** — D6(`error_vel` 생존시간 교란)과 같은 부류의
> 함정이며, 이쪽은 축이 보상 스케일이다.

→ **S2 후보 1번(`track_lin_vel_xy_exp` 단독 상향)은 낙상률을 개선하지 못했다.** 다음 후보는
이 결과를 근거로 재선정한다.

### F30 — S0 근사 진단 6종 완료 (260829 · `reports/S0b/`)

Run 05 체크포인트를 업로드해 **학습 0분**으로 6종 재생. 6개 로그 전부:
`Traceback`·`Error executing job`·`AttributeError`·🔴 = **0건**,
`Curriculum Manager: contains 0 active terms` = 1건,
`Loading model checkpoint from: .../exported/model_best.pt` = 1건,
`track_lin_vel_xy_exp.weight = 1.0` = 1건. `밀침 테스트 켜짐 — 4초마다 ±0.5 m/s`는 H7에만 1건.

| 시나리오 | 가중 | 명령 | mp4 (bytes) | 육안 판정 |
|---|---|---|---|---|
| H1 제자리 | 0.15 | (0,0,0) | 6,762,089 | 직립 유지, 화살표 없음(명령 0 반영) |
| H2 전진 | 0.20 | vx=0.75 | 9,008,625 | 보행, 파랑·초록 화살표 정렬 |
| H3 좌 | 0.10 | vy=+0.4 | 7,751,102 | 측방 보행 |
| H3 우 | 0.10 | vy=-0.4 | 8,005,701 | 측방 보행 |
| H4 회전복합 | 0.10 | vx=0.5,wz=0.5 | 8,762,704 | 선회 보행, 직립 |
| H7 밀침 | 0.15 | (0,0,0)+push 0.5 | 6,956,756 | **4회 밀침 후에도 직립** |

→ **명령 override가 실제로 걸렸다는 시각 증거**: H1·H7은 파란 마커가 점(길이 0), H2·H3·H4는
파란 화살표가 뻗어 있다. 파랑=지령 속도, 초록=실제 속도 마커이며 후자는 지령이 0인 두 종에서만
사라진다. 로그에는 명령값이 찍히지 않으므로(F28) **이것이 override 적용의 유일한 직접 증거다.**

> **판정 한계**: 20초 영상에서 8프레임(H4·H7은 4프레임 추가 확대)만 표본했다. 표본 시점에
> 넘어진 개체는 없었으나 **표본 사이의 낙상·리셋은 검출하지 못한다.** 생존율 수치로 인용 금지 —
> "표본 시점 전부 직립"까지만 말할 수 있다.

→ **시나리오 커버리지 0.15 → 0.80** (H1·H2·H3·H4·H7). 남은 것은 H5(요철 0.15)·H6(경사 0.15)
= 0.20. F24("측정된 것 0개")는 이로써 해소된다.

### F31 — **Run 05는 실패가 아니라 교환이었다 · 두 추종 항은 시소다** (260829 · 로그 재파싱)

F29는 Run 05를 「낙상률 무변화 / 보상 비교 불가」로만 적었다. 사실이지만 **결과의 본체가 아니다.**
`train_260829-05.log`를 항별로 재파싱하니(마지막 50 iter 평균) 정반대 그림이 나온다.

**증거 1 — 항별 성취율** (`Episode_Reward/<항목>` ÷ 가중치. F21이 세운 「상한 대비」 방식)

| 항목 | Run 04 | Run 05 | 변화 |
|---|---:|---:|---:|
| 직진 성취율 | 0.5828 | 0.6191 | **+6.2%** |
| 회전 성취율 | 0.4407 | 0.3207 | **-27.2%** |

> 생존 보정 불필요 — `Mean episode length`가 767.2 대 757.0으로 **1.3% 차이**다. 같은 생존
> 수준에서의 비교이므로 D6(생존시간 교란)이 걸리지 않는다.

**증거 2 — 추종 오차** (F18 방식 보정 `raw × 500 / ep_len`. statue = xy 0.7165 · yaw 0.6681)

| Run | 가중치 직진:회전 | ep_len | xy/step | statue 대비 | yaw/step | statue 대비 |
|---|---|---:|---:|---:|---:|---:|
| 04 | 0.5 : **1.0** | 767.2 | 0.2730 | 0.38x | **0.4190** | **0.63x** |
| 05 | **1.0** : 1.0 | 757.0 | **0.2243** | **0.31x** | 0.6255 | 0.94x |

**두 증거가 같은 말을 한다:**
1. **직진은 5 run 중 최고.** xy/step 0.2243 = statue의 0.31x — Run 02(0.32x)를 근소하게 넘었다.
2. **회전은 Run 04의 성과가 통째로 사라졌다.** 0.63x → 0.94x, 즉 회전 가중치를 올리기 **전인
   Run 02(0.92x) 수준으로 복귀**했다. 회전 가중치는 여전히 1.0인데도 그렇다.
3. → 효과를 내는 것은 **가중치의 절대값이 아니라 두 항의 비율**이다. Run 04는 1:2라서 회전이
   이겼고, Run 05는 1:1이라서 직진이 이겼다.

**예선 배점 환산**: 직진(H2 0.20 + H3 0.10) × (+0.0363) = **+0.0109**,
회전(H4 0.10) × (-0.1200) = **-0.0120**. → **본전 부근, 근소 손해.**
(H4는 `vx+wz` 복합이라 회전 손실을 과대평가한 보수적 계산인데도 그렇다.)

→ Run 05의 진짜 결과는 「낙상률이 안 좋아졌다」가 아니라
**「36분을 써서 저울을 반대로 기울였고 총합은 그대로였다」** 이다.

**F29 정정표** (같은 기준으로 다시 적음):

| 지표 | Run 04 | Run 05 | 판정 |
|---|---:|---:|---|
| 낙상률 `base_contact` | 0.3071 | 0.3050 | 변화 없음 (-0.21%p) |
| 탐색 std | 1.4191 | 1.672 | 악화 (+17.8%) |
| 최고보상 | 9.087 | 12.089 | 비교 불가 (가중치 변경) |
| 지형 난이도 | 5.3926 | 5.5399 | +2.7% |

재현 명령·기대 출력은 `REPORT_260829.md` 부록.

### F32 — **낙상률은 4 run · 3 노브 동안 못 움직였다** (260829)

| Run | 변경 노브 | `base_contact` |
|---|---|---:|
| 01 | — | 0.9244 |
| 02 | `termination_penalty` | **0.2979** ← 여기서 한 번 크게 떨어진 뒤 |
| 03 | `termination_penalty` | 0.2785 |
| 04 | `track_ang_vel_z_exp` | 0.3071 |
| 05 | `track_lin_vel_xy_exp` | 0.3050 |

Run 02 이후 **4개 run · 서로 다른 3개 노브를 건드리는 동안 28~31%대에 고정**이다. 우연이 아니라
**지금까지 시도한 노브가 낙상 축을 건드리지 못한다**는 뜻이다.

단 F22가 붙는다 — 이 30%는 **난이도 5.5 지형(계단 23cm·박스 50cm)의 낙상률**이고 예선 평가
지형의 70%는 평지다. S0 영상 6종에서 표본 시점 낙상 0건인 것과 모순되지 않는다.
→ **낙상률 30%를 예선 목표로 쫓는 것은 과녁이 틀렸을 가능성이 높다.** 다만 `survival_rate`는
점수식에서 **곱해지는 항**(F5)이므로 평지 생존의 확증이 필요한데, 현재 그 확증은 **8프레임 표본**뿐이다.

### F33 — **5 run 전부 평가받지 않을 지형에서 측정됐다** (260829 · 구조적)

우리가 가진 모든 수치(`error_vel_*`·`base_contact`·`terrain_levels`·항별 보상)는 커리큘럼
지형(난이도 5.5 = 계단·박스 혼합)에서 나왔다. 예선 평가는 **평지 0.70 + 요철 0.15 + 경사 0.15**다(F22).
**평지에서의 추종 오차·생존율은 5 run 동안 단 한 번도 측정된 적이 없다.**

그리고 F28에 따라 **현 도구로는 측정할 수 없다** — `play.py`는 성능 수치를 한 줄도 찍지 않는다.
영상의 파랑(지령)↔초록(실제) 마커 정렬이 유일한 추종 정보이며 정성적이다.

→ 손해라는 증거는 없다(전이 효과 미측정). 그러나 **측정 대상이 아니었다는 것은 확정 사실**이며,
이것이 S2 후보 1번(지형 분포 이동)의 근거다.

### F34 — **보상 노브 9개 중 6개가 미탐색** (260829 · `train_260829-05.log` `[report] 보상 변화` 3줄)

| # | 노브 | 기본 | 현재 | 권장 | 이력 |
|---|---|---:|---:|---|---|
| ① | `track_lin_vel_xy_exp` | 0.5 | **1.0** | 0.5~2.5 | Run 05 (F31) |
| ② | `track_ang_vel_z_exp` | 0.5 | **1.0** | 0.5~2.0 | Run 04 PASS |
| ③ | `feet_air_time` | 0.2 | 0.2 | 0.1~0.5 | **미탐색** |
| ④ | `termination_penalty` | -5 | **-50** | -400~-50 | Run 02 채택 · Run 03 기각(**포화**) |
| ⑤ | `flat_orientation_l2` | -1.5 | -1.5 | -3.0~-0.2 | **미탐색** |
| ⑥ | `ang_vel_xy_l2` | -0.05 | -0.05 | -0.3~-0.02 | **미탐색** |
| ⑦ | `joint_deviation_hip` | -0.2 | -0.2 | -0.5~-0.1 | **미탐색** |
| ⑧ | `joint_deviation_torso` | -0.1 | -0.1 | -0.3~-0.02 | **미탐색** |
| ⑨ | `action_rate_l2` | -0.0005 | -0.0005 | -0.008~-0.0005 | **미탐색** |

탐색한 3개 중 ④는 포화 실증, ①②는 서로 상쇄(F31). **실질 자유도는 대부분 미탐색 6개 쪽에 있다.**

Run 05 상한 대비 성취율 실측: 직진 **61.9%** · 회전 **32.1%** · `feet_air_time` **3.2%**(0.0063/0.2) ·
`flat_orientation_l2` 벌점 -0.0148.
→ **`feet_air_time` 축이 사실상 꺼져 있다** — 이 로봇은 발을 거의 안 든다. F15가 Go2에 대해
"올려야 잘 걷기 시작"이라 기록한 것과 같은 상태이고 H1에서는 한 번도 안 건드렸다.
→ `flat_orientation_l2` 벌점이 -0.0148뿐이라는 것은 **몸통이 이미 충분히 곧다**는 뜻이다 —
이 축을 더 조여도 얻을 것이 없다.

### F35 — **F31·F33·F34의 정규화·표현 오류 4건 정정** (260829 · 외부 검토 지적 접수)

원칙 3에 따라 위 본문은 지우지 않고 여기에 정정을 덧붙인다. 지적 4건 전부 **타당**하며 수용했다.
정정 전문과 재계산은 `REPORT_260829.md`의 ⚠️ 블록 5개.

| # | 대상 | 틀린 것 | 정정 | 결론 영향 |
|---|---|---|---|---|
| C-1 | 커버리지 | 0.80 / 1.00 | **0.70 / 1.00** (0.15+0.20+0.10+0.10+0.15). 산술 오류 | 없음 (미측정 0.30 동일) |
| C-2 | F34 성취율 | `Episode_Reward ÷ w` | 분모에 **생존 보정** 필요 — IsaacLab은 `Σ(r·w·dt) ÷ max_ep_len_s`로 찍는다. Run 05 생존 비율 0.7569 → 직진 **81.8%** · 회전 **42.4%** | 없음 (상대 비교 유지) |
| C-3 | F34 `feet_air_time` | 3.2% · "축이 꺼져 있다" | per-step 상한은 1.0이 아니라 **`threshold=0.4`** (`exported/env.yaml:903`) + 한발지지·\|명령\|>0.1 게이트. → **10.4%**. "발을 거의 안 든다"는 **과장** — 발 높이가 낮은 것인지 한발지지 게이트를 못 넘는 것인지 **이 수치로 구분 불가** | **있음** — S2 후보 2번 근거 약화, 영상 판별 선행 |
| C-4 | F31 예선 환산 | +0.0109 / -0.0120 을 예선 점수처럼 제시 | 공식 점수는 평가 지형의 `survival × tracking`(F5). 위는 **커리큘럼 지형 학습 보상**이다. **방향성 참고값**으로만 유효 | 없음 (부호·크기 유지) |
| C-5 | F33 제목 | "전부 **잘못된** 지형" | `env.yaml:263-355` 실측 — 경사 **22.2%** · 요철 **11.1%** 이 학습 분포에 있고 예선의 ±10°(slope 0.176)는 학습 범위 0~0.4의 아래쪽 끝. 정확히는 **"평가 분포와 다른 혼합 지형의 집계값만 있다"** | **있음** — S2 후보 1번 근거 약화 |

**F31의 「비율이 원인」은 [사실]이 아니라 [추론]으로 내린다.** Run 04→05의 실변경은
`track_lin_vel_xy_exp` **하나**라 비율과 직진 절대값이 완전 교락이다. 단 `termination_penalty=-50`
고정 3 run에서는 비율만 세 점을 설명한다(02 비율1.0→0.92x · 04 비율0.5→**0.63x** · 05 비율1.0→0.94x;
회전 절대값도 직진 절대값도 설명 실패). [사실]로 올리지 않는 이유 — (a) run당 seed 1개 + cudnn
비결정성 (b) Run 03(비율 1.0, term -90)이 yaw 1.73x라 `termination_penalty`도 이 축을 크게 움직인다
→ 비율은 유일 구동자가 아니다 (c) 비율 직접 조작 확인 실험 미실행.

**여파 — S2 후보 1·2번 모두 근거가 부족해졌다. 학습보다 판정이 먼저다** (→ D16).

### F36 — **H5·H6 지형 세기 「단위 미확인」 차단은 해소 (실은 판정이 불필요했다)** (260829)

진행 18이 걸어둔 차단은 "`slope_range`가 라디안인가 높이/거리 비율인가"였다. **10°에서는
두 해석의 차이가 1.0%뿐이라 실행을 막을 사유가 아니다:**

- `tan(10°)` = **0.17632698** (비율 해석)
- `radians(10°)` = **0.17453293** (라디안 해석)
- 차이 = 0.0018 = **1.03%**

IsaacLab `hf_terrains.pyramid_sloped_terrain` 구현은 `slope`를 높이/거리 **비율**로 쓴다
(docstring은 "radians"라 적혀 있어 문서와 코드가 어긋나 있다). 따라서 **0.17632698을 쓴다.**
`train_260829-05_add.md:230-278`에 기획자가 이미 등록해 둔 값과 같다.

`random_rough`의 `noise_range`는 **미터**이며, 학습 설정 실측은 `(0.02, 0.10)`
(`exported/env.yaml:316`). H5는 이 학습 범위를 그대로 쓴다 — "학습한 요철에서 버티는가"가
먼저 답해야 할 질문이고, 예선의 "약한 요철" 절대 세기는 미제공이다(D8).

> **교훈** — 「단위 미확인」을 차단으로 올리기 전에 **두 해석의 수치 차이를 먼저 계산했어야 했다.**
> 차이가 판정에 영향을 주지 않으면 그것은 차단이 아니다. 이 차단은 하루를 잡아먹었다.

### F37 — **S0c-1 완료: Run 05가 요철·경사 3종 전부에서 낙상 0 (커버리지 0.70 → 1.00)** (260829)

Run 05 정책(`exported/model_best.pt`, sha256 `2775a61e…9abc`)으로 `play.py` 3회. 학습 0분·제출 0회.

| 산출물 | 크기 | 지형 | 명령 | 낙상 |
|--------|------|------|------|------|
| `reports/S0c/H5_rough.mp4` | 9,291,697 B | `random_rough` 100%, `noise_range=[0.02,0.10]` m, `difficulty_range=[1.0,1.0]`, 6×4 타일 | `lin_vel_x=0.75` | **0** |
| `reports/S0c/H6_up.mp4` | 6,074,136 B | `hf_pyramid_slope_inv` 100%, `slope_range=[0.17632698,…]` (10°) | `lin_vel_x=0.75` | **0** |
| `reports/S0c/H6_down.mp4` | 6,406,601 B | `hf_pyramid_slope` 100%, 동일 세기 | `lin_vel_x=0.75` | **0** |

**판정 방법(실측)** — 각 mp4에서 40프레임 간격 25장(n=0,40,…,960)을 뽑아 5×5 대조표로 확인.
`video_length: 1000` = `episode_length_s 20.0` ÷ `dt 0.02` 이므로 **1 에피소드 전량**이다.
25/25 프레임 모두 직립·리셋 없음 → 세 시나리오 모두 에피소드 끝까지 생존.

**지형이 실제로 적용됐다는 증거** — 로그에는 지형 덤프가 없어(F28) 화면으로 검증했다.
평지 run(`S0b/H2_forward.mp4`)과 H6 두 run의 같은 프레임(n=400)을 회색조·동일 대비
(`eq=contrast=3.0:brightness=-0.55`)로 비교하면 **평지는 균일**하고 **H6 두 run은 대형 면
단위 밝기 편차와 높이장 계단 무늬**를 보인다. H5는 대비 처리 없이도 요철이 육안 확인된다.

**한계 3건 (인용 시 반드시 동반)**
1. **낙상 0은 `survival_rate`가 아니다.** `play.py`가 지표를 하나도 찍지 않으므로(F28)
   근거는 `num_envs=4` 중 **추적 env 1개**의 **1 에피소드**, 그것도 **25프레임 표본**이다.
2. **H5는 에피소드 후반이 요철이 아니다.** 로봇이 n≈440(≈8.8 s)에 요철 필드를 벗어나
   평지 border로 나간다. 요철 노출은 **약 44%**다. `num_rows` 1→6 수정으로 필드는
   8 m→48 m가 됐으나 `curriculum=false`의 랜덤 배치 탓에 시작 타일이 가장자리였다.
3. **H6의 up/down 라벨은 신뢰할 수 없다.** 로봇은 타일 중심에서 출발하므로 `pyramid_slope`
   (중심이 봉우리)는 **내리막**부터, `_inv`(중심이 웅덩이)는 **오르막**부터 시작한다 —
   내가 붙인 이름과 반대다. 게다가 타일을 넘으면 오르막·내리막이 섞인다. 따라서 두 run은
   **"±10° 경사에 노출됐고 넘어지지 않았다"까지만** 말할 수 있고, 오르막과 내리막을
   분리 측정한 것이 아니다.

`H5_rough_r1.*`(`num_rows=1` 초회)은 삭제하지 않았다 — 후반부가 완전 평지라
**5 run 통틀어 유일한 평지 표본**이다(F33 공백의 부분 관측).

### F38 — **S0c-2 완료: 영상은 Run 04와 Run 05를 구분하지 못했다 (음성 결과)** (260829)

Run 04(`model_best_20260828215939.pt`, sha256 `c785cc36…e28c7`)를 `exported/model_best.pt`로
교체해 4 시나리오 재생. `reports/S0d/` mp4 4 + log 4, 로그 4건 모두 `Traceback`·
`Error executing job` **0건**. 학습 0분·제출 0회.

| 시나리오 | Run 05 (S0c/S0b) | Run 04 (S0d) | 판별력 |
|---|---|---|---|
| H5 요철 | 25/25 직립 | 25/25 직립 | **없음** |
| H6 경사 A | 25/25 직립 | 25/25 직립 | **없음** |
| H6 경사 B | 25/25 직립 | 25/25 직립 | **없음** |
| H2 전진(평지) | 직립 | 25/25 직립 | **없음** |

**낙상 축은 두 정책을 가르지 못한다.** 학습 로그의 `base_contact`가 0.3071 vs 0.3050으로
이미 동률이었고, 영상도 같은 답을 냈다. S0c-2의 기대 산출("Run 04는 요철·경사에서
넘어지는가")에 대한 답은 **아니오**이며, 따라서 **기준선 판정에 새 정보를 주지 못했다.**

**추종 축을 영상으로 읽으려 한 시도는 실패했다 (기록용).** 지령 마커(파랑)와 실측 마커(초록)의
길이 차로 추종 오차를 읽으려 했으나, 첫 비교(H5, n=800/850/900)에서 Run 04 쪽 화살표가
훨씬 길게 보인 것은 **두 run의 로봇 진행 방향이 달라 카메라에 대한 투영이 달랐기 때문**이지
추종 차이가 아니었다. 같은 시나리오·같은 초기 프레임(H6_up, n=40~160)으로 통제해 다시 보면
**두 run의 마커 크기 차이는 육안으로 판별되지 않는다.** → **마커 길이를 추종 지표로 쓰지 않는다.**

> **비용 기록** — S0c-2는 사용자 시간 약 6분을 쓰고 판별 정보 0을 반환했다. 실행 전에
> "낙상률이 이미 동률이므로 영상도 동률일 가능성이 높다"는 것을 계산할 수 있었다.
> **교훈: 새 측정을 편성하기 전에 「기존 지표가 이미 그 답을 갖고 있지 않은가」를 먼저 본다.**

### F39 — **Run 04 → Run 05의 변경은 노브 1개였다 (`track_lin_vel_xy_exp` 0.5 → 1.0)** (260829)

`exported/env_20260828215939.yaml`(Run 04)과 `exported/env.yaml`(Run 05)의 보상 가중 9개 전수 대조:

| 항 | Run 04 | Run 05 |
|---|---:|---:|
| **`track_lin_vel_xy_exp`** | **0.5** | **1.0** |
| `track_ang_vel_z_exp` | 1.0 | 1.0 |
| `termination_penalty` | -50.0 | -50.0 |
| `flat_orientation_l2` | -1.5 | -1.5 |
| `ang_vel_xy_l2` | -0.05 | -0.05 |
| `action_rate_l2` | -0.0005 | -0.0005 |

나머지는 전부 동일. 즉 **Run 05는 직진 추종 가중을 2배로 올린 단일 변수 실험**이었고,
결과는 직진 76.0% → 81.8%(+7.7%) · 회전 57.4% → 42.4%(−26.3%)의 시소였다(F31).

> **`reports/experiment_history.csv`에 Run 05 행이 없다.** 원장은 Run 04에서 끊겨 있고,
> Run 05의 사전 등록·변경 사유·판정이 CSV에 기록되지 않았다. 다음 학습 전에 채워야 한다.

### F40 — **학습 곡선 원본이 PC에 있었다 · Run 05는 3000에서 절반만 수렴했다** (260830 · 실측)

**F28은 「`play` 로그에 수치가 없다」였지 「수치가 없다」가 아니었다.** 5 run 전부의 tensorboard
원본(`logs/rsl_rl/humanoid/<run>/events.out.tfevents.*`, 각 약 6.0 MB)이 PC에 내려와 있고,
**31개 태그 × 3000 iter**가 전부 들어 있다. 서버·GPU 없이 로컬에서 읽힌다.

- 도구: `tools/tfcurve.py` (tensorboard 의존 없음 — TFRecord 프레임 + Event protobuf 직접 파싱)
- 재현: `cd workspace/training/humanoid && python ../../../tools/tfcurve.py window Run04 Run05`
- **파서 검증**: 출력 `final` 열이 `experiment_history.csv` Run 04 행의 5개 값과 전부 일치 —
  `error_vel_xy=0.4102` · `error_vel_yaw=0.6230` · `base_contact=0.3071` ·
  `terrain_levels=5.3926` · `std=1.4191`. 5/5 재현.

**Run 04 vs Run 05 (마지막 500 iter 평균 · 마지막 1000 iter OLS 기울기/1k):**

| 지표 | R04 평균 | R05 평균 | Δ | R04 기울기 | R05 기울기 |
|---|---:|---:|---:|---:|---:|
| `error_vel_xy` | 0.4296 | 0.3385 | **−21.2%** | −0.0372 | −0.0099 |
| `error_vel_yaw` | 0.6501 | 0.9613 | **+47.9%** | −0.0498 | **−0.1063** |
| `base_contact` | 0.3073 | 0.3020 | −1.7% | −0.0198 | −0.0025 |
| `terrain_levels` | 5.2830 | 5.5646 | +5.3% | +0.4917 | +0.3279 |
| `mean_std` | 1.4163 | 1.6641 | +17.5% | +0.0023 | **−0.0135** |
| `mean_episode_length` | 776.9 | 762.6 | −1.8% | +6.00 | −2.30 |
| `feet_air_time` | 0.0077 | 0.0062 | −19.9% | +0.0010 | +0.0005 |

**읽는 법 — 수렴한 축과 안 한 축이 갈린다:**

- **수렴함**: `base_contact`(−0.0025/1k, 0.302에서 평평) · `mean_episode_length`(±0.3%) ·
  `mean_std`(Run 05는 iter 1000 부근에서 1.66에 안착한 뒤 **하락 중** −0.0135/1k) ·
  `learning_rate`(adaptive schedule이 0.0002에 안착).
- **미수렴**: `error_vel_yaw`가 **−0.1063/1k로 계속 내려가는 중**이고 이는 Run 04(−0.0498/1k)의
  **2.1배 속도**다. `terrain_levels`는 5.56인데 **최대는 9**(`env.yaml:257 num_rows:10`)이고
  아직 +0.33/1k로 오르는 중이다 — 커리큘럼이 포화하지 않았다.

→ **D18이 근거로 삼은 "회전 손해"는 정상상태 값이 아니라 아직 내려가는 중인 값이다.**
선형 외삽으로 5000 iter까지 가면 Run 05의 `error_vel_yaw` ≈ `0.9613 − 2×0.1063 = 0.749`,
Run 04 ≈ `0.6501 − 2×0.0498 = 0.550`. 격차는 +0.311 → +0.199로 **좁아지지만 닫히지는 않는다**
(외삽은 측정이 아니다 — 기울기가 감속할 수 있으므로 낙관 상한으로만 읽는다).

**부수 발견 — 두 run의 비교 자체가 교란돼 있다.** `terrain_levels`가 서로 다른 속도로
(0.49 vs 0.33 /1k) 오르고 있으므로, Run 04의 오차는 **더 빨리 어려워지는 지형**에서 측정됐다.
이 교란은 Run 04에 불리한 방향이므로 Run 05의 직진 우위는 다소 과대, 회전 열위는 다소 과소다.

### F41 — **`_finalize.py`는 학습 끝에 중간 체크포인트를 전부 삭제한다** (260830 · 실측)

`h1_task/_finalize.py:111-140` `_cleanup_intermediate_checkpoints()` — `run_dir.glob("model_*.pt")`
를 돌며 **best 1개(`keep_iter`)만 남기고 `p.unlink()`**. `finalize(cleanup=True)`가 기본값이고
`train.py:146`은 `finalize()`를 인자 없이 호출하므로 **`train.py` 경로로는 끌 수 없다**
(`--no-cleanup`은 `_finalize.py`를 직접 실행할 때만 걸리는 인자다 — `_finalize.py:938`).

실증: 로컬 5개 run 폴더에 `model_*.pt`가 **각각 1개씩만** 있다
(`model_2999` · `model_2800` · `model_2500` · `model_2800` · `model_2700`). `save_interval: 100`
이므로 원래는 run당 30개가 있어야 한다.

→ **"3000 체크포인트와 5000 체크포인트를 비교한다"는 계획은 그대로는 실패한다.** 학습 도중에
`model_3000.pt`를 다른 폴더로 복사해 두지 않으면 finalize가 지운다. 다만 **수치 비교는
`.pt` 없이도 된다** — tfevents는 삭제 대상이 아니고 iteration별 값을 전부 갖고 있다(F40).
`.pt` 구출은 그 체크포인트를 **play하거나 제출할 때만** 필요하다.

### F42 — **`model_best.pt`는 잡음 스파이크로 뽑힌다 · 영상의 정책 ≠ 지표의 정책** (260830 · 실측)

`_finalize.py:89` `best = max(eligible, key=lambda e: e.value)` — **`Train/mean_reward`의 argmax**
로 체크포인트를 고른다. 그런데 그 계열은 잡음이 크다:

| run | 마지막 500 iter 평균 | 표준편차 | 전역 최대 | 최대 위치 | 최종값 | 최대의 z |
|---|---:|---:|---:|---:|---:|---:|
| Run 04 | 7.680 | 0.545 | 9.087 | iter **2827** | 7.539 | **+2.58σ** |
| Run 05 | 10.159 | 0.635 | 12.089 | iter **2738** | 10.517 | **+3.04σ** |

표본 약 2,700개에서 뽑은 +2.6σ·+3.0σ 최대값은 **정책이 좋아서가 아니라 표본이 커서 나온다.**
즉 `model_best.pt` 선별은 품질 순위가 아니라 **잡음의 argmax**다.

**결과 1 — 두 가지가 어긋나 있다.** Run 05의 `model_best.pt`는 `model_2700.pt`(iter 2738)이고
(`train_260829-05.log:111233-111234` 실측), Run 04는 `model_2800.pt`다. 그런데 F40·F31·D18이
비교한 지표는 전부 **iter 2999 시점 값**이다. → **S0b·S0c·S0d의 영상 13종을 만든 정책은
지표를 비교한 그 정책이 아니다.** 차이는 iter 기준 약 9%이고 이 구간은 곡선이 평평하므로
(F40) 결론을 뒤집을 크기는 아니지만, **"영상과 수치가 같은 정책을 말한다"는 전제는 틀렸다.**

**결과 2 — `experiment_history.csv`의 `reward_best`는 run 성능이 아니다.** Run 04 행의
`reward_best=9.0870`은 잡음 계열의 최대값이며 **위로 편향된 추정량**이다. run 간 비교에
쓰면 안 된다(D6가 `error_vel_*`에 대해 규정한 것과 같은 부류).

> **⚠️ 보정 (260830 · 체크포인트 직접 열람)** — `torch.load('model_best.pt')['iter']` 실측:
> **Run 05 = 2700**. `iter 2738`은 reward argmax의 *step*이고, 저장된 체크포인트는
> `save_interval: 100` 격자 위의 `model_2700.pt`다. 정책을 특정할 때는 **2700**을 쓴다.
>
> **5 run 전체 best iter** (`model_best_*.pt`의 `iter` 필드 실측) —
> Run 01 **2999** · Run 02 **2800** · Run 03 **2500** · Run 04 **2800** · Run 05 **2700**.
> → **F42는 Run 04·05만의 문제가 아니라 5 run 전부에 걸린다.** `iter 2999` 지표와
> `model_best.pt` 정책이 일치하는 run은 Run 01 하나뿐이다.

### F43 — **`exported/policy.pt`는 지금 Run 04다 · 해시는 정책 동일성의 증거가 아니다** (260830 · 실측)

제출 3종(`policy.pt`·`env.yaml`·리포트) 정합성을 PC에서 직접 검증한 결과 **두 가지가 나왔다.**

**(1) 현재 `exported/policy.pt`는 Run 05가 아니라 Run 04다.**
가중치 대조(`torch.jit.load(...).state_dict()` ↔ `model_best_*.pt`의 `actor_state_dict['mlp.*']`):

| 파일 | 대응 checkpoint | iter |
|---|---|---:|
| `exported/policy.pt` (현재) | `model_best_20260828215939.pt` = **Run 04** | 2800 |
| `policy_20260829_171725.pt` | `model_best.pt` = **Run 05** | 2700 |
| `policy_20260829_212336.pt` | `model_best.pt` = **Run 05** | 2700 |
| `exported/env.yaml` | `env_20260829162232.yaml` = **Run 05** (sha256 `2e6bd68a…` 동일) | — |
| `exported/model_best.pt` | Run 05 (`restore.sh` `EXPECT_CKPT` 일치) | 2700 |

→ **지금 상태로 업로드하면 Run 04 정책 + Run 05 env.yaml + Run 05를 기술한 리포트가 나간다.**
원인은 D17: `play.py:165-209`가 매 play마다 `exported/policy.pt`를 덮어쓰고,
S0d(Run 04 대조군) 실행 시 `model_best.pt`를 Run 04로 바꿔 끼운 뒤 되돌렸기 때문이다.
**복구 방법: `policy_20260829_171725.pt`를 `policy.pt`로 복사.** (재학습·재play 불필요)

**(2) `policy.pt`의 파일 해시는 매번 다르다 — export가 바이트 비결정적이다.**
같은 `model_best.pt`(Run 05)에서 나온 두 export의 파일 해시는 다르지만
(`d04f692a…` vs `9df8fc18…`) 가중치는 `max|Δ| = 0.000e+00`으로 완전히 같다.

→ **정책 동일성 판정에 sha256을 쓰면 안 된다.** 해시는 "이 파일이 변조되지 않았다"의 증거일
뿐 "같은 정책이다"의 증거가 아니다. 동일성은 가중치 대조로 판정한다.
→ `TECH_REPORT_H1.md` §0이 정책 식별자로 적은 `policy.pt sha256 d04f692a…`는
**재현되지 않는 식별자**다. `model_best.pt sha256 2775a61e… · iter 2700`으로 교체해야 한다.
→ AGENTS.md 「자력 검증 우선」 3·4항으로 규정화했다.

### F44 — **공식 제출 0회 · 현재 확정 점수 0 / 200** (260830 · 원장 유도)

F26 정정으로 "학습 5회 = 제출 5회"가 무너졌으므로 실제 제출 횟수를 원장에서 확인했다.

**역할 구조상 이 값은 조회가 아니라 유도로 확정된다** — 서버·대시보드 조작은 전부 사용자가
메인 루프의 지시로 실행하고 그 결과가 「진행」 원장에 기록된다. 따라서 **지시가 없었으면
행위도 없다.**

- 「진행」 행 1~28 전체에 업로드·제출 행위 **0건**. 전부 학습·play·회수·문서다.
- `업로드`가 등장하는 곳은 F8(요건 서술) · F25/D13(세션 복구용 zip 업로드) ·
  F26 정정(코드 인용)뿐이며 **대회 대시보드 제출은 한 건도 없다.**
- Run 05는 `NO_AUTO_SUBMIT=1`(로그 `[backup] … 건너뜀`), 나머지는 백업만 나갔고
  그 백업은 제출이 아니다(F26 정정).

> **⚠️ 정정 (260830 · 사이클 2 팀장 판정 Q4 수용)**
>
> **제목의 「공식 제출 0회 · 확정 0/200」과 본문의 「지시가 없었으면 행위도 없다」는 과했다.**
>
> - **분모가 틀렸다.** D23으로 Go2가 폐기됐으므로 이 캠페인의 만점은 **200이 아니라 100**이다.
> - **「0회」로 단정할 수 없다.** 원장이 확정하는 것은 **우리 쪽 행위**(지시·검증·기록)이지
>   **사용자 쪽 행위**가 아니다. 사용자는 메인 루프의 지시 없이도 대시보드를 조작할 수 있고,
>   실제로 서버 파일 업로드가 그렇게 이뤄졌다. 원장 공백은 **부재의 증거가 아니다.**
>
> **정정 후 표기 — 이것이 현재 상태다:**
>
> | 항목 | 값 |
> |---|---|
> | 원장상 대시보드 업로드 **지시** | **0건** (이것은 확정 사실) |
> | 실제 공식 제출 여부 | **[미측정]** — 회수 방법: 대시보드 제출 내역 화면 |
> | 회수 시 보존할 것 | 로봇 유형(H1) · 제출 횟수 · 제출 시각 |
> | 제출됐을 경우의 점수 | **[미측정]** — 운영진 일괄 평가, 참가자 조회 불가(`PRELIM_RL_GUID.md:117`) |
> | 이 캠페인 만점 | **100** (H1 단독 · D23) |
>
> **판정에 미치는 영향:** U2 확인 전까지 **신규 제출 게이트와 재제출 게이트 중 어느 것을
> 적용할지 확정할 수 없다.** 제10조상 최고점만 남으므로 재제출은 무위험이지만, 그 논거는
> "이미 제출본이 있다"를 전제하며 그 전제가 아직 미확인이다.
>
> 근거: `AGENTS.md` 「자력 검증 우선」 1-b ⚠️정정 · `CAMPAIGN_SCHEDULE.md` §5.

> **아래 정정 전 결론(F44 말미)의 「H1 0점·제출 대기 확정」과 사용자 행위 유도 논리는 인용 금지다.**
> 현재 표기는 위 표의 `원장상 지시 0건 · 실제 제출 여부 [미측정]`이다.

### F45 — **현재 서버 세션은 미복원·학습 미실행 상태** (260831 · 사용자 터미널 실측)

- `tmux ls` → socket 없음.
- `pgrep -af 'train.py|isaaclab.sh'` → 결과 없음.
- `/workspace/_keep/train_260830-06.log`, `train_260830-07.log` → 둘 다 없음.
- 현재 `humanoid_rewards.py` → `track_lin_vel_xy_exp=0.5`, `track_ang_vel_z_exp=0.5`.

이는 **현재 서버 세션에 B2가 실행 중이지 않고 Run 05 복원도 되지 않았음**을 확정한다. 서버 세션은
초기화될 수 있으므로 과거 실행 자체의 부재까지 유도하지 않는다. 다만 회수된 artifact가 없으므로
현재 작업 기준에서 B2는 **미실행**으로 둔다. 당시 다음 동작은 `_bootstrap/restore.sh` 단독 복원이었으나,
서버 제한시간 제약이 확인된 뒤 D36이 이를 **Run 06 패키지 내부 사전검사**로 흡수했다.

### F46 — **서버는 접속할 때마다 초기화된다 · 로컬 training이 지속 정본이다** (260831 · 사용자 재확정)

- `/workspace`의 tmux·process·log·checkpoint·`_bootstrap`은 다음 접속까지 보존된다고 가정하지 않는다.
- 사용자는 매 작업 종료 시 서버 산출물을 내려받아 `C:\dev\Nconnect\workspace\training`에 반영한다.
- 다음 접속에서는 필요한 bootstrap·checkpoint·코드를 로컬에서 다시 업로드해야 한다.
- 따라서 “서버에 이전 log가 없다”는 과거 run 부재의 증거가 아니다. **로컬에 회수된 artifact가 있는가**가 지속성 판정 기준이다.

### F47 — **로컬 `workspace/training`에는 기존 학습 데이터가 보존돼 있다** (260831 · 재인벤토리)

| 영역 | 파일 수 | 크기 |
|---|---:|---:|
| `workspace/training/humanoid` | **190** | **466,878,876 B** |
| `humanoid/logs` | 25 | 66,110,150 B |
| `humanoid/reports` | 47 | 132,738,395 B |
| `humanoid/exported` | 76 | 246,677,367 B |
| `humanoid/_safe` | 1 | 7,151,477 B |
| `humanoid/_bootstrap` | 4 | 7,197,417 B |

유형별로 tfevents **5**, checkpoint `.pt` **34**, 영상 `.mp4` **37**, 원문 `.log` **22**가 있다.
따라서 서버가 비어 있어도 Run 01~05와 기존 평가가 사라진 것이 아니다. 새 실험은 이 데이터가
동일 대상·동일 조건·동일 측정단위로 이미 답을 갖고 있는지 먼저 확인한 뒤에만 편성한다.

### F48 — **Run 06 서버 패키지 준비 완료** (260831 · 로컬 검증)

- 파일: `workspace/training/humanoid/run06_server_package.zip`
- 크기: **6,654,894 B**
- SHA-256: `35707c80a1d5aae4d37b530bf1b5e1cf254b8330cff047618782c249a166aa36`
- ZIP 항목 14개: `train.py`, `play.py`, Run 05 `_bootstrap`, `h1_task/*.py`,
  `server_run06_long.sh`, Run 06 사전등록이 포함된 `experiment_history.csv`.
- 실행 스크립트는 restore·보상 9개 검증·from-scratch 학습·`NO_AUTO_SUBMIT=1`·tmux·
  3000/5000/10000/15000 근접 checkpoint 구출·최종 download bundle 생성을 한 번에 수행한다.
- `policy.pt`는 의도적으로 bundle에서 제외한다. `train.py`는 policy를 갱신하지 않으므로
  기존 Run 05 policy가 섞이는 것을 막고, 최종 checkpoint 선정 뒤 별도 export한다.

→ **H1 0점 · Go2 0점 · 합계 0 / 200이 현재 확정치다.** 이틀간의 산출물은 전부
제출 대기 상태이며, `policy.pt` 1회 복사 + 대시보드 업로드로 **GPU 0분에 H1 100점 축이 열린다.**
이것이 Go2 기준선(예상 2.4시간)보다 먼저다.

→ 교훈(AGENTS.md 「자력 검증 우선」 1항 보강): **역할 분담이 답을 결정하는 질문을 사용자에게
묻지 않는다.** 실행 주체가 사용자이고 지시 주체가 메인 루프이면, "그 행위를 했는가"는
메인 루프의 원장에 이미 있다.

**결과 3 — D21의 `.pt` 구출은 선택이 아니라 필수다.** D20 run에서도 `model_best.pt`는
스파이크 위치로 정해지므로, `model_3000.pt`·`model_5000.pt`를 이름으로 지정해 확보하지 않으면
**"3000의 정책"과 "5000의 정책"을 play·제출할 수단 자체가 없다.**

`train_260829-05.log:111180-111184` (iter 2999 최종값)이 F40 파서 출력과 4/4 일치:
`error_vel_xy=0.3303` · `error_vel_yaw=0.9290` · `base_contact=0.3042` · `terrain_levels=5.5734`.
→ F31 정정표의 `base_contact=0.3050` · `terrain_levels=5.5399`는 다른 출처(`report.html` 요약
추정)에서 온 값이다. **이후 정본 수치는 tfevents/로그 최종값으로 통일한다.**

## 방향 결정 (260829 — 사용자 지시 「리포트만 보고 향후 방향 결정」)

**결정: 학습을 멈추고 측정을 먼저 한다.** 근거는 F23 + F24다 — 1회 실험 비용이 36분으로
확정된 반면 **점수축 커버리지는 0.15 / 1.00**이다. 병목은 학습량이 아니라 **어느 축이 고장났는지
모른다는 것**이고, 학습을 한 번 더 도는 것은 그 병목을 건드리지 않는다.

| 단계 | 내용 | 학습 시간 | 획득 점수축 |
|---|---|---|---|
| **S0** | **예선 7 시나리오 근사 진단 play** — 평지 고정명령 + `NCRC_PLAY_PUSH`. Run 04·Run 02 체크포인트. **공식 평가 재현이 아니다**(D8) | **0분** | 시뮬 70의 **진단**, 의도분석 20의 **입력** |
| **S1** | 기술 개선 리포트 **초안** (S0 영상과 병행) — Run 01~04 의도·결과 대조는 `experiment_history.csv`에 이미 전부 있다. 최종본은 S3 정책 확정 후 | **0분** | 의도분석 20 + 리포트 10 = **최대 30점 축의 증거·문서 준비**(D10) |
| **S2** | S0에서 드러난 결함 축 **1개** 단일 변경. ~~현 최유력 후보 = `track_lin_vel_xy_exp` 0.5→1.0~~ → **Run 05로 소진**(F31: 교환이었고 총합 본전). 재선정표는 아래 D15 | 36분 × 2~3 run | 시뮬 70 |
| **S3** | 최종 장기 학습 | 2~3시간 | 시뮬 70 |

**총 GPU 벽시계 ≈ 5시간.** S0·S1은 학습 0분으로 30점 축의 증거를 만든다(획득이 아니라 준비 — D10).
Go2 착수는 S1과 병행한다(D11) — 유효 제출 1건의 한계 가치가 **최대 100점 축 개방**으로,
H1 reward 미세조정 어느 것보다 크다.

### 결정 근거 요약
- **S0가 먼저인 이유**: 어떤 축을 고칠지 정하는 정보가 없다. 지금 상태에서 reward를 바꾸면
  그것은 기획이 아니라 추측이다. 그리고 S0는 학습 0분이다 — 비용 게이트가 걸릴 수 없다.
- **S1이 S2보다 먼저인 이유**: 30점 축의 **재료 수집**이 학습 0분으로 가능하다. 4 run 분량의
  의도·결과 대조가 이미 CSV에 있으므로 지금이 가장 싸다. 다만 최종 정책이 S2·S3에서 바뀌므로
  **S1은 초안까지만이고, 최종 행동·`env.yaml`에 맞춰 재확정해야 한다**(D10).
- **S3를 마지막에 두는 이유**: F20으로 장기 학습의 유효성은 확인됐다(3000 iter에서 정체 없음).
  그러나 F21의 어긋난 보상 배분을 그대로 6배 굽는 것은 잘못을 고정하는 것이다.

## 결정 (추가)

| # | 결정 | 근거 |
|---|---|---|
| D5 | **`terrain_levels`를 예선 실험의 부작용 게이트로 쓰지 않는다.** 기록은 계속하되 FAIL 판정 근거에서 뺀다 | F22 — 예선 평가 7종에 계단·박스가 없다. Run 04의 `terrain_levels<5.0` 게이트는 예선 점수와 무관한 축으로 개선안을 차단한다 |
| D6 | **`error_vel_*`는 `mean_episode_length` 병기 없이 run 간 비교하지 않는다** | F18 — 생존시간 오염. Run 01의 332.6 step이 4 run 추세를 통째로 뒤집었다 |
| D7 | 게이트·수용기준의 개정 권한은 **사용자와 메인 루프**에 있다. 기획자·분석가는 확정된 기준을 **소비만** 한다. 만족 불가능한 기준을 발견하면 우회하지 말고 결정요청으로 올린다 | 원칙 12 — 피측정자는 자기 자를 소유하지 않는다 |
| **D8** | **S0 방향 승인. 단 S0는 「예선 시나리오 근사 진단」이며 「공식 평가 재현」이 아니다** — 운영진 evaluator 설정은 미제공이므로 H5·H6 재현을 공식이라 부르지 않는다. 승인 대상은 방향이지 현 `train_260829-05_add.md`의 실행이 아니다 | 사이클 1 판정 Q1 |
| **D9** | **사람 시간 예산과 GPU 벽시계 시간을 혼용하지 않는다. "2.4/50 = 4.8%" 표현 폐기** | 사이클 1 판정 §1 — D3가 이미 규정하고 있었고 F23 초판이 그것을 어겼다 |
| **D10** | **S1은 "30점 획득"이 아니라 "최대 30점 축의 증거·문서 준비"다.** 최종 정책이 S2·S3에서 바뀌므로 S1 리포트는 초안까지이며 최종 행동·`env.yaml`에 맞춰 재확정한다 | 사이클 1 판정 §2 |
| **D11** | **Go2를 S1과 병행 착수한다.** 배우는 기존 확장이 아니라 `go2-test-planner`·`go2-report-writer` **신설**안으로 상신 — 현 배우 2종은 H1 전용 계약이라 확장하면 기체별 reward·지형·시나리오·artifact 규칙이 섞인다. **배우 신설과 Go2 실행 착수는 사용자 승인 대상**(미승인 상태) | 사이클 1 판정 Q2·Q3 |
| **D12** | **계단 성능·terrain 경고는 본선 대비 진단으로 계속 기록하되 예선 제출을 막는 자동 FAIL 게이트와 분리한다** (D5의 일반화) | 사이클 1 판정 §7 |
| **D13** | **모든 서버 세션은 `bash _bootstrap/restore.sh`로 시작한다.** 통과(`[DONE]`) 전에는 `play.py`·`train.py`를 실행하지 않는다. 실행안·회람문은 이 STEP을 1번 단계로 포함해야 하며, 빠지면 `BLOCKED`다 | F25 — 서버 휘발. 수동 업로드 절차는 매 세션 재실행되므로 스크립트여야 한다 |
| **D15** | **S2 후보 재선정 (260829)** — 1순위 **학습 지형 분포를 예선 쪽으로 이동**(Hydra override로 `sub_terrains` proportion) · 2순위 `feet_air_time` 0.2→0.35 · 3순위 직진:회전 비율 1.5:1 · **낙상률 축은 추천하지 않음**. 근거는 각각 F33(5 run 전부 틀린 지형) · F34(성취율 3.2%, 축이 꺼져 있음) · F31(효과는 절대값 아닌 비율) · F32(4 run·3 노브가 못 움직였고 그나마 틀린 지형의 값). **1순위는 노브 하나가 아니므로 「무엇을 단일 변수로 볼 것인가」를 기획자가 먼저 확정해야 한다**(D7 · 원칙 12) | F31~F34 |
| **D16** | **S2보다 S0c가 먼저다 — 학습 0분.** F35가 D15의 1·2순위 근거를 둘 다 약화시켰고, 더 근본적으로 **Run 04와 Run 05 중 어느 것이 기준선인지 확정된 바 없다.** 두 정책은 시소의 양 끝이고 총합은 본전 부근이므로(F31), **어느 쪽 위에서 S2를 돌리느냐가 S2 결과보다 먼저 정해져야 한다.** S0c = ① Run 05 × H5·H6 3종(커버리지 0.70→1.00) ② Run 04 × 7 시나리오(Run 05와 쌍 비교) ③ 기준선 채택 판정 + `feet_air_time` 원인 ⓐ발 높이/ⓑ한발지지 게이트 영상 판별. **전부 play이므로 GPU 학습 0분·제출 0회**(D14 무관). Run 04 체크포인트는 PC에 보존됨(`exported/model_best_20260828215939.pt`, sha256 `c785cc36…e28c7`) | F35 · F31 · F28(영상 외 수단 없음) |
| **D17** | **`play.py`는 `exported/policy.pt`·`policy.onnx`를 매 실행 덮어쓴다.** 다른 체크포인트를 play하기 전에 현 제출 후보를 백업하고, 끝나면 해시로 복원을 확인한다 | `play.py:165-209` `_export_policy_to_exported()` 실측 — 이번 run 폴더의 export를 `exported/policy.pt`로 copy. Run 04를 play하면 제출 후보가 Run 04 것으로 바뀐다 |
| **D18** | **기준선은 Run 05로 확정한다 (260829).** 낙상 축은 두 정책이 동률이므로(F38) 추종 축이 판정한다. 예선 시나리오에서 **명령이 0이 아닌 가중은 0.70**(H2 0.20 · H3 0.10 · H4 0.10 · H5 0.15 · H6 0.15)이고 이 중 **회전이 걸리는 것은 H4 0.10뿐**이다. Run 05는 직진 +7.7% · 회전 −26.3%이므로, H4를 **전부 각속도로 계산해도** `0.60×7.7 − 0.10×26.3 = +1.99`로 Run 05 우세다. 역전에 필요한 각속도 지분은 `7.7/26.3 = 0.293` → 비제로 명령 가중의 **22.6%** 인데 H4는 **14.3%** 다. **여유가 있어 경계 판정이 아니다.** 단 근거는 학습 지형 집계 성취율이고(F35로 [추론] 등급), 예선 `tracking_score` 식은 미제공이다 — 새 측정이 이 결론과 충돌하면 재판정한다 | F38 · F39 · F31 · F5~F10 가중 |
| **D19** | **새 측정을 편성하기 전에 「기존 지표가 이미 그 답을 갖고 있는가」를 먼저 계산한다.** 계산해 보고 새 정보가 기대되지 않으면 편성하지 않는다 | F38 — S0c-2가 사용자 시간 6분을 쓰고 판별 정보 0을 반환했다. `base_contact` 0.3071 vs 0.3050 동률은 실행 전부터 알고 있었다 |
| **D14** | **학습 실행 전에 제출 의도를 명시한다.** 제출할 run이 아니면 `NO_AUTO_SUBMIT=1`을 붙인다. 실행안에 제출 여부 칸이 없으면 `BLOCKED` | F26 — 학습 횟수 = 제출 횟수. 진단용 학습이 순위표에 올라간다 |

> **⚠️ 260830 정정 — D18의 등급을 낮추고 수치를 교체한다** (외부 검토 지적 접수 + F40 실측)
>
> **(1) 등급.** D18은 「기준선은 Run 05로 **확정**」이라고 적었다. 근거는 미공개 예선
> `tracking_score` 식에 학습 지형 집계 성취율을 대입한 [추론]이므로 **"확정"은 과하다.**
> → **「Run 05를 다음 검증의 *작업* 기준선으로 채택한다. 예선 우세는 미확정이다」로 낮춘다.**
> 결정 자체(다음 학습을 Run 05 위에서 시작)는 유지된다 — 바뀌는 것은 주장의 강도다.
>
> **(2) 수치.** D18의 `+7.7% / −26.3%`는 F31의 성취율 표에서 온 값이다. F40이 tfevents
> 원본에서 **마지막 500 iter 평균**(단일 종점보다 잡음이 훨씬 작은 추정)으로 다시 재면
> **직진 오차 −21.2%(개선) · 회전 오차 +47.9%(악화)** 로, 교환의 폭이 양쪽 다 훨씬 크다.
> 결론 방향은 바뀌지 않는다:
>
> | 계산 | 식 | 결과 |
> |---|---|---:|
> | H4를 전부 각속도로 (최대 보수) | `0.60×21.2 − 0.10×47.9` | **+7.93** |
> | H4를 선/각 반반 | `0.65×21.2 − 0.05×47.9` | **+11.38** |
> | 역전에 필요한 각속도 **절대 가중** | `w×47.9 = (0.70−w)×21.2` → `w=0.2147` | 비제로 가중의 **30.7%** 필요 |
> | 실제 각속도 지분 (H4 0.10) | `0.10 / 0.70` | **14.3%** |
>
> 필요치가 22.6%에서 30.7%로 올라갔으므로 **여유는 오히려 커졌다.**
>
> > **⚠️ 재정정 (260830 · Codex 검토 §1 지적 접수 · 양측 채택)**
> >
> > 위 `−21.2% / +47.9%`도 **원값**이다. `humanoid-report-writer.md:51-59`는
> > *"`error_vel_*`를 표에 쓸 때는 `Train/mean_episode_length`를 같은 행에 반드시 병기하고,
> > 비교·추세 서술은 `error / mean_episode_length` 정규화값으로 한다. 원값만 적은 표는
> > 미완성이다"* 라고 규정한다. 일찍 죽은 run은 오차가 그만큼 작게 찍히기 때문이다.
> > 나도 Codex도 이 규율을 적용하지 않았다.
> >
> > 정규화값 = `error / mean_episode_length × 500` (`max_command_step`, 정의 61-66행 표와 동일 산식):
> >
> > | 지표 | Run 04 | Run 05 | 변화 |
> > |---|---:|---:|---:|
> > | ep_len (마지막 500 평균) | 776.88 | 762.61 | — |
> > | err_xy **/step** | 0.2765 | 0.2219 | **−19.7%** |
> > | err_yaw **/step** | 0.4184 | 0.6303 | **+50.6%** |
> >
> > 재계산: H4 전부 각속도 `0.60×19.7 − 0.10×50.6 = **+6.78**` · 역전 필요 지분
> > `0.70×19.7/(50.6+19.7) = 0.1963` → 비제로 가중의 **28.0%** (실제 14.3%).
> > **D18 결론은 유지되나 여유는 30.7% → 28.0%로 줄었다.**
> >
> > **새로 드러난 사실 — statue 대조 (정의 68-82행 규율).** statue(명령 무시하고 서 있기)
> > 기준선은 `xy 0.7165 · yaw 0.6681`이다.
> >
> > | Run | xy /statue | yaw /statue |
> > |---|---:|---:|
> > | 04 | 0.39x | 0.63x |
> > | **05** | **0.31x** | **0.94x** |
> >
> > **Run 05의 회전 추종은 statue의 0.94배 — 「명령을 따르느니 서 있는 편이 낫다」의 경계선이다.**
> > 정의 80행이 Run 02의 0.92x를 「statue 수준」으로 분류한 그 구간이다.
> > D18은 "Run 05가 총합 우세"까지만 말할 수 있고, **"회전축이 정상"이라는 뜻이 아니다.**
> > 직진축(0.31x)은 statue를 크게 이기므로 총합 판정은 그대로다.
>
> **(3) 누락 인정.** `track_lin_vel_xy_exp`는 **명령이 0일 때도 "정지 속도 추종"으로 작동**한다.
> 따라서 zero-command 가중 0.30(H1 0.15 · H7 0.15)을 계산에서 통째로 뺀 것은 근거가 없다.
> 다만 이 항을 넣으면 **Run 05 쪽에 가중이 더 붙으므로**, D18의 계산은 Run 05에게 불리한 쪽으로
> 보수적이었다. 결론은 강화된다.
>
> **(4) 미반영 지표.** D18의 산술에 `mean_std` 1.4191 → 1.672(+17.8%)가 들어가지 않았다.
> 이 값 자체는 F31·이 문서 341행에 **이미 기록돼 있었고** 빠진 것은 *기록*이 아니라 *산술*이다.
> 그리고 F40이 그 해석을 바꾼다 — **std 1.672는 발산 신호가 아니다.** Run 05 안에서 std는
> iter 1000 부근에 안착한 뒤 마지막 1000 iter 기울기가 **−0.0135/1k(하락)** 이다.
> 두 run의 std 차이는 수렴도 차이가 아니라 **보상 설정이 다른 두 정책의 평형 잡음 수준 차이**다.
> **진짜 미수렴 증거는 std가 아니라 `error_vel_yaw` 기울기(−0.106/1k)와
> `terrain_levels` 5.56/9.0 미포화다**(F40).

> **⚠️ 260830 정정 — D19에 적용 조건을 붙인다** (외부 검토 지적 접수)
>
> D19를 문자 그대로 적용하면 **측정 대상이 다른 과거 proxy로 새 측정을 막는** 사고가 난다.
> S0c-2의 실제 구조가 그랬다 — `base_contact`는 **혼합 커리큘럼 지형의 집계값**이고 영상은
> **격리한 H5·H6 근사 조건**이라 애초에 같은 것을 재고 있지 않았다.
> → **개정: 「기존 지표가 *동일 대상 · 동일 조건 · 동일 측정단위*로 이미 답을 갖고 있을 때만*
> 새 측정을 생략한다.」** 셋 중 하나라도 다르면 D19는 생략 근거가 되지 못하고,
> 「무엇이 달라서 새 측정이 필요한가」를 한 줄로 적은 뒤 편성한다.

| # | 결정 (260830 추가) | 근거 |
|---|---|---|
| **D20** | **다음 학습은 보상을 바꾸지 않는다. Run 05 설정 그대로 5000 iter · from scratch · `NO_AUTO_SUBMIT=1`.** 노브 선정은 그 다음이다 | F40 — 5 run 전부 3000에서 끊겼고, D18이 근거로 쓴 `error_vel_yaw` 격차는 **아직 −0.106/1k로 움직이는 중**이며 `terrain_levels`는 5.56/9.0로 미포화다. 지금 노브를 고르면 **"보상 결함"과 "학습 중간 상태"를 분리하지 못한 채** 후자를 전자로 오진할 수 있다. from scratch인 이유는 F41-보완: `--resume`은 정책·옵티마이저만 복원하고 **지형 커리큘럼 레벨은 env 상태라 복원되지 않아** 5.56에서 초기 분포(`max_init_terrain_level: 5`)로 리셋된다 — 비교가 깨진다 |
| **D21** | **D20 run은 3000 iter 지점의 값도 함께 남긴다.** 수치는 tfevents로 자동 확보되고(F40), `.pt`는 학습 중에 별도 폴더로 복사해 구출한다 | F41 — `finalize`가 best 1개만 남기고 나머지를 지운다. `.pt` 구출은 play·제출용이며, 수렴 판정 자체는 tfevents만으로 끝난다 |
| **D22** | **D20 run은 "5000이 3000보다 나은가"뿐 아니라 "3000끼리 얼마나 흔들리는가"도 준다.** `schedule: adaptive`라 `max_iterations`는 앞 3000 iter의 학습 궤적에 개입하지 않으므로, 새 run의 3000 지점을 Run 05의 3000 지점과 대조하면 **지금까지 한 번도 갖지 못한 run 간 재현 잡음 하한**이 나온다 | `agent.yaml:3-9,44-48` 실측 — `num_steps_per_env:24` · `save_interval:100` · `schedule: adaptive` · `entropy_coef: 0.008` 고정. iteration 수에 연동된 annealing 없음. 이 잡음 하한 없이는 지금까지의 모든 run 간 비교가 "차이인지 잡음인지" 판정 불능이다 |
| **D23** | **이 캠페인은 H1(2족) 전용이다. Go2 착수(D11)를 폐기하고 합산 상한 100점을 수용한다.** 다음 학습은 D20(보상 불변 5000 iter)이 아니라 **`track_ang_vel_z_exp` 1.0→1.5 단일 변경 3000 iter**다 | 사용자 확정(260830). **이 결정이 원장 어디에도 없어 메인 루프가 Go2 착수를 3턴 반복 제안했다** — 기록 실패이며 원칙 4(피드백 즉시 기록) 위반. 노브 근거: D18 재정정에서 Run 05의 `err_yaw /step = 0.6303`이 statue(0.6681)의 **0.94배**로 회전축이 사실상 무력화됐고, 원인은 Run 05가 `track_lin_vel_xy_exp`를 1.0으로 올려 선/각 배분이 1:1이 된 것이다. Run 04(선 0.5 : 각 1.0)의 yaw는 0.63배였다.<br><br>**⚠️ 정정 (260830) — 이 행의 「다음 학습」 절은 철회됐다(D24).** `track_ang_vel_z_exp` 1.0→1.5는 **D18 판정을 거꾸로 미는 제안**이었다: `error_vel_yaw`가 걸리는 시나리오는 **H4 하나(가중 0.10)** 뿐이고 `error_vel_xy`는 **직진 계열 0.60**에 걸리며, D18 재정정 실측은 선속도 우세(Run 05)가 회전 우세(Run 04)보다 **가중 +6.78 우세**다. 0.10을 얻으려 0.60을 거는 음의 기대값이다. **D23에서 유효한 것은 「H1 전용·상한 100점 수용」뿐이며, 학습 지시 절은 인용 금지.** |
| **D24** | **보상 노브 탐색을 종료한다.** 다음 시뮬 레버는 노브가 아니라 **수렴**이다 — Run 05 보상 그대로 10000~15000 iter | 시소 양 끝을 이미 측정(Run 04 선0.5:각1.0 · Run 05 선1.0:각1.0), `termination_penalty`는 Run 03에서 포화, 잔여 6종은 tracking에 직접 걸리지 않는다. F40 실측: Run 05는 3000 iter에서 `error_vel_yaw` −0.106/1k 하강 중 · `terrain_levels` 5.56/9.0 미포화 → **같은 보상으로 더 돌리면 좋아진다는 근거가 있다.** `PRELIM_RL_GUID.md:190`과도 일치 |
| **D25** | **메인 루프는 `prelim-campaign-manager`의 판정 자리를 대신 메우지 않는다.** 호출하지 않은 사이클은 원장에 `미호출`로 적는다 | 사용자 지적(260830): 배우 3종이 있는데 사이클 1 이후 한 번도 호출되지 않았고 메인 루프가 축·순서 배분을 직접 썼다. 동반 결함 — D23을 `.codex/agents/prelim-campaign-manager.md`에 반영하지 않아 **정의가 여전히 "Go2 첫 제출은 한계 이득이 크다"고 명령**하고 있었다(원칙 10 위반). 260830 개편으로 Go2 오염 11곳 제거 |
| **D26** | 사이클 2는 **의도분석 20 + 리포트 10** 정합을 우선한다(GPU 0분) | 팀장 Q1=(a). 시뮬 70은 운영진 일괄 평가라 **blind**이고, 30점 축은 직접 검증 가능하다. 확정 이득이 blind 이득에 우선 |
| **D27** | D24의 의미를 좁힌다 — **"미탐색 노브 전부 무효"가 아니라 "측정 근거 없는 추가 탐색 종료"** | 팀장 Q2=(a) 단서. 잔여 노브가 생존·자세·동작 품질에 간접 효과를 갖지 않는다는 뜻이 아니다 |
| ~~**D28**~~ | ~~장기 학습은 공백 C(마감일) 회수 전까지 `▲ blocked`~~ | ~~팀장 Q3=(b)~~ → **⚠️ 철회 (260830 · D29로 대체).** 이 판정은 성립하지 않는다. **팀장의 오류가 아니라 메인 루프가 틀린 선지를 준 결과다** — M0 지시 Q3의 선지 (b)를 메인 루프가 작성했고 팀장은 주어진 선지 중에서 골랐다. 상세는 D29 |
| **D29** | **최종 장기 학습(S3/B2)은 차단 대상이 아니다. 즉시 착수한다** — Run 05 보상 그대로 · from scratch · 15000 iter | **(1) 제출 요건이다.** `PRELIM_RL_GUID.md:190` 체크리스트 #3 "최종본은 충분히 길게(10000~15000 iter) 학습했나요?" — 현재 Run 05는 3000. 안 하면 게이트 #3이 영원히 미충족이고, 기다린다고 열리지 않는다. **(2) D28은 예산 혼용이었다.** 마감일(공백 C)은 **사람 시간** 축인데 그것으로 **벽시계** 작업을 막았다 — 관리자 정의 L91-96이 분리하라고 못박은 두 예산이고 D9가 경고한 바로 그 혼용이다. **(3) 정의는 동시 실행을 명령한다.** 관리자 정의 L101 "학습이 도는 동안 사람이 노는 것을 허용하지 않는다 — 그 시간에 리포트 30점 작업을 배치한다." D26(30점 우선)과 B2는 경쟁이 아니라 **병렬 설계**다. **(4) 기다려서 얻는 정보가 0이다.** D24로 보상 9개가 동결됐으므로 마감일을 알아도 학습 설정이 바뀌지 않는다. 마감일이 실제로 거는 것은 「시작 가부」가 아니라 **「10000 vs 15000」 선택**뿐이고, 15000으로 걸고 중간 체크포인트를 구출하면(D21·F41) 그것도 해소된다 |
| **D30** | **단계표에 「등급」 열을 둔다 — `필수(제출요건)` / `개선` / `조사`.** **`필수` 행은 `blocked` 상태를 가질 수 없다** | 260830 근본원인 분석. B2(제출 요건 #3)가 A2(개선)와 같은 행 형식으로 나란히 놓여 둘 다 blocked가 될 수 있었다. 제출 요건이 막히면 그것은 일정 조정이 아니라 **제출 불가**이므로 다른 등급의 사건이다. 필수 행이 막히면 `blocked`가 아니라 **`▲ 제출 불가 — 즉시 해소 대상`** 으로 적는다 |
| **D31** | **판정 요청의 선지는 원장 원문과 대조한 뒤 만든다.** 선지에 미검증 전제를 넣지 않는다 | 260830 실증. M0 지시 Q3의 선지 (b)「공백 C 미회수」는 메인 루프의 창작이었다. 원장 원문(L871)이 막은 것은 **① iter 상한** ② **공백 F**(50시간 해석)인데, 메인 루프가 이를 **① 시작 자체** ② **공백 C**(마감일)로 두 번 변형해 선지에 넣었다. 판정자는 선지 밖을 볼 수 없으므로 **틀린 선지는 반드시 틀린 판정을 낳는다.** 원칙 12(피측정자가 자를 소유하지 않는다)의 역방향 위반 — 질문자가 선지를 만들어 판정을 유도했다 |
| **D32** | **D24를 부분 철회한다.** Run 04/05를 파라미터 공간의 “시소 양 끝”이라 한 것은 오류이며 survival을 제외하고 탐색 종료를 일반화할 수 없다. 다만 `lin=2.0` 단일 3000-iter 1회는 즉시 장기학습 승급 실험으로 승인하지 않는다 | `track_lin_vel_xy_exp` 추천범위는 0.5~2.5인데 관측점은 0.5·1.0뿐이다. 공식 점수는 survival×tracking이다. 반면 +6.78은 Run04→05의 혼합 학습지형 tracking proxy일 뿐 1.0 이후 단조성·공식 survival을 증명하지 않는다. 새 pilot은 동일설정 control, 새 RUN_ID, restore 전 백업, `NO_AUTO_SUBMIT=1`, 고유 tmux, H1~H7 고정 평가, artifact 정합을 갖춘 screening으로만 허용한다. 활성 B2가 있으면 동시 학습 금지 |
| **D33** | **F45에 따라 B2 ‘착수’ 상태를 철회하고 현재 서버 세션을 미실행으로 확정한다.** 당시 복원 단독을 다음 명령으로 뒀으나 D36이 Run 06 원클릭 패키지 내부 preflight로 대체했다 | tmux/process/log가 모두 없고 reward가 배포값 0.5/0.5다. 복원 없이 `lin=2.0`을 적용하면 Run 05의 1.0/1.0에서 단일변수 변경하는 실험이 아니므로 비교가 무효다 |
| **D34** | **상태를 두 층으로 관리한다.** 캠페인 진척은 로컬 artifact 기준, 현재 실행 가능 여부는 매 서버 세션의 단계 0 preflight 기준이다 | F46. 서버 초기화는 캠페인 진척의 소실이 아니라 실행환경 리셋이다. 모든 실행안은 `업로드 → 해시 검증 → 해제 → restore → 실행 → bundle 생성 → 다운로드 → 로컬 검증`을 완주해야 done이다 |
| **D35** | **로컬 기존 학습 데이터 우선 게이트.** 서버 명령을 만들기 전에 `workspace/training`의 log·tfevents·checkpoint·영상·보고서를 검색한다 | F47·D19. 기존 데이터가 동일 질문에 답하면 재학습하지 않는다. 답이 없는 항목만 사전등록하고 서버에서 측정한다 |
| **D36** | **서버는 명령 패키지 완성 전 켜지 않는다.** 복원만을 위한 접속을 금지하고 `업로드 1회 → 실행 1줄 → bundle 다운로드`로 운용한다 | 사용자 확정(260831): 서버 사용시간 제한 + 매 접속 초기화. 실행 목적·예상시간·중단조건·회수 경로는 메인 루프가 로컬에서 먼저 완성한다 |
| **D37** | **H1 서기 및 기본 행동 튜닝은 완료 상태다.** 다음 Run 06은 서기 재튜닝이 아니라 Run 05 보상 고정 장기 수렴이다 | F30: H1 zero-command 직립, H2 전진, H3 좌우, H4 회전복합 관찰. F37: H5/H6 근사 1 episode 무낙상, H7 반복 push 무낙상. 남은 것은 최종 정책의 정량·격리 평가이지 서기 재개발이 아니다 |
| **D38** | **현재 캠페인 단계는 4/6 장기 학습이다.** H5~H7 근사 행동 확인은 단계 3 완료 기준을 충족하며, 단일 episode·노출구간·방향격리 한계는 단계 5 최종 정책 정량평가로 이관한다 | 표준 단계 3 완료 기준은 요철·경사·밀침 행동 확인이다. F37·F30에서 확인 완료. 증거 한계를 숨기지 않되 이미 통과한 서기·환경 행동 게이트를 다시 열어 서버 시간을 중복 사용하지 않는다 |

## 미해소 (실측된 공백)

- [ ] **A. 영상 관찰이 Run 02·03·04 전부 `미측정`** — 채택 조건 3항 중 하나가 계속 빈다.
      F9에 따라 **의도분석 20점 축이 이것 때문에 통째로 미확보**. 학습 1회 추가보다 우선순위가 높다.
- [ ] **B. Go2 run 0회 · 담당 배우 0종** — F7에 따라 합산 상한이 100점에 묶여 있다.
      배우 신설 또는 기존 배우의 Go2 확장이 필요 (사용자 결정 항목).
- [ ] **C. 예선 마감일 미확인** — `마감` 0건. 50시간 배치가 여기 걸린다. 회수처: 대시보드.
- [ ] D. 11강 「로봇 학습의 목표」 원문 결손 — U1이 F3로 해소돼 우선순위 하락.
- [ ] E. 서버 사용 시간/횟수 제한 미확인.
- [ ] **F. "학습 시간 50시간"의 정의 미확인 (260829 신설)** — 사용자 지시의 "학습 시간"이
      **GPU 벽시계**인지 **사람 시간**(강좌 학습·영상 관찰·문서 작성 포함)인지 확정된 바 없다.
      D9로 잔여율 계산을 폐기했으므로 현재 방향(S0~S3)은 두 해석 모두에서 성립하지만,
      **S3 최종 학습의 iter 상한과 Go2 일정은 이 값이 없으면 확정할 수 없다.** 회수처: 사용자.
- [ ] **G. 남은 사람 시간 실측 미확인** — 소진량을 기록한 적이 없다. F와 함께 회수.
- [ ] **H. 제출 횟수 제한 미확인 (260829 신설)** — F26으로 학습 1회 = 제출 1회가 확정됐다.
      제한이 있으면 S2의 run 수가 직접 제약된다. 회수처: 대회 공지·대시보드.
      **⚠️ 정정 (260830)** — F26 정정으로 전제가 무너졌다. 제출은 사람이 대시보드에 직접
      업로드하는 별도 행위이므로 **이 공백은 run 수를 제약하지 않는다.** 여전히 미확인이지만
      영향 범위는 "최종 제출을 몇 번 갈아끼울 수 있는가"로 축소된다. 우선순위 하향.

## 해소됨

- [x] ~~U1 예선 채점 기준~~ → `PRELIM_RL_GUID.md` §2에서 확보 (F5~F10)

## 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | 관리자는 두 배우의 산출을 **대신 쓰지 않는다.** 원문 그대로 중계 | 260828 감사 S7 — 코디네이터 대리작성이 `warnings=0` 하드코딩을 낳았다 |
| D2 | 연속성은 에이전트가 아니라 이 파일이 진다 | 서브에이전트 cold start |
| D3 | 사람 시간과 벽시계 시간을 분리 관리 | F13 |
| D4 | 판정 근거는 항상 **한계 점수** | F6·F7 |

## 진행

| no | 단위 | state | 산출물 |
|----|------|-------|--------|
| 1 | 강좌 HTML 19건 본문 추출(301,702자) | done | scratchpad/lectures/*.txt |
| 2 | 예선 요구·범위·비용 분석 | done | COURSE_ANALYSIS_260829.md |
| 3 | 상태 파일 신설 | done | 이 파일 |
| 4 | 채점 기준 회수 (U1) | done | F5~F10 |
| 5 | 캠페인 관리자 신설 | done | `.codex/agents/prelim-campaign-manager.md` |
| 6 | tfevents 직접 파싱 · statue 기준선 · 보상 배분 실측 | done | F18~F21 |
| 7 | 리포트 전량 재독 → 방향 결정 (S0~S3) | done | 위 「방향 결정」 절 · F22~F24 · D5~D7 |
| 8 | 배우 정의 3종에 방향·신규 사실 반영 | done | `.codex/agents/*.md` 3종 |
| 9 | 사이클 1 — 관리자 판정 (Q1 승인 / Q2 병행 / Q3 신설) | done | 판정문 · D8~D12 · 공백 F·G |
| 10 | 판정 지적 4건을 이 파일에 반영 (F23 정정 · S0/S1 명명 · D8~D12 · 공백 F·G) | done | 이 파일 |
| 11 | 서버 휘발 대응 — `_bootstrap/` + `restore.sh` + zip 작성, 검출력 실증(해시 손상·값 변조 둘 다 exit=1) | done | `bootstrap_run05.zip` (Run 05로 갱신, 6,606,766 B) |
| 12 | Run 05 학습 — S2 후보 1 단독 검증 | done | F29 · `reports/S0_260829/train_260829-05.log` |
| 13 | override 크래시 원인 규명 — `terrain_type=plane` + 커리큘럼 `terrain_levels` 충돌 | done | 해법 `env.curriculum.terrain_levels=null`, 실측 `Curriculum Manager: contains 0 active terms` |
| 14 | S0 H1(제자리) 근사 진단 — 업로드 체크포인트로 학습 0분 재생 | done | `reports/S0b/H1_stand.mp4` (6,762,089 B) · F27 |
| 15 | S0 나머지 5종 (H2·H3좌·H3우·H4·H7) + 6종 영상 육안 판정 | done | F30 · `reports/S0b/` mp4 6종 |
| 16 | Run 05 로그 항별 재파싱 — 추종 시소·낙상 고착·노브 탐색률 산출 | done | F31·F32·F33·F34 · D15 · `REPORT_260829.md` |
| 17 | 예선 준비 현황 보고서 작성 (사용자 지시 260829) | done | `REPORT_260829.md` (메인 루프 소유) |
| 18 | H5·H6 지형 세기 단위 확인 후 명령 확정 | done | F36 — `slope_range`는 라디안이든 비율이든 10°에서 1.0% 차이라 **판정 불필요**. 명령 확정 |
| 19 | 기획자 재호출 — Run 05 문서를 S0 근사 진단으로 재작성 | 대기 | — |
| 20 | 외부 검토 지적 4건 검증·수용 → 정정 반영 | done | F35 · D16 · D17 · `REPORT_260829.md` ⚠️ 블록 5개 |
| 21 | S0c 실행안 작성 (Run05 H5·H6 3종 + Run04 7종 쌍비교, 학습 0분) | done | 대화창 러너북 · D16 |
| 22 | S0c-1 실행 (Run 05 × H5·H6 3종) | done | F37 · `reports/S0c/` mp4 3종 + log 3종 · 커버리지 0.70→1.00 |
| 23 | S0c-2 실행 (Run 04 × 4 시나리오 → `reports/S0d/`) | done | F38 — 9종에서 4종으로 축소 실행. **판별 정보 0** (음성 결과) |
| 24 | S0c-3 기준선 판정 (Run 04 vs Run 05) | done | **D18 — Run 05 확정** · F39(단일 변수 확인) · D19(교훈) |
| 25 | 외부 검토 2차 지적 6건 검증 (등급 과잉·영상 정보0 과장·D19 무조건 적용·std 해석·CSV 사후행·보고서 첫화면) | done | D18 ⚠️정정(1)~(4) · D19 ⚠️정정 · F40 |
| 26 | tfevents 로컬 파서 작성 + 5 run 곡선 분석 (GPU 0분) | done | `tools/tfcurve.py` · **F40** — 파서 검증 5/5 (CSV Run 04 값 재현) |
| 27 | `_finalize.py` 체크포인트 정리 동작 실측 | done | **F41** — best 1개만 남기고 삭제. 3000·5000 비교 계획의 실패 지점 사전 검출 |
| 28 | 다음 학습 확정 — 보상 불변 5000 iter | done | **D20·D21·D22** |
| 29 | B2 15000 iter 착수 기록 검증 | **현재 서버 세션 미실행** | F45 — tmux/process/log 없음, reward 0.5/0.5 |
| 30 | D24 반론 3자 교차검증·D32 판정 | done | `REPORT_260831.md` · `CAMPAIGN_SCHEDULE.md` §0/D32 |
| 31 | F45 반영·B2 착수 상태 철회·복원 게이트 설정 | done | D33 · `CAMPAIGN_SCHEDULE.md` §0 |
| 32 | 휘발성 서버 운영 규칙·세션 런북 확정 | done | F46·D34 · `SERVER_SESSION_RUNBOOK.md` |
| 33 | 로컬 training 기존 데이터 재인벤토리·우선 게이트 확정 | done | F47·D35 |
| 34 | 서버 제한시간 운영 정정·Run 06 원클릭 패키지 작성 | done | D36·D37 · `server_run06_long.sh` · `run06_server_package.zip` |

NEXT: (0) **로컬 검증이 끝난 `run06_server_package.zip`을 서버 시작 직후 업로드한다.**
      서버에서는 해시 통과 후 `MAX_ITERS=10000 bash server_run06_long.sh` 한 줄로 Run 05 복원,
      보상 9개 검증, Run 06 장기 학습, 3000/5000/10000 checkpoint 구출, 다운로드 bundle 생성을 연속 수행한다.
      3시간 20분 이상 확보되면 `MAX_ITERS=15000`을 쓴다. 이 run은 **서기 재튜닝이 아니라 장기 수렴**이다.
      완료 bundle을 로컬 `workspace/training/humanoid`에 내려받기 전에는 run을 done으로 기록하지 않는다.

구 NEXT: (1) **S0 종료. 다음은 S2 후보 1개를 확정하는 일이다.** S0c는 전부 done —
      커버리지 1.00(F37), 기준선 Run 05 확정(D18). 이제 **Run 05 위에서** 노브 1개를 골라
      단일 변수 학습 1회(3000 iter ≈ 36분, `NO_AUTO_SUBMIT=1`)를 돈다.
      **후보 선정 전에 D19를 적용한다** — 각 후보에 대해 "기존 로그가 이미 답을 갖고 있는가"를
      먼저 계산하고, 그렇다면 편성하지 않는다. D15의 1·2순위는 F35·F37로 근거가 약화됐으므로
      **후보 목록부터 다시 세운다**. 사전 등록(변경 노브·기대 지표·성공 임계·부작용 게이트)을
      `reports/experiment_history.csv`에 **학습 시작 전에** 쓴다 — Run 05 행이 비어 있다(F39).
      (2) `humanoid-test-planner` 재호출 — `reports/train_260829-05_add.md`를 S0 근사 진단으로
      재작성. 차단 4건 중 (2)(4)는 해소(`terrain_type=plane` + `terrain_levels=null` · H4 복합
      명령), 남은 차단은 (1) H5·H6 명령 미확정 (3) D5로 폐기된 `terrain_levels>=5.0` FAIL 게이트
      잔존. 아울러 **F28의 수치 임계 수용기준 금지**와 **D15의 S2 재선정**을 기획자 정의에 반영.
      (3) **S1 리포트 초안** — S0 영상 6종이 F9가 요구하는 "의도대로 행동하는가"의 증거다.
      학습 0분으로 최대 30점 축의 재료가 준비된다(D10 — 획득 아님).
      회수: 공백 F·G·H·C를 사용자에게. Go2 배우 신설(D11)은 사용자 승인 대기 — 대리 생성 금지.
      **학습은 편성하지 않는다** — D16. 기준선(Run 04 vs Run 05)이 미확정이고 D15의 1·2순위는
      F35로 근거가 약화됐다. 그 확정 전에 도는 학습은 기획이 아니라 추측이다.

## 260831 최신 상태 정정 — Run06 회수 및 과학 근거 게이트

> 이 절은 위 D37·D38 및 과거 NEXT 가운데 충돌하는 상태·명령을 대체한다. 과거 기록은 감사 이력으로 남긴다.

### 추가 고정 사실

| # | 사실 | 근거 |
|---|---|---|
| **F48** | 최초 Run06 패키지는 CRLF 때문에 서버 실행이 실패했으나, 새 서버 세션에서 `sed -i 's/\r$//'` 후 `bash -n` 두 파일이 통과했다 | 사용자 제공 서버 출력: `server_run06_long.sh`, `_bootstrap/restore.sh` 모두 `[OK] shell syntax` |
| **F49** | Run06은 서버에서 실행 중이며 사용자 최신 관측 시 완료까지 약 30분이 남았다. 로컬에는 아직 완료 bundle이 없다 | 사용자 발화(260831). 실제 iter·종료 코드는 bundle 회수 전 `[미측정]` |
| **F50** | Run06은 Run05의 보상 9개를 유지한 단일 seed(42) 장기 수렴/학습시간 실험이다. reward 튜닝 run이나 독립 seed 재현이 아니다 | `server_run06_long.sh`, `train.py`, `H1_REWARD_EVIDENCE_MASTER.md` |
| **F51** | 서버의 `/workspace/training` 전체에는 업로드 당시의 과거 문서와 새 run이 섞인다. 최종 Run06 bundle은 기본적으로 `/workspace/_keep`에 있어 training 전체 다운로드만으로는 누락될 수 있다 | `server_run06_long.sh`: `KEEP=/workspace/_keep/...`, tar 생성 경로 확인 |

### 추가 결정

| # | 결정 | 근거 |
|---|---|---|
| **D39** | 현재 가장 이른 미완료 단계는 **2/6 짧은 pilot의 과학적 재검증**이다. D38의 단계 4/6 표기는 철회한다 | Run01~05는 단일 seed 탐색이며 독립 seed·고정 evaluator·시나리오별 survival×tracking 분리가 없다 |
| **D40** | 모든 reward 제안은 `H1_REWARD_EVIDENCE_MASTER.md`의 만족/부분 만족/미만족/미측정 판정과 8개 근거 항목을 먼저 채운다 | 반복 질문 방지 및 단일 run 과잉해석 방지. `AGENTS.md`와 H1 기획·캠페인 관리자 지침에 반영 |
| **D41** | Run06은 약 30분만 남은 최신 관측 때문에 중단하지 않고 10,000 iter를 자연 완료한다 | 추가 벽시계 비용이 작고, SIGINT보다 정상 종료 bundle 생성의 운영 위험이 낮다. 성능 보장 결정은 아님 |
| **D42** | 서버 `training` 전체는 보험 snapshot으로 다운로드할 수 있으나 로컬 정본에 통째로 덮어쓰지 않는다 | F51. `workspace/server_returns/<RUN_ID>/` 격리 → SHA/STATUS/source/checkpoint/tfevents 검증 → 새 artifact 선택 병합 |

### 최신 진행

| no | 등급 | 단위 | state | 산출물/게이트 |
|---:|---|---|---|---|
| 35 | 개선 | Run06 10k 단일-seed 수렴 표본 | **서버 실행 중·결과 미회수** | `[DONE] DOWNLOAD=`와 `TRAIN_RC`, tar SHA 확인 필요 |
| 36 | 조사 | reward 9개 과학 근거 마스터화 | done | `H1_REWARD_EVIDENCE_MASTER.md` |
| 37 | 필수(제출요건) | Run06 artifact 로컬 정합성 검증 | **▲ 제출 불가 — 즉시 해소 대상** | bundle 미회수 |
| 38 | 조사 | 고정 evaluator·독립 seed 설계 | 대기 | Run06 3k/5k/10k 분석 후 사전등록 |
| 39 | 필수(제출요건) | 최종 H1~H7 평가·env↔리포트 일치·대시보드 제출 확인 | **▲ 제출 불가 — 즉시 해소 대상** | 최종 후보 미확정·공식 제출 이력 미측정 |

**LATEST NEXT:** Run06을 건드리지 않고 자연 완료시킨다. `[DONE] DOWNLOAD=`가 나온 뒤
`/workspace/_keep/train_260831-06_run05cfg_10000_DOWNLOAD.tar.gz`와 전체 `training` 보험 snapshot을
내려받아 `workspace/server_returns/train_260831-06_run05cfg_10000/`에 격리한다. 로컬 정본에는
통째로 덮어쓰지 않는다. SHA256·`STATUS.txt`·`TRAIN_RC`·source hash·checkpoint·tfevents를 검증한
뒤에만 새 run artifact를 선택 병합하고 Run05 대비 3k/5k/10k 지표를 판정한다.

### 260831 중앙 artifact 운영 정본 추가

| # | 사실/결정 | 근거 |
|---|---|---|
| **F52** | `ARTIFACT_MANAGEMENT.md`가 파일 목록, 서버 회수·검증·선택 병합, artifact 작업 내역의 단일 진입점으로 신설됐다 | 사용자 지시: 지침·파일 리스트·작업 내역을 모든 담당자가 알 수 있게 문서화 |
| **D43** | 모든 서버·artifact 작업은 실행 전에 작업 ID를 등록하고 `PLANNED → RUNNING → RECEIVED → VERIFIED → MERGED → ANALYZED → REPORTED` 상태로 전진시킨다 | `AGENTS.md`와 기획자·스케줄러·보고서 작성자 지침에 강제 참조 반영 |

| no | 등급 | 단위 | state | 산출물 |
|---:|---|---|---|---|
| 40 | 조사 | 중앙 artifact 운영·파일·작업 원장 신설 및 전 담당 연결 | done | `ARTIFACT_MANAGEMENT.md`, `workspace/server_returns/README.md`, `AGENTS.md`, `.codex/agents/*.md` |

**LATEST NEXT (artifact 원장):** `A260831-02`는 Run06 `[DONE] DOWNLOAD=`까지 자연 완료를 기다린다.
두 tar가 로컬에 도착하면 `A260831-03`을 `RECEIVED`로 전이하고, 원본 격리 → SHA/STATUS/source/params/checkpoint/tfevents
검증 → `VERIFIED` 순서로 진행한다. `MERGE_PLAN.tsv` 승인 근거가 생기기 전에는 `workspace/training`을 변경하지 않는다.

### 260831 Run06 수신 검증·영상 게이트 정정

| # | 사실/결정 | 근거 |
|---|---|---|
| **F53** | Run06 학습 bundle과 전체 training snapshot을 로컬에서 회수했다. bundle은 `TRAIN_RC=0`, `MAX_ITERS=10000`, tar PASS, 서버 기록 SHA 일치, 내부 checksum 18/18 PASS다 | `workspace/server_returns/train_260831-06_run05cfg_10000/INGEST_STATUS.md` |
| **F54** | Run06 bundle에는 tfevents와 `model_9900.pt`는 있으나 `ckpt/`가 비어 3k·5k checkpoint가 없고, Run06 전용 영상도 0건이다 | bundle manifest·snapshot tar 실측 |
| **D44** | 학습 artifact만으로 서버 종료 가능하다는 직전 판정을 철회한다. Run06 최종 정책의 H1~H7 영상 tar를 회수한 뒤 서버를 종료한다 | 학습 곡선은 실제 행동·퇴행을 보장하지 않으며 단계 1·3·5 증거에 영상이 필요 |
| **D45** | `server_run06_videos.sh`로 core 4종을 먼저 안전 패키징하고 full 10종을 완성한다 | SHA `c28a36e2…abc65`, CRLF 0, `bash -n` PASS. 고정 seed·명령·지형·1000 step |

| no | 등급 | 단위 | state | 산출물 |
|---:|---|---|---|---|
| 41 | 필수(제출요건) | Run06 학습 bundle 격리·무결성 검증 | done | `INGEST_STATUS.md`, `FILE_MANIFEST.tsv`, `LOCAL_SHA256SUMS.txt`, `MERGE_PLAN.tsv` |
| 42 | 필수(제출요건) | Run06 H1~H7 영상 10종 | **실행 대기** | `server_run06_videos.sh` 준비·검증 완료 |

**LATEST NEXT (영상 게이트):** 서버를 종료하지 않고 `server_run06_videos.sh`를 업로드한 뒤
`VIDEO_SUITE=full bash server_run06_videos.sh`를 실행한다. `[DONE] Run06 video suite=full`과
`*_VIDEOS_FULL.tar.gz`·`.sha256`을 회수해 `A260831-05`를 `VERIFIED`로 전이한 뒤 서버를 종료한다.
3k·5k 정책 재생은 checkpoint 부재로 불가능하며, 해당 구간은 tfevents 곡선 분석으로만 판정한다.

### 260831 Run06 영상 PARTIAL 검증·재개 결정

| # | 사실/결정 | 근거 |
|---|---|---|
| **F55** | 다운로드된 영상 결과는 FULL이 아니라 CORE와 PARTIAL이다. H1·H2·H3 좌우·H4 양방향·H7의 평지 7종은 `play_rc=0`이고, H5 rough에서 중단되어 H6 ±10°는 실행되지 않았다 | `VIDEO_STATUS.tsv`, `launcher.log`, 로컬 mp4·policy 개수 실측 |
| **F56** | CORE/PARTIAL tar 외부 SHA와 tar 구조가 통과했고, PARTIAL 내부 checksum 36/36과 meta `model_best.pt` SHA가 Run06 model_9900 식별자 `8eb06e2…b636`에 일치한다 | `workspace/server_returns/train_260831-06_run05cfg_10000/videos/` 로컬 검증 |
| **F57** | H5 실패 원인은 정책 성능이 아니라 Hydra 설정 타입 오류다. `terrain_generator.seed`의 런타임 타입이 `NoneType`인데 정수 `42`를 override해 config 변환 전에 rc=1로 종료됐다 | H5 로그의 `Expected: <class 'NoneType'>, Received: <class 'int'>` |
| **D46** | 현재 서버는 종료하지 않는다. seed override를 제거하고 기존 평지 7종을 건너뛰는 resume 러너로 H5·H6만 재실행한 뒤 FULL tar·SHA를 회수한다 | 학습 후 영상 증거 게이트. 수정 script SHA `f89c925b…ed35`, CRLF 0, Git Bash `bash -n` PASS |

**LATEST NEXT (영상 resume):** 수정된 `server_run06_videos.sh`를 서버 humanoid 폴더에 덮어쓰고
SHA `f89c925b3889ff57e8ad64a16b07a8cfa4521173068d46456ea39a7dcb89ed35`와 `bash -n`을 확인한다.
`VIDEO_SUITE=full VIDEO_RESUME=1 bash server_run06_videos.sh`로 재개한다. `[DONE]`과 FULL tar·SHA를
로컬에서 검증하기 전에는 서버를 종료하지 않는다.

### 260831 Run06 영상 FULL 검증 완료

| # | 사실/결정 | 근거 |
|---|---|---|
| **F58** | Run06 FULL 영상 package가 로컬에 도착했고 외부 tar SHA, 안전한 tar 경로, 내부 checksum 47/47, 10개 시나리오, 영상·policy 10/10, model SHA 대응이 모두 통과했다 | `videos/FULL_VERIFY.json`, FULL tar SHA `c80f972c…9be` |
| **D47** | Run06 서버 세션의 필수 회수 조건이 충족됐으므로 서버를 종료한다 | 학습 bundle VERIFIED + 영상 FULL VERIFIED. 행동 품질 판정은 별도 A260831-04에서 수행 |
| **D48** | 정형 다운로드 검증은 `artifact-verifier`를 Luna로 호출하고 `tools/verify_download_artifact.py` 결과를 사용한다. 메인 팀장은 예상 입력과 최종 의미 판정을 소유한다 | 비용 절감과 판정 권한 분리 |

**LATEST NEXT:** 서버 종료 후 Run06 영상 10종의 육안·정량 행동 판정과 Run05 대비 tfevents 곡선
분석을 수행한다. package 무결성 PASS를 H1~H7 행동 PASS로 해석하지 않는다.

### 260831 Run06 분석 완료·고정 evaluator 승급

| # | 사실/결정 | 근거 |
|---|---|---|
| **F59** | Run06 마지막 500은 Run05 대비 xy 오차 9.7097%, yaw 오차 16.6120%, base_contact 14.76%, mean_std 4.39% 개선됐고 episode length는 7.25% 증가했다. `W=+7.487`이며 사전등록 게이트 5개가 모두 통과했다 | Run05/06 tfevents 직접 파싱, `experiment_history.csv` Run06 결과행 |
| **F60** | Run05 전체 3000 iter와 Run06의 최초 3000 iter는 성능/시간 태그를 제외한 TensorBoard 31개 태그가 bit-identical하다 | 두 tfevents 전수 tag 비교. 같은 seed·설정 prefix이므로 Run06은 학습시간 확장 실험이다 |
| **F61** | Run06 영상 10종에서 H1 제자리·H2 전진·H3 좌우·H5 rough는 시각 PASS다. H4 yaw 양, H6 실제 경사 통과 성능, H7 push 회복시간은 영상만으로 정량 확정할 수 없다 | FULL 영상 육안 감사. 모든 영상 약 20초·무명백 낙상, 명령·지형·push 로그 대응 확인 |
| **D49** | Run06 `model_9900.pt`(SHA `8eb06e2…b636`)를 고정 evaluator 후보로 승급하고 reward tuning을 동결한다 | F59의 사전 게이트 통과. `lin=2.0`, `feet_air_time`, `flat_orientation` 즉시 실험은 undertraining과 reward 효과를 다시 혼합할 위험이 있다 |
| **D50** | 가장 이른 미완료 표준 단계는 **1/6 H4 정량 게이트**다. 다음 서버 작업은 학습이 아니라 H4·H6·H7 fixed-policy telemetry다 | 단계 번호는 가장 이른 미완료 단계 규칙. H4 영상은 직립·이동만 보여 yaw tracking을 정량하지 못한다 |
| **D51** | A260831-08은 `VIDEO_NOT_REQUIRED`다. 새 학습·reward·env·checkpoint 변경이 없고 같은 Run06 model_9900의 FULL 영상 10종이 이미 검증됐으므로 기존 영상을 재사용한다 | 영상 예외 조건: 동일 tensor·동일 고정 시나리오 영상 VERIFIED. 이번 run은 raw telemetry만 추가한다 |

| no | 등급 | 단위 | state | 산출물/게이트 |
|---:|---|---|---|---|
| 43 | 조사 | Run05 대비 Run06 곡선·영상 분석 | done | F59~F61, D49 |
| 44 | 필수(제출요건) | H4·H6·H7 고정 정량 evaluator package | 로컬 준비·검증 완료 / 서버 실행 대기 | `run06_fixed_eval_package.zip`, SHA `bf90f194…aac93`, unittest 5/5, bash syntax PASS |

**LATEST NEXT:** 서버는 package가 완성되기 전에는 켜지 않는 D36을 유지한다. 현재 package는
검증 완료됐으므로 다음 접속에서 `run06_fixed_eval_package.zip` 업로드 후 런북의 한 줄 명령만
실행한다. 완료 tar와 `.sha256`을 내려받아 H4 yaw, H6 slope tracking/termination, H7 push recovery를
판정한다. 이 평가 전에는 추가 학습·reward 수정·공식 제출 승급을 하지 않는다.

### 260831 Run06 고정 정책 평가 완료·제출 후보 승격

| # | 사실/결정 | 근거 |
|---|---|---|
| **F62** | 고정 평가 5개 케이스(H4 좌·우, H6 ±10°, H7 push)가 모두 `EVAL_RC=0`으로 종료됐고 각 케이스의 `summary.json`·`steps.csv`가 회수됐다 | `workspace/server_returns/train_260831-06_run05cfg_10000/fixed_eval/original/` |
| **F63** | H4 명령 `wz=+0.5/-0.5`에 대한 평균 실측은 `+0.4646/-0.4856`, yaw MAE는 `0.1241/0.1263`, 조기 종료는 0이다 | `fixed_eval/derived/FIXED_EVAL_REPORT.json`, raw `steps.csv` |
| **F64** | H6 양 경사는 각각 32/32 환경이 timeout까지 생존했고 조기 종료 0, xy MAE `0.0643/0.0638`이다. H7은 관측 충격 114/114 회복, 중앙값 0.18초, 최대 0.88초, 조기 종료 0이다 | 동일 보고서와 raw telemetry |
| **F65** | 서버 runner의 `RC=127`은 평가 후 `python3` 명령 부재로 보고서 생성만 실패한 것이다. 원본 5개 평가는 모두 성공했고 보고서는 로컬 Python으로 재생성했다 | `launcher.log`, `RUNNER_STATUS.txt`, 로컬 `FIXED_EVAL_REPORT.*` |
| **F66** | 제출 후보 `policy.pt`의 TorchScript 텐서 8개와 Run06 iter 9900 checkpoint actor MLP 텐서 8개가 일치한다 | 로컬 `torch.jit.load(...).state_dict()` 대조 `MATCH True` |
| **D52** | Run06을 H1 제출 후보로 승격하고 reward tuning을 동결한다. 추가 학습보다 제출 3종 정합성과 공식 업로드 증거를 우선한다 | F59~F66. 단일 seed·내부 evaluator이므로 공식 점수/통과는 보장하지 않음 |
| **D53** | 가장 이른 미완료 단계는 **5/6 최종 평가·문서**다. H1~H7 내부 근거는 채웠고 공식 업로드 및 제출 이력 회수만 남았다 | 제출 후보: `workspace/submission_candidates/h1_run06_model9900/` |

**LATEST NEXT:** `workspace/submission_candidates/h1_run06_model9900/`의 `policy.pt`, `env.yaml`, `TECHNICAL_REPORT.md`를 최종 대조한 뒤 팀 대시보드에 업로드한다. 업로드 완료 화면/제출 이력은 외부 시스템 사실이므로 회수 전까지 `[미측정]`으로 유지한다.

### 260831 Run06 PASS 표현 정정

| # | 사실/결정 | 근거 |
|---|---|---|
| **F67** | 영상 감사 결과는 H1·H2·H3·H5만 시각상 요구 행동 확인이며, H4 yaw·H6 경사각·H7 외력 사건은 영상으로 판독할 수 없어 `UNKNOWN`이다 | `run06_video_audit`의 10개 영상 감사; 원거리 카메라와 명령·경사·외력 overlay 부재 |
| **F68** | H4·H6·H7 고정 telemetry는 긍정적 실측이지만 사전 등록된 수치 PASS 기준이 없고 운영진 evaluator도 아니다 | `experiment_history.csv`의 “No preregistered numeric PASS threshold”; D52의 공식 점수/통과 미보장 단서 |
| **D54** | D52의 “제출 후보 승격”은 artifact와 첫 공식 평가 기준선의 준비를 뜻한다. H1~H7 성능 PASS 또는 예선 합격 판정으로 해석하지 않는다 | 사용자 지적 수용. `측정 완료/양호`, `영상 판독`, `공식 PASS`를 분리 |
| **D55** | 가장 이른 미완료 단계는 다시 **5/6 최종 평가·문서**로 유지한다. 정정 리포트와 영상/telemetry 증거 수준을 팀장에게 보고한 뒤에만 단계 6 공식 제출로 이동한다 | 성능 판정 근거를 사용자에게 보고하지 않은 상태에서 제출 단계로 성급히 승급한 오류 교정 |

**LATEST NEXT:** 정정된 영상·telemetry 행렬을 팀장에게 보고한다. 현재 결론은 `제출 파일 정합성 VERIFIED / 내부 측정은 긍정적 / H1~H7 전체 PASS 미확정 / 공식 합격 미측정`이다.

### 260831 증거 계층·후속 학습 승인 규칙 확정

| # | 사실/결정 | 근거 |
|---|---|---|
| **F69** | 대회 제공 가이드는 시나리오 점수를 `survival_rate × tracking_score`로 명시한다. 이전 정정 리포트의 “팀 내부 가설” 표현은 잘못이다 | `workspace/training/PRELIM_RL_GUIDE.md:95-100` |
| **F70** | 위 공식 수식과 달리 Run06의 `base_contact`, 자체 MAE, recovery time, `W`는 운영진 evaluator의 공식 입력값과 동일성이 확인되지 않은 내부 proxy다 | 가이드는 evaluator 구현·threshold를 공개하지 않으며 참가자 사전 점수 확인 불가(`PRELIM_RL_GUIDE.md:117-119`) |
| **D56** | 판정은 `ARTIFACT_VERIFIED / VIDEO_OBSERVED·UNKNOWN / INTERNAL_GATE_PASS·FAIL·INCONCLUSIVE / OFFICIAL_RESULT` 네 계층으로 분리한다. bare `PASS`, 합격, 통과 가능 표현은 금지한다 | F67~F70 및 사용자 지적 재발 방지 |
| **D57** | Run06은 동결 기준선이다. 후속 개선은 공식 결과의 약점을 최우선으로 하며, 결과가 없을 때만 `lin 1.0→1.5`, `feet_air_time 0.2→0.3`, `flat_orientation -1.5→-1.0` 순의 단일변수 3k screening을 검토한다 | `H1_REWARD_EVIDENCE_MASTER.md` §9. 값은 최적값이 아니라 보수적 탐색점 |
| **D58** | screening은 survival·tracking proxy 동시 판정, 동일 3k 대조, 필수 영상·telemetry, 결과별 분기를 사전등록해야 한다. 승자만 독립 seed/공식 평가 후 10k 장기 후보가 된다 | 공식 점수의 곱 구조와 단일-run 일반화 한계 |

**LATEST NEXT:** 서버를 켜지 않는다. 수정된 보고서·마스터·일정의 증거 계층을 대조하고 Run06을 첫 공식 평가 기준선으로 제출할지, 공식 결과 전 단기 screening을 할지는 `공식 결과 정보가치 우선` 원칙으로 결정한다.

### 260831 제출 전 자체 70점 게이트 확정

| # | 사실/결정 | 근거 |
|---|---|---|
| **F71** | 운영진 평가는 제출본을 모아 일괄 진행하므로 공식 결과를 후속 튜닝 입력으로 기다리는 계획은 실전 개선 루프가 아니다 | `workspace/PRELIM_RL_GUID.md:113-119`, 사용자 정정 |
| **F72** | 현재 Run06 고정 평가는 H1·H2·H3·H5 격리 telemetry가 없고 H4는 600 step이라 완주 survival이 계산되지 않는다 | 기존 `FIXED_EVAL_REPORT.json`, `server_run06_fixed_eval.sh` |
| **D59** | D57의 “공식 결과 우선”을 철회한다. 제출 전 H1~H7 전체 자체 scorecard를 약점 지도로 사용한다 | F71; 공식 결과 공개 시점 이후에는 개선 기회가 없을 수 있음 |
| **D60** | 부분 결과는 `SELF_ASSESSMENT_INCOMPLETE`이며 자체 점수에 포함하지 않는다. Run06의 현재 성능 판정은 자체 PASS가 아니라 **평가 미완료**다 | F72; 영상·일부 telemetry와 H1~H7 점수 분리 |
| **D61** | 내부 proxy v1은 Run06 env의 `std=0.5`와 exponential tracking을 사용해 `exp(-(RMSE/0.5)^2)`로 계산한다. 각 시나리오는 survival≥0.95와 tracking≥0.70을 모두 요구하고, 쌍 방향은 최악값을 쓴다 | `_eval_run06/env.yaml:847-857`, 공식식은 survival×tracking이나 정확한 변환식은 비공개 |
| **D62** | 제출 성능 최소조건은 H1~H7 전부 내부 시나리오 통과, 가중 시뮬 proxy≥0.70, 문서 자체감사를 포함한 총 자체예상≥70/100이다. 불확실성 여유를 둔 운영 목표는 75/100 이상이다 | 사용자 목표와 공식 70/20/10 배점 |

**LATEST NEXT:** 새 학습이나 공식 결과 대기가 아니다. A260831-11 평가 전용 package로 Run06
H1~H7 10 case를 모두 20초 평가해 `시뮬 proxy /70`을 확정한다. 70점 게이트를 통과하면 제출
후보를 유지하고, 미달이면 `가중치 × (1-scenario_proxy)` 최대 감점 시나리오의 약한 인수만
3,000-iteration 단일변수 screening한다.
