# G-A029 판정 근거 정정 계획 (G-P-A029-REVISION-20260914)

근거:
- `workspace/training/quadruped/reports/GO2_POST_A029_PLAN_POLICY_AUDIT_20260914.md` §7·§7-6
- 2026-09-14 사용자 감사(채팅)
- 결정 `G-D-PRIORITY-20260914`

상태: **DONE(2026-09-14, R1~R6 시행). 사용자 결정과 결과는 §8 시행 기록에 있다.** 이 문서는 C(엔진 A017 등록·G-A030 패키지) 보류를 해제하지 않는다. 학습·서버 실행도 승인하지 않는다.

## 0. 예선 기준 현재 위치
- [예선 목표] Go2 시뮬레이션 70점 축. 후보 선정 규칙이 바뀐 뒤에도 A029 기록이 옛 규칙을 퍼뜨리지 않게 한다.
- [현재 단계] 2/6 짧은 학습 pilot. 동결 기준선은 A017(69case 39.76/70, 내부 proxy)이다.
- [확보] A029 감사 판정 `AUDIT_FAIL`, G-A018 유효 기각(−44.40/70), 우선순위 원칙 정본화(G-D-PRIORITY-20260914).
- [미확보] 후보 비교표, 측정 경로 (가)/(나) 결정, 잔여 GPU 실측.
- [이번 계획] 문서·검사만 다룬다(GPU 0). A029 값은 되살리지 않는다.
- [흐름] A029 감사 → 우선순위 원칙 정본화 → **A029 판정 근거 정정** → 후보 비교표 → 측정 경로 결정·C
- [지금 할 일] §7의 결정 세 가지를 확인한다.
- [보장하지 않음] 문서 정정은 점수를 올리지 않는다.

## 1. 결론

**A029 후보(`action_rate_l2 -0.01→-0.008`)는 기각을 유지한다. 판정 근거만 고친다.**

| 감사 행 | 새 기준에서 | 처리 |
|---|---|---|
| 1행: 같은 값이 G-A018에서 유효 기각 | **유지(주 근거).** Pilot-01은 A017과 track 1.2↔1.4 한 항만 다르다. F5를 반영해 재시도 금지를 "같은 값 재시도 금지"로 좁혀도 A029는 금지 대상이다 | 그대로 둔다 |
| 3행: 값 근거가 "20% 완화" | 유지(주 근거). 기체 AGENTS §4-2 | 그대로 둔다 |
| 4행: 가설 기전이 작다(약 0.02) | 유지(보조 근거) | 그대로 둔다 |
| 2행: 표적이 최대 감점 인수에서 벗어남 | **근거 무효.** 폐기된 "최대 감점 우선" 규칙(루트 AGENTS 학습 승인 5 원문)을 적용했다. 추종을 겨냥한 것 자체는 결함이 아니다 | 판정 근거에서 뺀다 |
| 5행: "기대 이득 ≈0.29/70" | **표기 오류.** 성공 기준(RMSE −5%)을 달성했다고 가정한 값이므로 민감도다. 계획의 `+1.06`과 같은 오류다 | "민감도 0.29/70, 기대 가중 이득 `미추정`"으로 바꾼다 |
| 새 근거: 달성 가능성 | 원장 §5 3항은 달성 가능성을 원인 확실성과 §1-a 이력으로 적게 한다. 이력은 유효 붕괴 1건이다 | 보조 근거로 추가한다 |

판정 `AUDIT_FAIL`과 lifecycle `REJECTED_BY_AUDIT`는 바뀌지 않는다.
단, G-A018도 학습 seed 42 하나의 `exploratory` 결과다. 따라서 "해롭다"는 확정 인과가 아니라 **같은 값을 다시 돌릴 근거가 없다**는 판정이다.

**계획 작성 중 정정:** 직전 채팅의 "원장 G-A029 절을 원본으로 재구성" 권고를 철회한다.
- 시행 계획 A4(`GO2_AUDIT_REMEDIATION_PLAN_20260914.md:96-99`)가 이미 "원문은 추측으로 복원하지 않는다"로 정했다.
- 원장 두 곳에는 CORRUPTED 표시가 이미 있다(`GO2_PROJECT_STATE.md:1717`, `GO2_REWARD_EVIDENCE_MASTER.md:420`).
- 정책 감사 보고서 §7-6과 원장의 "복원·재작성은 사용자 결정" 문구도 A4를 놓친 것이다. R5에서 바로잡는다.

## 2. 원칙
1. **원문은 지우지 않는다.** 감사 보고서 §0~4와 과거 원장 행은 그대로 둔다. 정정은 덧붙인 절이나 `SUPERSEDED` 표시로 한다(시행 계획 §1-1).
2. **`review/`는 불변이다.** `test_go2_g_a029_draft.py:45`가 `release_blockers` 4개 이상을 요구한다. 초안 JSON 1번(과거 report 누락)이 튜닝 요청 계약 3항에 어긋난다는 점은 원장에만 적는다.
3. **정정은 새 판정을 만들지 않는다.** 이미 있는 결정(G-D81, G-D-PRIORITY-20260914, F5 권고)을 A029 기록에 반영하는 작업이다.
4. 한국어 파일은 편집할 때마다 UTF-8을 검사한다. 쓰기는 편집 도구로만 한다(과거 `?` 손상 재발 방지).

## 3. 단계표

| 단계 | 등급 | 작업 | 대상 | 완료 기준 | GPU |
|---|---|---|---|---|---:|
| R1 | 필수 | A029 감사 보고서에 정정 절 추가 | `reports/GO2_G_A029_TUNING_AUDIT_20260914.md` 끝에 §5 | §4-R1 문안 반영, §0~4 불변 | 0 |
| R2 | 필수 | 옛 규칙을 퍼뜨리는 사본에 `SUPERSEDED` 표시 | 원장 1743·1746행, `PLANNER_BRIEF.md:108·110`, 캠페인 감사 §6-6 | §4-R2 표 전부 | 0 |
| R3 | 필수 | 재시도 금지 문구 좁히기(F5) | `GO2_NOW.md` §4, 시행 계획 A2 초안 표 | §4-R3 문안 | 0 |
| R4 | 필수 | A029 목표를 후보 비교표 입력 행으로 등록 | 이 문서 §4-R4(비교표 문서가 생기면 옮김) | 행 초안 확정 | 0 |
| R5 | 개선 | 인코딩 손상 격리 확장과 내 기록 정정 | §4-R5 목록 | 모든 `???` 구간 머리에 CORRUPTED 표시 | 0 |
| R6 | 필수 | 회귀 검사 2개 추가 | `tools/test_go2_canonical_consistency.py` | §4-R6 검사 통과 | 0 |

순서: R1 → R2 → R3 → R5 → R6 → R4.
R6을 R1~R5 뒤에 두는 이유가 있다. 검사 9는 R5 표시가 끝나야 통과하고, 검사 10은 R1·R2가 끝나야 통과한다.

## 4. 단계별 상세

### R1. A029 감사 보고서 §5 "2026-09-14 판정 근거 정정" (추가 절)

문안 초안:

> 판정 `AUDIT_FAIL`과 lifecycle `REJECTED_BY_AUDIT`는 유지한다. 근거를 다음과 같이 정정한다(G-D-PRIORITY-20260914).
> - **주 근거:** 1행(같은 값 G-A018 유효 기각), 3행(비율 값 근거).
> - **보조 근거:** 4행(기전 크기 약 0.02), 원장 §1-a 이력상 낮은 달성 가능성.
> - **2행 철회:** 최대 감점 인수 우선은 폐기된 규칙이다. 추종 표적은 결함이 아니다.
> - **5행 정정:** 0.29/70은 성공 기준 달성을 가정한 민감도다. 기대 가중 이득은 `미추정`이다.
> - **§0 "정책 순서대로", §1 "표적도 최대 감점 인수에서 벗어나 있다", §4 조건 ①** 은 효력이 없다. 다음 후보는 원장 §5 비교표로 고른다. 조건 ②(같은 항 과거 결과 인용)와 ③(비율이 아닌 값 근거)은 유지한다.
> - **§4 "유력 방향: flat"** 은 "비교 후보"로 읽는다.
> - 이 절은 작성자 자신의 정정이며 독립 감사가 아니다.

### R2. 사본의 `SUPERSEDED` 표시

행을 고치지 않고 바로 아래에 한 줄씩 붙인다.

| 위치 | 현재 | 붙일 표시 |
|---|---|---|
| `GO2_PROJECT_STATE.md:1743` | 원인 ②(학습 승인 5 위반)·④(기대 이득 0.29) | `SUPERSEDED(G-D-PRIORITY-20260914): ② 철회, ④는 민감도. 감사 보고서 §5` |
| `GO2_PROJECT_STATE.md:1746` | NEXT "G5 또는 G3 생존 표적… 유력 flat" | `SUPERSEDED: 후보는 원장 §5 비교표로 고른다. 조건 ②·③ 유지` |
| `PLANNER_BRIEF.md:108` | "표적도 최대 감점 인수와 어긋나고… 기대 이득 0.29" | 위와 같은 취지 |
| `PLANNER_BRIEF.md:110` | NEXT "G5 또는 G3 생존을 표적으로" | 위와 같은 취지 |
| `reports/GO2_TUNING_CAMPAIGN_AUDIT_20260914.md:121` 권고 6 | "G5 또는 G3 생존을 겨냥한 단일변수" | 권고 6 아래 주석: `2026-09-14 정정(G-D-PRIORITY-20260914): 표적은 비교표로 정한다` |

### R3. 재시도 금지 문구 (F5)

- `GO2_NOW.md` §4 현재: "재시도 금지(보행 기준선 유효 기각): `action_rate_l2` 완화, `feet_air_time` 0.35, `ang_vel_xy_l2` 강화."
- 바꿀 문구: "재시도 금지(같은 값, 보행 기준선 유효 기각): `action_rate_l2 -0.008`, `feet_air_time 0.35`, `ang_vel_xy_l2 -0.15`. 같은 방향 다른 크기는 새 근거와 §1-a 인용이 있을 때만 제안한다."
- 시행 계획 A2 초안 표(71~73행)의 "금지·강화 금지·완화 금지"는 고치지 않는다. 표 아래에 `SUPERSEDED(F5): 같은 값만 금지` 한 줄을 붙인다.
- 원장 §1-a는 "기각(완화)" 같은 **상태**만 적으므로 바꾸지 않는다.

### R4. 후보 비교표 입력 행 — A029 목표

A029가 노린 **G3 전진·G7 속도의 추종 부족**을 비교표 한 행으로 올린다. 수단은 비교표에서 정한다. 수치 출처는 A027 `SELF_EVAL_REPORT.json`과 `reports/GO2_G_A029_WEAKNESS_ANALYSIS_20260914.md` §1이다.

| 항목 | 값 |
|---|---|
| 목표 | G3 `rough_forward`, G7 `dr_seed_*`의 속도 추종 |
| 가중치·감점 | G3 .20·9.03/70, G7 .10·2.43/70 |
| 약한 인수 | G3 최저곱 case는 **생존** .4688(추종 .7574)이다. 전진 case 추종은 .6282, 실제 vx .221/.5이다. G7은 **추종** .7209(생존 .9063)이고, seed202 vx .244/.5이다 |
| 전달 한계 | G3는 생존 .47이 곱을 묶어 추종 개선이 점수로 적게 전달된다. 추종이 약한 인수인 G7이 이 목표의 주 전달 경로다 |
| 수단 이력(§1-a) | `action_rate_l2` 완화: 같은 값 금지, 다른 크기는 새 근거 없음. `track` 추가 상향: 1.2→1.4에서 G4·G6 손실. 추종에 직접 연결되는 미탐색 항은 비교표에서 판단 |
| 기대 가중 이득 | `미추정`(달성 확률·효과 크기 근거 없음) |
| 원인 확실성 | 낮음. 억제 가설과 떨림·리듬 가설을 가를 action/contact telemetry가 없다 |
| 실험 비용 | 1,000 iter 학습 1회 + 후보 69case 평가. 측정 경로 (가)/(나)에 따라 달라지므로 미정 |

### R5. 인코딩 손상 격리 확장

CORRUPTED 표시가 없는 `???` 구간:

| 파일 | 행 | 내용 | A029 관련 |
|---|---|---|---|
| `upload/plan/GO2_G_A029_DIFFICULTY_SCREENING.md` | 51–56 | G-A029 절 | ✓ |
| `upload/plan/GO2_DEFAULT_BASELINE_TEST_PRD.md` | 372–377 | G-A029 절 사본 | ✓ |
| `ARTIFACT_MANAGEMENT.md` | 611–619 | G-A029 절 2개 | ✓ |
| `GO2_CAMPAIGN_SCHEDULE.md` | 807–808 | G-A029 CLOSED 절 | ✓ |
| `ARTIFACT_MANAGEMENT.md` | 342–361, 592 | G-A008 표·절 | — |
| `GO2_CAMPAIGN_SCHEDULE.md` | 152–158 | G-A008 표 | — |
| `reports/NEW_SESSION_HANDOFF.md` | 117–134 | G-A007/A008 인계 | — |
| `upload/plan/GO2_FEET_AIR_TIME_020_SCREENING_PRD.md` | 129–149 | 260901 §9 | — |

- 각 구간 머리에 A4와 같은 한 줄을 붙인다: `> CORRUPTED — 인코딩 손상, 근거 사용 금지. 원문을 추측으로 복원하지 않는다. 대체 기록: <정상 절>`
- 정책 감사 보고서 §7-6 끝과 원장 `G-D-PRIORITY-20260914` 절의 "복원 여부는 사용자 결정" 문구에 정정 한 줄을 붙인다: "A4가 이미 복원하지 않기로 정했다. 원장 두 곳은 표시돼 있고, 나머지는 R5에서 표시한다."

### R6. 검사 추가 (`tools/test_go2_canonical_consistency.py`)
- **검사 9 — 손상 격리:** Go2 원장·계획·보고서에서 `???`가 있는 줄은 모두 CORRUPTED 표시가 있는 절 안에 있어야 한다. 기준은 가장 가까운 위쪽 제목부터 그 줄까지에 `CORRUPTED`가 있는지다. 백틱으로 감싼 `` `???` `` 인용은 제외한다.
- **검사 10 — 옛 표적 규칙 전파 차단:** "G5 또는 G3 생존"을 담은 문서는 `G-D-PRIORITY-20260914`도 인용해야 한다. A029 감사 보고서는 결정 ID를 인용해야 한다.
- 두 검사 모두 문구 검사다. 뜻을 바꾼 재서술은 잡지 못한다.

## 5. 완료 판정과 검증
- Go2 테스트 전부 통과: `canonical_consistency`, `tuning_engine_contract`, `report_recovery`, `g_a029_draft`, `report_first_contract`
- `python tools/validate_go2_campaign.py` → `GO2_CAMPAIGN_CONTRACT_OK`
- 편집한 모든 파일의 UTF-8 검사를 통과하고, 새 `???`가 없어야 한다
- `GO2_NOW.md`는 60줄 이하(검사 8)
- `review/` 파일 SHA가 바뀌지 않아야 한다
- 완료 시 `GO2_PROJECT_STATE.md`에 한 절을 쓰고, `GO2_NOW.md`에는 상태를 한 줄로 반영한다

## 6. 하지 않는 것
- `-0.008` 부활, 다른 `action_rate` 크기 제안(새 근거 없음)
- 손상 원문 재구성
- `review/` 파일·승인 ZIP 수정
- 감사 보고서 §0~4 본문 수정
- 후보 비교표 작성 전체, flat -1 채택 여부, 측정 경로 (가)/(나) 결정, C 착수

## 7. 사용자 결정 필요
1. **R1 방식:** 감사 보고서 끝에 정정 절을 덧붙인다(권장). 본문 행에 직접 줄을 긋는 방식도 가능하지만 원문 보존 원칙과 어긋난다.
2. **R3 범위:** F5 문구 좁히기를 A029의 `action_rate`만 할지, 세 다이얼 모두 할지 정해 주세요. 권장은 세 다이얼 모두다. 한 줄이고 근거가 같다(각각 한 값·seed 하나).
3. **R5 범위:** A029 관련 4구간만 할지, G-A007/A008 시기 4구간까지 할지 정해 주세요. 권장은 전부다. 검사 9가 전부를 요구하므로, 일부만 하면 검사에 예외 목록이 필요하다.

## 8. 시행 기록 (2026-09-14)

### 8-1. 사용자 결정 (이 계획 본문보다 우선)
1. **R1:** 감사 보고서 끝에 정정 절을 덧붙인다. 기존 본문과 당시 판정 기록은 보존한다.
2. **R3:** 세 다이얼 모두 정정한다. 단 "같은 값은 영구 재시도 금지"로 바꾸지 않는다. 문구는 다음과 같다.
   - "해당 값은 유효 기각 이력이 있어, 새로운 검증 근거 없는 재시도는 하지 않는다."
   - 같은 방향의 다른 크기도 기존 실패와 구별되는 근거가 필요하다.
   - A029 기각은 유지한다.
   - → §1 1행과 §4-R3의 "같은 값 재시도 금지" 표현은 이 문구로 대체된 것으로 읽는다.
3. **R5:** 표에 열거한 손상 구간 전체를 격리한다. 원문은 복원·삭제하지 않는다. 대체 기록은 실제 확인한 정상 기록만 연결하고, 없으면 "대체 기록 미확보"로 표시한다. 이유는 검사 편의가 아니라 손상 자료가 다시 인용되는 것을 막기 위해서다.
4. **범위:** 문서·회귀검사 정정에 한정한다. G-A030 후보 확정, 측정 경로 선택, C 보류 해제, 서버 실행은 포함하지 않는다.

### 8-2. 결과

| 단계 | 결과 |
|---|---|
| R1 | `reports/GO2_G_A029_TUNING_AUDIT_20260914.md` §5 추가. §0~4 불변 |
| R2 | 원장 1743·1746행 아래, `PLANNER_BRIEF.md` 108·110행 아래, 캠페인 감사 권고 6 아래에 SUPERSEDED 표시 |
| R3 | `GO2_NOW.md` §4 문구 교체. `PLANNER_BRIEF.md` 109행 아래와 시행 계획 A2 표 아래에 SUPERSEDED(F5) 표시 |
| R4 | 행 초안은 §4-R4에 확정. 비교표 문서가 아직 없어 옮기지 않았다 |
| R5 | 6개 파일 10구간에 CORRUPTED 표시(아래 8-3). 원문 바이트 불변 |
| R6 | 검사 9·10 추가 |

R5 과정에서 한 가지 더 정정했다. 정책 감사 보고서 §7-6과 원장의 "복원 여부는 사용자 결정" 문구에 정정 한 줄을 붙였다. 복원하지 않는다는 결정은 A4에서 이미 내려져 있었다.

### 8-3. R5 대체 기록 연결 (확인한 것만)

| 구간 | 연결한 정상 기록 | 미확보 |
|---|---|---|
| `ARTIFACT_MANAGEMENT.md` §13, `GO2_CAMPAIGN_SCHEDULE.md` §10 (G-A008) | `go2_feet_air_time_020_v2.VERIFICATION.md`(정상 영문, 전문 열람), `GO2_OPUS_REAUDIT_INDEPENDENT_260907.md:296` | 파일 변경 원장 문구, 단계 1~5 일정 상태 |
| `NEW_SESSION_HANDOFF.md` §8, `GO2_FEET_AIR_TIME_020_SCREENING_PRD.md` §9 (G-A008) | 같은 VERIFICATION. §9의 PARTIAL 수치는 원장 §10에 있으나, 그 절도 재구성 기록(원문 아님)이라고 표시했다 | 인계 지시 문구 |
| G-A029 "로컬 검증" 절 사본 3곳(`ARTIFACT_MANAGEMENT.md`, 난이도 검토, 기본 기준선 PRD) | 약점 분석 보고서, 초안 JSON | "35 unittest" 결과. `review/GO2_G_A029_TEST_OUTPUT.txt`는 32 tests로 다른 회차다 |
| `ARTIFACT_MANAGEMENT.md` G-A029 ID 절, `GO2_CAMPAIGN_SCHEDULE.md` G-A029 CLOSED 절 | 같은 날짜의 정상 G-A029 절. 내용이 같은지는 확인할 수 없다고 적었다 | — |
| `ARTIFACT_MANAGEMENT.md` `GO2-ALL-SCENARIO-PRIORITY-20260913` | 없음 | **대체 기록 미확보** |

### 8-4. 검증
- Go2 테스트 46개 통과: `canonical_consistency`(검사 9·10 포함 11개), `tuning_engine_contract`, `report_recovery`, `g_a029_draft`, `report_first_contract`
- `GO2_CAMPAIGN_CONTRACT_OK`
- 편집한 15개 파일 UTF-8 정상, 대체문자 0
- `GO2_NOW.md` 58줄
- 검사 9가 빈 검사가 아님을 확인했다
  - 손상 줄 94개(9개 파일)를 검사한다.
  - `NEW_SESSION_HANDOFF.md` 표시를 지운 변형은 8줄 실패로 잡힌다.
- `review/GO2_G_A029_TUNING_REVIEW.zip` SHA `284adb95…fbfc`가 기록값과 같다. `review/`는 수정하지 않았다

### 8-5. 남은 한계
- 검사 9·10은 문구 검사다. 표시가 있는 절 안에 새로 손상된 줄이 생기면 잡지 못한다. 뜻을 바꾼 재서술도 잡지 못한다.
- A4가 이전에 붙인 표시 5곳의 대체 기록 연결은 다시 감사하지 않았다(이번 R5 표 밖).
  - 예: 원장 1717행은 같은 내용이 아니라 이후 결정 절을 가리킨다.
- 표시를 넣은 6개 파일은 원래 CRLF·LF가 섞여 있다. 넣은 줄은 CRLF다.
- F3(C2 차순위 오인용)·F6~F11은 이번 범위가 아니다. F5는 R3로 해소했다.
