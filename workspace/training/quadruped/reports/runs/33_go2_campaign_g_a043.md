# 33. go2_campaign_g_a043

- 시각: `시각 미회수` (학습기 run 폴더 내장값)
- 산출물: `workspace/_keep/go2_campaign_g_a043`
- 4족 판별 근거: `GO2_G_A043_RESULT.zip` 외 1건 (이름이 아니라 산출물 경로로 가렸다)

## reward 설정

학습 `env.yaml`이 회수되지 않았다. reward 가중치 **모름**.

## 평가 — 정본 registry로 재채점

당시 쓰던 registry가 아니라 `config/go2_self_eval_registry.json` 하나로 다시 채점했다.

평가 case가 회수되지 않았다. 70점 **미측정**.

---

이 파일은 `tools/build_go2_run_reports.py`가 산출물에서 생성한다. 직접 고치지 않는다.
