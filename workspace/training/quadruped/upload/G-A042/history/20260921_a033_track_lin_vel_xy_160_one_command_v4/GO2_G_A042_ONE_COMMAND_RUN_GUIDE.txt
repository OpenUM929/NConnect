GO2 G-A042 ONE-COMMAND RUN GUIDE (파일 하나, 명령 하나, 결과 하나)

- G-A042: G-A033 위에서 `track_lin_vel_xy_exp` 1.5 → 1.6 하나만 바꾼다. 나머지 가중치·seed 42·4096 env·1000 iter는 G-A033과 같다.
근거: workspace/training/quadruped/upload/plan/GO2_FORWARD_STAIRS_POLICY_20260921.md §2(사실→해석→경쟁 가설)·§3(첫 실험 선택)
서버 실행은 사용자 결정 사항이다. 이 문서는 실행 절차이지 실행 승인이 아니다.

값을 고른 방법
- 원문 역할: Track linear velocity commands (xy axes) with an exponential kernel (Isaac Lab v2.3.1, reports/evidence/go2_reward_term_roles_20260917/isaaclab_envs_mdp_rewards.py): w * exp(-||v_cmd,xy - v_body,xy||^2 / std^2), std 0.5. The weight multiplies a bounded kernel, so raising it raises the value of closing the velocity error and, relative to every penalty, lowers the value of standing still. It says nothing about how the foot reaches the tread. G-A033 logs this term at 0.9436 of a 1.5 weight at iteration 999 (reports/runs/LEDGER.csv track_lin_vel_999), that is about 0.63 of the kernel's maximum.
- 걷기 구간: G-A033 walk margin +0.1359 -> +0.1801 at 1.6 (reports/evidence/go2_reward_mechanism_20260917/PROBES.csv, zone WALK, delta +0.0442, range_status OUT_OF_RANGE). The stop zone's top is +0.0155 and the walk-zone edge weight for this term is 1.22752, below the baseline, so the change moves away from the edge. Raising the objective term cannot lower this margin; the margin is therefore not evidence for the arm, counterfactual arithmetic that cannot rule out a stalled policy.
- 계단·흔들림·밀침: reports/evidence/go2_reward_mechanism_20260917/PROBE_SITUATIONS.csv at 1.6: walk +0.0442, climb +0.0204, sway +0.0111, push +0.0573. All four cells are positive, which is exactly why they may not be read as a forecast of this arm: the direct A017->A033 comparison (900/900) has the sway case going the other way (rough_lateral terminations 48 -> 58 of 96, posture falls 8 -> 45, reports/evidence/go2_stairs_behavior_20260916/CASE_BEHAVIOR.csv). Where the model and the measurement disagree, this spec follows the measurement and puts the case in stage 1 with a measured floor.
- 규칙: The plan selects 1.6 as one additional 0.1 increment, not an optimum or a calibrated prediction. The direct comparison A017->A033 uses checkpoint900/900; Pilot->A017 uses999/900 and is supporting observation only, not an identical-condition replication. Loss is observed but no scaling law or exclusive cause is established.
- 주장하지 않는 것: No score gain is estimated and no claim is made that a third raise repeats the first two. The partial margins are reward arithmetic over measured postures: they say what the reward prefers, not how many robots will climb, and for this dial all four situation cells point the same way while the measurement does not. The plan's own words are kept: 1.6 is an information value outside the observed range, not the current best value.
바꾸지 않은 레버: `ang_vel_xy_l2`(강화·완화 둘 다 계단 악화 — G-A038·G-A041), `feet_air_time`(0.01·0.1 1단계 FAIL, 0.35 정지), `lin_vel_z_l2`(G-A037 보류), `action_rate_l2`(A018 기각, 상황 칸 전부 빈칸), `flat_orientation_l2`(G-A040 보류), `dof_acc_l2`(G-A039 보류, 배포 목록 밖).

체크포인트 고정: 후보는 iter 900로 평가한다. 기준선 G-A033도 1000 중 iter 900에서 평가됐다.

LOCAL FILE TO UPLOAD (이것 하나만)
1. C:\dev\NConnect\workspace\training\quadruped\upload\G-A042\current\GO2_G_A042_a033_track_lin_vel_xy_160_one_command.zip
   SHA256 a95d3b6d2164354381e850ed7749d4c97d3a6df6ae02ef5bb4f29ef8dda06133
안에 회차 ZIP이 빌더 출력과 바이트 동일하게 들어 있다. 회차 ZIP을 따로 올리지 않는다.

SERVER DESTINATION
/workspace/GO2_G_A042_a033_track_lin_vel_xy_160_one_command.zip

ONE-LINE RUN (tmux 안에서 시작하고 즉시 리턴)
cd /workspace && echo 'a95d3b6d2164354381e850ed7749d4c97d3a6df6ae02ef5bb4f29ef8dda06133  GO2_G_A042_a033_track_lin_vel_xy_160_one_command.zip' | sha256sum -c - && unzip -oq GO2_G_A042_a033_track_lin_vel_xy_160_one_command.zip && bash /workspace/go2_campaign_g_a042/server_run_go2_campaign.sh

시작 전 러너가 하는 일
회차 ZIP의 SHA 확인 → /workspace 아래로 풀기 → 회차 preflight(평가기·registry·G-A033 SHA) → 학습·play 프로세스, tmux 세션, 이전 결과가 있으면 거부.

순서와 시간 [추정]
1. 1단계 약 100분: 학습 1000 iter, 파국 게이트, 표적 20 case(10cm·15cm 계단 각 3 seed, 험지 6 case, 밀침 3 case, 초기 보호 5 case; 서버 채점 12 case와 필수 측정 20 case를 구분), G-A033 표지 5 case, 영상 4+1개.
2. 서버 게이트가 통과시키면 2단계(나머지 case) 약 55분.
3. 결과 ZIP 하나로 묶는다.
1단계에서 끝나면 약 1시간 40분, 통과하면 약 2시간 35분. 접속 직후 잔여 GPU 시간을 실측해 기록한다.

서버 게이트 (go2_target_gate.py) — 2단계를 돌릴지만 정한다
판정 1항(표적 12 case proxy 평균 +0.05 이상, 세 묶음 중 2개 이상 상승)을 로컬 검증기와 같은 함수로 읽는다.
15cm는 서버 GPU 배분 점수에서는 제외하지만 로컬 계획 판정에서는 필수다. 두 높이 각각 전진거리 증가·등반수 비열등·정체 감소와 생존 보호를 요구한다. 유효 정책의 정지나 TARGET_FAIL에도 필수 계단·보호 기록·영상을 회수한다. A017→A033은 iter900/900 직접 비교1건, Pilot→A017은999/900 보조 관측이다.
- 묶음별 하한(fact_rules_v1): 평균이 한 묶음의 붕괴를 덮지 못한다. 묶음 평균 proxy 변화 ≥ stairs -0.02858 · rough -0.06145 · push -0.07561
- 계단 오른 로봇 수 하한(3 seed 합, 기준 workspace/_keep/go2_g_a033_a017_track_lin_vel_xy_150/evaluation/candidate/cases/seed_*/*/steps.csv): stairs_10_climb_ge1 90-6.671 · stairs_10_climb_ge2 43-13.739. 1단계에 세 seed가 모두 있는 묶음은 여기서, 나머지는 로컬 판정에서 본다.
- TARGET_PASS: 2단계를 돈다.
- FAIL: 필수 평가·영상 수집을 마친 뒤 끝낸다. 유효 정지 정책도 수집한다. 비유한/실행 불능은 실패 근거를 회수하고 중단한다.
- REMEASURE_BASELINE: 표지 case가 저장 G-A033과 어긋났다. 1단계를 G-A033 재측정으로 한 번 더 돌리고 다시 판정한다.
- UNDECIDED: 결측·계측 불일치다. 2단계를 돌리지 않고 로컬 검증으로 넘긴다.

로컬 판정 (2단계 후) — 다른 축을 지키는 조건
- 총점 +2.53/70 이상(2σ 검출 한계)
- 평지 G1·G2: 시나리오 proxy 하락 ≤ 0.054, case 생존 하락 ≤ 0.0625
- 나머지 축: 시나리오별 가중 손실 ≤ 자기 평가 표집 sd의 2배 (G3 2.03646 · G4 0.99632 · G5 0.05604 · G6 0.86196 · G7 0.40358 /70)
- 새 정지 case 0, POLICY_LOCOMOTES
- 목표 축 G3+G5의 가중 점수 합이 올라야 한다(분석 §8-3b). 보호 축은 G1·G2·G4·G6·G7이다.
- 계단 오른 로봇 수 하한(1단계 안내의 목록 전부)

MONITOR
tmux attach -t go2_campaign_g_a042
진행 기록: /workspace/_keep/go2_campaign_g_a042/CAMPAIGN_LOG.txt ([ARM], [GATE], [GATE READ], [RESULT] 줄)

첫 10분에 볼 것
학습 로그 머리의 reward 표에서 track_lin_vel_xy_exp가 1.6이고 나머지가 G-A033과 같은지 본다.

재시작 규칙
중단되면 `GO2_RESUME=1 bash /workspace/go2_campaign_g_a042/server_run_go2_campaign.sh`로 잇는다. 끝난 단계·case·영상은 건너뛴다.
기본 재실행은 이전 결과를 지우지 않고 거부한다. 버리려면 `GO2_DISCARD_PREVIOUS=1`.

DONE MARKER
[DONE] GO2_G_A042_CAMPAIGN_RESULT_READY

DOWNLOAD TO LOCAL workspace\_keep (이것 하나)
/workspace/_keep/GO2_G_A042_CAMPAIGN_RESULT.zip
/workspace/_keep/GO2_G_A042_CAMPAIGN_RESULT.zip.sha256
패키징이 실패하면 `_keep/go2_g_a042_a033_track_lin_vel_xy_160`, `_keep/go2_campaign_g_a042`를 그대로 가져온다.

결과를 받은 뒤 (로컬, GPU 0)
python -B tools/verify_go2_basic_motion_harvest.py G-A042 --harvest workspace/_keep/go2_g_a042_a033_track_lin_vel_xy_160 --out workspace/_keep/go2_g_a042_a033_track_lin_vel_xy_160/harvest_verification.json
통합 판독기는 artifact/fact_rules_v1 및 go2_screening_gate.py의 계획 §4를 함께 실행하고 통합 verdict를 저장한다.
python -B tools/go2_stall_diagnostics.py --candidate workspace/_keep/go2_g_a042_a033_track_lin_vel_xy_160/evaluation/candidate --out workspace/_keep/go2_g_a042_a033_track_lin_vel_xy_160/stall_diagnostics
정체 진단은 env별 검열·유효분모와 provenance sidecar를 보존한다.
게이트 판정과 로컬 판정이 다르면 로컬 판정을 따르고, 차이를 기록한다.

SERVER SHUTDOWN GATE
DONE 표시만으로 종료하지 않는다. 결과 ZIP·SHA/manifest, 해당 학습 원본 report.html·env·로그·checkpoint, 필수 telemetry 20 case+평지 전진, 후보 영상4개·새 기준선 영상1개 및 SHA로 재사용한 기준선 영상3개의 로컬 도착·무결성을 확인한다. 누락하면 종료 불가이며 성능 FAIL과 회수 완결은 별개다.

RESULT INTERPRETATION — 실행 전에 고정 (사후 재협상 금지)
판정 기준은 fact_rules_v1 AND forward_stairs_v1: workspace/training/quadruped/upload/plan/GO2_FORWARD_STAIRS_POLICY_20260921.md section 4. Combined locally by tools/verify_go2_basic_motion_harvest.py; the server fact_rules gate only allocates GPU time.이다.
1.6은 OUT_OF_RANGE 정보수집 값이다. 직접 비교1건(900/900)과 체크포인트가 다른 보조 관측(999/900)을 반복 증거로 합치지 않는다. 학습 seed42 하나이며 기전·일반화·공식 점수 개선은 미확정이다.
공식 evaluator·공식 점수는 OFFICIAL_RESULT_UNMEASURED다.

PLAN SCREENING — 1단계 회수부터 적용
위 단일 verifier 명령은 artifact/identity → fact_rules_v1 → go2_screening_gate.py(계획 §4)를 통합한다. 10·15cm 각각 전진거리 증가, 등반수 비열등(최소 한 항 증가), 정체 감소, 네 표적의 생존 보호와 험지 추종 보호를 모두 요구한다. 누락/지문 불일치는 INCONCLUSIVE다.
서버 게이트는 GPU 배분만 한다. screening 성공도 후보 승급이 아니며 전수69case·영상·독립 학습seed 양팔 반복이 남는다.
예산: 기존 A033/A041 학습 약59/57분, 본 사양 1단계100분·확장55분은 추정이다. 실제 잔여 GPU/TTL은 미측정이다. 회수 여유를 포함한 약3시간 이상 창을 확인하고 시작한다. 재측정 분기는 추가 시간이 필요하므로 해당 러너의 회수/중단 절차를 따른다.
