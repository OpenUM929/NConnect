GO2 G-A037 ONE-COMMAND RUN GUIDE (파일 하나, 명령 하나, 결과 하나)

- G-A037: G-A033 위에서 `lin_vel_z_l2` -2.0 → -1.0 하나만 바꾼다. 나머지 가중치·seed 42·4096 env·1000 iter는 G-A033과 같다.
근거: workspace/training/quadruped/reports/GO2_STAIRS_BEHAVIOR_ANALYSIS_20260916.md §8(계단 오르기)·§9(험지 옆걸음)
서버 실행은 사용자 결정 사항이다. 이 문서는 실행 절차이지 실행 승인이 아니다.

값을 고른 방법
- 측정: (0.124-0.021)/(0.985-0.679) = 34% of the climb's tracking gain is paid back as lin_vel_z_l2
- 추정: vz scales with step height at the same forward speed, so the penalty scales with (15/10)^2 = 2.25: 0.103*2.25/0.306 = 76%
- 규칙: choose the weight that brings the 15cm share back to the share G-A033 already climbs 10cm stairs with: 76% * (w/2) ~= 34%  ->  w ~= 0.9, rounded to 1.0 (share 38%)
- 주장하지 않는 것: The share is a reward-arithmetic quantity on two measurable terms. It does not predict how many robots will climb. ang_vel_xy, joint penalties and termination risk are not in the measurement.
바꾸지 않은 레버: `ang_vel_xy_l2`(좌우 구르기도 벌한다, 옆걸음 뒤집힘), `track_lin_vel_xy_exp`(옆걸음 종료 31→48→58), `feet_air_time`(내리면 경사, 올리면 붕괴).

체크포인트 고정: 후보는 iter 900로 평가한다. 기준선 G-A033도 1000 중 iter 900에서 평가됐다.

LOCAL FILE TO UPLOAD (이것 하나만)
1. C:\dev\NConnect\workspace\training\quadruped\upload\G-A037\current\GO2_G_A037_a033_lin_vel_z_m1_one_command.zip
   SHA256 40efbb6e4b3be2fdc11d92e60bf42df2db466dc69baf38c76bd2955e06d1797e
안에 회차 ZIP이 빌더 출력과 바이트 동일하게 들어 있다. 회차 ZIP을 따로 올리지 않는다.

SERVER DESTINATION
/workspace/GO2_G_A037_a033_lin_vel_z_m1_one_command.zip

ONE-LINE RUN (tmux 안에서 시작하고 즉시 리턴)
cd /workspace && echo '40efbb6e4b3be2fdc11d92e60bf42df2db466dc69baf38c76bd2955e06d1797e  GO2_G_A037_a033_lin_vel_z_m1_one_command.zip' | sha256sum -c - && unzip -oq GO2_G_A037_a033_lin_vel_z_m1_one_command.zip && bash /workspace/go2_campaign_g_a037/server_run_go2_campaign.sh

시작 전 러너가 하는 일
회차 ZIP의 SHA 확인 → /workspace 아래로 풀기 → 회차 preflight(평가기·registry·G-A033 SHA) → 학습·play 프로세스, tmux 세션, 이전 결과가 있으면 거부.

순서와 시간 [추정]
1. 1단계 약 90분: 학습 1000 iter, 파국 게이트, 표적 9 case(15cm 오르기·10cm 오르기·험지 옆걸음 각 3 seed), G-A033 표지 5 case, 영상 4+2개.
2. 서버 게이트가 통과시키면 2단계(나머지 case) 약 55분.
3. 결과 ZIP 하나로 묶는다.
1단계에서 끝나면 약 1시간 30분, 통과하면 약 2시간 25분. 접속 직후 잔여 GPU 시간을 실측해 기록한다.

서버 게이트 (go2_target_gate.py) — 2단계를 돌릴지만 정한다
판정 1항(표적 9 case proxy 평균 +0.05 이상, 세 묶음 중 2개 이상 상승)을 로컬 검증기와 같은 함수로 읽는다.
험지 옆걸음이 표적 묶음에 들어 있어, 뒤집힘이 늘면 1단계 평균이 내려가 2단계 전에 멈춘다.
- TARGET_PASS: 2단계를 돈다.
- FAIL: 끝이다. 파국 게이트 정지·발산도 FAIL이다.
- REMEASURE_BASELINE: 표지 case가 저장 G-A033과 어긋났다. 1단계를 G-A033 재측정으로 한 번 더 돌리고 다시 판정한다.
- UNDECIDED: 결측·계측 불일치다. 2단계를 돌리지 않고 로컬 검증으로 넘긴다.

로컬 판정 (2단계 후) — 다른 축을 지키는 조건
- 총점 +2.53/70 이상(2σ 검출 한계)
- 평지 G1·G2: 시나리오 proxy 하락 ≤ 0.054, case 생존 하락 ≤ 0.0625
- 나머지 축: 시나리오별 가중 손실 ≤ 자기 평가 표집 sd의 2배 (G3 2.03646 · G4 0.99632 · G5 0.05604 · G6 0.86196 · G7 0.40358 /70)
- 새 정지 case 0, POLICY_LOCOMOTES

MONITOR
tmux attach -t go2_campaign_g_a037
진행 기록: /workspace/_keep/go2_campaign_g_a037/CAMPAIGN_LOG.txt ([ARM], [GATE], [GATE READ], [RESULT] 줄)

첫 10분에 볼 것
학습 로그 머리의 reward 표에서 lin_vel_z_l2가 -1.0이고 나머지가 G-A033과 같은지 본다.

재시작 규칙
중단되면 `GO2_RESUME=1 bash /workspace/go2_campaign_g_a037/server_run_go2_campaign.sh`로 잇는다. 끝난 단계·case·영상은 건너뛴다.
기본 재실행은 이전 결과를 지우지 않고 거부한다. 버리려면 `GO2_DISCARD_PREVIOUS=1`.

DONE MARKER
[DONE] GO2_G_A037_CAMPAIGN_RESULT_READY

DOWNLOAD TO LOCAL workspace\_keep (이것 하나)
/workspace/_keep/GO2_G_A037_CAMPAIGN_RESULT.zip
/workspace/_keep/GO2_G_A037_CAMPAIGN_RESULT.zip.sha256
패키징이 실패하면 `_keep/go2_g_a037_a033_lin_vel_z_m1`, `_keep/go2_campaign_g_a037`를 그대로 가져온다.

결과를 받은 뒤 (로컬, GPU 0)
python -B tools/verify_go2_basic_motion_harvest.py G-A037 --harvest workspace/_keep/go2_g_a037_a033_lin_vel_z_m1 --out workspace/_keep/go2_g_a037_a033_lin_vel_z_m1/harvest_verification.json
python tools/go2_stairs_behavior.py   (오르기 단수·옆걸음 뒤집힘을 기준선과 같은 규칙으로 판독)
게이트 판정과 로컬 판정이 다르면 로컬 판정을 따르고, 차이를 기록한다.

SERVER SHUTDOWN GATE
DONE 표시만으로 종료하지 않는다. 결과를 받아 로컬 검증을 통과해야 종료 판단을 보고한다.

RESULT INTERPRETATION — 실행 전에 고정 (사후 재협상 금지)
판정 기준은 analysis section 8-3b (all axes kept while stairs rise) on the criteria of GO2_BASIC_MOTION_TUNING_PLAN_20260915.md section 6-1, limits measured as in G-A035 section 15-4이다.
학습 seed가 42 하나라, 한 회차로는 레버 효과와 seed 운을 가를 수 없다. 이득 추정은 없다.
공식 evaluator·공식 점수는 OFFICIAL_RESULT_UNMEASURED다.
