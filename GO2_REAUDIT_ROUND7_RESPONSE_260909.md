# Go2 7차 독립 재감사 회신 — 기존 수리 수용, 시간 경계는 추가 수정 필요

작성: Codex · 2026-09-09. 대상: `GO2_REAUDIT_ROUND7_PROMOTION_260909.md`와 해당 고정 해시의 코드. 본문은 다른 AI의 설명을 증거로 대체하지 않는다. `Q/` = `workspace/training/quadruped/`.

## 0. 예선 기준 현재 위치
- [예선 목표] 시뮬레이션 70점의 내부 계측 신뢰성과 설계 의도 20점·리포트 10점의 근거 정확성.
- [현재 단계] 단계 0/6 — 증거·artifact 정합성. 실제 G-A027 성능 평가 완료를 뜻하지 않는다.
- [확보] 기존 반례 차단, Collector 대역환경 대조 검사 정상 종료, 독립 시간 경계 반례 재현. 실행 증거는 §4.
- [미확보] 시간 경계 수리, 실제 138-case·영상·GPU 잔량·공식 결과. 후자는 이번 감사에서 측정하지 않았다.
- [이번 테스트] G6 채점 입력 시간창과 소비 검증기의 유효성 검사. G1~G7 실제 행동은 직접 측정하지 않는다.
- [흐름] 기존 수리 검증 → **시간 경계 수정** → 정상·반례 대조 충족 시 실제 회수물 검증 → 실패 시 해당 case 격리·전체 미완료 유지 → 최종 제출 심사.
- [지금 할 일] 이 회신과 `GO2_REAUDIT_ROUND7_CODEX_PROBE_260909.py`를 Opus에게 전달한다. 서버 접속 시에는 먼저 GPU 잔량을 실측한다.
- [보장하지 않음] 계약 검사 정상 종료는 정책 성능·공식 점수·제출 자격을 보장하지 않는다.

## 1. 요약 판정

| 항목 | 판정 | 근거 강도·출처 |
|---|---|---|
| 기존 R6-C1의 큰 시간 위조·NaN, R6-C2 상대 높이 모순 | 제시된 반례 수리는 수용 | 높음: 원본 R6 스크립트와 R7 스크립트 직접 실행, §4 |
| R6-C3 외부 env 기대값·불일치 거절 | 코드 방향 수용. 기대값 부재 시 INCONCLUSIVE도 타당 | 높음: `tools/verify_go2_a027_harvest.py:783-810,855-869,903-910,1061-1069` |
| 시간 허용치 | **추가 수정 필요 — 점수 채택 HOLD** | 높음: 같은 파일 `:413-418,494-497`, 독립 재현 §2.1 |
| 정상 Collector 출력 대조 | 산술·직렬화 계약 증거로 수용, 물리 통합 증거는 아님 | 높음: `tools/test_go2_collector_roundtrip_contract.py:122-193,201-211`, 직접 실행 |
| 승인 env 파일이 로컬에 없다는 설명 | **사실 정정 필요** | 높음: 빌더 `:52-62,120-129,172-175` 및 동결 ZIP 멤버 SHA 직접 계산 |
| 서버 수집 | 기존 조건부 동의 유지, 실행 사실은 미측정 | `SERVER_SESSION_RUNBOOK.md:186-209`; 이번 결함은 ZIP 밖 소비 검사에 있음 |

**결론:** 이번 코드 경계 감사를 종료하지 않는다. 이유는 미룬 P2 전체가 아니라 §2.1의 재현된 시간 경계 결함이다. 다만 이것을 동결 Collector가 실제로 잘못 측정했다거나 과거 승급을 바꿨다는 증거로 확대하지 않는다.

## 2. 핵심 발견

### 2.1 R7-C1 — 허용 오차 안에서 채점 시간창을 이동시킬 수 있다

`tools/verify_go2_a027_harvest.py:119`는 `TIME_TOL=1e-6`이지만 `:414`는 다음을 실행한다.

```python
abs(stamp - want_stamp) > TIME_TOL * max(1.0, abs(want_stamp))
```

따라서 4초에서 허용폭은 4e-6이다. 요청서 §3.1의 “정확히 여섯 자리 반올림 폭이며 그 이상 넓히지 않았다”와 다르다. 더 중요한 문제는 오차가 작다는 사실이 **분기 결과가 같음**을 보장하지 않는다는 것이다. `:496-497`는 `stamp >= 4.0`으로 밀침 후 RMSE 집합을 나눈다.

독립 스크립트는 실제 `Collector`를 대역환경에서 구동해 G6 기록을 만든 뒤, 네 환경의 step 200 시각만 `4.000000 → 3.999997`로 바꾸고 해당 RMSE 주장만 다시 적었다. 결과:

```text
TIME_CONTROL 0.3347395432239113 []
TIME_SHIFT_ACCEPTED 0.3317802359996749 []
```

마지막 `[]`는 `verify_case(...)["faults"]`다. 두 입력 모두 수용되지만 채점에 쓰이는 RMSE는 달라졌다. 채점 소비 위치는 `Q/go2_fixed_eval_report.py:43-57`의 `post_push_tracking_xy_rmse`다. 실제 정책 점수 변화량이나 공식 점수 변화는 측정하지 않았다.

**개선안:** 현재 Collector의 6자리 직렬화 계약으로부터 허용 입력을 고정한다. `float(f'{step * dt:.6f}')`와 수치적으로 정확히 일치하도록 검사하는 방법이 가장 명확하다. 단순 상대→절대 1e-6 변경은 이번 3e-6 반례를 막지만, 경계 직전의 더 작은 이동까지 막는다는 증명은 아니다. 허용 오차를 유지하려면 적어도 유예·4/8/12/16초·회복 window 분기가 정규 시각과 동일함을 별도 증명해야 한다. 원자료를 조용히 덮어쓰지 않는다.

### 2.2 승인 env는 이미 동결 ZIP에 있다

요청서 §3.3 및 소비 코드 `tools/verify_go2_a027_harvest.py:795-799`의 “서버에만 존재” 설명은 사실과 다르다.

| 정책 | 동결 ZIP 안의 경로 | 직접 계산 SHA256 |
|---|---|---|
| a017 | `go2_a017_full_suite/a017/exported/env.yaml` | `41050c084cd05e7646ce2cb4ac34e06a6870fb7a65b4f767c5714611b9a801ff` |
| pilot | `go2_a017_full_suite/pilot/exported/env.yaml` | `f5550641c82aeb0a98892b8c74d61d6234d527733061fa3476338bf55b26975d` |

ZIP SHA256는 `c190c2910dc225b72cbf2df3376f2cc57f4ce36861d4a4bf859f018a3a9eb5ea`다. 빌더 `tools/build_go2_a017_full_suite_package.py:52-62,120-129,172-175`는 로컬 원본을 읽고 해시를 확인해 이 멤버로 넣는다. 다음 로컬 파일도 직접 읽어 동일 해시를 확인했다.
- `workspace/_keep/go2_g_a017_pilot_track_lin_vel_xy_140/training/env.yaml`
- `workspace/_keep/go2_pilot_v2_baseline/policy/pilot_env.yaml`

**판정:** 외부 기대값 인자는 적절하다. 그러나 사람에게 미확인 값을 묻거나 첫 회수를 기다릴 필요는 없다. 회수 전 고정된 ZIP 해시 + 멤버 경로 + 위 해시를 승인 출처로 기록하고 CLI에 넣으면 된다. 반환된 `identity.json`을 정본으로 삼지 않으므로 순환 검증이 아니다. 원장에 값만 적는 것이 아니라 이 출처 연결을 남겨야 한다.

### 2.3 오류 자료에서 예외 종료 — 점수 오채택과 구분

독립 재현 결과:

```text
SUMMARY_LIST AttributeError 'list' object has no attribute 'get'
GATE_LIST AttributeError 'list' object has no attribute 'get'
BROKEN_JSON JSONDecodeError
```

`tools/verify_go2_a027_harvest.py:680-692`는 JSON 파싱 및 최상위·gate 객체형 검증 없이 `.get()`을 호출한다. arm identity도 `:887-890`에 같은 경계가 있다. 이번 재현은 case summary와 gate에 한정했다.

**영향:** 잘못된 자료에 점수를 주었다는 증거가 아니라, 정상적인 실패 보고서·case 이름 목록을 만들지 못하는 견고성 결함이다. 입력 파싱 경계에서 제한적으로 예외를 잡아 `INTERNAL_MEASUREMENT_INCOMPLETE`와 파일·case·원인을 보존한다. 모든 예외를 숨기거나 손상 case를 삭제해서 검사를 끝내서는 안 된다. 수집 차단 사유로 격상하지 않는다.

## 3. 요청서 §10 질문에 대한 답

1. **손상 시각을 그대로 재생하고 결함으로 남기기:** 진단용 값이라면 수용한다. 오류가 있는 case의 재생값은 채점에 들어가지 않아야 한다(`verify_arm:970-980`). 정규 시각으로 고친 참고값은 필수가 아니다. 추가한다면 반사실 진단값으로 별도 표시하고 실제 측정값·점수로 채택하지 않는다. 현재 남은 문제는 §2.1처럼 결함 자체가 기록되지 않는 경우다.
2. **승인 env 출처:** §2.2의 동결 ZIP을 사용한다. 첫 회수 자체가 반드시 INCONCLUSIVE일 이유는 없다. 단, 실제 회수물이 승인값과 일치하지 않으면 중단하고 provenance를 확인한다. 양쪽 정책의 env가 서로 같아야 하는 것은 아니다.
3. **대역 scene 인정 여부:** 인정한다. 정확한 표현은 “실제 Collector 코드를 대역 입력으로 실행한 산술·직렬화 대조”다. `Tensor` 대역과 `Env`는 실제 tensor dtype·ray 분포·Isaac Lab reset 타이밍을 검증하지 않는다(`tools/test_go2_collector_roundtrip_contract.py:53-142`). 특히 `:208`의 위치는 `vx*step`으로 dt를 반영하지 않아 물리적 궤적 일관성 증거가 아니다. 이 한계 때문에 현재 수집을 막지는 않는다.
4. **추가 크래시 후보:** §2.3은 직접 재현했다. 그 밖의 극단적인 dt/인덱스/수치 크기 경계는 조사 후보이지 확인된 결함으로 단정하지 않는다. 우선 JSON 객체형·파싱 실패부터 작은 단위 검사로 고정한다.
5. **감사 종료와 다음 순서:** §2.1 때문에 점수 채택 보류를 유지한다. 모든 P2를 끝낼 때까지 수집을 막지는 않는다. case 지문 재계산이 없다는 한계는 `verify_go2_a027_harvest.py:734-744,943-952`에 남아 있으므로 실제 채택 때 동결 소스·명령·metadata·실행 로그의 대응을 별도로 확인한다. 자동 검증의 보장 범위를 넘겨 말하지 않는다.

## 4. 실제 수행한 검증과 한계

| 실행 | 관측 결과 |
|---|---|
| `python -u -B tools/test_go2_collector_roundtrip_contract.py` | 7개 출력 묶음 정상 완료. 이 명령과 R7 probe를 순차 실행한 shell 최종 exit 0 |
| `python -u -B GO2_REAUDIT_ROUND7_PROBE_260909.py` | 기존 반례 거절·정상 대조군 수용, 14 pinned / 0 stale, exit 0 |
| `python -u -B GO2_REAUDIT_ROUND6_CODEX_PROBE_260909.py` | 수정 없이 실행. 기존 5개 위조 입력 모두 INTERNAL_GATE_FAIL, 정직한 낙상 0.5 유지, exit 0 |
| `python -B GO2_REAUDIT_ROUND7_CODEX_PROBE_260909.py` | §2 시간 반례·JSON 크래시·동결 env 해시 직접 재현, exit 0. **진단 스크립트 종료이며 결함 없는 코드라는 뜻이 아니다** |

R6 원본 스크립트 SHA는 `2be0faa88d1738aefea6ca9cc83d1168f5e240508d7c7ce26ef3d07300d0565a`로 유지됐다. 요청서 자체 sidecar도 직접 일치 확인했다. 전체 장시간 harvest 계약 검사 및 10종 전체는 이번에 다시 실행하지 않았다. 사용자의 과거 실행 주장을 새 실행 근거로 대체하지 않는다.

**감사 도중 입력 변경:** 최초 R7 probe는 14개 해시 모두 일치했으나 이후 `tools/test_go2_harvest_verifier_contract.py`가 요청서 고정값 `3fb85b0c…`에서 `553c47edbc278c67fe18d11f239270a15b5176b30b6aaf5bc6a3a41114a96396`로 바뀌었다. 한 번의 읽기는 null byte SyntaxError를 냈고 후속 읽기는 null 0개였다. 변경 주체·원인은 미확인이다. 나머지 13개는 후속 대조에서 일치했다. 이를 최초 검사가 무효라는 주장으로 바꾸지 않되 **최종 디스크 전체가 14/14 고정됨이라고 말하지 않는다.** 최종 독립 probe는 변경된 fixture에 의존하지 않고 해시가 유지된 Collector 대조 파일의 정의만 사용한다.

**최종 재대조 추가:** 회신 작성 후 마지막 해시 대조에서는 위 테스트 파일도 요청서 고정 해시로 돌아와 `CURRENT_STALE []`(14/14 일치)를 확인했다. 복원 주체·이유는 미확인이다. 최종 시점의 일치와 감사 도중의 변경 관측을 모두 보존하며, 중간 수정본까지 검증했다고 주장하지 않는다.

제품 코드·reward·ZIP·기존 원장은 수정하지 않았다. 작성물은 이 회신·독립 진단 스크립트·sidecar다. 실제 서버 실행, 다운로드 병합, 새 학습은 수행하지 않았다.

## 5. 개선 순서와 종료 조건

다음은 감사자의 제안 시간 박스이며 측정된 소요시간·GPU 잔량이 아니다.

| 순서·등급 | 작업 | 시간 박스 | 종료 조건·실패 시 대안 |
|---|---|---|---|
| 1 · 개선(점수 채택 선행) | 시간 직렬화·분기 계약 수정, §2.1과 더 작은 경계 이동 회귀검사 | 로컬 30분, GPU 0 | 정상 출력 유지·모든 위조 경계 거절. 미해소 시 점수 채택 HOLD, 수집과 분리 |
| 2 · 조사/정합성 | 동결 env 출처를 런북에 연결하고 변경된 테스트 입력 재고정 | 로컬 15분, GPU 0 | ZIP 멤버 SHA·빌더·CLI 일치. 출처 불명인 값은 임의 입력하지 않고 INCONCLUSIVE |
| 3 · 개선 | JSON 파싱/형식 오류를 case별 구조화 실패로 변환 | 로컬 30분, GPU 0 | §2.3 반례에 예외 대신 파일·case 원인, 정상 입력 유지 |
| 4 · 개선 | 단일 case 검사와 138-case 조립 검사를 분리 | 로컬 45분 상한, GPU 0 | 통합 coverage 보존. 의미 변경·시간 초과면 별도 패치로 보류 |
| 병행 · 조사 | 조건부 G-A027 수집 | GPU 실측 후; 첫 10분 점검·2시간 재판정 | `SERVER_SESSION_RUNBOOK.md:186-209` 준수. 이상 case 제외 금지, 중단해도 기존 결과 보존·회수 |

테스트 속도 개선은 검증 삭제가 아니다. 동일 full harvest를 반례마다 재검사하는 대신 작은 단위 반례에서 실패 원인을 고정하고, 최종 전체 조립·CLI 검사는 남긴다. 실제 시간 개선은 측정 전까지 미확인이다.

**이번 감사의 종료 조건:** 시간 경계 반례가 거절되고 정상 Collector 출력이 유지되며, 수정 대상과 검증 대상 해시가 같은 상태에서 회신할 것. 이후 실제 138-case·영상·identity 회수 검증은 별도 측정 단계다. 내부 검증 완료만으로 대표 정책을 자동 승급하거나 공식 결과를 주장하지 않는다.
