# GO2_NOW 이력 — 2026-09-19~20 PM 검토 정정 (이전)

2026-09-22: 이 절은 `GO2_NOW.md`에 있었다. 그 파일은 한 페이지 진입점(60줄 상한, 관문
`tools/test_go2_canonical_consistency.py::test_8_now_fits_one_page`)인데 이 이력 세 줄 때문에
61줄로 넘쳤다. 줄을 합쳐 관문만 통과시키는 대신 **이력을 여기로 옮기고 본문에는 한 줄
포인터**를 남겼다. 옮긴 세 줄의 내용은 한 글자도 바꾸지 않았다. 현재 상태의 정본은 여전히
`GO2_NOW.md`다.

## 2026-09-19~20 PM 검토 정정
- 최신 검토: `workspace/training/quadruped/upload/plan/GO2_PM_REPAIR_STRATEGY_20260919.md`. 마지막 학습 G-A038과 기준선 G-A033을 구분한다.
- G-A040의 과거 HOLD_CONTRADICTED/사용자 결정 대기는 INFORMATION_RUN(미실행 정보 실험 후보)으로 정정한다. 반대 증거 자체는 승인 요구가 아니며, 기존 15cm 보호 기준의 검출 불가 문제는 패키지 발행 전에 해결해야 한다. 현재 실행 패키지·성능 승급을 뜻하지 않는다. 2026-09-20: CSV 셀 결합·INFORMATION_RUN 공통 검사와 회귀 격리 수정; G-A035는 비실행 HOLD_UNSUPPORTED, 기존 ZIP 보존. 상세 G-D-EVIDENCE-RESPONSIBILITY-20260920(GO2_PROJECT_STATE.md). 독립 감사는 사용량 제한으로 미완료. **[2026-09-20 `ang_vel_xy_l2` 완화 감사 — PM 1안]** 감사 `workspace/training/quadruped/reports/GO2_ANG_VEL_RELAX_AUDIT_20260920.md`(관문 `tools/test_go2_ang_vel_relax_audit_contract.py` 11검사). 1안(`-0.05 → -0.04`)은 **정보 회차로 성립한다**: R-6 안(배포 6개 목록 ④), 기각 이력은 전부 반대 방향(`-0.08`·`-0.15` 강화), 배포 권장 대역 `추천 -0.3 ~ -0.02` 안, 걷기 margin `+0.1359 → +0.1569`(WALK). **PM 문장 두 곳을 고쳤다**: A016 `-0.15`는 "보행 악화"가 아니라 **보행 소실**(`terrain_999 0.0`)이고, **G5는 생존이 묶지 않는다** — 자세·생존을 다 고쳐도 회수분은 `1.43139/10.5`뿐이라 판정은 G5 점수가 아니라 **오른 로봇 수 + 전진거리**로 읽는다. 탐침 격자에 `-0.04`·`-0.03`을 넣어 다시 계산했다(생성기 `tools/go2_reward_mechanism.py`, 예측 문서 §8-1 요구): 걷기 `+0.0210` · 계단 `+0.0036` · **흔들림 `-0.1077` · 밀침 `-0.0512`**. **교정 비대칭이 성격을 정한다**(예측 문서 §9): 같은 다이얼 `-0.08`에서 흔들림 예측 `+0.3230`은 관측 `+0.356`로 맞았고 계단은 크기가 **약 13배** 틀렸다 — 잃는 쪽이 잘 예측된다. 그래서 상태는 `INFORMATION_RUN`(권고 아님)이고, 회수 배점으로는 G3(`4.22726/14.0`, 반사실 `10.71746`)를 겨눈 G-A040이 앞선다. **패키지 G-A041(한 파일·한 명령, 서버 실행 미해제)**: `workspace/training/quadruped/upload/G-A041/current/GO2_G_A041_a033_ang_vel_xy_m004_one_command.zip`(release `20260920_a033_ang_vel_xy_m004_one_command_v3`), 사양 `config/experiments/G_A041_a033_ang_vel_xy_m004.json`, 관문 `tools/test_go2_g_a041_campaign_contract.py`(서버 게이트·로컬 검증기를 가짜 수확물로 실행). 표적 3묶음 중 2개(`rough` 6쌍 전수·`push`)가 **예측된 손실**이라 1단계 기준 1을 통과하지 못할 수 있고 그것이 설계다 — 채택은 막고 측정은 남긴다. 역방향 관문은 10cm 오른 로봇 수이고 하한은 사양 `preregistered.climb_guard`에 측정값으로 있다. `stairs_15_climb_ge1`은 기준 합보다 잡음이 커 하한이 음수라 싣지 않았다(결함 S-2, G-A040 사양에는 아직 있다). 결함 3건 추가(`reports/GO2_DEFECT_LEDGER.md`): **C-1 FIXED**(첫 발행 안내문이 없는 이력을 주장 — 원천 수정+관문) · **C-2 OPEN, 사용자 결정 필요**(G-A035 발행 ZIP은 `INFORMATION_RUN`·사양은 `HOLD_UNSUPPORTED`라 `tools/test_go2_fact_rules_contract` test_6이 실패한다) · **C-3 OPEN**(안내문의 "가장 최근" 판을 이름 사전순으로 고른다). **[2026-09-20 계획 평가 — 사용자 요청]** `workspace/training/quadruped/upload/plan/GO2_PLAN_ASSESSMENT_20260920.md`(관문 `tools/test_go2_plan_assessment_contract.py` 10검사, 수치를 원자료 셀에서 재계산해 대조). **판단 문서이며 측정이 아니다.** 세 가지가 어긋나 있다: **A 목표 축** — 계획은 G5를 목표·G3를 보호로 두는데 회수 상한은 G3 `10.71746` 대 G5 `1.43139`이고, G5 12개 (case,seed) 전부 `completion < tracking_xy`(9개는 묶는 인수가 `completion`)라 나머지 약 9.0은 전진거리다 — R-6 밖 영역이다. **B 눈금** — 학습 18회에 70점 축 비교는 2건뿐이고, 기준선 승급 근거 `+2.76366`은 검출 한계 `2.52886`을 겨우 넘는다(구간 하한 `+0.04666`)인데 그 한계에는 학습 seed 흔들림이 들어 있지 않다(전 학습 seed 42, 측정 0건). **C 예산** — 비용은 계획서 `GO2_CAMPAIGN_SCHEDULE.md` §1 단계 4(`5k→10k→15k` = 30000 iter)·§2에 **있다**(초판의 "계획에 없다"는 계획서를 열지 않고 쓴 오류였고 원천에서 고쳤다). 빠진 것은 **대조**다 — 단계 4 하나가 지금까지의 학습 총량 18000 iter보다 큰데 잔여 GPU(실측 아님)와 빼 본 기록이 없다. 권고 순서 **P1 목표를 G3 1순위로 · P2 잔여 GPU 실측+제출본 길이 확정(둘 다 회차 0) · P3 G-A033 seed 복제(R-6 밖, 승인 필요) · P4 G-A040(S-2 수정 후) · P5 G-A041은 다이얼 규명으로 위치 변경**. 반증 조건 4개는 문서 §7. **D 계획서와 현 계획의 불일치(2026-09-20 추가)** — `GO2_CAMPAIGN_SCHEDULE.md` §1 단계표는 단계 2 상태가 `G-A010 package 검증 완료·서버 실행 대기`(2026-09-02)에서 멈춰 있고, 표의 단계 2 완료 기준(`목표 G 개선 + 타 G 비열등`)과 달리 현 기준선 G-A033은 2단계 FAIL 기록을 가진 채 사용자 결정으로 승급됐다. 표의 단계 5는 `다중 seed 최종 평가`를 **제출요건**으로 적는데 전 학습이 seed 42 하나다 — P3는 새 제안이 아니라 **계획서 미이행 항목**이다. 단계 4 학습량 `30000` iter는 지금까지 돌린 학습 18회 전부(18000)보다 많고 잔여 자원과 대조된 적이 없다. 그래서 **P0(회차 0): 단계표를 현재 상태로 갱신하고 승급 경로를 한 줄로 못 박는다**를 P1 앞에 둔다. 처음 적으려던 "6단계가 정의돼 있지 않다"는 확인 결과 **틀린 지적이어서 철회**했고 그 사실도 문서에 남겼다(§4-1). 외부 검토자에게 넘길 때는 그 문서 **§9(인계)**만 보면 된다 — 검증 명령 3개, 반박해야 할 지점과 각 주장이 걸린 단일 근거, 선행 결함 C-2가 이 작업의 실패가 아니라는 대조(G-A038), 결정 대기 3건. **[2026-09-21 계획 평가 PM 재검토 — 본문에 반영]** 반박 6건을 원자료에서 재확인해 **여섯 모두 본문에서 고쳤다**(별도 절로 쌓지 않음; 목록은 문서 부록 3, 관문 **10 → 23검사**). ① `7.5`배는 **도달점** 비율이고 회수량 비율은 `4.68046`배다. ② `1.43139`는 G5 튜닝 상한이 아니라 **생존 경로 회수분**이다(반사실이 `completion` 고정). ③ 단계 4는 이어 학습이면 `15000`, 처음부터면 `30000` iter — 계획서가 정하지 않는다(재평가 지점 5). ④ 중단 규칙은 **있고**(재평가 지점 4) 없는 것은 수치 문턱이다. ⑤ P3 근거를 단계 5 `다중 seed 최종 평가`(평가 seed 로도 읽힘)에서 계획서 §9 `독립 학습 seed를 먼저 수행한다`로 교체 — 결론 유지, 근거 강화. ⑥ **내 오류**: 걷는 기준선 유효 시도가 있는 다이얼은 둘이 아니라 **넷**이다(정본 §1-a가 G-A016·G-A018도 `유효 ✓`로 적는다). 0건인 것은 `lin_vel_z_l2`·`flat_orientation_l2` 둘이고, 재본 적 없는 것은 **방향 넷**이다. ⑦ 커리큘럼은 교락일 수도 **매개**일 수도 있어 "상관까지만"은 과했다. 또한 `GO2_CAMPAIGN_SCHEDULE.md`는 3행에서 스스로 닫혔다고 적은 문서이고 `5k→10k→15k`는 **G-D06**으로 살아 있어, **P0는 "단계표 갱신"이 아니라 살아 있는 단계표의 위치를 정하는 일**이다. **보고 정정**: `tools/go2_claim_check.py`는 이 평가 문서를 검사하지 않는다(`PLAN` 분류·백틱 마스킹) — 그 수치를 근거로 인용하면 안 된다. **[2026-09-21 사용자 지시 G-D-PLAN-AS-GIVEN-20260921]** "앞으로는 시키는 대로 만들어." 계획 평가의 **P1(목표 축 G3 1순위)은 채택되지 않았다** — 계획서의 `G5 계단 개선 + G3 험지 보호`와 단계 2 단일변수 pilot이 유효하다. 계획을 받으면 평가 문서가 아니라 **계획대로 실행 패키지**를 만들고, 사실 오류는 한 줄 보고 후 진행한다. **[2026-09-21 PM 2차 재감사 — 사실 오류 5건 원천 수정]** PM 2차 재감사(2026-09-21)가 사실 오류 다섯을 더 잡았고 **전부 원천에서 고쳤다**: ① `1.43139`는 회수분이 아니라 **도달점**이고 개선분은 `1.38666`이다 ② 유효 시도가 있는 넷을 "전부 한 방향만 재고 기각"으로 묶은 것은 틀렸다 — `track_lin_vel_xy_exp`은 상향을 재서 **채택**됐고 `feet_air_time`은 **양방향**을 다 쟀다 ③ 계획서 §9는 **「G-A007 실행 일정 — 260901」**이라 A033의 미이행 의무로 바로 옮길 수 없다 — P3는 의무가 아니라 **권고**로 낮췄다 ④ "이후 모든 비교의 눈금"은 과했다 — 한 회차가 주는 것은 **A033 조건의 초기 관측**이다 ⑤ "잔여 자원과 한 번도 비교된 적이 없다"는 전 기록 포괄 단정이라 "검토한 현행 문서에서 확인하지 못했다"로 바꿨다. ②는 검사 21이 다이얼 **종류**만 세고 방향·채택 여부를 보지 않아 놓친 것이라, 검사 21에 원표의 **변경 부호와 채택 이력** 대조를 넣었다. PM의 나머지 판정(G3 1순위 미확정)은 사용자 결정 G-D-PLAN-AS-GIVEN-20260921과 같은 결론이다 — P1은 이미 미채택이다. **[2026-09-21 G-A042 발행 — 사용자 계획 G-D-FORWARD-STAIRS-20260921]** 계획 `upload/plan/GO2_FORWARD_STAIRS_POLICY_20260921.md` §3의 첫 탐색을 그대로 실행 패키지로 만들었다. 이 다이얼은 걷는 기준선 위 인상이 **두 번 측정된 유일한 다이얼**이고 두 번 다 험지 전진거리와 10cm 오른 로봇 수를 함께 올렸다(`28`→`90`/96, ≥2단 `2`→`43`). 같은 두 인상이 옆걸음 손실의 유일한 출처이기도 해서(종료 `48`→`58`, 자세 낙상 `8`→`45`) 험지 6쌍 전수와 밀침이 1단계 표적이고 상태는 `INFORMATION_RUN`이다. 새 판독기 둘을 실제 수확물에 돌렸다: `tools/go2_stall_diagnostics.py`(정체시간비율·첫 단 도달시간)와 `tools/go2_screening_gate.py`(계획 §4) — A033↔A033은 `INTERNAL_GATE_FAIL`, A041 수확물은 15cm 결측 때문에 `INTERNAL_GATE_INCONCLUSIVE`다. 결함 **C-4·C-5 FIXED**(대장 `reports/GO2_DEFECT_LEDGER.md`): 발행문의 표적 case 수 상수 `9`와, 필수 기록을 조건부 2단계에 두어 G-A041이 15cm를 잃은 경로. 상세는 GO2_PROJECT_STATE.md의 같은 ID 절.
