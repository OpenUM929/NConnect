# Go2 험지 전진 보존·계단 정체 개선 정책 v1 — 2026-09-21

## 0. 예선 기준 현재 위치
- [예선 목표] 시뮬레이션 70점의 G3·G5 개선; 실패와 한계를 포함한 설계 의도20·리포트10의 근거 확보.
- [현재 단계] 2/6 짧은 학습 pilot. A033 기준선 유지.
- [확보] A033 전수 평가, A038 강화 및 A041 완화의 계단 악화 관측.
- [미확보] 계단 개선 가중치, 학습 seed 일반화, A041 15cm 평가, 잔여 서버 예산.
- [이번 테스트] 계획 작성. 새 학습·패키지 발행·서버 실행은 하지 않는다.
- [흐름] A041 판독 → **공동 목표 사전등록** → 내부 기준 충족 시 반복·전수 평가 → 실패 시 가설 수정 → 제출3종.
- [지금 할 일] 서버를 새로 켜지 않는다. 다음 구현은 아래 필수 수집·판정 계약을 실행 패키지에 반영하는 일이다.
- [보장하지 않음] 탐색값·단일 학습 seed·내부 proxy는 공식 점수나 합격을 보장하지 않는다.

## 1. PM 결정과 범위
결정 ID: G-D-FORWARD-STAIRS-20260921. STAY / 직접 계획 작성.
사용자 목적: 보상값에 따른 행동 변화 지식을 얻으면서, 험지 전진을 유지하고 계단 앞 정체를 줄인다.
G3 평균만 최대화하거나 흔들림 감소를 계단 개선의 대리 성공으로 삼지 않는다.
이번 산출물은 요청한 계획서다. 회차 ID는 패키지 제작 시 원장·upload 중복 검사 후 예약한다. 과거 release와 회수 ZIP은 불변이다.

## 2. 사실 → 해석 → 경쟁 가설
자료 정본: `reports/GO2_TUNING_BASE_DATA.md` §0-1·§1·§2, `GO2_REWARD_EVIDENCE_MASTER.md` §1-a, `reports/GO2_G_A041_READOUT.md` 및 LOCAL_VERIFY.json. 경로는 quadruped 기준이며 MASTER는 저장소 루트다.

| 사실/집단 | 관측 | 해석 한계 |
|---|---|---|
| A017→A033, track 1.4→1.5, 동일 평가 iter900 | 험지 전진 평균0.270→0.366m/s, 10cm ≥2단2→43/96 | 한 학습 seed의 조건부 공동 개선. 다음 증분의 효과나 직접 기전 확정 아님 |
| A033→A041, ang_vel_xy −0.05→−0.04 | 10cm ≥2단43→8/96; 자세 포함 낙상34→93/96; 험지 전진 낙상4→8/96 | 완화가 계단을 돕는다는 이번 예측 반박. 다른 가중치·seed 일반화 불가 |
| A038, ang_vel_xy −0.08 | 10cm ≥1단90→5/96, 옆걸음 개선 관측 | 흔들림과 계단 개선은 같은 목표가 아님. 러너 결함 판정과 행동 관측 분리 |
| A017→A033의 옆걸음 | 종료 증가 관측 | 전진 추종 강화의 반대 근거; 보호 평가에서 제외하지 않음 |

원자료의 96대는 평가 seed101/202/303×32대이지 독립 학습96회가 아니다. 기존 표의 속도 집계창과 새 alive-window 속도는 별도 지표다. 합치지 않는다.

학습 문서: 보존된 `reports/evidence/go2_reward_term_roles_20260917/isaaclab_envs_mdp_rewards.py`의 `track_lin_vel_xy_exp` 함수; SOURCE URL/SHA는 같은 폴더 SOURCES.csv. 식은 w·exp(−||v_cmd,xy−v_body,xy||²/std²), std=0.5. 정확한 body-frame 채널로만 원식을 재계산한다. 월드 높이·기울기 차분은 SURROGATE이며 실제 reward 채널로 부르지 않는다.
이 식은 명령 속도 오차를 줄이는 동기를 설명하지만 발 높이·장애물 넘기·학습 수렴을 보장하지 않는다. 기존 부분 margin은 COUNTERFACTUAL이며 행동 개선 예측량이 아니다.

주가설 H1: 추종 비중 증가가 움직임 비용 대비 전진의 이득을 높여 계단 정체를 줄일 수 있다.
경쟁 H2: 정체는 접촉·발 배치 실패여서 전진 보상만 높여도 해결되지 않고 충돌/낙상만 증가한다.
경쟁 H3: 학습 경로·커리큘럼 노출이 달라져 결과가 변한다. 평균 terrain으로 동일 노출이나 포화를 주장하지 않는다.
경쟁 H4: seed42에만 성립하는 결과다. 평가 seed 추가로 학습 seed 일반화를 대체하지 않는다.

REPORT_READ_STATUS: A033/A041 원 학습 HTML은 앞선 회수 검토에서 READ_MATCHED(같은 run)로 기록되어 있으며 이번에도 수치 본문을 대조했다. 상세 identity와 HTML/평가 iter 차이는 A041_READOUT의 ‘식별·학습 report’ 참조. 새 후보 HTML은 NOT_APPLICABLE(아직 생성되지 않음). HTML 평균 reward끼리 성능 우열을 정하지 않는다.

## 3. 첫 실험 선택 — exploratory, OUT_OF_RANGE
**A033 위 `track_lin_vel_xy_exp` 1.5→1.6 한 항 변경.**
상태는 **EXPLORATORY_INFORMATION_RUN**이다. 현재 최선 성능값/채택 권고가 아니다. 기존 기전 문서의 ‘track은1.5에서 멈춘다’는 성능 정책은 유지한다. 이번 공동 목표의 정보 탐색에 한정하여 그 경계 밖 한 점을 시험하도록 제안한다. A041은 track 추가 인상의 근거가 아니다. A017→A033에서 옆걸음 base-contact 종료48→58/96와 밀침 악화도 관측됐으므로 알려진 위험을 함께 기록한다. 이 수는 A041 보고서의 자세 포함59/96와 다른 정의다.
1.6은 실측 최적값이 아니라 직전 유효 비교의 증분0.1을 한 번 더 시험하는 설계 선택이다. 관측된 보행 값 상한1.5 밖이며 개선 확률·점수 이득을 수치로 예측하지 않는다.
배포 시작1.0 / Isaac Lab Go2 rough1.5 / 기준선1.5 / 후보1.6. upstream에서 멀어지는 비용을 인정하며 우리 공동 개선 관측을 탐색 이유로 삼는다.

유지값: feet_air_time0.2, lin_vel_z_l2−2.0, ang_vel_xy_l2−0.05, action_rate_l2−0.01, flat_orientation_l2=0; 나머지 env·알고리즘·학습 지형·명령·DR·초기화 방식은 A033과 동일.
학습 seed42,4096env,1000iter, 평가 checkpoint900 고정. A033 가중치에서 이어 학습한다고 해석하지 않는다: A033과 같은 초기화/학습 절차로 한 항만 달리 재학습한다. checkpoint700/800/900/999 및 실제 best를 보존한다.
R-6: 배포 reward 값만 수정. train.py/task/play.py/학습 환경 설치·구성은 변경하지 않는다. 평가 모듈은 정책을 읽는 별도 경로다.

대안 비교: ang_vel_xy 재조정은 양방향 계단 악화로 후순위; feet_air는 체공시간이지 발 높이가 아니며 기존 변경 실패가 있음; lin_vel_z 완화는 반대 자료가 있어 계단 해법으로 단정하지 않음; dof_acc는 관절 가속도 벌점이지 발 높이 보상이 아니며 허용범위 미확정 사항을 우회하지 않음. 따라서 이번은 기존 공동 개선 관측이 있는 track의 국소 탐색이다.

## 4. 측정 명세와 판정 — 패키지에 구현할 계약
계측/registry 지문은 A033과 고정. 기존 case 이름 `*_down`이 오르기임을 실제 지형 설정과 대조한다. 이름만으로 내려가기라 해석하지 않는다.
필수 표적: rough_forward, rough_lateral, stairs_10_down, stairs_15_down 각각 seed101/202/303×32대; 10·15cm는 별도 결과로 남긴다. 평가 horizon·dt·명령은 기존 registry 그대로.
초기 보호 표지에는 기존 A033 계획의 평지·좌우·양방향 밀침 case를 포함한다. 위12case는 최소 표적 수이며 전체 초기 평가 수가 아니다. 패키지 manifest에서 보호 case와 총수를 확정한다. terrain@700/800/900/999를 반드시 보존한다. terrain 변화는 학습 경로의 매개일 수 있으므로 그 변화만으로 단일변수 총효과 비교를 무효화하지 않되 직접 기전은 미확정으로 둔다.

1. 기존 정본 지표: 자세 포함 survival, tracking proxy, 전진거리, ≥1단/≥2단 수. `go2_climb_count.py`와 기존 evaluator를 재사용하고 원식/분모/창을 명세한다.
2. 신규 진단(점수 아님): 명령 전진이 양수인 첫 종료 전 구간에서 명령 방향 투영속도 <0.05m/s가 연속1초 이상인 구간의 시간 비율. 초기0.5초 제외, terminal row와 이후 제외. threshold는 설계값이며 정답이 아님. 유효 시간0이면 null+reason.
3. 첫 단 도달시간: 기존 ≥1단 판정의 최초 timestamp; 미도달은 null과 censored=true. 도달자만 평균내어 전체 개선으로 표시하지 않는다. 전체 도달률을 반드시 병기한다.
4. 새 표에는 run/model SHA/iter/evaluator/registry/학습seed/평가seed/단위/좌표/창/유효분모/제외기준/생성기 버전을 붙인다. 정보 부재는0으로 채우지 않는다. 기준선에도 같은 집계기를 적용한다.

**이번 screening의 보수적 설계 기준(공식 기준이나 통계적 유의수준이 아님):**
- 계단 개선: 10·15cm 각각 seed별 전진거리 차이의 중앙값>0; 각 높이의 pooled ≥1단·≥2단 수는 기준선 이상; 네 등반수 항 중 최소 하나 엄격 증가.
- 정체 감소: 두 높이 모두 seed별 정체시간비율 차이 중앙값<0. 조기 낙상으로 정체시간이 짧아진 것을 성공 처리하지 않도록 아래 생존 보호를 동시에 요구한다.
- 표적 보호: 위 네 case 각각 pooled 자세 낙상수≤기준선, seed별 survival 차이 중앙값≥0; rough_forward/rough_lateral tracking proxy 차이 중앙값≥0. 기존 fact_rules_v1도 통과해야 하며 새 조건은 그 규칙을 약화시키지 않는다.
- 필수 자료가 모두 있고 한 조건이라도 미달: INTERNAL_GATE_FAIL. 누락/지문 불일치/진단 구현 불능: INTERNAL_GATE_INCONCLUSIVE. 둘 다 승급하지 않지만 유효 관측은 지식으로 남긴다.
- 위 조건 충족: screening INTERNAL_GATE_PASS일 뿐. 그 후69case 전수 및 기존 보호축 기준, 영상, 독립 학습 seed 대칭 반복을 거쳐야 성능 승급 검토.

독립 학습 seed 반복은 후보와 A033 설정을 같은 새 seed로 각각 학습하는 한 쌍이다. 후보만 다른 seed로 돌린 뒤 seed42 기준선과 비교해 가중치 효과로 단정하지 않는다.

## 5. 실패해도 정보가 남는 실행·회수 설계
**필수 정보 수집은 성능 승급 분기 밖에서 항상 실행한다.** A041처럼 target FAIL 때문에15cm가 생략되지 않도록 한다. 69case 확대만 screening 이후 조건부다.
필수 영상: 위4case 각각seed101, 기준선/후보 양팔 각4개(각 기존 rollout 전체). 동일 identity·조건의 기존 영상은 SHA/경로 명시 후 재사용 가능. 실패 영상도 보존한다.
필수 회수: 원 학습 report.html(평가 전에 _keep/<회차>/exported/로 복사), env snapshot, reward diff, train/eval/video 로그, checkpoints, policy, telemetry, 지표표, STATUS/종료코드, manifest/SHA. 원본 HTML 누락은 회수 미완료다.
중단점: 학습 비정상 종료·NaN·시간 부족 시 보존 checkpoint/실패 로그 우선 회수; 성능 불량만으로 필수 평가를 생략하지 않는다. 회수 안전성이 우선이며 못 얻은 항목은 명시한다.
서버 종료는 실제 로컬 SHA/manifest·HTML·필수 telemetry/영상 도착 검사 후 별도 판단. 이번 계획으로 종료 승인하지 않는다.

## 6. 단계·비용·결과별 다음 행동
| 등급 | 단계 | 완료 기준 | 다음 분기 |
|---|---|---|---|
| 조사 | 패키지 구현·로컬 시험 | 단일변수 diff, LF/bash -n, SHA/CRC, FAIL에서도15cm/영상 회수하는 모의 실행 | 실패 시 로컬 수정 |
| 개선 | 첫 pilot1회 | 1000iter 및 필수12case/영상/HTML 회수 | 위 기준으로 판정 |
| 조사 | 성공 후보 재검증 | 69case·독립seed 양팔 비교·영상 | 재현 안 되면 INCONCLUSIVE/기준선 유지 |
| 개선 | 장기 학습 | 직전 승자와 부작용 게이트 충족 후 별도 계획 | 자동 장기 승급 없음 |
| 필수(제출요건) | 최종 문서·3종 | policy.pt/env.yaml/기술 개선 리포트 정합·자체 평가 완결 | 누락 시 ▲ 제출 불가—즉시 해소 |

최소 정보 경로=새 학습1회. 1차 계획 예산=첫 pilot만; 독립seed 후보·기준선2회는 성공 후 재평가 지점이다. 시간 총액은 잔여 예산과 실제 기존 단계별 소요시간을 패키지 제작 시 대조하며, 임의로 실행 가능이라 쓰지 않는다.
- 계단 개선+보호 충족: 전수·독립seed로 이동.
- 험지만 개선/계단 정체 유지: H1의 계단 전이 예측 반박. track 연속 인상을 자동 선택하지 않는다.
- 계단 개선/옆걸음 또는 생존 악화: 성능 승급 불가, trade-off 지식으로 기록.
- 모두 악화: 1.6 후보 기각, A033 유지. 1.5의 전역 최적성은 주장하지 않는다.
- 자료 누락: 학습 반복 대신 기존 checkpoint 읽기 전용 평가로 결측을 보완한다.

## 7. 검증과 검토 범위
증거 관리자 `/root/policy_evidence_review`의 PM_REVIEW: 성능 권고 REJECT, OUT_OF_RANGE 정보 실험 ACCEPT_LIMITED. 반대 관측·기존1.5유지 정책·15cm 무조건 회수·보호축을 추가 반영했다. A017 원 학습 HTML MISSING은 역사적 공백으로 남기며 원 평가 자료와 혼동하지 않는다. 최종 전수 판정은 기존 fact_rules_v1의 G3+G5 증가 및 보호축 비열등을 요구한다.
계획서 존재/인용 경로/필수 측정·분기·숫자 출처를 로컬 확인한다. 실행 코드 구현이나 서버 성능 검증과 혼동하지 않는다.
증거 관리자 검토 기록은 후속 절에 보존한다. 이번은 직접 계획 작성이며 분석가→기획자→독립 감사자 전체 라운드 완료로 표시하지 않는다. 수행하지 않은 독립 감사 단계는 UNREVIEWED다.
