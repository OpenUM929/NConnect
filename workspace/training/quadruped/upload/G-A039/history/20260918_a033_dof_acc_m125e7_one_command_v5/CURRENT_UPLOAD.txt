CURRENT GO2 UPLOAD — G-A039 (one command)

UPLOAD ONLY THIS ONE FILE
1. C:\dev\NConnect\workspace\training\quadruped\upload\G-A039\current\GO2_G_A039_a033_dof_acc_m125e7_one_command.zip
   SHA256 448a1422ea1f3c3e1c9451296104e221ccb70ace2664cf32610feaffa7a4d9bf

한 파일·한 명령으로 1단계 → 서버 게이트 → 통과 시 2단계 → 결과 ZIP 하나.
절차는 같은 폴더의 GO2_G_A039_ONE_COMMAND_RUN_GUIDE.txt. 회차 ZIP은 이 파일 안에 이미 들어 있다.
서버 실행은 사용자 결정 사항이다.

RELEASE_ID 20260918_a033_dof_acc_m125e7_one_command_v5
STATUS ARTIFACT_VERIFIED
이전 판 9개는 ../history 에 보존한다(가장 최근 20260918_a033_dof_acc_m125e7_staged_v5), 모두 서버에 올린 적 없다.
직전 판은 옛 판정 규칙(9 case 평균·이득 추정 waiver — G-A038의 10cm 오르기 붕괴를 통과시킴)으로 대체됐다. 이 판은 fact_rules_v1·사실 근거 추론 사슬을 싣는다. 그 전 판은 러너 결함(G-A033 영상 로그 폴더 미생성)으로 대체됐다(../history 에 보존, 모두 서버에 올린 적 없음).
권고 상태: 추론 사슬 RECOMMENDED — 보상 가중치 한 항만 바꾼다. 걷기 margin +0.1359→+0.2060 이고 margin 경계대(+0.0033~+0.0155) 밖에서는 반대 행이 없다(경계대 안에서는 A018 이 뒤집힌다). 이 항은 배포 `REWARD_WEIGHTS` 6개 목록 밖이라 후보 파일에 한 줄이 추가되고 `go2_task/env_cfg.py` 가 적용한다(서버 로그 `[OK] dof_acc_l2.weight`). **이것을 R-6 안으로 보는 해석은 아직 사용자 승인 전이다 — 열린 결정 U1-R6-ENV-REWARD-20260918 (`reports/GO2_OPEN_DECISIONS.md`). 배포 `go2_task/_finalize.py:586` 은 목록 밖 항을 판정에서 제외한다.** 서버 실행은 미해제.
