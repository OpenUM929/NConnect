# G-A038 판독 — 판정 없는 수확물 (2026-09-17)

회차: G-A033 + `ang_vel_xy_l2` `-0.05` → `-0.08` 하나만, 학습 seed `42`, `4096` env, `1000` iter, 평가 iter `900`.
생성 `tools/go2_g_a038_readout.py`, 증거 `reports/evidence/go2_g_a038_readout_20260917/`, 관문 `tools/test_go2_g_a038_readout_contract.py`.

## 1. 기록된 판정

- **판정 없음(INCONCLUSIVE)** [확인]. 러너가 표적 단계의 모든 측정을 마친 뒤, 첫 G-A033 영상에서 `RUNNER_RC=1`로 끝났다(`launcher.log:37492`, `tee`가 만들어지지 않은 `logs/g_a033_videos/`에 쓰려다 실패). `RUNNER_STATUS.txt`에 판정 필드가 없어 서버 게이트(`UNDECIDED`)와 로컬 검증기(`INCONCLUSIVE`)가 둘 다 판정을 거부했다. 거부는 규칙대로다.
- 서버는 2026-09-17 종료됐다. 재개하지 않는다. 재학습해서 판정을 받을 가치는 §4에 적었다.
- 결함은 러너 원본에서 고쳤다(`tools/test_go2_runner_video_log_contract.py`). 같은 결함을 실은 미실행 패키지 G-A035·G-A037은 새 판으로 재빌드했다. G-A030 현재 ZIP은 업로드 금지다.

## 2. 계측 지문 (`FINGERPRINT.csv`) [확인]

- 학습된 `env.yaml`의 보상 6항이 사양 후보값과 같다. 바뀐 항은 `ang_vel_xy_l2` 하나다.
- 평가 모델은 고정 체크포인트 iter `900`(SHA `97e1bb1e…`)이다. evaluator `353614…0d84`와 registry `8d8c34ca…9ba6`는 G-A033 저장 arm과 같다.
- G-A033 표지 5 case(`SENTINEL.csv`)는 저장값과 생존·추종 모두 차이 0이다. 같은 자로 쟀다.

## 3. 측정된 것

### 3-1. 표적 9 case (`TARGET_CASES.csv`, `TARGET_SUMMARY.csv`)

| 묶음 | 평균 proxy 변화 | 자세 낙상(seed별, G-A033 → G-A038) | 속도 |
|---|---|---|---|
| 험지 옆걸음 | +0.356 | 20·20·19 → 4·8·6 | 0.20·0.20·0.19 → 0.16·0.16·0.15 |
| 앞 밀침 | +0.049 | 5·1·2 → 1·0·2 | 거의 같음 |
| 10cm 오르기 | **−0.140** | 4·15·15 → **32·32·32** | 0.20·0.18·0.19 → **0.10·0.09·0.10** |

- 기준 1(표적 평균 `+0.05` 이상, 개선 묶음 2개 이상)은 **통과한다**: +0.088, 2개 [확인]. 서버가 살아서 판정 필드가 기록됐다면 전체 단계로 넘어갔을 것이다.

### 3-2. 10cm 오르기의 시간 변화 (`STAIRS_10_CLIMB_TIMELINE.csv`) [확인]

| 구간 | G-A033 서 있는 비율 (seed 101·202·303) | G-A038 서 있는 비율 | G-A038 몸 높이 |
|---|---|---|---|
| 4~6 s | 0.945·0.917·0.945 | 0.417·0.399·0.427 | 0.186·0.179·0.183 |
| 10~12 s | 0.972·0.795·0.739 | 0.162·0.262·0.110 | 0.155·0.157·0.150 |
| 18~20 s | 0.918·0.775·0.695 | 0.106·0.234·0.086 | 0.143·0.151·0.142 |

- G-A038은 오르기 구간에서 대부분 몸을 낮춘 채 기울어 멈춘다. 종료(`terminated`)는 0~1대라, 넘어져 끝난 것이 아니라 기운 자세로 머문 것이다.
- 해석 [추정]: 계단 한 단을 넘으려면 몸통이 앞뒤로 도는 순간이 필요한데, 그 회전 속도의 벌점이 커지자 회전을 피하고 몸을 낮췄다. 평지 case에서도 몸이 낮아지고 느려진 것(§3-3)과 맞는다. 계단 영상은 이번 목록에 없어서 영상으로는 확인하지 않았다.

### 3-3. 평지 기준 case (`FLAT_NOMINAL.csv`) [확인]

- `forward_nominal` seed 101: 생존 1.000 → 1.000, 추종 0.949 → 0.876, 속도 0.736 → 0.618(명령 `0.75`), 몸 높이 0.358 → 0.319.

## 4. 측정하지 않은 것과 판정 규칙의 빈틈

- **전체 단계 판정은 모른다** [모름]. 총점 차이(기준 2), 나머지 축 손실(기준 3), 정지 case(기준 4)는 69 case가 있어야 한다.
- **범위만 잡히는 것** (`GUARD_BOUNDS.csv`) [확인]: 시나리오 점수는 case·seed 전체의 최솟값이다(`build_policy`).
  - G1: G-A033 최솟값은 `forward_fast`@101 0.879다. G-A038에서 잰 평지 case는 0.876이라 손실 하한은 0.003이다. 한도 0.054를 넘는지는 모른다.
  - G5: G-A033 최솟값은 이미 `stairs_15_down`@202 0.0043이다. G-A038의 G5는 0이 확정이지만 손실은 **0.045/70으로 한도 0.056 안**이다.
- **빈틈** [확인]: 현재 사전 등록 규칙은 10cm 오르기 붕괴를 잡지 못한다.
  - 기준 1은 9 case 평균이라 옆걸음 개선이 오르기 손실을 덮는다.
  - 기준 3의 G5 최솟값은 G-A033에서 이미 0에 가까워 더 떨어질 곳이 없다.
  - 목표("모든 점수가 높은 상태에서 계단을 오른다")와 맞지 않는 규칙이다. 다음 사양부터 계단 case별 생존 하락을 막는 관문이 필요하다(§5).
- **재학습 가치**: 같은 seed 재학습은 결정론적이다(NOW §0). 판정 필드만 얻으려는 재학습은 새 정보가 없고, 전체 단계 69 case만 새 정보다(재학습 포함 약 2시간 25분 GPU, 안내서 추정). 목표 판단은 §3-1·§3-2로 이미 정해졌으므로 권하지 않는다.

## 5. 예측 대조와 다음 결정 (`FORECAST_CHECK.csv`)

| 상황 | 예측(부분 margin 변화) | 관측(표적 평균 proxy 변화) | 방향 |
|---|---|---|---|
| 흔들림 | +0.3230 | +0.356 | 같음 |
| 밀침 | +0.1535 | +0.049 | 같음 |
| 계단 | −0.0107 | −0.140(붕괴) | 같음, 크기는 크게 틀림 |

- 방향은 세 상황 모두 맞았다 [확인]. 예측은 보상의 선호 변화이지 로봇 수 변화의 예측이 아니므로, 크기를 대조할 척도가 없다. 계단 부분 margin은 "하한이라 더 클 수 있다"고 적었지만, 이 붕괴 규모는 예측에 없었다.
- 학습 seed가 하나라 레버 효과와 seed 운을 가를 수 없다 [확인].
- **다이얼 모델 반박** [확인]: 커리큘럼 도달점(terrain@999)이 G-A033 4.7087에서 G-A038 3.0348로 내려갔다. `track`은 둘 다 `1.5`이고 `ang_vel_xy_l2`는 사망 규칙 밖이다. "살아 있는 회차는 `track` 수준이 정한다"(`tools/go2_dial_model.py` 규칙 2)가 틀렸으므로, 모델은 `REFUTED_BY_G_A038`로 표시하고 예측 근거로 쓰지 않는다(`tools/test_go2_dial_model_contract.py` test_8).
- **커리큘럼 지연** [확인, 2026-09-18 추가, `CURRICULUM_LAG.csv`·원장 `reports/runs/TERRAIN_AT_PIN.csv`]: 두 팔 모두 iter 900에서 평가했는데, 그 시점의 지형 레벨은 G-A033 `4.4937` 대 G-A038 `1.9499`(차 `−2.5438`)였다. 700·800·999에서도 각각 `−2.4000`·`−2.9883`·`−1.6739`다. 따라서 10cm 오르기 붕괴에는 **레버 효과와 학습 진도 차이가 섞여 있고**, 이 판독만으로 둘을 가를 수 없다 [모름]. 위 결정 1·2는 그대로다 — 승급 후보가 아니라는 것과 조합 철회는 관측된 붕괴만으로 충분하다. 다만 "`ang_vel_xy_l2` 강화가 계단을 막는다"는 **기전 주장으로는 인용하지 않는다**. 지형 레벨 평균은 로봇 전체·지형 5종 평균이라 계단 열의 도달점은 여전히 [모름]이다.
- **결정** (근거 §3):
  1. G-A038은 승급 후보가 아니다.
  2. 앞서 제안한 `ang_vel_xy_l2` `-0.08` + `lin_vel_z_l2` `-1.0` 조합은 철회한다. `-0.08` 하나로 오르기가 무너졌다.
  3. 다음 패키지 전에 계단 부분 margin을 붕괴 규모까지 설명하도록 다시 설계한다.
  4. 다음 사양에는 계단 case별 생존 하락 관문을 넣는다.
