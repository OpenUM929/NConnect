# G-A019 검증 기록 — `track_lin_vel_xy_exp` 1.2 → 1.3 (260905)

## 1. 업로드 대상

| 파일 | 크기 | SHA-256 |
|---|---:|---|
| `go2_tuning_engine_v1_3.zip` | 12,781,997 B | `dfbe47aecb5584ad07583caea726d23a372764b22e12962e0cbd76b268877b1a` |
| `G_A019_pilot_track_lin_vel_xy_130.json` | 3,241 B | `643b36c9572520038ebf4467534590a961becffc25fcc9a39088b394f8305e17` |

엔진은 G-A015~G-A018에서 실행된 것과 **바이트 동일**하다. 이번 회차에 새로 검증할
대상은 실험 사양뿐이다.

## 2. 실행한 검증

| 항목 | 결과 |
|---|---|
| 사양 스키마 검증 | `{"sha256": "643b36c9…8305e17", "status": "VALID", "work_id": "G-A019"}` |
| 배포 ZIP 추출본으로 materialize | `{"status": "MATERIALIZED", "work_id": "G-A019"}` |
| 후보 arm 렌더 결과 | `track_lin_vel_xy_exp = 1.3`, 나머지 5개는 Pilot-01 값 그대로 |
| 기준선 arm 렌더 결과 | `track_lin_vel_xy_exp = 1.2`, 나머지 5개 동일 → 변경 1개 |
| 기준선 checkpoint | `sha256 c4d78adf…f89af8d` = 동결 Pilot-01 |
| 계약 테스트 | `python -m unittest discover -s tools` — `Ran 50 tests`, `FAILED (failures=6, errors=7)`.
13건 전부 기존에 알려진 실패(대형 원본 파일 미보유로 인한 재빌드 테스트류)이며 G-A019
관련 실패는 0건 |

## 3. 왜 이 다이얼·이 값인가

G-A017(1.2→1.4)이 참가자 파일 6개 reward 항 중 유일하게 **총점을 실제로 올린**
회차였다(46.49124→50.19916, `+3.70792/70`). 그런데 G4(경사 ±20°)의 생존이 단독으로
`1.0→0.78125`(`-0.21875`)로 후퇴 상한(0.10)의 두 배를 넘어 §19-b(생존 절 우선)에 따라
다이얼째 기각됐다(G-D79). 이후 마지막 미검증 항 `action_rate_l2`(G-A018,
−0.01→−0.008)도 전 시나리오(G1~G7) survival_proxy_v2가 동시에 붕괴(fallen_env_count
24~32/32, height_rel_mean 0.11~0.26m, speed_xy_mean 0.03~0.06)해 완전히 기각됐다.

이로써 참가자 파일이 명시한 6개 reward 항은 전부 소진됐다: 상한 확정 2건
(`feet_air_time` 0.20), 다이얼째 기각 2건(`ang_vel_xy_l2`, `action_rate_l2`), 부호
반대로 철회 1건(`flat_orientation_l2`), 방향 기확정 1건(`lin_vel_z_l2`), 그리고
`track_lin_vel_xy_exp` — 유일하게 총점 개선을 낸 항이지만 최댓값(1.4)에서 기각됐다.

**G-A019는 새 다이얼이 아니라 이미 총점 개선을 낸 유일한 항의 크기를 절반으로
줄인 재탐색이다.** 1.2→1.4(+0.2)에서 G4 생존이 무너졌다면, 1.2→1.3(+0.1)에서는
추종 개선 폭이 줄어드는 대신 G4 생존 후퇴가 0.10 문턱 아래로 들어올 가능성이 있다는
가설이다. 이 지점을 확인하지 않고 Pilot-01로 되돌아가는 것은 유일한 개선 신호를
검증 없이 버리는 셈이다.

기준선은 동결 Pilot-01, seed 42, 4096 env, 1000 iter로 이전 회차와 동일. 엔진은
v1.3(1.2.0)을 바이트 동일 재사용한다.

## 4. 증거 경계

이 값(1.3)이 G4 생존을 지키면서 추종을 개선하는 "달콤한 지점"인지는 **가설**이다.
측정된 사실은 다음과 같다.

1. 1.2(Pilot-01 자체)는 G4 생존 `1.0`, 추종 `0.5533` — 안전하지만 저추종.
2. 1.4(G-A017)는 G4 생존 `0.78125`(−0.21875), 추종 `0.7286`(+0.1752) — 고추종이지만
   생존 실패.
3. 1.2와 1.4 사이 어떤 지점도 **한 번도 측정된 적이 없다.** 생존 후퇴가 선형으로
   줄어드는지, 아니면 임계값 근처에서 급격히 무너지는지(문턱 효과) 알 수 없다.

가설이 틀렸을 경우(1.3에서도 G4 생존이 0.10 넘게 후퇴)의 함의: `track_lin_vel_xy_exp`는
1.2가 최종 상한으로 확정되고, 참가자 파일 안에서 Pilot-01(33.79311/70)을 넘는 단일
변수 경로는 **완전히 소진**된다. 다음 논의는 사양의 `branch.on_tier1_fail`에 이미
기록해 두었다.
