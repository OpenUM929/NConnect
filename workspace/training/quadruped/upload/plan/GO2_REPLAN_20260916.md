# Go2 재계획 — 2026-09-16 (정리판)

이 문서는 같은 날 다섯 번 고쳐 쓴 판(`history/GO2_REPLAN_20260916_superseded_rev1-5.md`)을 대체한다.
정정 위에 정정을 쌓지 않고, **산출물·코드·Isaac Lab 원문으로 다시 확인한 것만** 남겼다.
확신도: [확인] 파일·코드에서 직접 읽음 · [추정] 계산·추론 · [모름].
외부 기준: Isaac Lab v2.3.1 원 소스(`rough_env_cfg.py`·`rough.py`·`velocity_env_cfg.py`·`rsl_rl_ppo_cfg.py`·`terrain_generator.py`·`terrain_importer.py`·`curriculums.py`).

## 1. 기준선 G-A033 [확인]

| 항목 | 값 | 근거 |
|---|---|---|
| 정체 | A017 + `track_lin_vel_xy_exp` 1.4→1.5, seed 42, 1000 iter, **iter 900 평가** | `_keep/go2_g_a033…/reports/LOCAL_VERIFY_G_A033.json` |
| 체크포인트 | SHA `ccd60e19…6044` = `model_best.pt` = `model_iter900.pt` = `identity.json` = `GO2_NOW.md` | `training/CHECKPOINT_PIN.txt` |
| 69case | 42.52861/70. 감점 G5 10.46 · G3 9.77 · G7 1.89 · G4 1.48 · G6 1.31 · G2 1.29 · G1 1.28 (합 27.47) | 정본 채점기 재계산 |
| A017 대비 | +2.764 = **G4 +3.65** − (G1·G2·G3·G6 합 1.47) + G5·G7 0.58. 로컬 검증 판정 **FAIL**(비열등 위반 3건) | 같은 파일 `scenarios` |
| 최소 case | G5 `stairs_15_down` 생존 .03~.13 · 추종 .136~.149 / G3 `rough_lateral` 생존 .375~.41 · 추종 .81 | case `summary.json` |

## 2. 기준선이 서 있는 사실과 그 한계

| # | 사실 | 확신도 | 근거 |
|---|---|---|---|
| S1 | 70점 축(69case·낙상 검출 v2)에 점이 있는 정책은 Pilot-01·A017·G-A033 **셋**이다 | [확인] | `reports/runs/LEDGER.csv` |
| S2 | 모든 학습이 **seed 42**다. 같은 seed 재학습 2쌍은 체크포인트 SHA가 같다(결정론). **seed 간 흔들림 표본은 0건이다** | [확인] | 학습 로그 `Environment seed` · `test_go2_run_ledger_contract` test_13 |
| S3 | 평가 검출 한계 2.53/70은 생존만 재표집한 **하한**이다. 추종·학습 seed 흔들림은 들어 있지 않다 | [확인] | `tools/go2_eval_resolution.py` 머리말 · 계획 §12-7 |
| S4 | 로그의 지형 레벨은 **로봇 전체·지형 5종의 평균**이다. 시작 레벨은 0~5 균등이고, 최상단을 풀면 무작위 레벨로 보낸다. 단 L 계단 높이는 0.05+0.18·(L+U)/10 m다. 5단은 이미 0.14~0.158 m다 | [확인] | IL `curriculums.py`·`terrain_importer.py`·`terrain_generator.py` · G-A033 `env.yaml` |
| S5 | 비교 시점 iter 900의 평균 레벨: A017 3.43(@800 2.49 → @999 4.25, 급상승 중) · G-A033 4.49(@800 4.13 → @999 4.71) | [확인] | `reports/runs/TERRAIN_AT_PIN.csv` |
| S6 | Go2의 학습 길이 대 점수 자료는 **0건**이다. 배포 파일은 제출용 학습을 5000~15000 iter로 안내하고, 배포 `agent_cfg.py` 기본값은 5000이다. 5k 패키지 G-A011은 실행 전 철회됐다 | [확인] | `quadruped_rewards.py` 추천 순서 4 · `go2_task/agent_cfg.py:20` · `GO2_CAMPAIGN_SCHEDULE.md:285` |

**S4가 뒤집는 것:** "0.15 m 계단은 어떤 회차도 학습한 적이 없다"(계획 §14·§15-2)는 성립하지 않는다. 계단 열이 실제로 어디까지 갔는지는 로그에 없다 [모름].
**S2·S5가 뒤집는 것:** +2.764가 `track` 1.5의 효과인지, seed 운인지, "iter 900에서 커리큘럼이 앞서 있었을 뿐"인지 가릴 수 없다 [모름].

## 3. Isaac Lab v2.3.1 Go2 rough 대비 차이 — 전수 [확인]

| 항목 | IL Go2 rough | 우리 | 누가 정했나 |
|---|---|---|---|
| `feet_air_time` | 0.01 | 0.2 | 우리(G-A001 4항 동시 변경, 출처 무효) |
| 나머지 reward 9항 | — | 같음 | — |
| `max_iterations` | 1500 | 1000 | 우리 러너(배포 기본 5000) |
| `entropy_coef` | 0.01 | 0.008 | 배포 `go2_task/agent_cfg.py:45` |
| 명령 범위 | x ±1 · y ±1 · yaw ±1 | x −1~2 · y ±0.6 · yaw ±1 | 배포 `go2_task/env_cfg.py:72` |
| `boxes` 높이 | 0.025~0.10 m | 0.02~0.15 m | 배포 `go2_task/env_cfg.py:60` |
| 계단 · 경사 · 험지 노이즈 · 행동 scale 0.25 · 밀침 없음 · 질량 −1~3 · 초기 레벨 5 | — | 같음 | — |

R-6(reward 가중치만)으로 바꿀 수 있는 차이는 `feet_air_time` 하나다. 나머지는 R-6 밖이다.

## 4. 철회한 주장 (다시 쓰지 않는다)

| 철회 | 이유 |
|---|---|
| "학습 흔들림 0이라 한 번에 답이 나온다" | S2. 같은 seed의 결정론일 뿐이다 |
| "0.15 m 계단은 학습한 적이 없다", "학습한 최고 ≈ 0.14 m" | S4 |
| "지형 레벨은 0~9 축이고 회차 간 차이 1.1이라 잡음에 묻히지 않는다" | 1.1은 보상이 다른 회차 사이의 차이다. 평균 레벨로는 계단 열을 분리할 수 없다(S4) |
| "러너가 `model_900.pt` SHA로 결정론을 확인한다" | 러너는 iter 1450과 finalize 선택본만 남긴다 |
| "reward 밖 차이는 명령 범위뿐" | §3 |
| "G-A035 기대 이득 +2.76" | 보상 변경의 이득이지 학습 길이의 이득이 아니다. 그리고 G4 하나에서 나왔다 |
| "(나) 탐색 1회면 P1이 1순위" | 근거 넷 중 둘(흔들림 0, 원인 판별)이 위 철회에 걸린다 |

이 철회는 사양 `config/experiments/G_A035_a033_iter1500.json`과 G-A035 패키지 안내문에도 반영했다. 패키지는 재빌드했다(`one_command_v2`, 러너·게이트 코드 바이트 불변).

## 5. 서버 후보 판정 — 옛 규칙(70점 기대 이득 추정)으로 내린 판정, 재판정 대기

> **2026-09-17 규칙 교체(G-D-FACT-RULES-20260917).** 아래 표의 "70점 기대 이득" 열과 비권고 판정은 폐기된 규칙으로 내린 것이다. 후보는 이제 사실 근거 추론 사슬(`GO2_REWARD_EVIDENCE_MASTER.md` §5-3)로 고르고, 판정은 `fact_rules_v1`(`tools/go2_fact_rules.py`)로 한다. 이 표는 기록으로 두고, 정책 수립 때 사슬로 다시 판정한다. G-A035는 `INFORMATION_RUN`, G-A037은 `HOLD_CONTRADICTED`로 사양에 적었다.

| 후보 | 답하는 질문 | 70점 기대 이득 | 판정 |
|---|---|---|---|
| P1 G-A035 (G-A033 설정, 1500 iter, iter 1450 평가) | 같은 seed의 G-A033 설정을 1500 iter까지 학습하면 69case 점수가 어떻게 되나. **왜 변했는지**는 답하지 못한다(S4) | [모름] | 비권고 |
| P2 G-A036 (기존 15개 정책 재평가) | 70점 축의 점을 3개에서 최대 18개로 늘린다. G-A033 위 정책은 0개이고, 평가 iteration이 700~999로 섞인다 | 해당 없음(측정 회차) | 비권고 |
| G-A033 + `feet_air_time` 0.01 | IL과 reward 10항이 전부 같아지면 어떤가. 유일한 측정(A031)은 A017 위·10case였다 | [모름] | 비권고 |
| `track` 1.25~1.35 | A017 계열의 문턱 질문이다. 현 기준선은 1.5다 | [모름] | 비권고 |
| G-A030 `flat_orientation` −1.0 | 기준 arm이 A017이다. MASTER §1-b D-1이 반대한다 | [모름] | 비권고·폐기 권고 |

**(옛 규칙) 권고하는 서버 회차: 0개.** — 재판정 대기.

## 6. 옛 규칙이 막던 것 — 2026-09-17 규칙 교체로 해소

> 아래 "어떤 후보든 이득을 추정할 수 없다 → 규칙을 그대로 두면 계속 막힌다"는 진단이 규칙 교체의 근거가 됐다. seed 반복의 R-6 해석은 여전히 사용자 결정이다(`reports/GO2_SEED_SENSITIVITY.md` §5).

70점 축의 점이 3개이고(S1), 학습 seed 흔들림(S2)과 학습 길이 효과(S6)의 표본이 없다. 그래서 **어떤 후보든 이득을 추정할 수 없다.** 규칙을 그대로 두면 서버 회차는 계속 막힌다. 풀지 말지는 사용자 결정이다.

- **(가) 서버를 쓰지 않는다.** 제출 후보는 G-A033(iter 900)이다. 약점: S2·S5(승급 근거가 seed 운일 수 있다)와 S6(배포 안내 길이의 1/5~1/15로 학습했다).
- **(나) "이득 미추정" 탐색을 명시적으로 산다.** 이 문서는 **순위를 매기지 않는다.** 순위를 뒷받침할 추정이 없기 때문이다. 대신 탐색별로 무엇이 풀리는지만 적는다.

| 탐색 | 풀리는 모름 | 패키지 |
|---|---|---|
| P1 G-A035 | S6 일부: 같은 seed에서 1500 iter 점수. 원인은 풀리지 않는다 | 있음(재빌드·종단 테스트 통과) |
| G-A033 설정 seed 43 재학습(1000 iter, iter 900, 69case) | S2: 첫 seed 흔들림 표본. 승급 근거 G4 +3.65가 재현되는가 | **없음** — 만들려면 사용자 요청이 필요하다 |
| P2 G-A036 | S1: 70점 축 점 추가. iteration이 섞여 있다 | 있음 |

## 7. 이번 재검토에서 고친 원천 (문서 기록만이 아니다)

| # | 구멍 | 조치 | 관문 |
|---|---|---|---|
| H1 | "학습 seed 흔들림 0" 오독 | `GO2_NOW.md` 문장 교체 | `test_go2_run_ledger_contract` test_13: seed 집합 = {42} · 금지 문구 |
| H2·H3 | 평균 레벨을 최고 계단으로 읽음 | G-A035 사양 `why`·`waiver`·`mechanism_readout` 교체 · 계획 §14·§15-2에 철회 표시 · `GO2_NOW.md` 교체 | test_13 금지 문구 |
| H4 | G-A035 안내문 거짓 약속 2건 | 빌더 README 교체 · `staged_v3`/`one_command_v2` 재빌드 | 계약 25건 통과 · ZIP 내 금지 문구 0건 확인 |
| H5 | 기대 이득 2.76이 검출력 관문을 수치로 통과할 수 있었다 | `expected_weighted_gain_70: null`(waiver 경로) | `test_go2_detectability_gate` |
| H7·H10 | IL 대조표에 `entropy_coef`·`boxes` 높이가 빠짐 | §3 · MASTER §1-b 표 B에 행 추가 | — |
| H8 | 보고서 22의 "전수가 아니다" 경고가 arm을 밝히지 않음 | 생성기가 arm 이름을 적음 | 보고서 재생성 |
| H9 | LEDGER가 회차 env.yaml 하나를 모든 arm 행에 적음(Pilot이 Default 가중치로 적힘) | arm별 `identity.json` env SHA로 연결하고, 모르면 비움. 학습 곡선 열은 학습된 arm에만 적음 | test_12 |

H6(학습 길이 대 점수 0건)은 자료가 없는 문제라 코드로 고칠 수 없다. §2 S6·§6에 기록했다.

## 8. 보장하지 않는 것

- 어떤 회차도 점수 상승을 보장하지 않는다. 내부 proxy는 공식 점수와 다르다(`OFFICIAL_RESULT_UNMEASURED`).
- 잔여 GPU "약 30h"는 사용자 보고이고 실측이 아니다 [모름].
- 서버 실행·장기 학습·정책 승급은 해제되지 않았다. 로컬 패키지 제작만 허용됐다.
